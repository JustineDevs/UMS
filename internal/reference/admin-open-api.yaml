openapi: 3.1.0
info:
  title: Universal Music Store Admin Open API
  version: 1.0.0
  description: >
    English reference documentation for protected administration operations.

    All endpoints are implemented under the Next.js admin application and are
    subject to staff authentication, permission checks, audit logging, and
    domain-specific validation.
servers:
  - url: /api
    description: Admin application API
security:
  - AdminSession: []
tags:
  - name: "Admin / Analytics"
    description: "Operations for Admin / Analytics."
  - name: "Admin / Audit Logs"
    description: "Operations for Admin / Audit Logs."
  - name: "Admin / Campaigns"
    description: "Operations for Admin / Campaigns."
  - name: "Admin / Catalog"
    description: "Operations for Admin / Catalog."
  - name: "Admin / Channels"
    description: "Operations for Admin / Channels."
  - name: "Admin / Chat Orders"
    description: "Operations for Admin / Chat Orders."
  - name: "Admin / Cms"
    description: "Operations for Admin / Cms."
  - name: "Admin / Commerce"
    description: "Operations for Admin / Commerce."
  - name: "Admin / Commerce Recovery Metrics"
    description: "Operations for Admin / Commerce Recovery Metrics."
  - name: "Admin / Cost Visibility"
    description: "Operations for Admin / Cost Visibility."
  - name: "Admin / Crm"
    description: "Operations for Admin / Crm."
  - name: "Admin / Delivery Logistics"
    description: "Operations for Admin / Delivery Logistics."
  - name: "Admin / Devices"
    description: "Operations for Admin / Devices."
  - name: "Admin / Employees"
    description: "Operations for Admin / Employees."
  - name: "Admin / Feature Mappings"
    description: "Operations for Admin / Feature Mappings."
  - name: "Admin / Integration Health"
    description: "Operations for Admin / Integration Health."
  - name: "Admin / Integrations"
    description: "Operations for Admin / Integrations."
  - name: "Admin / Inventory"
    description: "Operations for Admin / Inventory."
  - name: "Admin / Invoices"
    description: "Operations for Admin / Invoices."
  - name: "Admin / Loyalty"
    description: "Operations for Admin / Loyalty."
  - name: "Admin / Medusa"
    description: "Operations for Admin / Medusa."
  - name: "Admin / Offline Queue"
    description: "Operations for Admin / Offline Queue."
  - name: "Admin / Operator Notes"
    description: "Operations for Admin / Operator Notes."
  - name: "Admin / Orders"
    description: "Operations for Admin / Orders."
  - name: "Admin / Payment Attempts"
    description: "Operations for Admin / Payment Attempts."
  - name: "Admin / Payment Health"
    description: "Operations for Admin / Payment Health."
  - name: "Admin / Payments"
    description: "Operations for Admin / Payments."
  - name: "Admin / Pin Approval"
    description: "Operations for Admin / Pin Approval."
  - name: "Admin / Pos"
    description: "Operations for Admin / Pos."
  - name: "Admin / Profile"
    description: "Operations for Admin / Profile."
  - name: "Admin / Receipts"
    description: "Operations for Admin / Receipts."
  - name: "Admin / Reconciliation"
    description: "Operations for Admin / Reconciliation."
  - name: "Admin / Reviews"
    description: "Operations for Admin / Reviews."
  - name: "Admin / Roles"
    description: "Operations for Admin / Roles."
  - name: "Admin / Segments"
    description: "Operations for Admin / Segments."
  - name: "Admin / Shifts"
    description: "Operations for Admin / Shifts."
  - name: "Admin / Sse"
    description: "Operations for Admin / Sse."
  - name: "Admin / Storefront Home"
    description: "Operations for Admin / Storefront Home."
  - name: "Admin / Storefront Public Metadata"
    description: "Operations for Admin / Storefront Public Metadata."
  - name: "Admin / Tasks"
    description: "Operations for Admin / Tasks."
  - name: "Admin / Terminal Open Drawer"
    description: "Operations for Admin / Terminal Open Drawer."
  - name: "Admin / Terminal Print"
    description: "Operations for Admin / Terminal Print."
  - name: "Admin / Terminal Print Label"
    description: "Operations for Admin / Terminal Print Label."
  - name: "Admin / Voids"
    description: "Operations for Admin / Voids."
  - name: "Admin / Workflow"
    description: "Operations for Admin / Workflow."
  - name: "Auth / ...Nextauth"
    description: "Operations for Auth / ...Nextauth."
  - name: "Integrations / Channels"
    description: "Operations for Integrations / Channels."
  - name: "Integrations / Chat Orders"
    description: "Operations for Integrations / Chat Orders."
  - name: "Integrations / Couriers"
    description: "Operations for Integrations / Couriers."
  - name: "Medusa / Orders"
    description: "Operations for Medusa / Orders."
  - name: "Medusa / Shipments"
    description: "Operations for Medusa / Shipments."
  - name: "Pos / Medusa"
    description: "Operations for Pos / Medusa."
  - name: "Webhooks / Nango"
    description: "Operations for Webhooks / Nango."
paths:
  /admin/analytics/clv:
    get:
      operationId: get_admin_analytics_clv
      summary: "Read Admin / Analytics / Clv"
      description: "Admin / Analytics / Clv. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Analytics"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { computeClv } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"analytics:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const email = req.nextUrl.searchParams.get(\"email\");\n  if (!email) {\n    return correlatedJson(cid, { error: \"email query param required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const clv = await computeClv(sb, email);\n  if (!clv) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: clv });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_analytics_clvResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/analytics/retention:
    get:
      operationId: get_admin_analytics_retention
      summary: "Read Admin / Analytics / Retention"
      description: "Admin / Analytics / Retention. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Analytics"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { computeRetention } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"analytics:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const months = Number(req.nextUrl.searchParams.get(\"months\") ?? \"6\");\n  const data = await computeRetention(sb, months);\n  return correlatedJson(cid, { data });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_analytics_retentionResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/analytics/sales-trends:
    get:
      operationId: get_admin_analytics_sales_trends
      summary: "Read Admin / Analytics / Sales Trends"
      description: "Admin / Analytics / Sales Trends. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Analytics"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { computeSalesTrends } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"analytics:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const months = Number(req.nextUrl.searchParams.get(\"months\") ?? \"6\");\n  const data = await computeSalesTrends(sb, months);\n  return correlatedJson(cid, { data });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_analytics_sales_trendsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/audit-logs:
    get:
      operationId: get_admin_audit_logs
      summary: "Read Admin / Audit Logs"
      description: "Admin / Audit Logs. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Audit Logs"]
      x-source: "import { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { auditActorCsvCell } from \"@/lib/audit-actor-format\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  correlatedError,\n  correlatedJson,\n  tagResponse,\n} from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\n\nexport const dynamic = \"force-dynamic\";\n\nfunction escapeCsvCell(value: string): string {\n  if (/[\",\\n\\r]/.test(value)) {\n    return `\"${value.replace(/\"/g, '\"\"')}\"`;\n  }\n  return value;\n}\n\n/**\n * Staff audit rows (timelines, compliance view, export).\n * JSON: `dashboard:read`. CSV export: `analytics:export`.\n */\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const url = new URL(req.url);\n  const format = url.searchParams.get(\"format\")?.trim().toLowerCase() ?? \"\";\n  const isCsv = format === \"csv\" || format === \"text/csv\";\n  const staff = await requireStaffApiSession(\n    isCsv ? \"analytics:export\" : \"dashboard:read\",\n  );\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n\n  const defaultLimit = isCsv ? 500 : 20;\n  const limit = Math.min(500, Math.max(1, Number(url.searchParams.get(\"limit\")) || defaultLimit));\n  const resourcePrefix = url.searchParams.get(\"resource_prefix\")?.trim() ?? \"\";\n  const actionPrefix = url.searchParams.get(\"action_prefix\")?.trim() ?? \"\";\n  const actionExact = url.searchParams.get(\"action\")?.trim() ?? \"\";\n  const from = url.searchParams.get(\"from\")?.trim() ?? \"\";\n  const to = url.searchParams.get(\"to\")?.trim() ?? \"\";\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  let q = sup.client\n    .from(\"audit_logs\")\n    .select(\"id,action,resource,details,created_at,actor_id,users(email,name)\")\n    .order(\"created_at\", { ascending: false })\n    .limit(limit);\n\n  if (resourcePrefix) {\n    q = q.ilike(\"resource\", `${resourcePrefix}%`);\n  }\n  if (actionExact) {\n    q = q.eq(\"action\", actionExact);\n  } else if (actionPrefix) {\n    q = q.ilike(\"action\", `${actionPrefix}%`);\n  }\n  if (from) {\n    q = q.gte(\"created_at\", from);\n  }\n  if (to) {\n    q = q.lte(\"created_at\", to);\n  }\n\n  const { data, error } = await q;\n  if (error) {\n    return correlatedError(\n      correlationId,\n      502,\n      \"Audit log service unavailable\",\n      \"SERVICE_UNAVAILABLE\",\n    );\n  }\n\n  const rows = data ?? [];\n\n  if (isCsv) {\n    const header = [\"id\", \"created_at\", \"actor\", \"action\", \"resource\", \"details_json\"].join(\",\");\n    const lines = rows.map((r) => {\n      const rec = r as Record<string, unknown>;\n      const details =\n        rec.details != null ? JSON.stringify(rec.details) : \"\";\n      const actor = auditActorCsvCell(\n        rec as {\n          users?: { email?: string | null; name?: string | null } | null;\n          details: Record<string, unknown> | null;\n        },\n      );\n      return [\n        escapeCsvCell(String(rec.id ?? \"\")),\n        escapeCsvCell(String(rec.created_at ?? \"\")),\n        escapeCsvCell(actor),\n        escapeCsvCell(String(rec.action ?? \"\")),\n        escapeCsvCell(String(rec.resource ?? \"\")),\n        escapeCsvCell(details),\n      ].join(\",\");\n    });\n    const csv = [header, ...lines].join(\"\\r\\n\");\n    return new Response(csv, {\n      status: 200,\n      headers: {\n        \"Content-Type\": \"text/csv; charset=utf-8\",\n        \"Content-Disposition\": `attachment; filename=\"audit_logs_${correlationId.slice(0, 8)}.csv\"`,\n        \"x-correlation-id\": correlationId,\n      },\n    });\n  }\n\n  return correlatedJson(correlationId, { entries: rows });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_audit_logsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/campaigns:
    get:
      operationId: get_admin_campaigns
      summary: "Read Admin / Campaigns"
      description: "Admin / Campaigns. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Campaigns"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listCampaigns,\n  createCampaign,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { validateCampaignAgainstMedusaPromotions } from \"@/lib/campaign-medusa-governance\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"campaigns:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const type = req.nextUrl.searchParams.get(\"type\") ?? undefined;\n  const data = await listCampaigns(sb, { type: type as \"winback\" | \"birthday\" | undefined, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"campaigns:write\");\n  if (!auth.ok) return auth.response;\n  const schema = z.object({\n    name: z.string().trim().min(1).max(160),\n    type: z.enum([\"winback\", \"birthday\", \"first_purchase\", \"upsell\", \"custom\"]),\n    segment_id: z.string().uuid().nullable().optional(),\n    subject: z.string().trim().max(240).nullable().optional(),\n    body_template: z.string().max(100_000).nullable().optional(),\n    schedule_cron: z.string().trim().max(120).nullable().optional(),\n  }).strict();\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const body = parsed.data;\n  const gov = await validateCampaignAgainstMedusaPromotions({\n    bodyTemplate: String(body.body_template ?? \"\"),\n    subject: String(body.subject ?? \"\"),\n  });\n  if (!gov.ok) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Campaign copy references codes not present in Medusa promotions\",\n        governance: gov,\n      },\n      { status: 409 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  if (body.segment_id) {\n    const { data: segment } = await sb.from(\"customer_segments\").select(\"id\").eq(\"id\", body.segment_id).eq(\"organization_id\", organization.id).maybeSingle();\n    if (!segment) return correlatedJson(cid, { error: \"Segment not found\" }, { status: 404 });\n  }\n  const campaign = await createCampaign(sb, {\n    ...body,\n    segment_id: body.segment_id ?? undefined,\n    subject: body.subject ?? undefined,\n    body_template: body.body_template ?? undefined,\n    schedule_cron: body.schedule_cron ?? undefined,\n    organization_id: organization.id,\n  });\n  await insertStaffAuditLog(sb, {\n    actorEmail: auth.session.user.email ?? \"unknown\",\n    action: \"campaign.create\",\n    resource: \"campaign\",\n    resourceId: campaign.id,\n    details: { name: campaign.name, type: campaign.type, segment_id: campaign.segment_id },\n  });\n  return correlatedJson(cid, { data: campaign }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/campaigns:POST\", post);\n"
      x-permission: "campaigns:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_campaignsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_campaigns
      summary: "Create or execute Admin / Campaigns"
      description: "Admin / Campaigns. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Campaigns"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listCampaigns,\n  createCampaign,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { validateCampaignAgainstMedusaPromotions } from \"@/lib/campaign-medusa-governance\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"campaigns:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const type = req.nextUrl.searchParams.get(\"type\") ?? undefined;\n  const data = await listCampaigns(sb, { type: type as \"winback\" | \"birthday\" | undefined, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"campaigns:write\");\n  if (!auth.ok) return auth.response;\n  const schema = z.object({\n    name: z.string().trim().min(1).max(160),\n    type: z.enum([\"winback\", \"birthday\", \"first_purchase\", \"upsell\", \"custom\"]),\n    segment_id: z.string().uuid().nullable().optional(),\n    subject: z.string().trim().max(240).nullable().optional(),\n    body_template: z.string().max(100_000).nullable().optional(),\n    schedule_cron: z.string().trim().max(120).nullable().optional(),\n  }).strict();\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const body = parsed.data;\n  const gov = await validateCampaignAgainstMedusaPromotions({\n    bodyTemplate: String(body.body_template ?? \"\"),\n    subject: String(body.subject ?? \"\"),\n  });\n  if (!gov.ok) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Campaign copy references codes not present in Medusa promotions\",\n        governance: gov,\n      },\n      { status: 409 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  if (body.segment_id) {\n    const { data: segment } = await sb.from(\"customer_segments\").select(\"id\").eq(\"id\", body.segment_id).eq(\"organization_id\", organization.id).maybeSingle();\n    if (!segment) return correlatedJson(cid, { error: \"Segment not found\" }, { status: 404 });\n  }\n  const campaign = await createCampaign(sb, {\n    ...body,\n    segment_id: body.segment_id ?? undefined,\n    subject: body.subject ?? undefined,\n    body_template: body.body_template ?? undefined,\n    schedule_cron: body.schedule_cron ?? undefined,\n    organization_id: organization.id,\n  });\n  await insertStaffAuditLog(sb, {\n    actorEmail: auth.session.user.email ?? \"unknown\",\n    action: \"campaign.create\",\n    resource: \"campaign\",\n    resourceId: campaign.id,\n    details: { name: campaign.name, type: campaign.type, segment_id: campaign.segment_id },\n  });\n  return correlatedJson(cid, { data: campaign }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/campaigns:POST\", post);\n"
      x-permission: "campaigns:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_campaignsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_campaignsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/campaigns/{id}:
    patch:
      operationId: patch_admin_campaigns_id
      summary: "Update Admin / Campaigns / Id"
      description: "Admin / Campaigns / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Campaigns"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { z } from \"zod\";\nimport { updateCampaign } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport const dynamic = \"force-dynamic\";\nconst patchSchema = z.object({\n  name: z.string().trim().min(1).max(160).optional(),\n  subject: z.string().trim().max(240).optional(),\n  body_template: z.string().max(100_000).optional(),\n  is_active: z.boolean().optional(),\n  schedule_cron: z.string().trim().max(120).optional(),\n  segment_id: z.string().uuid().optional(),\n}).strict();\ntype Ctx = { params: Promise<{ id: string }> };\n\nasync function patch(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"campaigns:write\")) return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  const parsed = await parseAdminJson(req, patchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  if (parsed.data.segment_id) {\n    const { data: segment } = await sup.client.from(\"customer_segments\").select(\"id\").eq(\"id\", parsed.data.segment_id).eq(\"organization_id\", organization.id).maybeSingle();\n    if (!segment) return correlatedJson(cid, { error: \"Segment not found\" }, { status: 404 });\n  }\n  const campaign = await updateCampaign(sup.client, id, organization.id, parsed.data);\n  await insertStaffAuditLog(sup.client, { actorEmail: session.user.email, action: \"campaign.update\", resource: \"campaign\", resourceId: id, details: parsed.data });\n  return correlatedJson(cid, { data: campaign });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/campaigns/[id]:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_campaigns_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_campaigns_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/campaigns/{id}/execute:
    post:
      operationId: post_admin_campaigns_id_execute
      summary: "Create or execute Admin / Campaigns / Id / Execute"
      description: "Admin / Campaigns / Id / Execute. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Campaigns"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { enqueueJob } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { validateCampaignAgainstMedusaPromotions } from \"@/lib/campaign-medusa-governance\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { getIdempotencyKey } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nasync function post(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user)\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"campaigns:execute\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const executionKey = getIdempotencyKey(req);\n  if (!executionKey) {\n    return correlatedJson(\n      cid,\n      { error: \"Idempotency-Key is required\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { data: campRow } = await sb\n    .from(\"campaigns\")\n    .select(\"subject,body_template,execution_key,execution_status\")\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .maybeSingle();\n  if (!campRow)\n    return correlatedJson(\n      cid,\n      { error: \"Campaign not found\" },\n      { status: 404 },\n    );\n  if (\n    campRow.execution_key === executionKey &&\n    campRow.execution_status === \"completed\"\n  ) {\n    return correlatedJson(cid, { sent: 0, replayed: true });\n  }\n  const { data: claimed, error: claimError } = await sb\n    .from(\"campaigns\")\n    .update({ execution_key: executionKey, execution_status: \"running\" })\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .or(\n      `execution_status.eq.idle,execution_status.eq.failed,execution_key.eq.${executionKey}`,\n    )\n    .select(\"id\")\n    .maybeSingle();\n  if (claimError || !claimed)\n    return correlatedJson(\n      cid,\n      { error: \"Campaign is already executing\" },\n      { status: 409 },\n    );\n  const gov = await validateCampaignAgainstMedusaPromotions({\n    bodyTemplate: String(campRow?.body_template ?? \"\"),\n    subject: String(campRow?.subject ?? \"\"),\n  });\n  if (!gov.ok) {\n    await sb\n      .from(\"campaigns\")\n      .update({ execution_status: \"failed\" })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id)\n      .eq(\"execution_key\", executionKey);\n    return correlatedJson(\n      cid,\n      {\n        error:\n          \"Campaign copy references codes not present in Medusa promotions\",\n        governance: gov,\n      },\n      { status: 409 },\n    );\n  }\n  const jobId = await enqueueJob(\n    sb,\n    \"campaign.execute\",\n    {\n      campaignId: id,\n      organizationId: organization.id,\n      executionKey,\n    },\n    session.user.email ?? undefined,\n  );\n  if (!jobId) {\n    await sb\n      .from(\"campaigns\")\n      .update({ execution_status: \"failed\" })\n      .eq(\"id\", id)\n      .eq(\"execution_key\", executionKey)\n      .eq(\"organization_id\", organization.id);\n    return correlatedJson(\n      cid,\n      { error: \"Campaign could not be queued\" },\n      { status: 503 },\n    );\n  }\n  await insertStaffAuditLog(sb, {\n    actorEmail: session.user.email ?? \"unknown\",\n    action: \"campaign.execute\",\n    resource: \"campaign\",\n    resourceId: id,\n    details: { queued: true, jobId },\n  });\n  return correlatedJson(cid, { queued: true, jobId }, { status: 202 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/campaigns/[id]/execute:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_campaigns_id_executeRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_campaigns_id_executeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/catalog/categories:
    get:
      operationId: get_admin_catalog_categories
      summary: "Read Admin / Catalog / Categories"
      description: "Admin / Catalog / Categories. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  createAdminProductCategory,\n  listAdminProductCategories,\n} from \"@/lib/medusa-product-categories\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = (await req.json().catch(() => ({}))) as {\n    name?: string;\n    handle?: string;\n  };\n  const name = typeof body.name === \"string\" ? body.name : \"\";\n  const handle = typeof body.handle === \"string\" ? body.handle : undefined;\n  const result = await createAdminProductCategory({ name, handle });\n  if (!result.ok) {\n    return correlatedJson(correlationId, { error: result.message }, { status: 400 });\n  }\n  return correlatedJson(correlationId, { category: result.category }, { status: 201 });\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"catalog:read\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const categories = await listAdminProductCategories();\n  return correlatedJson(correlationId, { categories });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/catalog/categories:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_catalog_categoriesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_catalog_categories
      summary: "Create or execute Admin / Catalog / Categories"
      description: "Admin / Catalog / Categories. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  createAdminProductCategory,\n  listAdminProductCategories,\n} from \"@/lib/medusa-product-categories\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = (await req.json().catch(() => ({}))) as {\n    name?: string;\n    handle?: string;\n  };\n  const name = typeof body.name === \"string\" ? body.name : \"\";\n  const handle = typeof body.handle === \"string\" ? body.handle : undefined;\n  const result = await createAdminProductCategory({ name, handle });\n  if (!result.ok) {\n    return correlatedJson(correlationId, { error: result.message }, { status: 400 });\n  }\n  return correlatedJson(correlationId, { category: result.category }, { status: 201 });\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"catalog:read\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const categories = await listAdminProductCategories();\n  return correlatedJson(correlationId, { categories });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/catalog/categories:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_catalog_categoriesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_catalog_categoriesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/catalog/media:
    get:
      operationId: get_admin_catalog_media
      summary: "Read Admin / Catalog / Media"
      description: "Admin / Catalog / Media. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  insertCmsMedia,\n  listCmsMedia,\n  type ListCmsMediaOptions,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport {\n  requireStaffApiSession,\n  requireStaffApiSessionAny,\n} from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst BUCKET = \"catalog\";\nconst MAX_IMAGE_BYTES = 25 * 1024 * 1024;\nconst MAX_VIDEO_BYTES = 100 * 1024 * 1024;\n\nfunction safeSegment(s: string): string {\n  return s.replace(/[^a-zA-Z0-9_-]/g, \"\").slice(0, 128);\n}\n\nfunction guessVideoMimeFromName(name: string): string {\n  const lower = name.toLowerCase();\n  if (lower.endsWith(\".webm\")) return \"video/webm\";\n  if (lower.endsWith(\".mov\")) return \"video/quicktime\";\n  if (lower.endsWith(\".ogg\")) return \"video/ogg\";\n  return \"video/mp4\";\n}\n\n/** Lists `cms_media` rows tagged for the catalog bucket (same rows as CMS media library, scoped). */\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\"catalog:read\", \"catalog:write\"]);\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const opts: ListCmsMediaOptions = {\n    limit: Math.min(Number(sp.get(\"limit\")) || 200, 500),\n    search: sp.get(\"q\") ?? undefined,\n    mimePrefix: sp.get(\"mime\") ?? undefined,\n    sort: (sp.get(\"sort\") as ListCmsMediaOptions[\"sort\"]) || \"created_desc\",\n    tag: CMS_MEDIA_TAG_CATALOG_PRODUCT,\n    organizationId: organization.id,\n  };\n  const data = await listCmsMedia(sup.client, opts);\n  return correlatedJson(cid, {\n    data,\n    // The browser session can hydrate after this request (and local auth has\n    // no NextAuth cookie), so expose the server's already-authorized result.\n    canWrite: staffSessionAllows(auth.session, \"catalog:write\"),\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"catalog:write\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sb = sup.client;\n\n  let form: FormData;\n  try {\n    form = await req.formData();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid form data\" }, { status: 400 });\n  }\n  const file = form.get(\"file\");\n  const altRaw = typeof form.get(\"alt\") === \"string\" ? form.get(\"alt\") : \"\";\n  const altText = typeof altRaw === \"string\" ? altRaw.trim() : \"\";\n  const productIdRaw =\n    typeof form.get(\"productId\") === \"string\" ? form.get(\"productId\") : \"\";\n  const productId =\n    typeof productIdRaw === \"string\" ? safeSegment(productIdRaw.trim()) : \"\";\n\n  if (!(file instanceof Blob) || file.size === 0) {\n    return correlatedJson(cid, { error: \"Missing file\" }, { status: 400 });\n  }\n\n  const rawName = typeof (file as File).name === \"string\" ? (file as File).name : \"upload\";\n  const mime = (file as File).type || \"\";\n  const videoExtOk = /\\.(mp4|webm|mov|ogg)$/i.test(rawName);\n  const isImage = mime.startsWith(\"image/\");\n  const isVideo =\n    mime.startsWith(\"video/\") ||\n    ((mime === \"\" || mime === \"application/octet-stream\") && videoExtOk);\n\n  if (!isImage && !isVideo) {\n    return correlatedJson(\n      cid,\n      { error: \"Only image or video uploads are allowed\" },\n      { status: 400 },\n    );\n  }\n\n  if (isVideo) {\n    const allowed = new Set([\n      \"video/mp4\",\n      \"video/webm\",\n      \"video/quicktime\",\n      \"video/ogg\",\n    ]);\n    const looseVideo = !mime || mime === \"application/octet-stream\";\n    if (looseVideo && !videoExtOk) {\n      return correlatedJson(\n        cid,\n        { error: \"Could not detect video type. Use a .mp4, .webm, .mov, or .ogg file.\" },\n        { status: 400 },\n      );\n    }\n    if (!looseVideo && !allowed.has(mime)) {\n      return correlatedJson(\n        cid,\n        { error: \"Unsupported video type. Use mp4, webm, mov, or ogg.\" },\n        { status: 400 },\n      );\n    }\n  }\n\n  const maxBytes = isImage ? MAX_IMAGE_BYTES : MAX_VIDEO_BYTES;\n  if (file.size > maxBytes) {\n    return correlatedJson(\n      cid,\n      { error: `File exceeds limit of ${maxBytes} bytes` },\n      { status: 400 },\n    );\n  }\n  const safe = rawName.replace(/[^a-zA-Z0-9._-]+/g, \"_\").slice(0, 120);\n  const stem = safe.replace(/\\.[^.]+$/, \"\") || \"file\";\n  const prefix = productId ? `products/${productId}` : \"public\";\n  const path = `${prefix}/${crypto.randomUUID()}-${safe}`;\n\n  const declaredType = (file as File).type?.trim();\n  const resolvedContentType =\n    declaredType && declaredType !== \"application/octet-stream\"\n      ? declaredType\n      : isVideo\n        ? guessVideoMimeFromName(rawName)\n        : \"application/octet-stream\";\n\n  const ab = await file.arrayBuffer();\n  const { error: upErr } = await sb.storage.from(BUCKET).upload(path, ab, {\n    contentType: resolvedContentType,\n    upsert: false,\n  });\n  if (upErr) {\n    return correlatedJson(cid, { error: upErr.message }, { status: 500 });\n  }\n\n  const { data: pub } = sb.storage.from(BUCKET).getPublicUrl(path);\n  const publicUrl = pub.publicUrl;\n\n  const altResolved = isImage\n    ? altText || `Product image: ${stem}`\n    : null;\n\n  const row = await insertCmsMedia(sb, {\n    storage_path: path,\n    public_url: publicUrl,\n    alt_text: altResolved,\n    mime_type: declaredType || (isVideo ? guessVideoMimeFromName(rawName) : null),\n    width: null,\n    height: null,\n    display_name: safe,\n    byte_size: file.size,\n    tags: [CMS_MEDIA_TAG_CATALOG_PRODUCT],\n    organization_id: organization.id,\n  });\n  if (!row) {\n    await sb.storage.from(BUCKET).remove([path]).catch(() => undefined);\n    return correlatedJson(cid, { error: \"Unable to save metadata\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/catalog/media:POST\", post);\n"
      x-permission: "catalog:write"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_catalog_mediaResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_catalog_media
      summary: "Create or execute Admin / Catalog / Media"
      description: "Admin / Catalog / Media. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  insertCmsMedia,\n  listCmsMedia,\n  type ListCmsMediaOptions,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport {\n  requireStaffApiSession,\n  requireStaffApiSessionAny,\n} from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst BUCKET = \"catalog\";\nconst MAX_IMAGE_BYTES = 25 * 1024 * 1024;\nconst MAX_VIDEO_BYTES = 100 * 1024 * 1024;\n\nfunction safeSegment(s: string): string {\n  return s.replace(/[^a-zA-Z0-9_-]/g, \"\").slice(0, 128);\n}\n\nfunction guessVideoMimeFromName(name: string): string {\n  const lower = name.toLowerCase();\n  if (lower.endsWith(\".webm\")) return \"video/webm\";\n  if (lower.endsWith(\".mov\")) return \"video/quicktime\";\n  if (lower.endsWith(\".ogg\")) return \"video/ogg\";\n  return \"video/mp4\";\n}\n\n/** Lists `cms_media` rows tagged for the catalog bucket (same rows as CMS media library, scoped). */\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\"catalog:read\", \"catalog:write\"]);\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const opts: ListCmsMediaOptions = {\n    limit: Math.min(Number(sp.get(\"limit\")) || 200, 500),\n    search: sp.get(\"q\") ?? undefined,\n    mimePrefix: sp.get(\"mime\") ?? undefined,\n    sort: (sp.get(\"sort\") as ListCmsMediaOptions[\"sort\"]) || \"created_desc\",\n    tag: CMS_MEDIA_TAG_CATALOG_PRODUCT,\n    organizationId: organization.id,\n  };\n  const data = await listCmsMedia(sup.client, opts);\n  return correlatedJson(cid, {\n    data,\n    // The browser session can hydrate after this request (and local auth has\n    // no NextAuth cookie), so expose the server's already-authorized result.\n    canWrite: staffSessionAllows(auth.session, \"catalog:write\"),\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"catalog:write\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sb = sup.client;\n\n  let form: FormData;\n  try {\n    form = await req.formData();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid form data\" }, { status: 400 });\n  }\n  const file = form.get(\"file\");\n  const altRaw = typeof form.get(\"alt\") === \"string\" ? form.get(\"alt\") : \"\";\n  const altText = typeof altRaw === \"string\" ? altRaw.trim() : \"\";\n  const productIdRaw =\n    typeof form.get(\"productId\") === \"string\" ? form.get(\"productId\") : \"\";\n  const productId =\n    typeof productIdRaw === \"string\" ? safeSegment(productIdRaw.trim()) : \"\";\n\n  if (!(file instanceof Blob) || file.size === 0) {\n    return correlatedJson(cid, { error: \"Missing file\" }, { status: 400 });\n  }\n\n  const rawName = typeof (file as File).name === \"string\" ? (file as File).name : \"upload\";\n  const mime = (file as File).type || \"\";\n  const videoExtOk = /\\.(mp4|webm|mov|ogg)$/i.test(rawName);\n  const isImage = mime.startsWith(\"image/\");\n  const isVideo =\n    mime.startsWith(\"video/\") ||\n    ((mime === \"\" || mime === \"application/octet-stream\") && videoExtOk);\n\n  if (!isImage && !isVideo) {\n    return correlatedJson(\n      cid,\n      { error: \"Only image or video uploads are allowed\" },\n      { status: 400 },\n    );\n  }\n\n  if (isVideo) {\n    const allowed = new Set([\n      \"video/mp4\",\n      \"video/webm\",\n      \"video/quicktime\",\n      \"video/ogg\",\n    ]);\n    const looseVideo = !mime || mime === \"application/octet-stream\";\n    if (looseVideo && !videoExtOk) {\n      return correlatedJson(\n        cid,\n        { error: \"Could not detect video type. Use a .mp4, .webm, .mov, or .ogg file.\" },\n        { status: 400 },\n      );\n    }\n    if (!looseVideo && !allowed.has(mime)) {\n      return correlatedJson(\n        cid,\n        { error: \"Unsupported video type. Use mp4, webm, mov, or ogg.\" },\n        { status: 400 },\n      );\n    }\n  }\n\n  const maxBytes = isImage ? MAX_IMAGE_BYTES : MAX_VIDEO_BYTES;\n  if (file.size > maxBytes) {\n    return correlatedJson(\n      cid,\n      { error: `File exceeds limit of ${maxBytes} bytes` },\n      { status: 400 },\n    );\n  }\n  const safe = rawName.replace(/[^a-zA-Z0-9._-]+/g, \"_\").slice(0, 120);\n  const stem = safe.replace(/\\.[^.]+$/, \"\") || \"file\";\n  const prefix = productId ? `products/${productId}` : \"public\";\n  const path = `${prefix}/${crypto.randomUUID()}-${safe}`;\n\n  const declaredType = (file as File).type?.trim();\n  const resolvedContentType =\n    declaredType && declaredType !== \"application/octet-stream\"\n      ? declaredType\n      : isVideo\n        ? guessVideoMimeFromName(rawName)\n        : \"application/octet-stream\";\n\n  const ab = await file.arrayBuffer();\n  const { error: upErr } = await sb.storage.from(BUCKET).upload(path, ab, {\n    contentType: resolvedContentType,\n    upsert: false,\n  });\n  if (upErr) {\n    return correlatedJson(cid, { error: upErr.message }, { status: 500 });\n  }\n\n  const { data: pub } = sb.storage.from(BUCKET).getPublicUrl(path);\n  const publicUrl = pub.publicUrl;\n\n  const altResolved = isImage\n    ? altText || `Product image: ${stem}`\n    : null;\n\n  const row = await insertCmsMedia(sb, {\n    storage_path: path,\n    public_url: publicUrl,\n    alt_text: altResolved,\n    mime_type: declaredType || (isVideo ? guessVideoMimeFromName(rawName) : null),\n    width: null,\n    height: null,\n    display_name: safe,\n    byte_size: file.size,\n    tags: [CMS_MEDIA_TAG_CATALOG_PRODUCT],\n    organization_id: organization.id,\n  });\n  if (!row) {\n    await sb.storage.from(BUCKET).remove([path]).catch(() => undefined);\n    return correlatedJson(cid, { error: \"Unable to save metadata\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/catalog/media:POST\", post);\n"
      x-permission: "catalog:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_catalog_mediaRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_catalog_mediaResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/catalog/products:
    post:
      operationId: post_admin_catalog_products
      summary: "Create or execute Admin / Catalog / Products"
      description: "Admin / Catalog / Products. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { createMedusaCatalogOperations } from \"@/domain/operations/catalog-operations\";\nimport { upsertEntityWorkflow } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { jsonFromAdminOperationResult } from \"@/lib/staff-api-operation\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { fetchCatalogProductDetail } from \"@/lib/medusa-catalog-service\";\nimport { parseOptionalStockQuantity } from \"@/lib/parse-optional-stock-quantity\";\nimport {\n  parseStorefrontMetadataFromBody,\n  parseVariantBarcodeFromBody,\n  catalogProductRequestSchema,\n} from \"@/lib/parse-catalog-product-body\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { parseCatalogOptionArray } from \"@/lib/parse-catalog-option-array\";\nimport { collectCatalogMediaUrlsFromBody } from \"@/lib/catalog-product-media-db\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  ensureExternalCatalogProductMediaRows,\n  upsertPaymentProviderArtifact,\n} from \"@universal-music-store/platform-data\";\nimport {\n  listCatalogProviderProjections,\n  upsertCatalogProviderProjection,\n} from \"@universal-music-store/platform-data\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport {\n  buildStorefrontCommerceInvalidationPayload,\n  notifyStorefrontCommerceInvalidation,\n} from \"@/lib/storefront-commerce-invalidation\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { catalogSyncIdempotencyKey } from \"@/lib/catalog-sync-idempotency\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function syncStripeCatalogAfterCreate(input: {\n  productId: string;\n  title: string;\n  description?: string;\n  handle?: string;\n  pricePhp: number;\n  correlationId: string;\n  actorEmail?: string;\n}) {\n  const sup = adminSupabaseOr503(input.correlationId);\n  if (!(\"client\" in sup)) return { state: \"unavailable\" as const };\n  if (!stripeAvailableForMerchant())\n    return {\n      state: \"unavailable\" as const,\n      reason: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n    };\n  const projections = await listCatalogProviderProjections(sup.client, {\n    medusaProductId: input.productId,\n  });\n  const existing = new Map(\n    projections\n      .filter((row) => row.provider === \"stripe\")\n      .map((row) => [row.artifact_type, row]),\n  );\n  const idempotencyKey = catalogSyncIdempotencyKey({\n    productId: input.productId,\n    title: input.title,\n    description: input.description,\n    handle: input.handle,\n    pricePhp: input.pricePhp,\n    operation: \"create\",\n  });\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken)\n    return {\n      state: \"pending\" as const,\n      reason: \"MEDUSA_INTERNAL_ADMIN_TOKEN is not configured\",\n    };\n  const merchantIdentity = input.actorEmail?.trim().toLowerCase();\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    input.actorEmail,\n  );\n  if (!organization)\n    return {\n      state: \"unavailable\" as const,\n      reason: \"ORGANIZATION_NOT_CONFIGURED\",\n    };\n  const { data: nangoConnection } =\n    merchantIdentity && organization\n      ? await sup.client\n          .from(\"payment_nango_connections\")\n          .select(\"nango_connection_id,provider_config_key\")\n          .eq(\"organization_id\", organization.id)\n          .eq(\"provider\", \"stripe\")\n          .eq(\"active\", true)\n          .order(\"updated_at\", { ascending: false })\n          .limit(1)\n          .maybeSingle()\n      : { data: null };\n  try {\n    const response = await medusaAdminFetch(\"/admin/catalog/provider-sync\", {\n      method: \"POST\",\n      headers: { \"x-uvs-internal-token\": internalToken },\n      body: JSON.stringify({\n        productId: input.productId,\n        title: input.title,\n        description: input.description ?? null,\n        handle: input.handle ?? null,\n        amountMinor: Math.round(input.pricePhp * 100),\n        currency: \"PHP\",\n        siteOrigin:\n          process.env.STOREFRONT_PUBLIC_URL ??\n          process.env.NEXT_PUBLIC_SITE_URL ??\n          null,\n        includePaymentLink: true,\n        productExternalId: existing.get(\"product\")?.external_id ?? null,\n        priceExternalId: existing.get(\"price\")?.external_id ?? null,\n        paymentLinkExternalId:\n          existing.get(\"payment_link\")?.external_id ?? null,\n        idempotencyKey,\n        ...(nangoConnection?.nango_connection_id\n          ? {\n              nango_connection_id: nangoConnection.nango_connection_id,\n              nango_provider_config_key: nangoConnection.provider_config_key,\n            }\n          : {}),\n      }),\n    });\n    const payload = (await response.json().catch(() => ({}))) as {\n      data?: {\n        productId?: string;\n        priceId?: string;\n        paymentLinkId?: string;\n        paymentLinkUrl?: string;\n      };\n      error?: string;\n      code?: string;\n    };\n    if (!response.ok || !payload.data?.productId || !payload.data.priceId) {\n      for (const artifactType of [\n        \"product\",\n        \"price\",\n        \"payment_link\",\n      ] as const) {\n        await upsertCatalogProviderProjection(sup.client, {\n          medusa_product_id: input.productId,\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          sync_state: \"failed\",\n          sync_mode: \"automatic\",\n          last_error_code: payload.code ?? \"STRIPE_CATALOG_SYNC_FAILED\",\n          last_error: payload.error ?? \"Stripe catalog synchronization failed\",\n          last_failed_step: artifactType,\n          correlation_id: input.correlationId,\n          idempotency_key: idempotencyKey,\n          updated_by_email: input.actorEmail ?? null,\n        });\n      }\n      return {\n        state: \"failed\" as const,\n        reason: payload.error ?? \"Stripe catalog synchronization failed\",\n      };\n    }\n    const values = {\n      product: { external_id: payload.data.productId },\n      price: { external_id: payload.data.priceId },\n      payment_link: {\n        external_id: payload.data.paymentLinkId ?? null,\n        external_url: payload.data.paymentLinkUrl ?? null,\n      },\n    } as const;\n    for (const artifactType of [\"product\", \"price\", \"payment_link\"] as const) {\n      await upsertCatalogProviderProjection(sup.client, {\n        medusa_product_id: input.productId,\n        provider: \"stripe\",\n        artifact_type: artifactType,\n        ...values[artifactType],\n        sync_state: \"synced\",\n        sync_mode: \"automatic\",\n        last_synced_at: new Date().toISOString(),\n        correlation_id: input.correlationId,\n        idempotency_key: idempotencyKey,\n        updated_by_email: input.actorEmail ?? null,\n      });\n      const externalId = values[artifactType].external_id;\n      if (externalId) {\n        await upsertPaymentProviderArtifact(sup.client, {\n          organization_id: organization.id,\n          merchant_identity:\n            input.actorEmail?.trim().toLowerCase() || \"local-admin\",\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          external_id: externalId,\n          parent_external_id:\n            artifactType === \"price\" ? values.product.external_id : null,\n          status: \"synced\",\n          currency: \"PHP\",\n          amount_minor:\n            artifactType === \"price\" ? Math.round(input.pricePhp * 100) : null,\n          metadata: { medusa_product_id: input.productId },\n          idempotency_key: idempotencyKey,\n        });\n      }\n    }\n    return {\n      state: \"synced\" as const,\n      paymentLinkUrl: payload.data.paymentLinkUrl ?? null,\n    };\n  } catch (error) {\n    return {\n      state: \"failed\" as const,\n      reason: error instanceof Error ? error.message : String(error),\n    };\n  }\n}\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n  const parsedBody = await parseAdminJson(\n    req,\n    catalogProductRequestSchema,\n    512_000,\n  );\n  if (!parsedBody.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: parsedBody.error },\n      { status: parsedBody.status },\n    );\n  }\n  const body = parsedBody.data;\n  const title = typeof body.title === \"string\" ? body.title : \"\";\n  const pricePhp = Number(body.pricePhp);\n  const status = body.status === \"published\" ? \"published\" : \"draft\";\n  const categoryIds = Array.isArray(body.categoryIds)\n    ? body.categoryIds.filter(\n        (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n      )\n    : [];\n  const sizeLabel =\n    typeof body.sizeLabel === \"string\" ? body.sizeLabel : undefined;\n  const colorLabel =\n    typeof body.colorLabel === \"string\" ? body.colorLabel : undefined;\n  const sizeLabelsArr = parseCatalogOptionArray(body.sizeLabels);\n  const colorLabelsArr = parseCatalogOptionArray(body.colorLabels);\n\n  const stockParsed = parseOptionalStockQuantity(body);\n  if (!stockParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: stockParsed.error },\n      { status: 400 },\n    );\n  }\n\n  const storefrontMetadata = parseStorefrontMetadataFromBody(body);\n  const variantBarcode = parseVariantBarcodeFromBody(body);\n\n  const imageUrlsRaw = body.imageUrls;\n  const imageUrls = Array.isArray(imageUrlsRaw)\n    ? imageUrlsRaw\n        .filter(\n          (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n        )\n        .map((s) => s.trim())\n    : undefined;\n\n  const ops = createMedusaCatalogOperations();\n  const result = await ops.createProduct({\n    title,\n    handle: typeof body.handle === \"string\" ? body.handle : undefined,\n    description:\n      typeof body.description === \"string\" ? body.description : undefined,\n    status,\n    pricePhp: Number.isFinite(pricePhp) ? pricePhp : NaN,\n    sku: typeof body.sku === \"string\" ? body.sku : null,\n    imageUrls,\n    thumbnail: typeof body.thumbnail === \"string\" ? body.thumbnail : null,\n    categoryIds,\n    sizeLabel,\n    colorLabel,\n    sizeLabels: sizeLabelsArr,\n    colorLabels: colorLabelsArr,\n    stockQuantity: stockParsed.value,\n    variantBarcode: variantBarcode ?? null,\n    storefrontMetadata,\n  });\n\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, 502);\n  }\n\n  const actorEmail = session.user.email?.trim();\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"client\" in sup) {\n    await ensureExternalCatalogProductMediaRows(\n      sup.client,\n      collectCatalogMediaUrlsFromBody(body),\n    );\n    if (actorEmail) {\n      const organization = await resolveStaffOrganization(\n        sup.client,\n        actorEmail,\n      );\n      await insertStaffAuditLog(sup.client, {\n        actorEmail,\n        action: \"catalog.product.create\",\n        resource: `product:${result.data.productId}`,\n        details: { title: title.trim() },\n      });\n      if (organization) {\n        await upsertEntityWorkflow(sup.client, {\n          organizationId: organization.id,\n          entityType: \"catalog_product\",\n          entityId: result.data.productId,\n          state: status === \"published\" ? \"published\" : \"draft\",\n          actorEmail,\n        });\n      }\n    }\n  }\n\n  const createdDetail = await fetchCatalogProductDetail(\n    result.data.productId,\n  ).catch(() => null);\n  const invalidation = await notifyStorefrontCommerceInvalidation(\n    buildStorefrontCommerceInvalidationPayload({\n      classification:\n        status === \"published\" ? \"sellability_affecting\" : \"editorial_only\",\n      after: createdDetail,\n      actorEmail,\n      reason:\n        status === \"published\"\n          ? \"A new product was published. Browse surfaces refresh and live availability updates where relevant.\"\n          : undefined,\n    }),\n  );\n  if (!invalidation.ok) {\n    console.warn(\n      \"[admin catalog create] storefront invalidation:\",\n      invalidation.error,\n    );\n  }\n\n  const stripeSync = await syncStripeCatalogAfterCreate({\n    productId: result.data.productId,\n    title,\n    description:\n      typeof body.description === \"string\" ? body.description : undefined,\n    handle: typeof body.handle === \"string\" ? body.handle : undefined,\n    pricePhp,\n    correlationId,\n    actorEmail,\n  });\n\n  return correlatedJson(\n    correlationId,\n    {\n      productId: result.data.productId,\n      mutationClassification:\n        status === \"published\" ? \"sellability_affecting\" : \"editorial_only\",\n      storefrontInvalidation: invalidation.ok ? \"ok\" : invalidation.error,\n      stripeCatalogSync: stripeSync,\n    },\n    { status: 201 },\n  );\n}\n\nexport const POST = withAdminMutationIdempotency(\n  \"/admin/catalog/products:POST\",\n  post,\n);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_catalog_productsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_catalog_productsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/catalog/products/{id}:
    delete:
      operationId: delete_admin_catalog_products_id
      summary: "Delete Admin / Catalog / Products / Id"
      description: "Admin / Catalog / Products / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { createMedusaCatalogOperations } from \"@/domain/operations/catalog-operations\";\nimport { upsertEntityWorkflow } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { jsonFromAdminOperationResult } from \"@/lib/staff-api-operation\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { fetchCatalogProductDetail } from \"@/lib/medusa-catalog-service\";\nimport {\n  parseOptionalMatrixCellStocks,\n  parseOptionalStockQuantity,\n  parseOptionalVariantStocks,\n} from \"@/lib/parse-optional-stock-quantity\";\nimport {\n  parseStorefrontMetadataFromBody,\n  parseVariantBarcodeFromBody,\n  catalogProductRequestSchema,\n} from \"@/lib/parse-catalog-product-body\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { parseCatalogOptionArray } from \"@/lib/parse-catalog-option-array\";\nimport { collectCatalogMediaUrlsFromBody } from \"@/lib/catalog-product-media-db\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { ensureExternalCatalogProductMediaRows } from \"@universal-music-store/platform-data\";\nimport {\n  buildStorefrontCommerceInvalidationPayload,\n  classifyCatalogMutation,\n  notifyStorefrontCommerceInvalidation,\n} from \"@/lib/storefront-commerce-invalidation\";\nimport { logAdminCatalogMutationClassified } from \"@/lib/commerce-observability-log\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport {\n  listCatalogProviderProjections,\n  upsertPaymentProviderArtifact,\n  upsertCatalogProviderProjection,\n} from \"@universal-music-store/platform-data\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { catalogSyncIdempotencyKey } from \"@/lib/catalog-sync-idempotency\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function syncStripeCatalogAfterUpdate(input: {\n  productId: string;\n  title: string;\n  description?: string | null;\n  handle?: string | null;\n  pricePhp: number;\n  correlationId: string;\n  actorEmail?: string;\n}) {\n  const sup = adminSupabaseOr503(input.correlationId);\n  if (!(\"client\" in sup)) return { state: \"unavailable\" as const };\n  if (!stripeAvailableForMerchant())\n    return {\n      state: \"unavailable\" as const,\n      reason: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n    };\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken) {\n    return {\n      state: \"pending\" as const,\n      reason: \"MEDUSA_INTERNAL_ADMIN_TOKEN is not configured\",\n    };\n  }\n  const merchantIdentity = input.actorEmail?.trim().toLowerCase();\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    input.actorEmail,\n  );\n  if (!organization)\n    return {\n      state: \"unavailable\" as const,\n      reason: \"ORGANIZATION_NOT_CONFIGURED\",\n    };\n  const { data: nangoConnection } =\n    merchantIdentity && organization\n      ? await sup.client\n          .from(\"payment_nango_connections\")\n          .select(\"nango_connection_id,provider_config_key\")\n          .eq(\"organization_id\", organization.id)\n          .eq(\"provider\", \"stripe\")\n          .eq(\"active\", true)\n          .order(\"updated_at\", { ascending: false })\n          .limit(1)\n          .maybeSingle()\n      : { data: null };\n  const projections = await listCatalogProviderProjections(sup.client, {\n    medusaProductId: input.productId,\n  });\n  const existing = new Map(\n    projections\n      .filter((row) => row.provider === \"stripe\")\n      .map((row) => [row.artifact_type, row]),\n  );\n  const idempotencyKey = catalogSyncIdempotencyKey({\n    productId: input.productId,\n    title: input.title,\n    description: input.description,\n    handle: input.handle,\n    pricePhp: input.pricePhp,\n    operation: \"update\",\n  });\n  try {\n    const response = await medusaAdminFetch(\"/admin/catalog/provider-sync\", {\n      method: \"POST\",\n      headers: { \"x-uvs-internal-token\": internalToken },\n      body: JSON.stringify({\n        productId: input.productId,\n        title: input.title,\n        description: input.description ?? null,\n        handle: input.handle ?? null,\n        amountMinor: Math.round(input.pricePhp * 100),\n        currency: \"PHP\",\n        siteOrigin:\n          process.env.STOREFRONT_PUBLIC_URL ??\n          process.env.NEXT_PUBLIC_SITE_URL ??\n          null,\n        includePaymentLink: true,\n        productExternalId: existing.get(\"product\")?.external_id ?? null,\n        priceExternalId: existing.get(\"price\")?.external_id ?? null,\n        paymentLinkExternalId:\n          existing.get(\"payment_link\")?.external_id ?? null,\n        idempotencyKey,\n        ...(nangoConnection?.nango_connection_id\n          ? {\n              nango_connection_id: nangoConnection.nango_connection_id,\n              nango_provider_config_key: nangoConnection.provider_config_key,\n            }\n          : {}),\n      }),\n    });\n    const payload = (await response.json().catch(() => ({}))) as {\n      data?: {\n        productId?: string;\n        priceId?: string;\n        paymentLinkId?: string;\n        paymentLinkUrl?: string;\n      };\n      error?: string;\n      code?: string;\n    };\n    if (!response.ok || !payload.data?.productId || !payload.data.priceId) {\n      for (const artifactType of [\n        \"product\",\n        \"price\",\n        \"payment_link\",\n      ] as const) {\n        await upsertCatalogProviderProjection(sup.client, {\n          medusa_product_id: input.productId,\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          sync_state: \"failed\",\n          sync_mode: \"automatic\",\n          last_error_code: payload.code ?? \"STRIPE_CATALOG_SYNC_FAILED\",\n          last_error: payload.error ?? \"Stripe catalog synchronization failed\",\n          last_failed_step: artifactType,\n          correlation_id: input.correlationId,\n          idempotency_key: idempotencyKey,\n          updated_by_email: input.actorEmail ?? null,\n        });\n      }\n      return {\n        state: \"failed\" as const,\n        reason: payload.error ?? \"Stripe catalog synchronization failed\",\n      };\n    }\n    const values = {\n      product: { external_id: payload.data.productId },\n      price: { external_id: payload.data.priceId },\n      payment_link: {\n        external_id: payload.data.paymentLinkId ?? null,\n        external_url: payload.data.paymentLinkUrl ?? null,\n      },\n    } as const;\n    for (const artifactType of [\"product\", \"price\", \"payment_link\"] as const) {\n      await upsertCatalogProviderProjection(sup.client, {\n        medusa_product_id: input.productId,\n        provider: \"stripe\",\n        artifact_type: artifactType,\n        ...values[artifactType],\n        sync_state: \"synced\",\n        sync_mode: \"automatic\",\n        last_synced_at: new Date().toISOString(),\n        correlation_id: input.correlationId,\n        idempotency_key: idempotencyKey,\n        updated_by_email: input.actorEmail ?? null,\n      });\n      const externalId = values[artifactType].external_id;\n      if (externalId) {\n        await upsertPaymentProviderArtifact(sup.client, {\n          organization_id: organization.id,\n          merchant_identity:\n            input.actorEmail?.trim().toLowerCase() || \"local-admin\",\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          external_id: externalId,\n          parent_external_id:\n            artifactType === \"price\" ? values.product.external_id : null,\n          status: \"synced\",\n          currency: \"PHP\",\n          amount_minor:\n            artifactType === \"price\" ? Math.round(input.pricePhp * 100) : null,\n          metadata: { medusa_product_id: input.productId },\n          idempotency_key: idempotencyKey,\n        });\n      }\n    }\n    return {\n      state: \"synced\" as const,\n      paymentLinkUrl: payload.data.paymentLinkUrl ?? null,\n    };\n  } catch (error) {\n    return {\n      state: \"failed\" as const,\n      reason: error instanceof Error ? error.message : String(error),\n    };\n  }\n}\n\ntype RouteParams = { params: Promise<{ id: string }> };\n\nasync function patch(req: Request, ctx: RouteParams) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n  const { id: productId } = await ctx.params;\n  if (!productId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing id\" },\n      { status: 400 },\n    );\n  }\n\n  const parsedBody = await parseAdminJson(\n    req,\n    catalogProductRequestSchema,\n    512_000,\n  );\n  if (!parsedBody.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: parsedBody.error },\n      { status: parsedBody.status },\n    );\n  }\n  const body = parsedBody.data;\n  const beforeDetail = await fetchCatalogProductDetail(productId).catch(\n    () => null,\n  );\n  const expectedRevision =\n    typeof body.expected_revision === \"string\"\n      ? body.expected_revision.trim()\n      : \"\";\n  if (expectedRevision && beforeDetail && beforeDetail.revision !== expectedRevision) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Product changed; reload before saving\", code: \"CATALOG_CONFLICT\" },\n      { status: 409 },\n    );\n  }\n  const title = typeof body.title === \"string\" ? body.title : \"\";\n  const handle = typeof body.handle === \"string\" ? body.handle : \"\";\n  const pricePhp = Number(body.pricePhp);\n  const status = body.status === \"published\" ? \"published\" : \"draft\";\n  const categoryIds = Array.isArray(body.categoryIds)\n    ? body.categoryIds.filter(\n        (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n      )\n    : [];\n  const sizeLabelsArr = parseCatalogOptionArray(body.sizeLabels);\n  const colorLabelsArr = parseCatalogOptionArray(body.colorLabels);\n  if (\n    !sizeLabelsArr ||\n    !colorLabelsArr ||\n    sizeLabelsArr.length < 1 ||\n    colorLabelsArr.length < 1\n  ) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Select at least one size and one color.\" },\n      { status: 400 },\n    );\n  }\n\n  const stockParsed = parseOptionalStockQuantity(body);\n  if (!stockParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: stockParsed.error },\n      { status: 400 },\n    );\n  }\n  const variantStocksParsed = parseOptionalVariantStocks(body);\n  if (!variantStocksParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: variantStocksParsed.error },\n      { status: 400 },\n    );\n  }\n  const matrixCellStocksParsed = parseOptionalMatrixCellStocks(body);\n  if (!matrixCellStocksParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: matrixCellStocksParsed.error },\n      { status: 400 },\n    );\n  }\n\n  const storefrontMetadata = parseStorefrontMetadataFromBody(body);\n  const variantBarcode = parseVariantBarcodeFromBody(body);\n\n  const imageUrlsRaw = body.imageUrls;\n  const imageUrls = Array.isArray(imageUrlsRaw)\n    ? imageUrlsRaw\n        .filter(\n          (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n        )\n        .map((s) => s.trim())\n    : undefined;\n\n  const ops = createMedusaCatalogOperations();\n  const result = await ops.updateProduct(productId, {\n    title,\n    handle,\n    description: typeof body.description === \"string\" ? body.description : null,\n    status,\n    pricePhp: Number.isFinite(pricePhp) ? pricePhp : NaN,\n    sku: typeof body.sku === \"string\" ? body.sku : null,\n    imageUrls,\n    thumbnail: typeof body.thumbnail === \"string\" ? body.thumbnail : null,\n    categoryIds,\n    sizeLabels: sizeLabelsArr,\n    colorLabels: colorLabelsArr,\n    stockQuantity: stockParsed.value,\n    variantStocks: variantStocksParsed.value,\n    matrixCellStocks: matrixCellStocksParsed.value,\n    variantBarcode,\n    storefrontMetadata,\n  });\n\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, 502);\n  }\n\n  const actorEmail = session.user.email?.trim();\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"client\" in sup) {\n    await ensureExternalCatalogProductMediaRows(\n      sup.client,\n      collectCatalogMediaUrlsFromBody(body),\n    );\n    if (actorEmail) {\n      const organization = await resolveStaffOrganization(sup.client, actorEmail);\n      await insertStaffAuditLog(sup.client, {\n        actorEmail,\n        action: \"catalog.product.update\",\n        resource: `product:${productId}`,\n        details: { title: title.trim() },\n      });\n      if (organization) {\n        await upsertEntityWorkflow(sup.client, {\n          organizationId: organization.id,\n          entityType: \"catalog_product\",\n          entityId: productId,\n          state: status === \"published\" ? \"published\" : \"draft\",\n          actorEmail,\n        });\n      }\n    }\n  }\n  const afterDetail = await fetchCatalogProductDetail(\n    result.data.productId,\n  ).catch(() => null);\n  const classification = classifyCatalogMutation(beforeDetail, afterDetail);\n  logAdminCatalogMutationClassified({\n    classification,\n    productId: result.data.productId,\n    correlationId,\n    actorEmail,\n  });\n  const invalidation = await notifyStorefrontCommerceInvalidation(\n    buildStorefrontCommerceInvalidationPayload({\n      classification,\n      before: beforeDetail,\n      after: afterDetail,\n      actorEmail,\n      reason:\n        classification === \"checkout_affecting\"\n          ? \"A catalog change affected your order. Review the updated price, availability, and total before continuing.\"\n          : classification === \"sellability_affecting\"\n            ? \"Availability, publish state, or variant options changed for an item in your order. Review before continuing.\"\n            : undefined,\n    }),\n  );\n  if (!invalidation.ok) {\n    console.warn(\n      \"[admin catalog update] storefront invalidation:\",\n      invalidation.error,\n    );\n  }\n\n  const stripeSync = await syncStripeCatalogAfterUpdate({\n    productId: result.data.productId,\n    title,\n    description: typeof body.description === \"string\" ? body.description : null,\n    handle,\n    pricePhp,\n    correlationId,\n    actorEmail,\n  });\n\n  return correlatedJson(correlationId, {\n    productId: result.data.productId,\n    storefrontInvalidation: invalidation.ok ? \"ok\" : invalidation.error,\n    mutationClassification: classification,\n    stripeCatalogSync: stripeSync,\n  });\n}\n\nasync function deleteHandler(req: Request, ctx: RouteParams) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n  const { id: productId } = await ctx.params;\n  if (!productId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing id\" },\n      { status: 400 },\n    );\n  }\n  const beforeDetail = await fetchCatalogProductDetail(productId).catch(\n    () => null,\n  );\n\n  const actorEmail = session.user.email?.trim();\n  let stripeCatalogArchive: { state: string; reason?: string } = {\n    state: \"unavailable\",\n  };\n  const supForProjection = adminSupabaseOr503(correlationId);\n  if (\"client\" in supForProjection) {\n    const projectionRows = await listCatalogProviderProjections(\n      supForProjection.client,\n      { medusaProductId: productId },\n    );\n    const stripe = new Map(\n      projectionRows\n        .filter((row) => row.provider === \"stripe\")\n        .map((row) => [row.artifact_type, row.external_id]),\n    );\n    const hasStripeArtifacts = Boolean(\n      stripe.get(\"product\") ||\n        stripe.get(\"price\") ||\n        stripe.get(\"payment_link\"),\n    );\n    if (hasStripeArtifacts) {\n      const token = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n      const organization = actorEmail\n        ? await resolveStaffOrganization(supForProjection.client, actorEmail)\n        : null;\n      const { data: nangoConnection } = organization\n        ? await supForProjection.client\n            .from(\"payment_nango_connections\")\n            .select(\"nango_connection_id,provider_config_key\")\n            .eq(\"organization_id\", organization.id)\n            .eq(\"provider\", \"stripe\")\n            .eq(\"active\", true)\n            .order(\"updated_at\", { ascending: false })\n            .limit(1)\n            .maybeSingle()\n        : { data: null };\n      if (!token) {\n        return correlatedJson(\n          correlationId,\n          {\n            error: \"Provider-backed products require Stripe archival before deletion\",\n            code: \"CATALOG_PROVIDER_ARCHIVE_UNAVAILABLE\",\n          },\n          { status: 503 },\n        );\n      }\n      const response = await medusaAdminFetch(\"/admin/catalog/provider-sync\", {\n        method: \"DELETE\",\n        headers: { \"x-uvs-internal-token\": token },\n        body: JSON.stringify({\n          productExternalId: stripe.get(\"product\"),\n          priceExternalId: stripe.get(\"price\"),\n          paymentLinkExternalId: stripe.get(\"payment_link\"),\n          ...(nangoConnection?.nango_connection_id\n            ? {\n                nango_connection_id: nangoConnection.nango_connection_id,\n                nango_provider_config_key: nangoConnection.provider_config_key,\n              }\n            : {}),\n        }),\n      });\n      if (!response.ok) {\n        return correlatedJson(\n          correlationId,\n          {\n            error: \"Provider archival failed; product was not deleted\",\n            code: \"CATALOG_PROVIDER_ARCHIVE_FAILED\",\n          },\n          { status: 502 },\n        );\n      }\n      stripeCatalogArchive = { state: \"archived\" };\n      for (const row of projectionRows.filter(\n        (projection) => projection.provider === \"stripe\",\n      )) {\n        await upsertCatalogProviderProjection(supForProjection.client, {\n          medusa_product_id: productId,\n          provider: \"stripe\",\n          artifact_type: row.artifact_type,\n          external_id: row.external_id,\n          external_url: row.external_url,\n          sync_state: \"disabled\",\n          sync_mode: \"disabled\",\n          last_synced_at: new Date().toISOString(),\n          last_error_code: null,\n          last_error: null,\n          last_failed_step: null,\n          correlation_id: correlationId,\n          idempotency_key: row.idempotency_key,\n          updated_by_email: actorEmail ?? null,\n        });\n      }\n    }\n  }\n\n  const ops = createMedusaCatalogOperations();\n  const result = await ops.deleteProduct(productId);\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, 502);\n  }\n\n  if (actorEmail) {\n    const sup = adminSupabaseOr503(correlationId);\n    if (\"client\" in sup) {\n      await insertStaffAuditLog(sup.client, {\n        actorEmail,\n        action: \"catalog.product.delete\",\n        resource: `product:${productId}`,\n        details: {},\n      });\n      await sup.client\n        .from(\"admin_entity_workflow\")\n        .delete()\n        .eq(\"organization_id\", (await resolveStaffOrganization(sup.client, actorEmail))?.id ?? \"\")\n        .eq(\"entity_type\", \"catalog_product\")\n        .eq(\"entity_id\", productId);\n    }\n  }\n  const invalidation = await notifyStorefrontCommerceInvalidation(\n    buildStorefrontCommerceInvalidationPayload({\n      classification:\n        beforeDetail?.status === \"published\"\n          ? \"sellability_affecting\"\n          : \"editorial_only\",\n      before: beforeDetail,\n      actorEmail,\n      reason:\n        beforeDetail?.status === \"published\"\n          ? \"A product was removed from the catalog. Review your order before continuing.\"\n          : undefined,\n    }),\n  );\n  if (!invalidation.ok) {\n    console.warn(\n      \"[admin catalog delete] storefront invalidation:\",\n      invalidation.error,\n    );\n  }\n\n  return correlatedJson(correlationId, {\n    deleted: result.data.deleted,\n    mutationClassification:\n      beforeDetail?.status === \"published\"\n        ? \"sellability_affecting\"\n        : \"editorial_only\",\n    storefrontInvalidation: invalidation.ok ? \"ok\" : invalidation.error,\n    stripeCatalogArchive,\n  });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/catalog/products/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/catalog/products/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_catalog_products_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_catalog_products_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_catalog_products_id
      summary: "Update Admin / Catalog / Products / Id"
      description: "Admin / Catalog / Products / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { createMedusaCatalogOperations } from \"@/domain/operations/catalog-operations\";\nimport { upsertEntityWorkflow } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { jsonFromAdminOperationResult } from \"@/lib/staff-api-operation\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { fetchCatalogProductDetail } from \"@/lib/medusa-catalog-service\";\nimport {\n  parseOptionalMatrixCellStocks,\n  parseOptionalStockQuantity,\n  parseOptionalVariantStocks,\n} from \"@/lib/parse-optional-stock-quantity\";\nimport {\n  parseStorefrontMetadataFromBody,\n  parseVariantBarcodeFromBody,\n  catalogProductRequestSchema,\n} from \"@/lib/parse-catalog-product-body\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { parseCatalogOptionArray } from \"@/lib/parse-catalog-option-array\";\nimport { collectCatalogMediaUrlsFromBody } from \"@/lib/catalog-product-media-db\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { ensureExternalCatalogProductMediaRows } from \"@universal-music-store/platform-data\";\nimport {\n  buildStorefrontCommerceInvalidationPayload,\n  classifyCatalogMutation,\n  notifyStorefrontCommerceInvalidation,\n} from \"@/lib/storefront-commerce-invalidation\";\nimport { logAdminCatalogMutationClassified } from \"@/lib/commerce-observability-log\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport {\n  listCatalogProviderProjections,\n  upsertPaymentProviderArtifact,\n  upsertCatalogProviderProjection,\n} from \"@universal-music-store/platform-data\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { catalogSyncIdempotencyKey } from \"@/lib/catalog-sync-idempotency\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function syncStripeCatalogAfterUpdate(input: {\n  productId: string;\n  title: string;\n  description?: string | null;\n  handle?: string | null;\n  pricePhp: number;\n  correlationId: string;\n  actorEmail?: string;\n}) {\n  const sup = adminSupabaseOr503(input.correlationId);\n  if (!(\"client\" in sup)) return { state: \"unavailable\" as const };\n  if (!stripeAvailableForMerchant())\n    return {\n      state: \"unavailable\" as const,\n      reason: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n    };\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken) {\n    return {\n      state: \"pending\" as const,\n      reason: \"MEDUSA_INTERNAL_ADMIN_TOKEN is not configured\",\n    };\n  }\n  const merchantIdentity = input.actorEmail?.trim().toLowerCase();\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    input.actorEmail,\n  );\n  if (!organization)\n    return {\n      state: \"unavailable\" as const,\n      reason: \"ORGANIZATION_NOT_CONFIGURED\",\n    };\n  const { data: nangoConnection } =\n    merchantIdentity && organization\n      ? await sup.client\n          .from(\"payment_nango_connections\")\n          .select(\"nango_connection_id,provider_config_key\")\n          .eq(\"organization_id\", organization.id)\n          .eq(\"provider\", \"stripe\")\n          .eq(\"active\", true)\n          .order(\"updated_at\", { ascending: false })\n          .limit(1)\n          .maybeSingle()\n      : { data: null };\n  const projections = await listCatalogProviderProjections(sup.client, {\n    medusaProductId: input.productId,\n  });\n  const existing = new Map(\n    projections\n      .filter((row) => row.provider === \"stripe\")\n      .map((row) => [row.artifact_type, row]),\n  );\n  const idempotencyKey = catalogSyncIdempotencyKey({\n    productId: input.productId,\n    title: input.title,\n    description: input.description,\n    handle: input.handle,\n    pricePhp: input.pricePhp,\n    operation: \"update\",\n  });\n  try {\n    const response = await medusaAdminFetch(\"/admin/catalog/provider-sync\", {\n      method: \"POST\",\n      headers: { \"x-uvs-internal-token\": internalToken },\n      body: JSON.stringify({\n        productId: input.productId,\n        title: input.title,\n        description: input.description ?? null,\n        handle: input.handle ?? null,\n        amountMinor: Math.round(input.pricePhp * 100),\n        currency: \"PHP\",\n        siteOrigin:\n          process.env.STOREFRONT_PUBLIC_URL ??\n          process.env.NEXT_PUBLIC_SITE_URL ??\n          null,\n        includePaymentLink: true,\n        productExternalId: existing.get(\"product\")?.external_id ?? null,\n        priceExternalId: existing.get(\"price\")?.external_id ?? null,\n        paymentLinkExternalId:\n          existing.get(\"payment_link\")?.external_id ?? null,\n        idempotencyKey,\n        ...(nangoConnection?.nango_connection_id\n          ? {\n              nango_connection_id: nangoConnection.nango_connection_id,\n              nango_provider_config_key: nangoConnection.provider_config_key,\n            }\n          : {}),\n      }),\n    });\n    const payload = (await response.json().catch(() => ({}))) as {\n      data?: {\n        productId?: string;\n        priceId?: string;\n        paymentLinkId?: string;\n        paymentLinkUrl?: string;\n      };\n      error?: string;\n      code?: string;\n    };\n    if (!response.ok || !payload.data?.productId || !payload.data.priceId) {\n      for (const artifactType of [\n        \"product\",\n        \"price\",\n        \"payment_link\",\n      ] as const) {\n        await upsertCatalogProviderProjection(sup.client, {\n          medusa_product_id: input.productId,\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          sync_state: \"failed\",\n          sync_mode: \"automatic\",\n          last_error_code: payload.code ?? \"STRIPE_CATALOG_SYNC_FAILED\",\n          last_error: payload.error ?? \"Stripe catalog synchronization failed\",\n          last_failed_step: artifactType,\n          correlation_id: input.correlationId,\n          idempotency_key: idempotencyKey,\n          updated_by_email: input.actorEmail ?? null,\n        });\n      }\n      return {\n        state: \"failed\" as const,\n        reason: payload.error ?? \"Stripe catalog synchronization failed\",\n      };\n    }\n    const values = {\n      product: { external_id: payload.data.productId },\n      price: { external_id: payload.data.priceId },\n      payment_link: {\n        external_id: payload.data.paymentLinkId ?? null,\n        external_url: payload.data.paymentLinkUrl ?? null,\n      },\n    } as const;\n    for (const artifactType of [\"product\", \"price\", \"payment_link\"] as const) {\n      await upsertCatalogProviderProjection(sup.client, {\n        medusa_product_id: input.productId,\n        provider: \"stripe\",\n        artifact_type: artifactType,\n        ...values[artifactType],\n        sync_state: \"synced\",\n        sync_mode: \"automatic\",\n        last_synced_at: new Date().toISOString(),\n        correlation_id: input.correlationId,\n        idempotency_key: idempotencyKey,\n        updated_by_email: input.actorEmail ?? null,\n      });\n      const externalId = values[artifactType].external_id;\n      if (externalId) {\n        await upsertPaymentProviderArtifact(sup.client, {\n          organization_id: organization.id,\n          merchant_identity:\n            input.actorEmail?.trim().toLowerCase() || \"local-admin\",\n          provider: \"stripe\",\n          artifact_type: artifactType,\n          external_id: externalId,\n          parent_external_id:\n            artifactType === \"price\" ? values.product.external_id : null,\n          status: \"synced\",\n          currency: \"PHP\",\n          amount_minor:\n            artifactType === \"price\" ? Math.round(input.pricePhp * 100) : null,\n          metadata: { medusa_product_id: input.productId },\n          idempotency_key: idempotencyKey,\n        });\n      }\n    }\n    return {\n      state: \"synced\" as const,\n      paymentLinkUrl: payload.data.paymentLinkUrl ?? null,\n    };\n  } catch (error) {\n    return {\n      state: \"failed\" as const,\n      reason: error instanceof Error ? error.message : String(error),\n    };\n  }\n}\n\ntype RouteParams = { params: Promise<{ id: string }> };\n\nasync function patch(req: Request, ctx: RouteParams) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n  const { id: productId } = await ctx.params;\n  if (!productId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing id\" },\n      { status: 400 },\n    );\n  }\n\n  const parsedBody = await parseAdminJson(\n    req,\n    catalogProductRequestSchema,\n    512_000,\n  );\n  if (!parsedBody.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: parsedBody.error },\n      { status: parsedBody.status },\n    );\n  }\n  const body = parsedBody.data;\n  const beforeDetail = await fetchCatalogProductDetail(productId).catch(\n    () => null,\n  );\n  const expectedRevision =\n    typeof body.expected_revision === \"string\"\n      ? body.expected_revision.trim()\n      : \"\";\n  if (expectedRevision && beforeDetail && beforeDetail.revision !== expectedRevision) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Product changed; reload before saving\", code: \"CATALOG_CONFLICT\" },\n      { status: 409 },\n    );\n  }\n  const title = typeof body.title === \"string\" ? body.title : \"\";\n  const handle = typeof body.handle === \"string\" ? body.handle : \"\";\n  const pricePhp = Number(body.pricePhp);\n  const status = body.status === \"published\" ? \"published\" : \"draft\";\n  const categoryIds = Array.isArray(body.categoryIds)\n    ? body.categoryIds.filter(\n        (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n      )\n    : [];\n  const sizeLabelsArr = parseCatalogOptionArray(body.sizeLabels);\n  const colorLabelsArr = parseCatalogOptionArray(body.colorLabels);\n  if (\n    !sizeLabelsArr ||\n    !colorLabelsArr ||\n    sizeLabelsArr.length < 1 ||\n    colorLabelsArr.length < 1\n  ) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Select at least one size and one color.\" },\n      { status: 400 },\n    );\n  }\n\n  const stockParsed = parseOptionalStockQuantity(body);\n  if (!stockParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: stockParsed.error },\n      { status: 400 },\n    );\n  }\n  const variantStocksParsed = parseOptionalVariantStocks(body);\n  if (!variantStocksParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: variantStocksParsed.error },\n      { status: 400 },\n    );\n  }\n  const matrixCellStocksParsed = parseOptionalMatrixCellStocks(body);\n  if (!matrixCellStocksParsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: matrixCellStocksParsed.error },\n      { status: 400 },\n    );\n  }\n\n  const storefrontMetadata = parseStorefrontMetadataFromBody(body);\n  const variantBarcode = parseVariantBarcodeFromBody(body);\n\n  const imageUrlsRaw = body.imageUrls;\n  const imageUrls = Array.isArray(imageUrlsRaw)\n    ? imageUrlsRaw\n        .filter(\n          (x): x is string => typeof x === \"string\" && x.trim().length > 0,\n        )\n        .map((s) => s.trim())\n    : undefined;\n\n  const ops = createMedusaCatalogOperations();\n  const result = await ops.updateProduct(productId, {\n    title,\n    handle,\n    description: typeof body.description === \"string\" ? body.description : null,\n    status,\n    pricePhp: Number.isFinite(pricePhp) ? pricePhp : NaN,\n    sku: typeof body.sku === \"string\" ? body.sku : null,\n    imageUrls,\n    thumbnail: typeof body.thumbnail === \"string\" ? body.thumbnail : null,\n    categoryIds,\n    sizeLabels: sizeLabelsArr,\n    colorLabels: colorLabelsArr,\n    stockQuantity: stockParsed.value,\n    variantStocks: variantStocksParsed.value,\n    matrixCellStocks: matrixCellStocksParsed.value,\n    variantBarcode,\n    storefrontMetadata,\n  });\n\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, 502);\n  }\n\n  const actorEmail = session.user.email?.trim();\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"client\" in sup) {\n    await ensureExternalCatalogProductMediaRows(\n      sup.client,\n      collectCatalogMediaUrlsFromBody(body),\n    );\n    if (actorEmail) {\n      const organization = await resolveStaffOrganization(sup.client, actorEmail);\n      await insertStaffAuditLog(sup.client, {\n        actorEmail,\n        action: \"catalog.product.update\",\n        resource: `product:${productId}`,\n        details: { title: title.trim() },\n      });\n      if (organization) {\n        await upsertEntityWorkflow(sup.client, {\n          organizationId: organization.id,\n          entityType: \"catalog_product\",\n          entityId: productId,\n          state: status === \"published\" ? \"published\" : \"draft\",\n          actorEmail,\n        });\n      }\n    }\n  }\n  const afterDetail = await fetchCatalogProductDetail(\n    result.data.productId,\n  ).catch(() => null);\n  const classification = classifyCatalogMutation(beforeDetail, afterDetail);\n  logAdminCatalogMutationClassified({\n    classification,\n    productId: result.data.productId,\n    correlationId,\n    actorEmail,\n  });\n  const invalidation = await notifyStorefrontCommerceInvalidation(\n    buildStorefrontCommerceInvalidationPayload({\n      classification,\n      before: beforeDetail,\n      after: afterDetail,\n      actorEmail,\n      reason:\n        classification === \"checkout_affecting\"\n          ? \"A catalog change affected your order. Review the updated price, availability, and total before continuing.\"\n          : classification === \"sellability_affecting\"\n            ? \"Availability, publish state, or variant options changed for an item in your order. Review before continuing.\"\n            : undefined,\n    }),\n  );\n  if (!invalidation.ok) {\n    console.warn(\n      \"[admin catalog update] storefront invalidation:\",\n      invalidation.error,\n    );\n  }\n\n  const stripeSync = await syncStripeCatalogAfterUpdate({\n    productId: result.data.productId,\n    title,\n    description: typeof body.description === \"string\" ? body.description : null,\n    handle,\n    pricePhp,\n    correlationId,\n    actorEmail,\n  });\n\n  return correlatedJson(correlationId, {\n    productId: result.data.productId,\n    storefrontInvalidation: invalidation.ok ? \"ok\" : invalidation.error,\n    mutationClassification: classification,\n    stripeCatalogSync: stripeSync,\n  });\n}\n\nasync function deleteHandler(req: Request, ctx: RouteParams) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n  if (!staffSessionAllows(session, \"catalog:write\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n  const { id: productId } = await ctx.params;\n  if (!productId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing id\" },\n      { status: 400 },\n    );\n  }\n  const beforeDetail = await fetchCatalogProductDetail(productId).catch(\n    () => null,\n  );\n\n  const actorEmail = session.user.email?.trim();\n  let stripeCatalogArchive: { state: string; reason?: string } = {\n    state: \"unavailable\",\n  };\n  const supForProjection = adminSupabaseOr503(correlationId);\n  if (\"client\" in supForProjection) {\n    const projectionRows = await listCatalogProviderProjections(\n      supForProjection.client,\n      { medusaProductId: productId },\n    );\n    const stripe = new Map(\n      projectionRows\n        .filter((row) => row.provider === \"stripe\")\n        .map((row) => [row.artifact_type, row.external_id]),\n    );\n    const hasStripeArtifacts = Boolean(\n      stripe.get(\"product\") ||\n        stripe.get(\"price\") ||\n        stripe.get(\"payment_link\"),\n    );\n    if (hasStripeArtifacts) {\n      const token = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n      const organization = actorEmail\n        ? await resolveStaffOrganization(supForProjection.client, actorEmail)\n        : null;\n      const { data: nangoConnection } = organization\n        ? await supForProjection.client\n            .from(\"payment_nango_connections\")\n            .select(\"nango_connection_id,provider_config_key\")\n            .eq(\"organization_id\", organization.id)\n            .eq(\"provider\", \"stripe\")\n            .eq(\"active\", true)\n            .order(\"updated_at\", { ascending: false })\n            .limit(1)\n            .maybeSingle()\n        : { data: null };\n      if (!token) {\n        return correlatedJson(\n          correlationId,\n          {\n            error: \"Provider-backed products require Stripe archival before deletion\",\n            code: \"CATALOG_PROVIDER_ARCHIVE_UNAVAILABLE\",\n          },\n          { status: 503 },\n        );\n      }\n      const response = await medusaAdminFetch(\"/admin/catalog/provider-sync\", {\n        method: \"DELETE\",\n        headers: { \"x-uvs-internal-token\": token },\n        body: JSON.stringify({\n          productExternalId: stripe.get(\"product\"),\n          priceExternalId: stripe.get(\"price\"),\n          paymentLinkExternalId: stripe.get(\"payment_link\"),\n          ...(nangoConnection?.nango_connection_id\n            ? {\n                nango_connection_id: nangoConnection.nango_connection_id,\n                nango_provider_config_key: nangoConnection.provider_config_key,\n              }\n            : {}),\n        }),\n      });\n      if (!response.ok) {\n        return correlatedJson(\n          correlationId,\n          {\n            error: \"Provider archival failed; product was not deleted\",\n            code: \"CATALOG_PROVIDER_ARCHIVE_FAILED\",\n          },\n          { status: 502 },\n        );\n      }\n      stripeCatalogArchive = { state: \"archived\" };\n      for (const row of projectionRows.filter(\n        (projection) => projection.provider === \"stripe\",\n      )) {\n        await upsertCatalogProviderProjection(supForProjection.client, {\n          medusa_product_id: productId,\n          provider: \"stripe\",\n          artifact_type: row.artifact_type,\n          external_id: row.external_id,\n          external_url: row.external_url,\n          sync_state: \"disabled\",\n          sync_mode: \"disabled\",\n          last_synced_at: new Date().toISOString(),\n          last_error_code: null,\n          last_error: null,\n          last_failed_step: null,\n          correlation_id: correlationId,\n          idempotency_key: row.idempotency_key,\n          updated_by_email: actorEmail ?? null,\n        });\n      }\n    }\n  }\n\n  const ops = createMedusaCatalogOperations();\n  const result = await ops.deleteProduct(productId);\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, 502);\n  }\n\n  if (actorEmail) {\n    const sup = adminSupabaseOr503(correlationId);\n    if (\"client\" in sup) {\n      await insertStaffAuditLog(sup.client, {\n        actorEmail,\n        action: \"catalog.product.delete\",\n        resource: `product:${productId}`,\n        details: {},\n      });\n      await sup.client\n        .from(\"admin_entity_workflow\")\n        .delete()\n        .eq(\"organization_id\", (await resolveStaffOrganization(sup.client, actorEmail))?.id ?? \"\")\n        .eq(\"entity_type\", \"catalog_product\")\n        .eq(\"entity_id\", productId);\n    }\n  }\n  const invalidation = await notifyStorefrontCommerceInvalidation(\n    buildStorefrontCommerceInvalidationPayload({\n      classification:\n        beforeDetail?.status === \"published\"\n          ? \"sellability_affecting\"\n          : \"editorial_only\",\n      before: beforeDetail,\n      actorEmail,\n      reason:\n        beforeDetail?.status === \"published\"\n          ? \"A product was removed from the catalog. Review your order before continuing.\"\n          : undefined,\n    }),\n  );\n  if (!invalidation.ok) {\n    console.warn(\n      \"[admin catalog delete] storefront invalidation:\",\n      invalidation.error,\n    );\n  }\n\n  return correlatedJson(correlationId, {\n    deleted: result.data.deleted,\n    mutationClassification:\n      beforeDetail?.status === \"published\"\n        ? \"sellability_affecting\"\n        : \"editorial_only\",\n    storefrontInvalidation: invalidation.ok ? \"ok\" : invalidation.error,\n    stripeCatalogArchive,\n  });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/catalog/products/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/catalog/products/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_catalog_products_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_catalog_products_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/catalog/products/suggestions:
    get:
      operationId: get_admin_catalog_products_suggestions
      summary: "Read Admin / Catalog / Products / Suggestions"
      description: "Admin / Catalog / Products / Suggestions. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Catalog"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (\n    !staffSessionAllows(session, \"catalog:read\") &&\n    !staffSessionAllows(session, \"catalog:write\")\n  ) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const q = (req.nextUrl.searchParams.get(\"q\") ?? \"\").trim().slice(0, 120);\n  const path = `/admin/products?limit=30${q ? `&q=${encodeURIComponent(q)}` : \"\"}`;\n  try {\n    const res = await medusaAdminFetch(path);\n    const json = (await res.json()) as {\n      products?: Array<{\n        id?: string;\n        title?: string;\n        handle?: string;\n      }>;\n      message?: string;\n    };\n    if (!res.ok) {\n      return correlatedJson(\n        correlationId,\n        { error: json.message ?? res.statusText },\n        { status: res.status },\n      );\n    }\n    const items = (json.products ?? [])\n      .map((p) => ({\n        id: String(p.id ?? \"\"),\n        title: String(p.title ?? \"\").trim() || \"(untitled)\",\n        handle: String(p.handle ?? \"\").trim(),\n      }))\n      .filter((p) => p.id && p.handle);\n    return correlatedJson(correlationId, { items });\n  } catch (e) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          e instanceof Error ? e.message : \"Store catalog request unavailable\",\n      },\n      { status: 502 },\n    );\n  }\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_catalog_products_suggestionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/channels/events:
    get:
      operationId: get_admin_channels_events
      summary: "Read Admin / Channels / Events"
      description: "Admin / Channels / Events. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Channels"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"channels:manage\");\n  if (!staff.ok) return staff.response;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const rawLimit = Number(new URL(req.url).searchParams.get(\"limit\") ?? 80);\n  const limit = Math.min(Math.max(Number.isFinite(rawLimit) ? Math.floor(rawLimit) : 80, 1), 100);\n  const { data, error } = await sup.client.from(\"channel_sync_events\").select(\"id, channel, event_type, received_at, processed_at\").order(\"received_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load channel events\" }, { status: 502 });\n  return correlatedJson(correlationId, { events: data ?? [] });\n}\n"
      x-permission: "channels:manage"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_channels_eventsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/channels/events/{id}/process:
    post:
      operationId: post_admin_channels_events_id_process
      summary: "Create or execute Admin / Channels / Events / Id / Process"
      description: "Admin / Channels / Events / Id / Process. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Channels"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nasync function post(req: Request, { params }: { params: Promise<{ id: string }> }) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"channels:manage\");\n  if (!staff.ok) return staff.response;\n  const { id } = await params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const processedAt = new Date().toISOString();\n  const { data, error } = await sup.client.from(\"channel_sync_events\").update({ processed_at: processedAt }).eq(\"id\", id).is(\"processed_at\", null).select(\"id, processed_at\").maybeSingle();\n  if (error) return correlatedJson(correlationId, { error: \"Unable to process channel event\" }, { status: 502 });\n  if (!data) return correlatedJson(correlationId, { error: \"Event is unavailable or already processed\" }, { status: 409 });\n  await insertStaffAuditLog(sup.client, { actorEmail: staff.session.user?.email ?? \"local-admin@localhost\", action: \"channel_event.process\", resource: \"channel_sync_event\", resourceId: id, details: { processed_at: processedAt } });\n  return correlatedJson(correlationId, { event: data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/channels/events/[id]/process:POST\", post);\n"
      x-permission: "channels:manage"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_channels_events_id_processRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_channels_events_id_processResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/chat-orders/{id}/settle:
    post:
      operationId: post_admin_chat_orders_id_settle
      summary: "Create or execute Admin / Chat Orders / Id / Settle"
      description: "Admin / Chat Orders / Id / Settle. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Chat Orders"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst schema = z.object({\n  provider: z.enum([\"stripe\", \"paypal\", \"xendit\"]),\n  payment_external_id: z.string().trim().min(1).max(255),\n  payment_request_id: z.string().trim().min(8).max(160).optional(),\n  action: z.enum([\"retrieve\", \"capture\"]).default(\"capture\"),\n}).strict();\n\nfunction settled(provider: string, status: string | undefined) {\n  const value = String(status ?? \"\").toLowerCase();\n  return provider === \"stripe\" ? value === \"succeeded\" : [\"completed\", \"succeeded\", \"paid\", \"captured\"].includes(value);\n}\n\nexport async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"chat_orders:manage\");\n  if (!staff.ok) return staff.response;\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const { id } = await params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `chat-order.settle:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const current = await sup.client.from(\"chat_order_intake\").select(\"id,status,payment_provider,payment_external_id,payment_status\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (current.error || !current.data) {\n    const body = { error: \"Chat order not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  if ([\"completed\", \"cancelled\"].includes(String(current.data.status))) {\n    const body = { error: \"Chat order is not available for settlement\", code: \"CHAT_ORDER_TERMINAL\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const connection = await sup.client.from(\"payment_nango_connections\").select(\"nango_connection_id,provider_config_key\").eq(\"organization_id\", organization.id).eq(\"provider\", parsed.data.provider).eq(\"active\", true).order(\"updated_at\", { ascending: false }).limit(1).maybeSingle();\n  if (connection.error || !connection.data?.nango_connection_id) {\n    const body = { error: `${parsed.data.provider} is not connected for this organization`, code: \"PAYMENT_CONNECTION_REQUIRED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken) return correlatedJson(correlationId, { error: \"Payment operations are not configured\" }, { status: 503 });\n  const operation = parsed.data.provider === \"stripe\"\n    ? { operation: \"payment\", action: parsed.data.action, payment_id: parsed.data.payment_external_id, idempotency_key: idempotencyKey }\n    : parsed.data.provider === \"paypal\"\n      ? { operation: \"payment\", action: parsed.data.action, order_id: parsed.data.payment_external_id, idempotency_key: idempotencyKey }\n      : { operation: \"payment\", action: parsed.data.action, payment_request_id: parsed.data.payment_request_id ?? parsed.data.payment_external_id, payment_id: parsed.data.payment_external_id, idempotency_key: idempotencyKey };\n  const response = await medusaAdminFetch(`/admin/payment-provider/${parsed.data.provider}`, { method: \"POST\", headers: { \"x-uvs-internal-token\": internalToken, \"x-nango-connection-id\": connection.data.nango_connection_id, ...(connection.data.provider_config_key ? { \"x-nango-provider-config-key\": connection.data.provider_config_key } : {}) }, body: JSON.stringify(operation) });\n  const providerBody = await response.json().catch(() => ({})) as { data?: { id?: string; status?: string; payment_request_id?: string }; error?: string };\n  if (!response.ok) {\n    const body = { error: providerBody.error ?? \"Provider settlement failed\", code: \"PAYMENT_SETTLEMENT_FAILED\" };\n    await sup.client.from(\"chat_order_intake\").update({ payment_provider: parsed.data.provider, payment_external_id: parsed.data.payment_external_id, payment_status: \"failed\", payment_last_error: body.error }).eq(\"id\", id).eq(\"organization_id\", organization.id);\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n  const status = String(providerBody.data?.status ?? \"\").toLowerCase();\n  const nextPaymentStatus = settled(parsed.data.provider, status) ? \"settled\" : status || \"pending\";\n  const update = await sup.client.from(\"chat_order_intake\").update({ payment_provider: parsed.data.provider, payment_external_id: parsed.data.payment_external_id, payment_status: nextPaymentStatus, payment_settled_at: nextPaymentStatus === \"settled\" ? new Date().toISOString() : null, payment_last_error: null, status: nextPaymentStatus === \"settled\" ? \"processing\" : current.data.status }).eq(\"id\", id).eq(\"organization_id\", organization.id).select(\"id,status,updated_at,payment_provider,payment_external_id,payment_status,payment_settled_at\").single();\n  if (update.error || !update.data) {\n    const body = { error: \"Payment settled but chat order state could not be recorded\", code: \"PAYMENT_STATE_PERSIST_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"chat_order.payment_settle\", resource: \"chat_order_intake\", resourceId: id, details: { organization_id: organization.id, provider: parsed.data.provider, external_id: parsed.data.payment_external_id, status: nextPaymentStatus } });\n  const body = { data: update.data, provider: providerBody.data ?? null };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "chat_orders:manage"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_chat_orders_id_settleRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_chat_orders_id_settleResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/chat-orders/{id}/status:
    post:
      operationId: post_admin_chat_orders_id_status
      summary: "Create or execute Admin / Chat Orders / Id / Status"
      description: "Admin / Chat Orders / Id / Status. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Chat Orders"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { getMedusaAdminSdk } from \"@/lib/medusa-pos\";\nimport {\n  chatOrderCompletionStatus,\n  isAllowedChatOrderTransition,\n} from \"@/lib/chat-intake-bridge\";\n\nconst schema = z\n  .object({\n    status: z.enum([\n      \"pending\",\n      \"draft_created\",\n      \"processing\",\n      \"pending_payment\",\n      \"completed\",\n      \"failed\",\n      \"cancelled\",\n    ]),\n    expected_updated_at: z.string().datetime({ offset: true }).optional(),\n  })\n  .strict();\n\nasync function post(\n  req: Request,\n  { params }: { params: Promise<{ id: string }> },\n) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"chat_orders:manage\");\n  if (!staff.ok) return staff.response;\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const { id } = await params;\n  if (!id?.trim())\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid chat order\" },\n      { status: 400 },\n    );\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const current = await sup.client\n    .from(\"chat_order_intake\")\n    .select(\n      \"id,status,updated_at,medusa_draft_order_id,medusa_order_id,medusa_order_display_id,medusa_order_payment_status,payment_status\",\n    )\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .maybeSingle();\n  if (current.error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to load chat order\" },\n      { status: 502 },\n    );\n  if (!current.data)\n    return correlatedJson(\n      correlationId,\n      { error: \"Chat order not found\" },\n      { status: 404 },\n    );\n  const currentStatus = String(current.data.status ?? \"pending\");\n  if (currentStatus === parsed.data.status) {\n    return correlatedJson(correlationId, { data: current.data, replayed: true });\n  }\n  if (parsed.data.status === \"completed\" && current.data.medusa_order_id) {\n    return correlatedJson(correlationId, {\n      data: current.data,\n      replayed: true,\n    });\n  }\n  if (!isAllowedChatOrderTransition(currentStatus, parsed.data.status)) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid chat order transition\", code: \"WORKFLOW_TRANSITION\" },\n      { status: 409 },\n    );\n  }\n  if (\n    parsed.data.expected_updated_at &&\n    current.data.updated_at !== parsed.data.expected_updated_at\n  ) {\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Chat order changed; reload before updating\",\n        code: \"WORKFLOW_CONFLICT\",\n      },\n      { status: 409 },\n    );\n  }\n\n  if (parsed.data.status === \"completed\") {\n    const medusaPaymentStatus = String(current.data.medusa_order_payment_status ?? \"\").toLowerCase();\n    if (current.data.payment_status !== \"settled\" && ![\"captured\", \"partially_captured\", \"paid\"].includes(medusaPaymentStatus)) {\n      return correlatedJson(correlationId, { error: \"Settle the provider payment before completing this chat order\", code: \"PAYMENT_SETTLEMENT_REQUIRED\" }, { status: 409 });\n    }\n    const draftOrderId =\n      typeof current.data.medusa_draft_order_id === \"string\"\n        ? current.data.medusa_draft_order_id.trim()\n        : \"\";\n    if (!draftOrderId) {\n      return correlatedJson(\n        correlationId,\n        {\n          error: \"Chat order has no Medusa draft order to complete\",\n          code: \"BAD_REQUEST\",\n        },\n        { status: 400 },\n      );\n    }\n    let claimed = sup.client\n      .from(\"chat_order_intake\")\n      .update({ status: \"processing\" })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id)\n      .eq(\"status\", currentStatus)\n      .is(\"medusa_order_id\", null);\n    if (parsed.data.expected_updated_at)\n      claimed = claimed.eq(\"updated_at\", parsed.data.expected_updated_at);\n    const claim = await claimed\n      .select(\n        \"id,status,updated_at,medusa_draft_order_id,medusa_order_id,medusa_order_display_id\",\n      )\n      .maybeSingle();\n    if (claim.error)\n      return correlatedJson(\n        correlationId,\n        { error: \"Unable to update chat order\" },\n        { status: 502 },\n      );\n    if (!claim.data)\n      return correlatedJson(\n        correlationId,\n        {\n          error: \"Chat order changed; reload before updating\",\n          code: \"WORKFLOW_CONFLICT\",\n        },\n        { status: 409 },\n      );\n\n    const adminSdk = getMedusaAdminSdk();\n    if (!adminSdk) {\n      await sup.client\n        .from(\"chat_order_intake\")\n        .update({ status: currentStatus })\n        .eq(\"id\", id)\n        .eq(\"organization_id\", organization.id)\n        .is(\"medusa_order_id\", null);\n      return correlatedJson(\n        correlationId,\n        {\n          error: \"Medusa admin SDK is not configured\",\n          code: \"MEDUSA_UNAVAILABLE\",\n        },\n        { status: 503 },\n      );\n    }\n\n    let order:\n      | {\n          id?: unknown;\n          display_id?: unknown;\n          payment_status?: unknown;\n        }\n      | undefined;\n    try {\n      ({ order } = await adminSdk.admin.draftOrder.convertToOrder(draftOrderId));\n    } catch {\n      await sup.client\n        .from(\"chat_order_intake\")\n        .update({ status: currentStatus })\n        .eq(\"id\", id)\n        .eq(\"organization_id\", organization.id)\n        .is(\"medusa_order_id\", null);\n      return correlatedJson(\n        correlationId,\n        { error: \"Unable to convert Medusa draft order\", code: \"MEDUSA_UNAVAILABLE\" },\n        { status: 502 },\n      );\n    }\n    const medusaOrderId =\n      order?.id != null && String(order.id).trim() ? String(order.id) : \"\";\n    if (!medusaOrderId) {\n      await sup.client\n        .from(\"chat_order_intake\")\n        .update({ status: currentStatus })\n        .eq(\"id\", id)\n        .eq(\"organization_id\", organization.id)\n        .is(\"medusa_order_id\", null);\n      return correlatedJson(\n        correlationId,\n        {\n          error: \"Converted draft order missing id from Medusa\",\n          code: \"MEDUSA_UNAVAILABLE\",\n        },\n        { status: 502 },\n      );\n    }\n\n    const nextStatus = chatOrderCompletionStatus(order?.payment_status);\n    const completed = await sup.client\n      .from(\"chat_order_intake\")\n      .update({\n        status: nextStatus,\n        medusa_order_id: medusaOrderId,\n        medusa_order_display_id:\n          order?.display_id != null ? String(order.display_id) : null,\n        medusa_order_payment_status:\n          order?.payment_status != null ? String(order.payment_status) : null,\n      })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id)\n      .is(\"medusa_order_id\", null)\n      .select(\n        \"id,status,updated_at,medusa_draft_order_id,medusa_order_id,medusa_order_display_id,medusa_order_payment_status\",\n      )\n      .maybeSingle();\n    if (completed.error || !completed.data)\n      return correlatedJson(\n        correlationId,\n        { error: \"Unable to persist completed chat order\" },\n        { status: 502 },\n      );\n\n    await insertStaffAuditLog(sup.client, {\n      actorEmail: staff.session.user?.email ?? \"local-admin@localhost\",\n      action: \"chat_order.complete_draft\",\n      resource: \"chat_order_intake\",\n      resourceId: id,\n      details: {\n        organization_id: organization.id,\n        medusa_draft_order_id: draftOrderId,\n        medusa_order_id: medusaOrderId,\n        medusa_order_payment_status: completed.data.medusa_order_payment_status,\n        status: completed.data.status,\n      },\n      before: current.data,\n      after: completed.data,\n    });\n    return correlatedJson(correlationId, { data: completed.data });\n  }\n\n  let update = sup.client\n    .from(\"chat_order_intake\")\n    .update({ status: parsed.data.status })\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id);\n  if (parsed.data.expected_updated_at)\n    update = update.eq(\"updated_at\", parsed.data.expected_updated_at);\n  const { data, error } = await update\n    .select(\"id,status,updated_at\")\n    .maybeSingle();\n  if (error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to update chat order\" },\n      { status: 502 },\n    );\n  if (!data)\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Chat order changed; reload before updating\",\n        code: \"WORKFLOW_CONFLICT\",\n      },\n      { status: 409 },\n    );\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staff.session.user?.email ?? \"local-admin@localhost\",\n    action: \"chat_order.status\",\n    resource: \"chat_order_intake\",\n    resourceId: id,\n    details: { status: parsed.data.status, organization_id: organization.id },\n  });\n  return correlatedJson(correlationId, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/chat-orders/[id]/status:POST\", post);\n"
      x-permission: "chat_orders:manage"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_chat_orders_id_statusRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_chat_orders_id_statusResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/chat-orders/variant-suggestions:
    get:
      operationId: get_admin_chat_orders_variant_suggestions
      summary: "Read Admin / Chat Orders / Variant Suggestions"
      description: "Admin / Chat Orders / Variant Suggestions. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Chat Orders"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { searchCatalogVariantLines } from \"@/lib/chat-order-catalog-search\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"chat_orders:manage\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const q = req.nextUrl.searchParams.get(\"q\") ?? \"\";\n  const lines = await searchCatalogVariantLines(q, 12);\n  return correlatedJson(correlationId, { lines });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_chat_orders_variant_suggestionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/announcement:
    delete:
      operationId: delete_admin_cms_announcement
      summary: "Delete Admin / Cms / Announcement"
      description: "Admin / Cms / Announcement. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsAnnouncement,\n  getCmsAnnouncementAnalyticsMap,\n  listCmsAnnouncementsAdmin,\n  upsertCmsAnnouncement,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsAnnouncementSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const [rows, analyticsMap] = await Promise.all([\n    listCmsAnnouncementsAdmin(sup.client, organization.id),\n    getCmsAnnouncementAnalyticsMap(sup.client, organization.id),\n  ]);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsAnnouncementSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid announcement payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  try {\n    await upsertCmsAnnouncement(sup.client, { ...parsed.data, organization_id: organization.id });\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to save\" },\n      { status: 500 },\n    );\n  }\n  const rows = await listCmsAnnouncementsAdmin(sup.client, organization.id);\n  const analyticsMap = await getCmsAnnouncementAnalyticsMap(sup.client, organization.id);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function deleteHandler(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const id = req.nextUrl.searchParams.get(\"id\")?.trim();\n  const locale = req.nextUrl.searchParams.get(\"locale\")?.trim() || \"en\";\n  if (!id) {\n    return correlatedJson(cid, { error: \"Missing id\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsAnnouncement(sup.client, id, locale, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/announcement:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/announcement:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_announcementRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_announcementResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_cms_announcement
      summary: "Read Admin / Cms / Announcement"
      description: "Admin / Cms / Announcement. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsAnnouncement,\n  getCmsAnnouncementAnalyticsMap,\n  listCmsAnnouncementsAdmin,\n  upsertCmsAnnouncement,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsAnnouncementSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const [rows, analyticsMap] = await Promise.all([\n    listCmsAnnouncementsAdmin(sup.client, organization.id),\n    getCmsAnnouncementAnalyticsMap(sup.client, organization.id),\n  ]);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsAnnouncementSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid announcement payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  try {\n    await upsertCmsAnnouncement(sup.client, { ...parsed.data, organization_id: organization.id });\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to save\" },\n      { status: 500 },\n    );\n  }\n  const rows = await listCmsAnnouncementsAdmin(sup.client, organization.id);\n  const analyticsMap = await getCmsAnnouncementAnalyticsMap(sup.client, organization.id);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function deleteHandler(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const id = req.nextUrl.searchParams.get(\"id\")?.trim();\n  const locale = req.nextUrl.searchParams.get(\"locale\")?.trim() || \"en\";\n  if (!id) {\n    return correlatedJson(cid, { error: \"Missing id\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsAnnouncement(sup.client, id, locale, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/announcement:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/announcement:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_announcementResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_announcement
      summary: "Update Admin / Cms / Announcement"
      description: "Admin / Cms / Announcement. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsAnnouncement,\n  getCmsAnnouncementAnalyticsMap,\n  listCmsAnnouncementsAdmin,\n  upsertCmsAnnouncement,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsAnnouncementSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const [rows, analyticsMap] = await Promise.all([\n    listCmsAnnouncementsAdmin(sup.client, organization.id),\n    getCmsAnnouncementAnalyticsMap(sup.client, organization.id),\n  ]);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsAnnouncementSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid announcement payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  try {\n    await upsertCmsAnnouncement(sup.client, { ...parsed.data, organization_id: organization.id });\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to save\" },\n      { status: 500 },\n    );\n  }\n  const rows = await listCmsAnnouncementsAdmin(sup.client, organization.id);\n  const analyticsMap = await getCmsAnnouncementAnalyticsMap(sup.client, organization.id);\n  const analytics: Record<string, { impressions: number; clicks: number; dismisses: number }> = {};\n  for (const [k, v] of analyticsMap) {\n    analytics[k] = {\n      impressions: v.impressions,\n      clicks: v.clicks,\n      dismisses: v.dismisses,\n    };\n  }\n  return correlatedJson(cid, { data: { rows, analytics } });\n}\n\nasync function deleteHandler(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const id = req.nextUrl.searchParams.get(\"id\")?.trim();\n  const locale = req.nextUrl.searchParams.get(\"locale\")?.trim() || \"en\";\n  if (!id) {\n    return correlatedJson(cid, { error: \"Missing id\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsAnnouncement(sup.client, id, locale, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/announcement:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/announcement:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_announcementRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_announcementResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/block-presets:
    get:
      operationId: get_admin_cms_block_presets
      summary: "Read Admin / Cms / Block Presets"
      description: "Admin / Cms / Block Presets. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport type { CmsBlock } from \"@universal-music-store/platform-data\";\nimport {\n  insertCmsPageBlockPreset,\n  listCmsPageBlockPresets,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { cmsPresetSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsPageBlockPresets(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsPresetSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid preset payload\" }, { status: 400 });\n  const { name, blocks } = parsed.data as { name: string; blocks: CmsBlock[] };\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const row = await insertCmsPageBlockPreset(sup.client, {\n    name,\n    blocks,\n    organizationId: organization.id,\n  });\n  if (!row) {\n    return correlatedJson(cid, { error: \"Unable to save preset\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/block-presets:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_block_presetsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_block_presets
      summary: "Create or execute Admin / Cms / Block Presets"
      description: "Admin / Cms / Block Presets. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport type { CmsBlock } from \"@universal-music-store/platform-data\";\nimport {\n  insertCmsPageBlockPreset,\n  listCmsPageBlockPresets,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { cmsPresetSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsPageBlockPresets(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsPresetSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid preset payload\" }, { status: 400 });\n  const { name, blocks } = parsed.data as { name: string; blocks: CmsBlock[] };\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const row = await insertCmsPageBlockPreset(sup.client, {\n    name,\n    blocks,\n    organizationId: organization.id,\n  });\n  if (!row) {\n    return correlatedJson(cid, { error: \"Unable to save preset\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/block-presets:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_block_presetsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_block_presetsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/block-presets/{id}:
    delete:
      operationId: delete_admin_cms_block_presets_id
      summary: "Delete Admin / Cms / Block Presets / Id"
      description: "Admin / Cms / Block Presets / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { deleteCmsPageBlockPreset } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nasync function deleteHandler(_req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(_req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsPageBlockPreset(sup.client, id, organization.id);\n  if (!ok) {\n    return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/block-presets/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_block_presets_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_block_presets_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/blog:
    get:
      operationId: get_admin_cms_blog
      summary: "Read Admin / Cms / Blog"
      description: "Admin / Cms / Blog. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsBlogPosts, upsertCmsBlogPost, type UpsertCmsBlogInput } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsBlogPosts(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsBlogSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid blog payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsBlogPost(sup.client, { ...(parsed.data as UpsertCmsBlogInput), organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/blog:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_blogResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_blog
      summary: "Create or execute Admin / Cms / Blog"
      description: "Admin / Cms / Blog. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsBlogPosts, upsertCmsBlogPost, type UpsertCmsBlogInput } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsBlogPosts(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsBlogSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid blog payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsBlogPost(sup.client, { ...(parsed.data as UpsertCmsBlogInput), organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/blog:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_blogRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_blogResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/blog/{id}:
    delete:
      operationId: delete_admin_cms_blog_id
      summary: "Delete Admin / Cms / Blog / Id"
      description: "Admin / Cms / Blog / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsBlogPost,\n  getCmsBlogPostById,\n  upsertCmsBlogPost,\n  type UpsertCmsBlogInput,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await getCmsBlogPostById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsBlogSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || (parsed.data.id && parsed.data.id !== id)) {\n    return correlatedJson(cid, { error: \"Invalid blog payload\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsBlogPost(sup.client, { ...parsed.data, id, organization_id: organization.id } as UpsertCmsBlogInput);\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsBlogPost(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_blog_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_blog_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_cms_blog_id
      summary: "Read Admin / Cms / Blog / Id"
      description: "Admin / Cms / Blog / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsBlogPost,\n  getCmsBlogPostById,\n  upsertCmsBlogPost,\n  type UpsertCmsBlogInput,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await getCmsBlogPostById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsBlogSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || (parsed.data.id && parsed.data.id !== id)) {\n    return correlatedJson(cid, { error: \"Invalid blog payload\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsBlogPost(sup.client, { ...parsed.data, id, organization_id: organization.id } as UpsertCmsBlogInput);\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsBlogPost(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_blog_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_blog_id
      summary: "Update Admin / Cms / Blog / Id"
      description: "Admin / Cms / Blog / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsBlogPost,\n  getCmsBlogPostById,\n  upsertCmsBlogPost,\n  type UpsertCmsBlogInput,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await getCmsBlogPostById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsBlogSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || (parsed.data.id && parsed.data.id !== id)) {\n    return correlatedJson(cid, { error: \"Invalid blog payload\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsBlogPost(sup.client, { ...parsed.data, id, organization_id: organization.id } as UpsertCmsBlogInput);\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsBlogPost(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/blog/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_blog_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_blog_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/blog/bulk:
    post:
      operationId: post_admin_cms_blog_bulk
      summary: "Create or execute Admin / Cms / Blog / Bulk"
      description: "Admin / Cms / Blog / Bulk. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { deleteCmsBlogPost } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsBlogBulkSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsBlogBulkSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid blog bulk payload\" }, { status: 400 });\n  const ids = parsed.data.ids;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  let deleted = 0;\n  for (const id of ids as string[]) {\n    const ok = await deleteCmsBlogPost(sup.client, id.trim(), organization.id);\n    if (ok) deleted += 1;\n  }\n  return correlatedJson(cid, { ok: true, deleted });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/blog/bulk:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_blog_bulkRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_blog_bulkResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/blog/export:
    get:
      operationId: get_admin_cms_blog_export
      summary: "Read Admin / Cms / Blog / Export"
      description: "Admin / Cms / Blog / Export. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nfunction csvEscape(s: string): string {\n  if (/[\",\\n\\r]/.test(s)) return `\"${s.replace(/\"/g, '\"\"')}\"`;\n  return s;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return new Response(\"Unauthorized\", { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return new Response(\"Forbidden\", { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return new Response(\"Tenant scope unavailable\", { status: 403 });\n  const rawIds = req.nextUrl.searchParams.get(\"ids\");\n  const ids = rawIds\n    ?.split(\",\")\n    .map((x) => x.trim())\n    .filter(Boolean);\n  if (\n    ids &&\n    (ids.length > 100 || ids.some((id) => !/^[0-9a-f-]{36}$/i.test(id)))\n  ) {\n    return new Response(\"Invalid ids\", { status: 400 });\n  }\n  let query = sup.client\n    .from(\"cms_blog_posts\")\n    .select(\n      \"id,slug,locale,title,status,published_at,scheduled_publish_at,author_name,tags,rss_include,canonical_url,og_image_url,updated_at\",\n    )\n    .order(\"updated_at\", { ascending: false })\n    .limit(1000);\n  if (ids?.length) query = query.in(\"id\", ids);\n  query = query.eq(\"organization_id\", organization.id);\n  const { data, error } = await query;\n  if (error)\n    return new Response(\"Unable to export blog posts\", { status: 502 });\n  const filtered = (data ?? []) as Array<Record<string, unknown>>;\n\n  const header = [\n    \"id\",\n    \"slug\",\n    \"locale\",\n    \"title\",\n    \"status\",\n    \"published_at\",\n    \"scheduled_publish_at\",\n    \"author_name\",\n    \"tags\",\n    \"rss_include\",\n    \"canonical_url\",\n    \"og_image_url\",\n    \"updated_at\",\n  ];\n  const lines = [\n    header.join(\",\"),\n    ...filtered.map((r) =>\n      [\n        r.id,\n        r.slug,\n        r.locale,\n        r.title,\n        r.status,\n        r.published_at ?? \"\",\n        r.scheduled_publish_at ?? \"\",\n        r.author_name ?? \"\",\n        Array.isArray(r.tags) ? r.tags.map(String).join(\";\") : \"\",\n        r.rss_include === true ? \"1\" : \"0\",\n        r.canonical_url ?? \"\",\n        r.og_image_url ?? \"\",\n        r.updated_at,\n      ]\n        .map((c) => csvEscape(String(c)))\n        .join(\",\"),\n    ),\n  ];\n  const body = lines.join(\"\\r\\n\");\n  if (new TextEncoder().encode(body).byteLength > 5 * 1024 * 1024) {\n    return new Response(\"Export too large\", { status: 413 });\n  }\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email ?? \"unknown\",\n    action: \"cms.blog.export\",\n    resource: \"cms_blog_posts\",\n    details: {\n      organization_id: organization.id,\n      count: filtered.length,\n      correlation_id: cid,\n    },\n  });\n  return new Response(body, {\n    status: 200,\n    headers: {\n      \"Content-Type\": \"text/csv; charset=utf-8\",\n      \"Content-Disposition\": 'attachment; filename=\"blog-posts.csv\"',\n    },\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_blog_exportResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/category-content:
    get:
      operationId: get_admin_cms_category_content
      summary: "Read Admin / Cms / Category Content"
      description: "Admin / Cms / Category Content. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport type { CmsBlock } from \"@universal-music-store/platform-data\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsCategoryContent, upsertCmsCategoryContent } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsCategoryContentSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsCategoryContent(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsCategoryContentSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid category content payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsCategoryContent(sup.client, {\n    ...parsed.data,\n    organization_id: organization.id,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n  });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/category-content:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_category_contentResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_category_content
      summary: "Create or execute Admin / Cms / Category Content"
      description: "Admin / Cms / Category Content. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport type { CmsBlock } from \"@universal-music-store/platform-data\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsCategoryContent, upsertCmsCategoryContent } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsCategoryContentSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsCategoryContent(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsCategoryContentSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid category content payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsCategoryContent(sup.client, {\n    ...parsed.data,\n    organization_id: organization.id,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n  });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/category-content:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_category_contentRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_category_contentResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/category-content/catalog-gaps:
    get:
      operationId: get_admin_cms_category_content_catalog_gaps
      summary: "Read Admin / Cms / Category Content / Catalog Gaps"
      description: "Admin / Cms / Category Content / Catalog Gaps. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsCategoryContent } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { listAdminProductCategories } from \"@/lib/medusa-product-categories\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const locale = req.nextUrl.searchParams.get(\"locale\")?.trim() || \"en\";\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const [cmsRows, categories] = await Promise.all([\n    listCmsCategoryContent(sup.client, organization.id),\n    listAdminProductCategories(),\n  ]);\n  const cmsHandles = new Set(\n    cmsRows.filter((r) => r.locale === locale).map((r) => r.collection_handle.trim().toLowerCase()),\n  );\n  const missing = categories.filter((c) => !cmsHandles.has(c.handle.trim().toLowerCase()));\n  return correlatedJson(cid, {\n    data: {\n      locale,\n      catalog_count: categories.length,\n      cms_rows_for_locale: cmsRows.filter((r) => r.locale === locale).length,\n      missing,\n    },\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_category_content_catalog_gapsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/category-content/medusa-categories:
    get:
      operationId: get_admin_cms_category_content_medusa_categories
      summary: "Read Admin / Cms / Category Content / Medusa Categories"
      description: "Admin / Cms / Category Content / Medusa Categories. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { listAdminProductCategories } from \"@/lib/medusa-product-categories\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\n/**\n * Product categories for CMS category storytelling (handles must match shop filters).\n * Uses content:read so editors without catalog:read can still pick a valid handle.\n */\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const categories = await listAdminProductCategories();\n  return correlatedJson(correlationId, {\n    categories: categories.map((c) => ({\n      id: c.id,\n      name: c.name,\n      handle: c.handle,\n    })),\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_category_content_medusa_categoriesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/category-content/sync-from-medusa:
    post:
      operationId: post_admin_cms_category_content_sync_from_medusa
      summary: "Create or execute Admin / Cms / Category Content / Sync From Medusa"
      description: "Admin / Cms / Category Content / Sync From Medusa. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listCmsCategoryContent,\n  upsertCmsCategoryContent,\n} from \"@universal-music-store/platform-data\";\nimport { listAdminProductCategories } from \"@/lib/medusa-product-categories\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport const dynamic = \"force-dynamic\";\n\n/**\n * Creates missing cms_category_content rows for Medusa product category handles (default locale).\n * Does not overwrite existing rows.\n */\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const locale =\n    req.nextUrl.searchParams.get(\"locale\")?.trim() ||\n    process.env.NEXT_PUBLIC_CMS_LOCALE?.trim() ||\n    \"en\";\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n\n  const [categories, existing] = await Promise.all([\n    listAdminProductCategories(),\n    listCmsCategoryContent(sup.client, organization.id),\n  ]);\n\n  const keys = new Set(\n    existing\n      .filter((r) => r.locale === locale)\n      .map((r) => r.collection_handle.trim())\n      .filter(Boolean),\n  );\n\n  let created = 0;\n  for (const c of categories) {\n    const handle = (c.handle ?? \"\").trim();\n    if (!handle || keys.has(handle)) continue;\n    const row = await upsertCmsCategoryContent(sup.client, {\n      collection_handle: handle,\n      locale,\n      intro_html: `<p>${escapeHtml(c.name ?? handle)}</p>`,\n      blocks: [],\n      organization_id: organization.id,\n    });\n    if (row) {\n      keys.add(handle);\n      created += 1;\n    }\n  }\n\n  return correlatedJson(cid, {\n    data: { created, locale, totalCategories: categories.length },\n  });\n}\n\nfunction escapeHtml(s: string): string {\n  return s\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\");\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/category-content/sync-from-medusa:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_category_content_sync_from_medusaRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_category_content_sync_from_medusaResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/components:
    get:
      operationId: get_admin_cms_components
      summary: "Read Admin / Cms / Components"
      description: "Admin / Cms / Components. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import {\n  listCmsComponentDefinitionsForOrganization,\n  mergeCmsComponentDefinitions,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\nexport async function GET(req: Request) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) {\n    return auth.response.status === 401\n      ? correlatedError(requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\")\n      : correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  }\n\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  const stored = await listCmsComponentDefinitionsForOrganization(sup.client, organization.id);\n  return correlatedJson(requestId, {\n    data: mergeCmsComponentDefinitions(stored),\n    meta: {\n      version: Math.max(1, ...stored.map((row) => row.version)),\n      contract: \"cms-component-editor-v2\",\n      source: stored.length ? \"organization\" : \"platform-data-defaults\",\n      records: stored.map((row) => ({ id: row.component_key, version: row.version, status: row.status })),\n    },\n  });\n}\n\nasync function post(req: Request) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  const existing = await listCmsComponentDefinitionsForOrganization(sup.client, organization.id);\n  const current = existing.find((row) => row.component_key === parsed.data.definition.id);\n  if (current && parsed.data.expectedVersion === undefined) {\n    return correlatedError(requestId, 409, \"Component version required\", \"CONFLICT\");\n  }\n  const saved = await saveCmsComponentDefinition(sup.client, {\n    organizationId: organization.id,\n    componentKey: parsed.data.definition.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(requestId, { data: saved });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_componentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_components
      summary: "Create or execute Admin / Cms / Components"
      description: "Admin / Cms / Components. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import {\n  listCmsComponentDefinitionsForOrganization,\n  mergeCmsComponentDefinitions,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\nexport async function GET(req: Request) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) {\n    return auth.response.status === 401\n      ? correlatedError(requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\")\n      : correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  }\n\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  const stored = await listCmsComponentDefinitionsForOrganization(sup.client, organization.id);\n  return correlatedJson(requestId, {\n    data: mergeCmsComponentDefinitions(stored),\n    meta: {\n      version: Math.max(1, ...stored.map((row) => row.version)),\n      contract: \"cms-component-editor-v2\",\n      source: stored.length ? \"organization\" : \"platform-data-defaults\",\n      records: stored.map((row) => ({ id: row.component_key, version: row.version, status: row.status })),\n    },\n  });\n}\n\nasync function post(req: Request) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\");\n  const existing = await listCmsComponentDefinitionsForOrganization(sup.client, organization.id);\n  const current = existing.find((row) => row.component_key === parsed.data.definition.id);\n  if (current && parsed.data.expectedVersion === undefined) {\n    return correlatedError(requestId, 409, \"Component version required\", \"CONFLICT\");\n  }\n  const saved = await saveCmsComponentDefinition(sup.client, {\n    organizationId: organization.id,\n    componentKey: parsed.data.definition.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(requestId, { data: saved });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_componentsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_componentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/components/{id}:
    delete:
      operationId: delete_admin_cms_components_id
      summary: "Delete Admin / Cms / Components / Id"
      description: "Admin / Cms / Components / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport {\n  archiveCmsComponentDefinition,\n  getCmsComponentDefinitionForOrganization,\n  publishCmsComponentDefinition,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsComponentActionSchema, cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\ntype Context = { params: Promise<{ id: string }> };\n\nasync function context(req: NextRequest, ctx: Context) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return { requestId, response: auth.response } as const;\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return { requestId, response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return { requestId, response: correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\") } as const;\n  return { requestId, auth, sup, organization, id: (await ctx.params).id } as const;\n}\n\nexport async function GET(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const row = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!row) return correlatedError(resolved.requestId, 404, \"Not found\", \"NOT_FOUND\");\n  return correlatedJson(resolved.requestId, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  if (!resolved.auth.ok || !resolved.auth.session) return correlatedError(resolved.requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\");\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || parsed.data.definition.id !== resolved.id) return correlatedError(resolved.requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const current = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!current || parsed.data.expectedVersion === undefined) return correlatedError(resolved.requestId, 409, \"Component version required\", \"CONFLICT\");\n  const saved = await saveCmsComponentDefinition(resolved.sup.client, {\n    organizationId: resolved.organization.id,\n    componentKey: resolved.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: resolved.auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function post(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const parsed = cmsComponentActionSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(resolved.requestId, 400, \"Invalid component action\", \"VALIDATION_ERROR\");\n  const saved = await publishCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, parsed.data.expectedVersion, resolved.auth.session?.user?.email ?? undefined);\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function del(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const version = Number(new URL(req.url).searchParams.get(\"version\"));\n  if (!Number.isInteger(version) || version < 1) return correlatedError(resolved.requestId, 400, \"version required\", \"VALIDATION_ERROR\");\n  const archived = await archiveCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, version, resolved.auth.session?.user?.email ?? undefined);\n  if (!archived) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: { archived: true } });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/components/[id]:PATCH\", patch);\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components/[id]:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/components/[id]:DELETE\", del);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_components_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_components_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_cms_components_id
      summary: "Read Admin / Cms / Components / Id"
      description: "Admin / Cms / Components / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport {\n  archiveCmsComponentDefinition,\n  getCmsComponentDefinitionForOrganization,\n  publishCmsComponentDefinition,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsComponentActionSchema, cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\ntype Context = { params: Promise<{ id: string }> };\n\nasync function context(req: NextRequest, ctx: Context) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return { requestId, response: auth.response } as const;\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return { requestId, response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return { requestId, response: correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\") } as const;\n  return { requestId, auth, sup, organization, id: (await ctx.params).id } as const;\n}\n\nexport async function GET(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const row = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!row) return correlatedError(resolved.requestId, 404, \"Not found\", \"NOT_FOUND\");\n  return correlatedJson(resolved.requestId, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  if (!resolved.auth.ok || !resolved.auth.session) return correlatedError(resolved.requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\");\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || parsed.data.definition.id !== resolved.id) return correlatedError(resolved.requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const current = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!current || parsed.data.expectedVersion === undefined) return correlatedError(resolved.requestId, 409, \"Component version required\", \"CONFLICT\");\n  const saved = await saveCmsComponentDefinition(resolved.sup.client, {\n    organizationId: resolved.organization.id,\n    componentKey: resolved.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: resolved.auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function post(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const parsed = cmsComponentActionSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(resolved.requestId, 400, \"Invalid component action\", \"VALIDATION_ERROR\");\n  const saved = await publishCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, parsed.data.expectedVersion, resolved.auth.session?.user?.email ?? undefined);\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function del(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const version = Number(new URL(req.url).searchParams.get(\"version\"));\n  if (!Number.isInteger(version) || version < 1) return correlatedError(resolved.requestId, 400, \"version required\", \"VALIDATION_ERROR\");\n  const archived = await archiveCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, version, resolved.auth.session?.user?.email ?? undefined);\n  if (!archived) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: { archived: true } });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/components/[id]:PATCH\", patch);\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components/[id]:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/components/[id]:DELETE\", del);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_components_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_cms_components_id
      summary: "Update Admin / Cms / Components / Id"
      description: "Admin / Cms / Components / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport {\n  archiveCmsComponentDefinition,\n  getCmsComponentDefinitionForOrganization,\n  publishCmsComponentDefinition,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsComponentActionSchema, cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\ntype Context = { params: Promise<{ id: string }> };\n\nasync function context(req: NextRequest, ctx: Context) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return { requestId, response: auth.response } as const;\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return { requestId, response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return { requestId, response: correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\") } as const;\n  return { requestId, auth, sup, organization, id: (await ctx.params).id } as const;\n}\n\nexport async function GET(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const row = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!row) return correlatedError(resolved.requestId, 404, \"Not found\", \"NOT_FOUND\");\n  return correlatedJson(resolved.requestId, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  if (!resolved.auth.ok || !resolved.auth.session) return correlatedError(resolved.requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\");\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || parsed.data.definition.id !== resolved.id) return correlatedError(resolved.requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const current = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!current || parsed.data.expectedVersion === undefined) return correlatedError(resolved.requestId, 409, \"Component version required\", \"CONFLICT\");\n  const saved = await saveCmsComponentDefinition(resolved.sup.client, {\n    organizationId: resolved.organization.id,\n    componentKey: resolved.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: resolved.auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function post(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const parsed = cmsComponentActionSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(resolved.requestId, 400, \"Invalid component action\", \"VALIDATION_ERROR\");\n  const saved = await publishCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, parsed.data.expectedVersion, resolved.auth.session?.user?.email ?? undefined);\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function del(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const version = Number(new URL(req.url).searchParams.get(\"version\"));\n  if (!Number.isInteger(version) || version < 1) return correlatedError(resolved.requestId, 400, \"version required\", \"VALIDATION_ERROR\");\n  const archived = await archiveCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, version, resolved.auth.session?.user?.email ?? undefined);\n  if (!archived) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: { archived: true } });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/components/[id]:PATCH\", patch);\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components/[id]:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/components/[id]:DELETE\", del);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_cms_components_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_cms_components_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_components_id
      summary: "Create or execute Admin / Cms / Components / Id"
      description: "Admin / Cms / Components / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport {\n  archiveCmsComponentDefinition,\n  getCmsComponentDefinitionForOrganization,\n  publishCmsComponentDefinition,\n  saveCmsComponentDefinition,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsComponentActionSchema, cmsComponentWriteSchema } from \"@/lib/cms-component-contract\";\nimport type { CmsComponentDefinition } from \"@universal-music-store/platform-data\";\n\ntype Context = { params: Promise<{ id: string }> };\n\nasync function context(req: NextRequest, ctx: Context) {\n  const requestId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return { requestId, response: auth.response } as const;\n  const sup = adminSupabaseOr503(requestId);\n  if (\"response\" in sup) return { requestId, response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return { requestId, response: correlatedError(requestId, 403, \"Forbidden\", \"FORBIDDEN\") } as const;\n  return { requestId, auth, sup, organization, id: (await ctx.params).id } as const;\n}\n\nexport async function GET(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const row = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!row) return correlatedError(resolved.requestId, 404, \"Not found\", \"NOT_FOUND\");\n  return correlatedJson(resolved.requestId, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  if (!resolved.auth.ok || !resolved.auth.session) return correlatedError(resolved.requestId, 401, \"Unauthorized\", \"UNAUTHORIZED\");\n  const parsed = cmsComponentWriteSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || parsed.data.definition.id !== resolved.id) return correlatedError(resolved.requestId, 400, \"Invalid component definition\", \"VALIDATION_ERROR\");\n  const current = await getCmsComponentDefinitionForOrganization(resolved.sup.client, resolved.organization.id, resolved.id);\n  if (!current || parsed.data.expectedVersion === undefined) return correlatedError(resolved.requestId, 409, \"Component version required\", \"CONFLICT\");\n  const saved = await saveCmsComponentDefinition(resolved.sup.client, {\n    organizationId: resolved.organization.id,\n    componentKey: resolved.id,\n    definition: parsed.data.definition as CmsComponentDefinition,\n    expectedVersion: parsed.data.expectedVersion,\n    actorId: resolved.auth.session.user?.email ?? undefined,\n  });\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function post(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const parsed = cmsComponentActionSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedError(resolved.requestId, 400, \"Invalid component action\", \"VALIDATION_ERROR\");\n  const saved = await publishCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, parsed.data.expectedVersion, resolved.auth.session?.user?.email ?? undefined);\n  if (!saved) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: saved });\n}\n\nasync function del(req: NextRequest, ctx: Context) {\n  const resolved = await context(req, ctx);\n  if (\"response\" in resolved) return resolved.response;\n  const version = Number(new URL(req.url).searchParams.get(\"version\"));\n  if (!Number.isInteger(version) || version < 1) return correlatedError(resolved.requestId, 400, \"version required\", \"VALIDATION_ERROR\");\n  const archived = await archiveCmsComponentDefinition(resolved.sup.client, resolved.organization.id, resolved.id, version, resolved.auth.session?.user?.email ?? undefined);\n  if (!archived) return correlatedError(resolved.requestId, 409, \"Component changed; reload and retry\", \"CONFLICT\");\n  return correlatedJson(resolved.requestId, { data: { archived: true } });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/components/[id]:PATCH\", patch);\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/components/[id]:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/components/[id]:DELETE\", del);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_components_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_components_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/experiments:
    get:
      operationId: get_admin_cms_experiments
      summary: "Read Admin / Cms / Experiments"
      description: "Admin / Cms / Experiments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsAbExperiments, upsertCmsAbExperiment } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsExperimentSchema } from \"@/lib/cms-route-contracts\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listCmsAbExperiments(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const parsed = cmsExperimentSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid experiment payload\" }, { status: 400 });\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await upsertCmsAbExperiment(sup.client, { ...parsed.data, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/experiments:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_experimentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_experiments
      summary: "Create or execute Admin / Cms / Experiments"
      description: "Admin / Cms / Experiments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsAbExperiments, upsertCmsAbExperiment } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsExperimentSchema } from \"@/lib/cms-route-contracts\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listCmsAbExperiments(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const parsed = cmsExperimentSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid experiment payload\" }, { status: 400 });\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await upsertCmsAbExperiment(sup.client, { ...parsed.data, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/experiments:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_experimentsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_experimentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/experiments/{id}:
    put:
      operationId: put_admin_cms_experiments_id
      summary: "Update Admin / Cms / Experiments / Id"
      description: "Admin / Cms / Experiments / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { upsertCmsAbExperiment } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsExperimentSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsExperimentSchema.safeParse(\n    body && typeof body === \"object\" && !Array.isArray(body) ? { ...body, id } : { id },\n  );\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid experiment payload\" }, { status: 400 });\n  const data = await upsertCmsAbExperiment(sup.client, { ...parsed.data, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/experiments/[id]:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_experiments_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_experiments_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/forms/settings:
    get:
      operationId: get_admin_cms_forms_settings
      summary: "Read Admin / Cms / Forms / Settings"
      description: "Admin / Cms / Forms / Settings. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCmsFormSettings, upsertCmsFormSettings } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsFormSettingsSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await getCmsFormSettings(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsFormSettingsSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid form settings payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsFormSettings(sup.client, {\n    webhook_url: parsed.data.webhook_url,\n    notify_email: parsed.data.notify_email,\n  }, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/forms/settings:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_forms_settingsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_forms_settings
      summary: "Update Admin / Cms / Forms / Settings"
      description: "Admin / Cms / Forms / Settings. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCmsFormSettings, upsertCmsFormSettings } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsFormSettingsSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await getCmsFormSettings(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsFormSettingsSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid form settings payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsFormSettings(sup.client, {\n    webhook_url: parsed.data.webhook_url,\n    notify_email: parsed.data.notify_email,\n  }, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/forms/settings:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_forms_settingsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_forms_settingsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/forms/submissions:
    get:
      operationId: get_admin_cms_forms_submissions
      summary: "Read Admin / Cms / Forms / Submissions"
      description: "Admin / Cms / Forms / Submissions. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsFormSubmissions } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const result = await listCmsFormSubmissions(sup.client, {\n    form_key: sp.get(\"form_key\") ?? undefined,\n    from: sp.get(\"from\") ?? undefined,\n    to: sp.get(\"to\") ?? undefined,\n    limit: Number(sp.get(\"limit\")) || 50,\n    offset: Number(sp.get(\"offset\")) || 0,\n    organization_id: organization.id,\n  });\n  if (Array.isArray(result)) {\n    return correlatedJson(cid, { data: result, meta: { total: result.length } });\n  }\n  return correlatedJson(cid, { data: result.rows, meta: { total: result.total } });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_forms_submissionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/forms/submissions/{id}:
    patch:
      operationId: patch_admin_cms_forms_submissions_id
      summary: "Update Admin / Cms / Forms / Submissions / Id"
      description: "Admin / Cms / Forms / Submissions / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { updateCmsFormSubmission } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsFormSubmissionSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nasync function patch(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsFormSubmissionSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid form submission payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const row = await updateCmsFormSubmission(sup.client, id, {\n    read_at: parsed.data.read_at,\n    assigned_to: parsed.data.assigned_to,\n    spam_score: parsed.data.spam_score,\n  }, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: row });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/forms/submissions/[id]:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_cms_forms_submissions_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_cms_forms_submissions_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/forms/submissions/export:
    get:
      operationId: get_admin_cms_forms_submissions_export
      summary: "Read Admin / Cms / Forms / Submissions / Export"
      description: "Admin / Cms / Forms / Submissions / Export. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nfunction csvEscape(s: string) {\n  if (s.includes('\"') || s.includes(\",\") || s.includes(\"\\n\")) {\n    return `\"${s.replace(/\"/g, '\"\"')}\"`;\n  }\n  return s;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return new Response(\"Unauthorized\", { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return new Response(\"Forbidden\", { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return new Response(\"Tenant scope unavailable\", { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const formKey = sp.get(\"form_key\")?.trim();\n  const from = sp.get(\"from\")?.trim();\n  const to = sp.get(\"to\")?.trim();\n  if ([formKey, from, to].some((value) => value && value.length > 64)) {\n    return new Response(\"Invalid filters\", { status: 400 });\n  }\n  if ([from, to].some((value) => value && Number.isNaN(Date.parse(value)))) {\n    return new Response(\"Invalid date filter\", { status: 400 });\n  }\n  let query = sup.client\n    .from(\"cms_form_submissions\")\n    .select(\"id,form_key,created_at,read_at,assigned_to,spam_score,payload\")\n    .eq(\"organization_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(1000);\n  if (formKey) query = query.eq(\"form_key\", formKey);\n  if (from) query = query.gte(\"created_at\", from);\n  if (to) query = query.lte(\"created_at\", to);\n  const { data, error } = await query;\n  if (error)\n    return new Response(\"Unable to export form submissions\", { status: 502 });\n  const rows = (data ?? []) as Array<Record<string, unknown>>;\n  const lines = [\n    [\n      \"id\",\n      \"form_key\",\n      \"created_at\",\n      \"read_at\",\n      \"assigned_to\",\n      \"spam_score\",\n      \"payload_json\",\n    ].join(\",\"),\n  ];\n  for (const r of rows) {\n    lines.push(\n      [\n        csvEscape(String(r.id ?? \"\")),\n        csvEscape(String(r.form_key ?? \"\")),\n        csvEscape(String(r.created_at ?? \"\")),\n        csvEscape(String(r.read_at ?? \"\")),\n        csvEscape(String(r.assigned_to ?? \"\")),\n        String(Number(r.spam_score) || 0),\n        csvEscape(JSON.stringify(r.payload ?? {})),\n      ].join(\",\"),\n    );\n  }\n  const body = lines.join(\"\\n\");\n  if (new TextEncoder().encode(body).byteLength > 5 * 1024 * 1024) {\n    return new Response(\"Export too large\", { status: 413 });\n  }\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email ?? \"unknown\",\n    action: \"cms.form_submissions.export\",\n    resource: \"cms_form_submissions\",\n    details: {\n      organization_id: organization.id,\n      count: rows.length,\n      correlation_id: cid,\n    },\n  });\n  return new Response(body, {\n    status: 200,\n    headers: {\n      \"Content-Type\": \"text/csv; charset=utf-8\",\n      \"Content-Disposition\": `attachment; filename=\"form-submissions-${cid.slice(0, 8)}.csv\"`,\n    },\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_forms_submissions_exportResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/media:
    get:
      operationId: get_admin_cms_media
      summary: "Read Admin / Cms / Media"
      description: "Admin / Cms / Media. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  insertCmsMedia,\n  listCmsMedia,\n  type ListCmsMediaOptions,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSessionAny } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\n    \"content:read\",\n    \"catalog:read\",\n    \"catalog:write\",\n  ]);\n  if (!auth.ok) return auth.response;\n  const canContentRead = auth.permission === \"content:read\";\n  const canCatalogList = auth.permission !== \"content:read\";\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const opts: ListCmsMediaOptions = {\n    limit: Math.min(Number(sp.get(\"limit\")) || 200, 500),\n    search: sp.get(\"q\") ?? undefined,\n    mimePrefix: sp.get(\"mime\") ?? undefined,\n    sort: (sp.get(\"sort\") as ListCmsMediaOptions[\"sort\"]) || \"created_desc\",\n    tag: sp.get(\"tag\") ?? undefined,\n    organizationId: organization.id,\n  };\n  if (!canContentRead && canCatalogList) {\n    opts.tag = CMS_MEDIA_TAG_CATALOG_PRODUCT;\n  }\n  const data = await listCmsMedia(sup.client, opts);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\"content:write\", \"catalog:write\"]);\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sb = sup.client;\n\n  let form: FormData;\n  try {\n    form = await req.formData();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid form data\" }, { status: 400 });\n  }\n  const file = form.get(\"file\");\n  const altText = typeof form.get(\"alt\") === \"string\" ? (form.get(\"alt\") as string) : \"\";\n  if (!(file instanceof Blob) || file.size === 0) {\n    return correlatedJson(cid, { error: \"Missing file\" }, { status: 400 });\n  }\n  const mime = (file as File).type || \"\";\n  const extensionAllowed = /\\.(png|jpe?g|gif|webp|avif|svg|bmp|mp4|webm|mov|ogg)$/i.test(\n    typeof (file as File).name === \"string\" ? (file as File).name : \"\",\n  );\n  if (!(mime.startsWith(\"image/\") || mime.startsWith(\"video/\")) && !extensionAllowed) {\n    return correlatedJson(cid, { error: \"Only image or video uploads are allowed\" }, { status: 400 });\n  }\n  if (mime.startsWith(\"image/\") && altText.trim().length === 0) {\n    return correlatedJson(\n      cid,\n      { error: \"Alt text is required for image uploads\" },\n      { status: 400 },\n    );\n  }\n  const MAX_BYTES = 25 * 1024 * 1024;\n  if (file.size > MAX_BYTES) {\n    return correlatedJson(\n      cid,\n      { error: `File exceeds limit of ${MAX_BYTES} bytes` },\n      { status: 400 },\n    );\n  }\n\n  const rawName = typeof (file as File).name === \"string\" ? (file as File).name : \"upload\";\n  const safe = rawName.replace(/[^a-zA-Z0-9._-]+/g, \"_\").slice(0, 120);\n  const path = `public/${crypto.randomUUID()}-${safe}`;\n\n  const ab = await file.arrayBuffer();\n  const { error: upErr } = await sb.storage.from(\"cms\").upload(path, ab, {\n    contentType: (file as File).type || \"application/octet-stream\",\n    upsert: false,\n  });\n  if (upErr) {\n    return correlatedJson(cid, { error: upErr.message }, { status: 500 });\n  }\n\n  const { data: pub } = sb.storage.from(\"cms\").getPublicUrl(path);\n  const publicUrl = pub.publicUrl;\n\n  const row = await insertCmsMedia(sb, {\n    storage_path: path,\n    public_url: publicUrl,\n    alt_text: altText || null,\n    mime_type: (file as File).type || null,\n    width: null,\n    height: null,\n    display_name: safe,\n    byte_size: file.size,\n    organization_id: organization.id,\n  });\n  if (!row) {\n    await sb.storage.from(\"cms\").remove([path]).catch(() => undefined);\n    return correlatedJson(cid, { error: \"Unable to save metadata\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/media:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_mediaResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_media
      summary: "Create or execute Admin / Cms / Media"
      description: "Admin / Cms / Media. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  insertCmsMedia,\n  listCmsMedia,\n  type ListCmsMediaOptions,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSessionAny } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\n    \"content:read\",\n    \"catalog:read\",\n    \"catalog:write\",\n  ]);\n  if (!auth.ok) return auth.response;\n  const canContentRead = auth.permission === \"content:read\";\n  const canCatalogList = auth.permission !== \"content:read\";\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sp = req.nextUrl.searchParams;\n  const opts: ListCmsMediaOptions = {\n    limit: Math.min(Number(sp.get(\"limit\")) || 200, 500),\n    search: sp.get(\"q\") ?? undefined,\n    mimePrefix: sp.get(\"mime\") ?? undefined,\n    sort: (sp.get(\"sort\") as ListCmsMediaOptions[\"sort\"]) || \"created_desc\",\n    tag: sp.get(\"tag\") ?? undefined,\n    organizationId: organization.id,\n  };\n  if (!canContentRead && canCatalogList) {\n    opts.tag = CMS_MEDIA_TAG_CATALOG_PRODUCT;\n  }\n  const data = await listCmsMedia(sup.client, opts);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSessionAny([\"content:write\", \"catalog:write\"]);\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const sb = sup.client;\n\n  let form: FormData;\n  try {\n    form = await req.formData();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid form data\" }, { status: 400 });\n  }\n  const file = form.get(\"file\");\n  const altText = typeof form.get(\"alt\") === \"string\" ? (form.get(\"alt\") as string) : \"\";\n  if (!(file instanceof Blob) || file.size === 0) {\n    return correlatedJson(cid, { error: \"Missing file\" }, { status: 400 });\n  }\n  const mime = (file as File).type || \"\";\n  const extensionAllowed = /\\.(png|jpe?g|gif|webp|avif|svg|bmp|mp4|webm|mov|ogg)$/i.test(\n    typeof (file as File).name === \"string\" ? (file as File).name : \"\",\n  );\n  if (!(mime.startsWith(\"image/\") || mime.startsWith(\"video/\")) && !extensionAllowed) {\n    return correlatedJson(cid, { error: \"Only image or video uploads are allowed\" }, { status: 400 });\n  }\n  if (mime.startsWith(\"image/\") && altText.trim().length === 0) {\n    return correlatedJson(\n      cid,\n      { error: \"Alt text is required for image uploads\" },\n      { status: 400 },\n    );\n  }\n  const MAX_BYTES = 25 * 1024 * 1024;\n  if (file.size > MAX_BYTES) {\n    return correlatedJson(\n      cid,\n      { error: `File exceeds limit of ${MAX_BYTES} bytes` },\n      { status: 400 },\n    );\n  }\n\n  const rawName = typeof (file as File).name === \"string\" ? (file as File).name : \"upload\";\n  const safe = rawName.replace(/[^a-zA-Z0-9._-]+/g, \"_\").slice(0, 120);\n  const path = `public/${crypto.randomUUID()}-${safe}`;\n\n  const ab = await file.arrayBuffer();\n  const { error: upErr } = await sb.storage.from(\"cms\").upload(path, ab, {\n    contentType: (file as File).type || \"application/octet-stream\",\n    upsert: false,\n  });\n  if (upErr) {\n    return correlatedJson(cid, { error: upErr.message }, { status: 500 });\n  }\n\n  const { data: pub } = sb.storage.from(\"cms\").getPublicUrl(path);\n  const publicUrl = pub.publicUrl;\n\n  const row = await insertCmsMedia(sb, {\n    storage_path: path,\n    public_url: publicUrl,\n    alt_text: altText || null,\n    mime_type: (file as File).type || null,\n    width: null,\n    height: null,\n    display_name: safe,\n    byte_size: file.size,\n    organization_id: organization.id,\n  });\n  if (!row) {\n    await sb.storage.from(\"cms\").remove([path]).catch(() => undefined);\n    return correlatedJson(cid, { error: \"Unable to save metadata\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/media:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_mediaRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_mediaResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/media/{id}:
    delete:
      operationId: delete_admin_cms_media_id
      summary: "Delete Admin / Cms / Media / Id"
      description: "Admin / Cms / Media / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  cmsMediaRowIsCatalogProduct,\n  findCmsMediaReferences,\n  getCmsMediaById,\n  softDeleteCmsMedia,\n  updateCmsMedia,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { findMedusaProductMediaReferences } from \"@/lib/medusa-product-media-refs\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsMediaMetadataSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentRead = staffSessionAllows(session, \"content:read\");\n  const canCatalogRead =\n    staffSessionAllows(session, \"catalog:read\") ||\n    staffSessionAllows(session, \"catalog:write\");\n  const mode = req.nextUrl.searchParams.get(\"refs\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const row = await getCmsMediaById(sup.client, id, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(row);\n  if (!canContentRead && !(isCatalog && canCatalogRead)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (mode === \"1\") {\n    const [cmsRefs, medusaRefs] = await Promise.all([\n      findCmsMediaReferences(sup.client, row.public_url, organization.id),\n      findMedusaProductMediaReferences(row.public_url),\n    ]);\n    const refs = [...cmsRefs, ...medusaRefs];\n    return correlatedJson(cid, { data: { row, refs } });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const parsed = cmsMediaMetadataSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid media metadata\" }, { status: 400 });\n  const b = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let tags = b.tags;\n  if (isCatalog && !canContentWrite && tags !== undefined) {\n    const merged = new Set(tags);\n    merged.add(CMS_MEDIA_TAG_CATALOG_PRODUCT);\n    tags = Array.from(merged);\n  }\n  const row = await updateCmsMedia(sup.client, id, {\n    alt_text: b.alt_text,\n    display_name: b.display_name,\n    tags,\n  }, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Unable to update\" }, { status: 500 });\n  return correlatedJson(cid, { data: row });\n}\n\nasync function deleteHandler(_req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(_req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const [cmsRefs, medusaRefs] = await Promise.all([\n    findCmsMediaReferences(sup.client, existing.public_url, organization.id),\n    findMedusaProductMediaReferences(existing.public_url),\n  ]);\n  const references = [...cmsRefs, ...medusaRefs];\n  if (references.length > 0) {\n    return correlatedJson(\n      cid,\n      { error: \"Media is still referenced by published content or catalog products\", code: \"MEDIA_IN_USE\" },\n      { status: 409 },\n    );\n  }\n  const ok = await softDeleteCmsMedia(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  const bucket = isCatalog ? \"catalog\" : \"cms\";\n  const storageCleanup = existing.storage_path.startsWith(\"external/\")\n    ? { ok: true as const, skipped: true as const }\n    : await sup.client.storage.from(bucket).remove([existing.storage_path]);\n  if (\"error\" in storageCleanup && storageCleanup.error) {\n    console.error(\"[cms-media] storage cleanup failed\", {\n      correlationId: cid,\n      mediaId: id,\n      bucket,\n      error: storageCleanup.error.message,\n    });\n  }\n  return correlatedJson(cid, {\n    ok: true,\n    storageCleanup: \"error\" in storageCleanup && storageCleanup.error ? \"pending\" : \"removed\",\n  });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/media/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/media/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_media_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_media_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_cms_media_id
      summary: "Read Admin / Cms / Media / Id"
      description: "Admin / Cms / Media / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  cmsMediaRowIsCatalogProduct,\n  findCmsMediaReferences,\n  getCmsMediaById,\n  softDeleteCmsMedia,\n  updateCmsMedia,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { findMedusaProductMediaReferences } from \"@/lib/medusa-product-media-refs\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsMediaMetadataSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentRead = staffSessionAllows(session, \"content:read\");\n  const canCatalogRead =\n    staffSessionAllows(session, \"catalog:read\") ||\n    staffSessionAllows(session, \"catalog:write\");\n  const mode = req.nextUrl.searchParams.get(\"refs\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const row = await getCmsMediaById(sup.client, id, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(row);\n  if (!canContentRead && !(isCatalog && canCatalogRead)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (mode === \"1\") {\n    const [cmsRefs, medusaRefs] = await Promise.all([\n      findCmsMediaReferences(sup.client, row.public_url, organization.id),\n      findMedusaProductMediaReferences(row.public_url),\n    ]);\n    const refs = [...cmsRefs, ...medusaRefs];\n    return correlatedJson(cid, { data: { row, refs } });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const parsed = cmsMediaMetadataSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid media metadata\" }, { status: 400 });\n  const b = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let tags = b.tags;\n  if (isCatalog && !canContentWrite && tags !== undefined) {\n    const merged = new Set(tags);\n    merged.add(CMS_MEDIA_TAG_CATALOG_PRODUCT);\n    tags = Array.from(merged);\n  }\n  const row = await updateCmsMedia(sup.client, id, {\n    alt_text: b.alt_text,\n    display_name: b.display_name,\n    tags,\n  }, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Unable to update\" }, { status: 500 });\n  return correlatedJson(cid, { data: row });\n}\n\nasync function deleteHandler(_req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(_req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const [cmsRefs, medusaRefs] = await Promise.all([\n    findCmsMediaReferences(sup.client, existing.public_url, organization.id),\n    findMedusaProductMediaReferences(existing.public_url),\n  ]);\n  const references = [...cmsRefs, ...medusaRefs];\n  if (references.length > 0) {\n    return correlatedJson(\n      cid,\n      { error: \"Media is still referenced by published content or catalog products\", code: \"MEDIA_IN_USE\" },\n      { status: 409 },\n    );\n  }\n  const ok = await softDeleteCmsMedia(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  const bucket = isCatalog ? \"catalog\" : \"cms\";\n  const storageCleanup = existing.storage_path.startsWith(\"external/\")\n    ? { ok: true as const, skipped: true as const }\n    : await sup.client.storage.from(bucket).remove([existing.storage_path]);\n  if (\"error\" in storageCleanup && storageCleanup.error) {\n    console.error(\"[cms-media] storage cleanup failed\", {\n      correlationId: cid,\n      mediaId: id,\n      bucket,\n      error: storageCleanup.error.message,\n    });\n  }\n  return correlatedJson(cid, {\n    ok: true,\n    storageCleanup: \"error\" in storageCleanup && storageCleanup.error ? \"pending\" : \"removed\",\n  });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/media/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/media/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_media_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_cms_media_id
      summary: "Update Admin / Cms / Media / Id"
      description: "Admin / Cms / Media / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  CMS_MEDIA_TAG_CATALOG_PRODUCT,\n  cmsMediaRowIsCatalogProduct,\n  findCmsMediaReferences,\n  getCmsMediaById,\n  softDeleteCmsMedia,\n  updateCmsMedia,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { findMedusaProductMediaReferences } from \"@/lib/medusa-product-media-refs\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsMediaMetadataSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentRead = staffSessionAllows(session, \"content:read\");\n  const canCatalogRead =\n    staffSessionAllows(session, \"catalog:read\") ||\n    staffSessionAllows(session, \"catalog:write\");\n  const mode = req.nextUrl.searchParams.get(\"refs\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const row = await getCmsMediaById(sup.client, id, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(row);\n  if (!canContentRead && !(isCatalog && canCatalogRead)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (mode === \"1\") {\n    const [cmsRefs, medusaRefs] = await Promise.all([\n      findCmsMediaReferences(sup.client, row.public_url, organization.id),\n      findMedusaProductMediaReferences(row.public_url),\n    ]);\n    const refs = [...cmsRefs, ...medusaRefs];\n    return correlatedJson(cid, { data: { row, refs } });\n  }\n  return correlatedJson(cid, { data: row });\n}\n\nasync function patch(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const parsed = cmsMediaMetadataSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid media metadata\" }, { status: 400 });\n  const b = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let tags = b.tags;\n  if (isCatalog && !canContentWrite && tags !== undefined) {\n    const merged = new Set(tags);\n    merged.add(CMS_MEDIA_TAG_CATALOG_PRODUCT);\n    tags = Array.from(merged);\n  }\n  const row = await updateCmsMedia(sup.client, id, {\n    alt_text: b.alt_text,\n    display_name: b.display_name,\n    tags,\n  }, organization.id);\n  if (!row) return correlatedJson(cid, { error: \"Unable to update\" }, { status: 500 });\n  return correlatedJson(cid, { data: row });\n}\n\nasync function deleteHandler(_req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(_req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canContentWrite = staffSessionAllows(session, \"content:write\");\n  const canCatalogWrite = staffSessionAllows(session, \"catalog:write\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is required\" }, { status: 403 });\n  const existing = await getCmsMediaById(sup.client, id, organization.id);\n  if (!existing) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const isCatalog = cmsMediaRowIsCatalogProduct(existing);\n  if (!canContentWrite && !(isCatalog && canCatalogWrite)) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const [cmsRefs, medusaRefs] = await Promise.all([\n    findCmsMediaReferences(sup.client, existing.public_url, organization.id),\n    findMedusaProductMediaReferences(existing.public_url),\n  ]);\n  const references = [...cmsRefs, ...medusaRefs];\n  if (references.length > 0) {\n    return correlatedJson(\n      cid,\n      { error: \"Media is still referenced by published content or catalog products\", code: \"MEDIA_IN_USE\" },\n      { status: 409 },\n    );\n  }\n  const ok = await softDeleteCmsMedia(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  const bucket = isCatalog ? \"catalog\" : \"cms\";\n  const storageCleanup = existing.storage_path.startsWith(\"external/\")\n    ? { ok: true as const, skipped: true as const }\n    : await sup.client.storage.from(bucket).remove([existing.storage_path]);\n  if (\"error\" in storageCleanup && storageCleanup.error) {\n    console.error(\"[cms-media] storage cleanup failed\", {\n      correlationId: cid,\n      mediaId: id,\n      bucket,\n      error: storageCleanup.error.message,\n    });\n  }\n  return correlatedJson(cid, {\n    ok: true,\n    storageCleanup: \"error\" in storageCleanup && storageCleanup.error ? \"pending\" : \"removed\",\n  });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/media/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/media/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_cms_media_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_cms_media_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/navigation:
    get:
      operationId: get_admin_cms_navigation
      summary: "Read Admin / Cms / Navigation"
      description: "Admin / Cms / Navigation. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  getCmsNavigationDraftPayload,\n  getCmsNavigationPayloadAdmin,\n  mergeNavigationDraftOverLive,\n  normalizeNavigationPayloadInput,\n  upsertCmsNavigationDraftPayload,\n  upsertCmsNavigationPayload,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsNavigationSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const live = await getCmsNavigationPayloadAdmin(sup.client, organization.id);\n  const draft = await getCmsNavigationDraftPayload(sup.client, organization.id);\n  const merged = mergeNavigationDraftOverLive(live, draft);\n  return correlatedJson(cid, {\n    data: merged,\n    meta: { hasDraft: draft != null },\n  });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsNavigationSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid navigation payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const rec = parsed.data as Record<string, unknown>;\n  try {\n    if (rec.mode === \"draft\") {\n      const payload = normalizeNavigationPayloadInput(rec.payload ?? rec);\n      await upsertCmsNavigationDraftPayload(sup.client, payload, organization.id);\n    } else {\n      const payload = normalizeNavigationPayloadInput(parsed.data);\n      await upsertCmsNavigationPayload(sup.client, payload, organization.id);\n      await upsertCmsNavigationDraftPayload(sup.client, {}, organization.id);\n    }\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to save\" },\n      { status: 500 },\n    );\n  }\n  const live = await getCmsNavigationPayloadAdmin(sup.client, organization.id);\n  const draft = await getCmsNavigationDraftPayload(sup.client, organization.id);\n  const merged = mergeNavigationDraftOverLive(live, draft);\n  return correlatedJson(cid, {\n    data: merged,\n    meta: { hasDraft: draft != null },\n  });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/navigation:PUT\", put);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_navigationResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_navigation
      summary: "Update Admin / Cms / Navigation"
      description: "Admin / Cms / Navigation. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  getCmsNavigationDraftPayload,\n  getCmsNavigationPayloadAdmin,\n  mergeNavigationDraftOverLive,\n  normalizeNavigationPayloadInput,\n  upsertCmsNavigationDraftPayload,\n  upsertCmsNavigationPayload,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsNavigationSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const live = await getCmsNavigationPayloadAdmin(sup.client, organization.id);\n  const draft = await getCmsNavigationDraftPayload(sup.client, organization.id);\n  const merged = mergeNavigationDraftOverLive(live, draft);\n  return correlatedJson(cid, {\n    data: merged,\n    meta: { hasDraft: draft != null },\n  });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsNavigationSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid navigation payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const rec = parsed.data as Record<string, unknown>;\n  try {\n    if (rec.mode === \"draft\") {\n      const payload = normalizeNavigationPayloadInput(rec.payload ?? rec);\n      await upsertCmsNavigationDraftPayload(sup.client, payload, organization.id);\n    } else {\n      const payload = normalizeNavigationPayloadInput(parsed.data);\n      await upsertCmsNavigationPayload(sup.client, payload, organization.id);\n      await upsertCmsNavigationDraftPayload(sup.client, {}, organization.id);\n    }\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to save\" },\n      { status: 500 },\n    );\n  }\n  const live = await getCmsNavigationPayloadAdmin(sup.client, organization.id);\n  const draft = await getCmsNavigationDraftPayload(sup.client, organization.id);\n  const merged = mergeNavigationDraftOverLive(live, draft);\n  return correlatedJson(cid, {\n    data: merged,\n    meta: { hasDraft: draft != null },\n  });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/navigation:PUT\", put);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_navigationRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_navigationResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/navigation/publish:
    post:
      operationId: post_admin_cms_navigation_publish
      summary: "Create or execute Admin / Cms / Navigation / Publish"
      description: "Admin / Cms / Navigation / Publish. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getCmsNavigationPayloadAdmin,\n  publishCmsNavigationDraft,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const canPublish =\n    staffSessionAllows(session, \"content:publish\") ||\n    staffSessionAllows(session, \"content:write\");\n  if (!canPublish) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  try {\n    await publishCmsNavigationDraft(sup.client, organization.id);\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Unable to publish\" },\n      { status: 500 },\n    );\n  }\n  const data = await getCmsNavigationPayloadAdmin(sup.client, organization.id);\n  return correlatedJson(cid, { data, meta: { hasDraft: false } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/navigation/publish:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_navigation_publishRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_navigation_publishResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/pages:
    get:
      operationId: get_admin_cms_pages
      summary: "Read Admin / Cms / Pages"
      description: "Admin / Cms / Pages. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsPages, upsertCmsPage, type CmsBlock } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsPageSchema } from \"@/lib/cms-route-contracts\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const locale = req.nextUrl.searchParams.get(\"locale\") ?? undefined;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listCmsPages(sup.client, { locale, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsPageSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid page payload\" }, { status: 400 });\n  const merged = await upsertCmsPage(sup.client, {\n    ...parsed.data,\n    expectedVersion: parsed.data.expectedVersion,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n    mutations: parsed.data.mutations,\n    organization_id: organization.id,\n  });\n  if (!merged) {\n    return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/pages:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_pagesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_pages
      summary: "Create or execute Admin / Cms / Pages"
      description: "Admin / Cms / Pages. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsPages, upsertCmsPage, type CmsBlock } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsPageSchema } from \"@/lib/cms-route-contracts\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const locale = req.nextUrl.searchParams.get(\"locale\") ?? undefined;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listCmsPages(sup.client, { locale, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsPageSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid page payload\" }, { status: 400 });\n  const merged = await upsertCmsPage(sup.client, {\n    ...parsed.data,\n    expectedVersion: parsed.data.expectedVersion,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n    mutations: parsed.data.mutations,\n    organization_id: organization.id,\n  });\n  if (!merged) {\n    return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  }\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/pages:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_pagesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_pagesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/pages/{id}:
    delete:
      operationId: delete_admin_cms_pages_id
      summary: "Delete Admin / Cms / Pages / Id"
      description: "Admin / Cms / Pages / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsPage,\n  getCmsPageById,\n  upsertCmsPage,\n  type CmsBlock,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsPageSchema } from \"@/lib/cms-route-contracts\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await getCmsPageById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsPageSchema.safeParse(\n    body && typeof body === \"object\" && !Array.isArray(body) ? { ...body, id } : { id },\n  );\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid page payload\" }, { status: 400 });\n  const merged = await upsertCmsPage(sup.client, {\n    ...parsed.data,\n    expectedVersion: parsed.data.expectedVersion,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n    mutations: parsed.data.mutations,\n    organization_id: organization.id,\n  });\n  if (!merged) {\n    const current = await getCmsPageById(sup.client, id, organization.id);\n    return correlatedJson(\n      cid,\n      { error: current ? \"Page changed elsewhere; reload before saving\" : \"Unable to save\" },\n      { status: current ? 409 : 500 },\n    );\n  }\n  return correlatedJson(cid, { data: merged });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const ok = await deleteCmsPage(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_pages_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_pages_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_cms_pages_id
      summary: "Read Admin / Cms / Pages / Id"
      description: "Admin / Cms / Pages / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsPage,\n  getCmsPageById,\n  upsertCmsPage,\n  type CmsBlock,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsPageSchema } from \"@/lib/cms-route-contracts\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await getCmsPageById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsPageSchema.safeParse(\n    body && typeof body === \"object\" && !Array.isArray(body) ? { ...body, id } : { id },\n  );\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid page payload\" }, { status: 400 });\n  const merged = await upsertCmsPage(sup.client, {\n    ...parsed.data,\n    expectedVersion: parsed.data.expectedVersion,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n    mutations: parsed.data.mutations,\n    organization_id: organization.id,\n  });\n  if (!merged) {\n    const current = await getCmsPageById(sup.client, id, organization.id);\n    return correlatedJson(\n      cid,\n      { error: current ? \"Page changed elsewhere; reload before saving\" : \"Unable to save\" },\n      { status: current ? 409 : 500 },\n    );\n  }\n  return correlatedJson(cid, { data: merged });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const ok = await deleteCmsPage(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_pages_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_pages_id
      summary: "Update Admin / Cms / Pages / Id"
      description: "Admin / Cms / Pages / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  deleteCmsPage,\n  getCmsPageById,\n  upsertCmsPage,\n  type CmsBlock,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { cmsPageSchema } from \"@/lib/cms-route-contracts\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await getCmsPageById(sup.client, id, organization.id);\n  if (!data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = cmsPageSchema.safeParse(\n    body && typeof body === \"object\" && !Array.isArray(body) ? { ...body, id } : { id },\n  );\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid page payload\" }, { status: 400 });\n  const merged = await upsertCmsPage(sup.client, {\n    ...parsed.data,\n    expectedVersion: parsed.data.expectedVersion,\n    blocks: parsed.data.blocks as CmsBlock[] | undefined,\n    mutations: parsed.data.mutations,\n    organization_id: organization.id,\n  });\n  if (!merged) {\n    const current = await getCmsPageById(sup.client, id, organization.id);\n    return correlatedJson(\n      cid,\n      { error: current ? \"Page changed elsewhere; reload before saving\" : \"Unable to save\" },\n      { status: current ? 409 : 500 },\n    );\n  }\n  return correlatedJson(cid, { data: merged });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const ok = await deleteCmsPage(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/pages/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_pages_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_pages_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/pages/{id}/mutations:
    get:
      operationId: get_admin_cms_pages_id_mutations
      summary: "Read Admin / Cms / Pages / Id / Mutations"
      description: "Admin / Cms / Pages / Id / Mutations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { listCmsPageMutations } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) {\n    return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  }\n  const page = await sup.client\n    .from(\"cms_pages\")\n    .select(\"id\")\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .maybeSingle();\n  if (page.error || !page.data) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  const data = await listCmsPageMutations(sup.client, id, organization.id);\n  return correlatedJson(cid, { data });\n}\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_pages_id_mutationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects:
    get:
      operationId: get_admin_cms_redirects
      summary: "Read Admin / Cms / Redirects"
      description: "Admin / Cms / Redirects. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsRedirects, upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsRedirectSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsRedirects(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsRedirectSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid redirect payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsRedirect(sup.client, { ...parsed.data, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/redirects:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_redirectsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_cms_redirects
      summary: "Create or execute Admin / Cms / Redirects"
      description: "Admin / Cms / Redirects. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listCmsRedirects, upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsRedirectSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await listCmsRedirects(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"content:write\");\n  if (!auth.ok) return auth.response;\n  const parsed = cmsRedirectSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid redirect payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsRedirect(sup.client, { ...parsed.data, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/redirects:POST\", post);\n"
      x-permission: "content:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_redirectsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_redirectsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects/{id}:
    delete:
      operationId: delete_admin_cms_redirects_id
      summary: "Delete Admin / Cms / Redirects / Id"
      description: "Admin / Cms / Redirects / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { deleteCmsRedirect, upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsRedirectSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsRedirectSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || (parsed.data.id && parsed.data.id !== id)) {\n    return correlatedJson(cid, { error: \"Invalid redirect payload\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsRedirect(sup.client, { ...parsed.data, id, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsRedirect(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/redirects/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/redirects/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_cms_redirects_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_cms_redirects_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_cms_redirects_id
      summary: "Update Admin / Cms / Redirects / Id"
      description: "Admin / Cms / Redirects / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { deleteCmsRedirect, upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsRedirectSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype RouteCtx = { params: Promise<{ id: string }> };\n\nasync function put(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsRedirectSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success || (parsed.data.id && parsed.data.id !== id)) {\n    return correlatedJson(cid, { error: \"Invalid redirect payload\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const data = await upsertCmsRedirect(sup.client, { ...parsed.data, id, organization_id: organization.id });\n  if (!data) return correlatedJson(cid, { error: \"Unable to save\" }, { status: 500 });\n  return correlatedJson(cid, { data });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const ok = await deleteCmsRedirect(sup.client, id, organization.id);\n  if (!ok) return correlatedJson(cid, { error: \"Unable to delete\" }, { status: 500 });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/cms/redirects/[id]:PUT\", put);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/cms/redirects/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_cms_redirects_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_cms_redirects_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects/bulk:
    patch:
      operationId: patch_admin_cms_redirects_bulk
      summary: "Update Admin / Cms / Redirects / Bulk"
      description: "Admin / Cms / Redirects / Bulk. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { cmsRedirectBulkSchema } from \"@/lib/cms-route-contracts\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = cmsRedirectBulkSchema.safeParse(await req.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid redirect bulk payload\" }, { status: 400 });\n  const { ids, active } = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  let updated = 0;\n  for (const id of ids) {\n    const { data } = await sup.client.from(\"cms_redirects\").select(\"*\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n    if (!data) continue;\n    const r = data as Record<string, unknown>;\n    const row = await upsertCmsRedirect(sup.client, {\n      id,\n      from_path: String(r.from_path ?? \"\"),\n      to_path: String(r.to_path ?? \"\"),\n      status_code: Number(r.status_code) as 301 | 302 | 307 | 308,\n      active,\n      preserve_query: Boolean(r.preserve_query),\n      organization_id: organization.id,\n    });\n    if (row) updated++;\n  }\n  return correlatedJson(cid, { data: { updated } });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/cms/redirects/bulk:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_cms_redirects_bulkRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_cms_redirects_bulkResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects/export:
    get:
      operationId: get_admin_cms_redirects_export
      summary: "Read Admin / Cms / Redirects / Export"
      description: "Admin / Cms / Redirects / Export. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsRedirects } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nfunction esc(s: string) {\n  if (/[\",\\n]/.test(s)) return `\"${s.replace(/\"/g, '\"\"')}\"`;\n  return s;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return new Response(\"Unauthorized\", { status: 401 });\n  if (!staffSessionAllows(session, \"content:read\")) return new Response(\"Forbidden\", { status: 403 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return new Response(\"Tenant scope unavailable\", { status: 403 });\n  const rows = await listCmsRedirects(sup.client, organization.id);\n  const lines = [\n    [\"from_path\", \"to_path\", \"status_code\", \"active\", \"preserve_query\"].join(\",\"),\n  ];\n  for (const r of rows) {\n    lines.push(\n      [\n        esc(r.from_path),\n        esc(r.to_path),\n        String(r.status_code),\n        r.active ? \"1\" : \"0\",\n        r.preserve_query ? \"1\" : \"0\",\n      ].join(\",\"),\n    );\n  }\n  return new Response(lines.join(\"\\n\"), {\n    headers: {\n      \"Content-Type\": \"text/csv; charset=utf-8\",\n      \"Content-Disposition\": `attachment; filename=\"cms-redirects-${cid.slice(0, 8)}.csv\"`,\n    },\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_redirects_exportResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects/import:
    post:
      operationId: post_admin_cms_redirects_import
      summary: "Create or execute Admin / Cms / Redirects / Import"
      description: "Admin / Cms / Redirects / Import. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { upsertCmsRedirect } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nfunction parseCsvLine(line: string): string[] {\n  const out: string[] = [];\n  let cur = \"\";\n  let q = false;\n  for (let i = 0; i < line.length; i++) {\n    const c = line[i];\n    if (c === '\"') {\n      if (q && line[i + 1] === '\"') {\n        cur += '\"';\n        i++;\n      } else q = !q;\n    } else if (c === \",\" && !q) {\n      out.push(cur);\n      cur = \"\";\n    } else cur += c;\n  }\n  out.push(cur);\n  return out.map((s) => s.trim());\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const contentLength = Number(req.headers.get(\"content-length\") ?? \"0\");\n  if (Number.isFinite(contentLength) && contentLength > 1_048_576) {\n    return correlatedJson(cid, { error: \"CSV too large\" }, { status: 413 });\n  }\n  const text = await req.text();\n  if (new TextEncoder().encode(text).byteLength > 1_048_576) {\n    return correlatedJson(cid, { error: \"CSV too large\" }, { status: 413 });\n  }\n  const lines = text.split(/\\r?\\n/).filter((l) => l.trim().length > 0);\n  if (\n    lines.length < 2 ||\n    lines.length > 1001 ||\n    lines.some((line) => line.length > 16_384)\n  ) {\n    return correlatedJson(\n      cid,\n      { error: \"CSV must include header and one row\" },\n      { status: 400 },\n    );\n  }\n  const header = parseCsvLine(lines[0]!).map((h) => h.toLowerCase());\n  const fi = header.indexOf(\"from_path\");\n  const ti = header.indexOf(\"to_path\");\n  const si = header.indexOf(\"status_code\");\n  const ai = header.indexOf(\"active\");\n  const pi = header.indexOf(\"preserve_query\");\n  if (fi < 0 || ti < 0) {\n    return correlatedJson(\n      cid,\n      { error: \"CSV must include from_path and to_path columns\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Tenant scope unavailable\" },\n      { status: 403 },\n    );\n  const warnings: string[] = [];\n  let imported = 0;\n  for (let r = 1; r < lines.length; r++) {\n    const cols = parseCsvLine(lines[r]!);\n    const from_path = cols[fi]?.trim() ?? \"\";\n    const to_path = cols[ti]?.trim() ?? \"\";\n    if (\n      !from_path ||\n      !to_path ||\n      from_path.length > 2048 ||\n      to_path.length > 2048 ||\n      /[\\u0000-\\u001f\\u007f]/.test(`${from_path}${to_path}`)\n    ) {\n      warnings.push(`Row ${r + 1}: skipped (empty path)`);\n      continue;\n    }\n    const normalizedFrom = from_path.startsWith(\"/\")\n      ? from_path\n      : `/${from_path}`;\n    if (\n      normalizedFrom.startsWith(\"//\") ||\n      !to_path.startsWith(\"/\") ||\n      to_path.startsWith(\"//\")\n    ) {\n      warnings.push(`Row ${r + 1}: skipped (paths must be local)`);\n      continue;\n    }\n    const parsedStatus = si < 0 || !cols[si]?.trim() ? 301 : Number(cols[si]);\n    if (![301, 302, 307, 308].includes(parsedStatus)) {\n      warnings.push(`Row ${r + 1}: skipped (invalid status code)`);\n      continue;\n    }\n    const status_code = parsedStatus as 301 | 302 | 307 | 308;\n    const active =\n      ai < 0 ? true : cols[ai] === \"1\" || cols[ai]?.toLowerCase() === \"true\";\n    const preserve_query =\n      pi < 0 ? false : cols[pi] === \"1\" || cols[pi]?.toLowerCase() === \"true\";\n    const { data: hit } = await sup.client\n      .from(\"cms_redirects\")\n      .select(\"id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"from_path\", normalizedFrom)\n      .maybeSingle();\n    const existingId = hit ? String((hit as { id?: string }).id ?? \"\") : \"\";\n    const row = await upsertCmsRedirect(sup.client, {\n      id: existingId || undefined,\n      from_path: normalizedFrom,\n      to_path,\n      status_code,\n      active,\n      preserve_query,\n      organization_id: organization.id,\n    });\n    if (!row) warnings.push(`Row ${r + 1}: could not save ${normalizedFrom}`);\n    else imported++;\n  }\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email ?? \"unknown\",\n    action: \"cms.redirects.import\",\n    resource: \"cms_redirects\",\n    details: {\n      organization_id: organization.id,\n      imported,\n      warning_count: warnings.length,\n      correlation_id: cid,\n    },\n  });\n  return correlatedJson(cid, {\n    data: { imported, warnings: warnings.slice(0, 100) },\n  });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/cms/redirects/import:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_cms_redirects_importRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_cms_redirects_importResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cms/redirects/resolve:
    get:
      operationId: get_admin_cms_redirects_resolve
      summary: "Read Admin / Cms / Redirects / Resolve"
      description: "Admin / Cms / Redirects / Resolve. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cms"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { listCmsRedirects } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nfunction normPath(p: string) {\n  const t = p.trim();\n  if (!t) return \"/\";\n  return t.startsWith(\"/\") ? t : `/${t}`;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"content:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const raw = req.nextUrl.searchParams.get(\"path\") ?? \"/\";\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization required\" }, { status: 403 });\n  const all = await listCmsRedirects(sup.client, organization.id);\n  const active = all.filter((r) => r.active);\n  const byFrom = new Map(active.map((r) => [normPath(r.from_path), r]));\n\n  const fromPaths = all.map((r) => normPath(r.from_path));\n  const counts = new Map<string, number>();\n  for (const p of fromPaths) counts.set(p, (counts.get(p) ?? 0) + 1);\n  const duplicateFrom = [...counts.entries()].filter(([, n]) => n > 1).map(([p]) => p);\n\n  const chain: {\n    from_path: string;\n    to_path: string;\n    status_code: number;\n    preserve_query: boolean;\n  }[] = [];\n  let cur = normPath(raw);\n  const seen = new Set<string>();\n  for (let i = 0; i < 16; i++) {\n    if (seen.has(cur)) {\n      return correlatedJson(cid, {\n        data: { chain, loop: true, duplicate_from_warnings: [...new Set(duplicateFrom)] },\n      });\n    }\n    seen.add(cur);\n    const row = byFrom.get(cur);\n    if (!row) {\n      return correlatedJson(cid, {\n        data: {\n          chain,\n          resolved: false,\n          final_path: cur,\n          duplicate_from_warnings: [...new Set(duplicateFrom)],\n        },\n      });\n    }\n    chain.push({\n      from_path: row.from_path,\n      to_path: row.to_path,\n      status_code: row.status_code,\n      preserve_query: row.preserve_query,\n    });\n    if (row.to_path.trim().startsWith(\"http\")) {\n      return correlatedJson(cid, {\n        data: {\n          chain,\n          resolved: true,\n          final_url: row.to_path.trim(),\n          status_code: row.status_code,\n          duplicate_from_warnings: [...new Set(duplicateFrom)],\n        },\n      });\n    }\n    cur = normPath(row.to_path);\n  }\n  return correlatedJson(cid, {\n    data: { chain, loop: true, duplicate_from_warnings: [...new Set(duplicateFrom)] },\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cms_redirects_resolveResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/commerce-recovery-metrics:
    get:
      operationId: get_admin_commerce_recovery_metrics
      summary: "Read Admin / Commerce Recovery Metrics"
      description: "Admin / Commerce Recovery Metrics. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Commerce Recovery Metrics"]
      x-source: "import { fetchPaymentAttemptInvalidationDayBuckets } from \"@universal-music-store/platform-data\";\nimport { randomUUID } from \"crypto\";\n\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(request: Request) {\n  const correlationId = randomUUID();\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n\n  const { searchParams } = new URL(request.url);\n  const days = Math.min(90, Math.max(1, Number(searchParams.get(\"days\")) || 14));\n\n  try {\n    const buckets = await fetchPaymentAttemptInvalidationDayBuckets(sup.client, days);\n    const total = buckets.reduce((acc, b) => acc + b.count, 0);\n    return correlatedJson(correlationId, {\n      days,\n      buckets,\n      totalInvalidationsInWindow: total,\n    });\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"Metrics failed\";\n    return correlatedJson(correlationId, { error: msg }, { status: 500 });\n  }\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_commerce_recovery_metricsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/commerce/products/lookup:
    get:
      operationId: get_admin_commerce_products_lookup
      summary: "Read Admin / Commerce / Products / Lookup"
      description: "Admin / Commerce / Products / Lookup. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Commerce"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  collectCommerceProductLookupRows,\n  parseCommerceProductLookupParams,\n} from \"@/lib/commerce-product-lookup\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nfunction pickThumb(p: Record<string, unknown>): string | null {\n  const img = p.thumbnail ?? p.thumbnail_url;\n  if (typeof img === \"string\" && img) return img;\n  const imgs = p.images;\n  if (Array.isArray(imgs) && imgs[0] && typeof imgs[0] === \"object\") {\n    const u = (imgs[0] as { url?: string }).url;\n    if (typeof u === \"string\") return u;\n  }\n  return null;\n}\n\nfunction mapCommerceLookupRow(raw: unknown) {\n  const p = raw as Record<string, unknown>;\n  const variants = Array.isArray(p.variants) ? p.variants : [];\n  const firstSku =\n    variants[0] && typeof variants[0] === \"object\"\n      ? String((variants[0] as { sku?: string }).sku ?? \"\")\n      : \"\";\n  const cats = Array.isArray(p.categories) ? p.categories : [];\n  const catIds = cats\n    .map((c) => (c && typeof c === \"object\" ? String((c as { id?: string }).id ?? \"\") : \"\"))\n    .filter(Boolean);\n  return {\n    id: String(p.id ?? \"\"),\n    title: String(p.title ?? \"\"),\n    handle: String(p.handle ?? \"\"),\n    sku: firstSku,\n    status: String(p.status ?? \"\"),\n    thumbnail_url: pickThumb(p),\n    category_ids: catIds,\n  };\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const can =\n    staffSessionAllows(session, \"catalog:read\") || staffSessionAllows(session, \"content:read\");\n  if (!can) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const lookup = parseCommerceProductLookupParams(req.nextUrl.searchParams);\n  try {\n    const filtered = await collectCommerceProductLookupRows(lookup, async ({ limit, offset, query }) => {\n      const params = new URLSearchParams({\n        limit: String(limit),\n        offset: String(offset),\n        ...(query ? { q: query } : {}),\n        fields:\n          \"+id,+title,+handle,+thumbnail,+status,+variants,*variants,*variants.prices,*categories\",\n      });\n      const path = `/admin/products?${params.toString()}`;\n      const res = await medusaAdminFetch(path);\n      const json = (await res.json()) as Record<string, unknown>;\n      if (!res.ok) {\n        throw new Error(\n          typeof json.message === \"string\" ? json.message : (res.statusText ?? \"Catalog error\"),\n        );\n      }\n      const productsRaw = json.products;\n      const productsIn = Array.isArray(productsRaw) ? productsRaw : [];\n      return productsIn.map(mapCommerceLookupRow);\n    });\n    return correlatedJson(cid, { data: { products: filtered } });\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Store catalog request unavailable\" },\n      { status: 502 },\n    );\n  }\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_commerce_products_lookupResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/commerce/products/search:
    get:
      operationId: get_admin_commerce_products_search
      summary: "Read Admin / Commerce / Products / Search"
      description: "Admin / Commerce / Products / Search. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Commerce"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const can =\n    staffSessionAllows(session, \"catalog:read\") ||\n    staffSessionAllows(session, \"content:read\") ||\n    staffSessionAllows(session, \"pos:use\");\n  if (!can) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const q = req.nextUrl.searchParams.get(\"q\") ?? \"\";\n  try {\n    const path = `/admin/products?limit=30${q ? `&q=${encodeURIComponent(q)}` : \"\"}`;\n    const res = await medusaAdminFetch(path);\n    const json = (await res.json()) as unknown;\n    if (!res.ok) {\n      return correlatedJson(\n        cid,\n        { error: typeof json === \"object\" && json && \"message\" in json ? String((json as { message: unknown }).message) : res.statusText },\n        { status: res.status },\n      );\n    }\n    return correlatedJson(cid, { data: json });\n  } catch (e) {\n    return correlatedJson(\n      cid,\n      { error: e instanceof Error ? e.message : \"Store catalog request unavailable\" },\n      { status: 502 },\n    );\n  }\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_commerce_products_searchResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/cost-visibility:
    get:
      operationId: get_admin_cost_visibility
      summary: "Read Admin / Cost Visibility"
      description: "Admin / Cost Visibility. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Cost Visibility"]
      x-source: "import { NextResponse } from \"next/server\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\n\nexport type CostLineItem = {\n  service: string;\n  category: \"hosting\" | \"database\" | \"cache\" | \"psp_fees\" | \"email\" | \"tracking\" | \"cdn\" | \"other\";\n  monthlyCostPhp: number;\n  note: string;\n};\n\nexport type CostVisibilitySummary = {\n  month: string;\n  totalMonthlyCostPhp: number;\n  items: CostLineItem[];\n  breakdown: Record<string, number>;\n};\n\nexport async function GET() {\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n\n  const items: CostLineItem[] = [\n    { service: \"Vercel (Storefront)\", category: \"hosting\", monthlyCostPhp: 0, note: \"Free tier / Pro plan\" },\n    { service: \"Vercel (Admin)\", category: \"hosting\", monthlyCostPhp: 0, note: \"Free tier / Pro plan\" },\n    { service: \"Render (Medusa)\", category: \"hosting\", monthlyCostPhp: 0, note: \"Starter plan\" },\n    { service: \"Render (API)\", category: \"hosting\", monthlyCostPhp: 0, note: \"Starter plan\" },\n    { service: \"Supabase (Database)\", category: \"database\", monthlyCostPhp: 0, note: \"Free tier / Pro plan\" },\n    { service: \"Supabase (Auth)\", category: \"database\", monthlyCostPhp: 0, note: \"Included\" },\n    { service: \"Redis Cloud\", category: \"cache\", monthlyCostPhp: 0, note: \"If used\" },\n    { service: \"Stripe Fees (~2.9%+30c)\", category: \"psp_fees\", monthlyCostPhp: 0, note: \"Variable based on volume\" },\n    { service: \"PayPal Fees (~3.49%+49c)\", category: \"psp_fees\", monthlyCostPhp: 0, note: \"Variable based on volume\" },\n    { service: \"PayMongo Fees (~3.5%)\", category: \"psp_fees\", monthlyCostPhp: 0, note: \"Variable based on volume\" },\n    { service: \"Maya Fees (~2.5%)\", category: \"psp_fees\", monthlyCostPhp: 0, note: \"Variable based on volume\" },\n    { service: \"Resend (Email)\", category: \"email\", monthlyCostPhp: 0, note: \"Free tier / usage based\" },\n    { service: \"AfterShip (Tracking)\", category: \"tracking\", monthlyCostPhp: 0, note: \"Plan-dependent\" },\n    { service: \"Vercel Edge Network\", category: \"cdn\", monthlyCostPhp: 0, note: \"Included with Vercel\" },\n  ];\n\n  const breakdown: Record<string, number> = {};\n  for (const item of items) {\n    breakdown[item.category] = (breakdown[item.category] ?? 0) + item.monthlyCostPhp;\n  }\n\n  const summary: CostVisibilitySummary = {\n    month: new Date().toISOString().slice(0, 7),\n    totalMonthlyCostPhp: items.reduce((sum, i) => sum + i.monthlyCostPhp, 0),\n    items,\n    breakdown,\n  };\n\n  return NextResponse.json(summary);\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_cost_visibilityResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/bridge:
    get:
      operationId: get_admin_crm_bridge
      summary: "Read Admin / Crm / Bridge"
      description: "Admin / Crm / Bridge. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  buildNangoCrmConnectionMetadata,\n  buildNangoCrmConnectionTags,\n  buildNangoCrmRecordMetadata,\n  listCrmIntegrationConnections,\n  listCrmIntegrationMappings,\n  listCrmIntegrationRecords,\n  NANGO_CRM_SUPPORTED_APPS,\n  upsertCrmIntegrationConnection,\n  upsertCrmIntegrationMapping,\n  upsertCrmIntegrationRecord,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nfunction cleanText(value: unknown): string | null {\n  return typeof value === \"string\" && value.trim() ? value.trim() : null;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"crm:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const [connections, mappings] = await Promise.all([\n    listCrmIntegrationConnections(sup.client, { limit: 25, organizationId: organization.id }),\n    listCrmIntegrationMappings(sup.client, { limit: 25, organizationId: organization.id }),\n  ]);\n  const recordGroups = await Promise.all(\n    connections.map((connection) =>\n      listCrmIntegrationRecords(sup.client, {\n        connectionId: connection.connection_id,\n        limit: 25,\n      }),\n    ),\n  );\n  const records = recordGroups.flat().slice(0, 25);\n\n  return correlatedJson(cid, {\n    data: {\n      connections,\n      mappings,\n      records,\n      supportedApps: NANGO_CRM_SUPPORTED_APPS.map((app) => ({\n        provider_config_key: app.provider_config_key,\n        label: app.label,\n        category: app.category,\n      })),\n      summary: {\n        connections: connections.length,\n        mappings: mappings.length,\n        records: records.length,\n      },\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"crm:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n\n  const rec = body as Record<string, unknown>;\n  const kind = cleanText(rec.kind);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const organizationId = organization.id;\n  const actorEmail = session.user.email?.trim().toLowerCase() ?? null;\n\n  async function organizationConnectionExists(\n    providerConfigKey: string,\n    connectionId: string,\n  ) {\n    const { data, error } = await sb\n      .from(\"crm_nango_connections\")\n      .select(\"id\")\n      .eq(\"organization_id\", organizationId)\n      .eq(\"provider_config_key\", providerConfigKey)\n      .eq(\"connection_id\", connectionId)\n      .eq(\"active\", true)\n      .maybeSingle();\n    return !error && Boolean(data);\n  }\n\n  if (kind === \"connection\") {\n    const providerConfigKey = cleanText(rec.provider_config_key);\n    const connectionId = cleanText(rec.connection_id);\n    const requestedOrganizationId = organizationId;\n    if (!providerConfigKey || !connectionId) {\n      return correlatedJson(\n        cid,\n        { error: \"provider_config_key and connection_id are required\" },\n        { status: 400 },\n      );\n    }\n\n    const tags = buildNangoCrmConnectionTags({\n      endUserId: cleanText(rec.staff_user_id) ?? connectionId,\n      endUserEmail: cleanText(rec.staff_email) ?? actorEmail ?? \"\",\n      organizationId: requestedOrganizationId,\n      branchId: cleanText(rec.branch_id),\n      staffUserId: cleanText(rec.staff_user_id),\n      staffEmail: cleanText(rec.staff_email) ?? actorEmail ?? undefined,\n    });\n    const metadata = buildNangoCrmConnectionMetadata({\n      providerConfigKey,\n      organizationId,\n      syncScope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      branchId: cleanText(rec.branch_id),\n      workspaceId: cleanText(rec.workspace_id),\n      staffUserId: cleanText(rec.staff_user_id),\n      staffEmail: cleanText(rec.staff_email) ?? actorEmail ?? undefined,\n      enabledEntities:\n        Array.isArray(rec.enabled_entities) &&\n        rec.enabled_entities.every((v) => v === \"contact\" || v === \"deal\")\n          ? (rec.enabled_entities as Array<\"contact\" | \"deal\">)\n          : undefined,\n      source: \"admin-bridge\",\n      branchLabel: cleanText(rec.branch_label),\n      accountLabel: cleanText(rec.connection_name),\n    });\n\n    const row = await upsertCrmIntegrationConnection(sb, {\n      provider_config_key: providerConfigKey,\n      connection_id: connectionId,\n      connection_name: cleanText(rec.connection_name),\n      organization_id: organizationId,\n      branch_id: cleanText(rec.branch_id),\n      staff_user_id: cleanText(rec.staff_user_id),\n      staff_email: cleanText(rec.staff_email) ?? actorEmail,\n      sync_scope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      active: rec.active == null ? true : Boolean(rec.active),\n      tags,\n      metadata: {\n        ...metadata,\n        source: \"admin-bridge\",\n        operator_note: \"Account linked from the admin bridge console\",\n      },\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"record\") {\n    const providerConfigKey = cleanText(rec.provider_config_key);\n    const connectionId = cleanText(rec.connection_id);\n    const localEntityType = rec.local_entity_type === \"deal\" ? \"deal\" : rec.local_entity_type === \"contact\" ? \"contact\" : null;\n    const localRecordId = cleanText(rec.local_record_id);\n    if (!providerConfigKey || !connectionId || !localEntityType || !localRecordId) {\n      return correlatedJson(\n        cid,\n        { error: \"provider_config_key, connection_id, local_entity_type, and local_record_id are required\" },\n        { status: 400 },\n      );\n    }\n    if (!(await organizationConnectionExists(providerConfigKey, connectionId))) {\n      return correlatedJson(cid, { error: \"CRM connection is not available for this organization\" }, { status: 403 });\n    }\n\n    const row = await upsertCrmIntegrationRecord(sb, {\n      provider_config_key: providerConfigKey,\n      connection_id: connectionId,\n      local_entity_type: localEntityType,\n      local_record_id: localRecordId,\n      local_record_label: cleanText(rec.local_record_label),\n      external_entity_type:\n        rec.external_entity_type === \"deal\" ? \"deal\" : rec.external_entity_type === \"contact\" ? \"contact\" : undefined,\n      external_record_id: cleanText(rec.external_record_id),\n      external_account_id: cleanText(rec.external_account_id),\n      sync_scope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      sync_state: cleanText(rec.sync_state) as\n        | \"pending\"\n        | \"synced\"\n        | \"partial\"\n        | \"failed\"\n        | \"manual_only\"\n        | \"disabled\"\n        | \"stale\"\n        | undefined,\n      sync_mode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n      metadata: buildNangoCrmRecordMetadata({\n        providerConfigKey,\n        connectionId,\n        localEntityType,\n        localRecordId,\n        localRecordLabel: cleanText(rec.local_record_label),\n        externalEntityType:\n          rec.external_entity_type === \"deal\"\n            ? \"deal\"\n            : rec.external_entity_type === \"contact\"\n              ? \"contact\"\n              : undefined,\n        externalRecordId: cleanText(rec.external_record_id),\n        externalAccountId: cleanText(rec.external_account_id),\n        syncScope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n        syncState: cleanText(rec.sync_state) as\n          | \"pending\"\n          | \"synced\"\n          | \"partial\"\n          | \"failed\"\n          | \"manual_only\"\n          | \"disabled\"\n          | \"stale\"\n          | undefined,\n        syncMode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n        lastDirection:\n          rec.last_direction === \"to_crm\" || rec.last_direction === \"from_crm\" || rec.last_direction === \"bidirectional\"\n            ? rec.last_direction\n            : undefined,\n        staffEmail: actorEmail,\n        source: \"admin-bridge\",\n      }),\n      last_direction:\n        rec.last_direction === \"to_crm\" || rec.last_direction === \"from_crm\" || rec.last_direction === \"bidirectional\"\n          ? rec.last_direction\n          : undefined,\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"mapping\") {\n    const customerEmail = cleanText(rec.customer_email);\n    const mappingConnectionId = cleanText(rec.connection_id);\n    const mappingProviderConfigKey = cleanText(rec.provider_config_key);\n    if (!customerEmail) {\n      return correlatedJson(cid, { error: \"customer_email is required\" }, { status: 400 });\n    }\n    if (mappingConnectionId && mappingProviderConfigKey && !(await organizationConnectionExists(mappingProviderConfigKey, mappingConnectionId))) {\n      return correlatedJson(cid, { error: \"CRM connection is not available for this organization\" }, { status: 403 });\n    }\n\n    const row = await upsertCrmIntegrationMapping(sb, {\n      organization_id: organizationId,\n      customer_email: customerEmail,\n      medusa_customer_id: cleanText(rec.medusa_customer_id),\n      provider: \"nango\",\n      connection_id: cleanText(rec.connection_id),\n      external_contact_id: cleanText(rec.external_contact_id),\n      external_account_id: cleanText(rec.external_account_id),\n      sync_state: cleanText(rec.sync_state) as\n        | \"pending\"\n        | \"synced\"\n        | \"partial\"\n        | \"failed\"\n        | \"manual_only\"\n        | \"disabled\"\n        | \"stale\"\n        | undefined,\n      sync_mode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n      metadata: {\n        source: \"admin-bridge\",\n        customer_name: cleanText(rec.customer_name),\n        notes: cleanText(rec.notes),\n      },\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  return correlatedJson(cid, { error: \"kind must be connection, record, or mapping\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/bridge:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_crm_bridgeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_crm_bridge
      summary: "Create or execute Admin / Crm / Bridge"
      description: "Admin / Crm / Bridge. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  buildNangoCrmConnectionMetadata,\n  buildNangoCrmConnectionTags,\n  buildNangoCrmRecordMetadata,\n  listCrmIntegrationConnections,\n  listCrmIntegrationMappings,\n  listCrmIntegrationRecords,\n  NANGO_CRM_SUPPORTED_APPS,\n  upsertCrmIntegrationConnection,\n  upsertCrmIntegrationMapping,\n  upsertCrmIntegrationRecord,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nfunction cleanText(value: unknown): string | null {\n  return typeof value === \"string\" && value.trim() ? value.trim() : null;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"crm:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const [connections, mappings] = await Promise.all([\n    listCrmIntegrationConnections(sup.client, { limit: 25, organizationId: organization.id }),\n    listCrmIntegrationMappings(sup.client, { limit: 25, organizationId: organization.id }),\n  ]);\n  const recordGroups = await Promise.all(\n    connections.map((connection) =>\n      listCrmIntegrationRecords(sup.client, {\n        connectionId: connection.connection_id,\n        limit: 25,\n      }),\n    ),\n  );\n  const records = recordGroups.flat().slice(0, 25);\n\n  return correlatedJson(cid, {\n    data: {\n      connections,\n      mappings,\n      records,\n      supportedApps: NANGO_CRM_SUPPORTED_APPS.map((app) => ({\n        provider_config_key: app.provider_config_key,\n        label: app.label,\n        category: app.category,\n      })),\n      summary: {\n        connections: connections.length,\n        mappings: mappings.length,\n        records: records.length,\n      },\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"crm:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n\n  const rec = body as Record<string, unknown>;\n  const kind = cleanText(rec.kind);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const organizationId = organization.id;\n  const actorEmail = session.user.email?.trim().toLowerCase() ?? null;\n\n  async function organizationConnectionExists(\n    providerConfigKey: string,\n    connectionId: string,\n  ) {\n    const { data, error } = await sb\n      .from(\"crm_nango_connections\")\n      .select(\"id\")\n      .eq(\"organization_id\", organizationId)\n      .eq(\"provider_config_key\", providerConfigKey)\n      .eq(\"connection_id\", connectionId)\n      .eq(\"active\", true)\n      .maybeSingle();\n    return !error && Boolean(data);\n  }\n\n  if (kind === \"connection\") {\n    const providerConfigKey = cleanText(rec.provider_config_key);\n    const connectionId = cleanText(rec.connection_id);\n    const requestedOrganizationId = organizationId;\n    if (!providerConfigKey || !connectionId) {\n      return correlatedJson(\n        cid,\n        { error: \"provider_config_key and connection_id are required\" },\n        { status: 400 },\n      );\n    }\n\n    const tags = buildNangoCrmConnectionTags({\n      endUserId: cleanText(rec.staff_user_id) ?? connectionId,\n      endUserEmail: cleanText(rec.staff_email) ?? actorEmail ?? \"\",\n      organizationId: requestedOrganizationId,\n      branchId: cleanText(rec.branch_id),\n      staffUserId: cleanText(rec.staff_user_id),\n      staffEmail: cleanText(rec.staff_email) ?? actorEmail ?? undefined,\n    });\n    const metadata = buildNangoCrmConnectionMetadata({\n      providerConfigKey,\n      organizationId,\n      syncScope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      branchId: cleanText(rec.branch_id),\n      workspaceId: cleanText(rec.workspace_id),\n      staffUserId: cleanText(rec.staff_user_id),\n      staffEmail: cleanText(rec.staff_email) ?? actorEmail ?? undefined,\n      enabledEntities:\n        Array.isArray(rec.enabled_entities) &&\n        rec.enabled_entities.every((v) => v === \"contact\" || v === \"deal\")\n          ? (rec.enabled_entities as Array<\"contact\" | \"deal\">)\n          : undefined,\n      source: \"admin-bridge\",\n      branchLabel: cleanText(rec.branch_label),\n      accountLabel: cleanText(rec.connection_name),\n    });\n\n    const row = await upsertCrmIntegrationConnection(sb, {\n      provider_config_key: providerConfigKey,\n      connection_id: connectionId,\n      connection_name: cleanText(rec.connection_name),\n      organization_id: organizationId,\n      branch_id: cleanText(rec.branch_id),\n      staff_user_id: cleanText(rec.staff_user_id),\n      staff_email: cleanText(rec.staff_email) ?? actorEmail,\n      sync_scope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      active: rec.active == null ? true : Boolean(rec.active),\n      tags,\n      metadata: {\n        ...metadata,\n        source: \"admin-bridge\",\n        operator_note: \"Account linked from the admin bridge console\",\n      },\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"record\") {\n    const providerConfigKey = cleanText(rec.provider_config_key);\n    const connectionId = cleanText(rec.connection_id);\n    const localEntityType = rec.local_entity_type === \"deal\" ? \"deal\" : rec.local_entity_type === \"contact\" ? \"contact\" : null;\n    const localRecordId = cleanText(rec.local_record_id);\n    if (!providerConfigKey || !connectionId || !localEntityType || !localRecordId) {\n      return correlatedJson(\n        cid,\n        { error: \"provider_config_key, connection_id, local_entity_type, and local_record_id are required\" },\n        { status: 400 },\n      );\n    }\n    if (!(await organizationConnectionExists(providerConfigKey, connectionId))) {\n      return correlatedJson(cid, { error: \"CRM connection is not available for this organization\" }, { status: 403 });\n    }\n\n    const row = await upsertCrmIntegrationRecord(sb, {\n      provider_config_key: providerConfigKey,\n      connection_id: connectionId,\n      local_entity_type: localEntityType,\n      local_record_id: localRecordId,\n      local_record_label: cleanText(rec.local_record_label),\n      external_entity_type:\n        rec.external_entity_type === \"deal\" ? \"deal\" : rec.external_entity_type === \"contact\" ? \"contact\" : undefined,\n      external_record_id: cleanText(rec.external_record_id),\n      external_account_id: cleanText(rec.external_account_id),\n      sync_scope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n      sync_state: cleanText(rec.sync_state) as\n        | \"pending\"\n        | \"synced\"\n        | \"partial\"\n        | \"failed\"\n        | \"manual_only\"\n        | \"disabled\"\n        | \"stale\"\n        | undefined,\n      sync_mode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n      metadata: buildNangoCrmRecordMetadata({\n        providerConfigKey,\n        connectionId,\n        localEntityType,\n        localRecordId,\n        localRecordLabel: cleanText(rec.local_record_label),\n        externalEntityType:\n          rec.external_entity_type === \"deal\"\n            ? \"deal\"\n            : rec.external_entity_type === \"contact\"\n              ? \"contact\"\n              : undefined,\n        externalRecordId: cleanText(rec.external_record_id),\n        externalAccountId: cleanText(rec.external_account_id),\n        syncScope: cleanText(rec.sync_scope) as \"global\" | \"organization\" | \"branch\" | \"customer\" | undefined,\n        syncState: cleanText(rec.sync_state) as\n          | \"pending\"\n          | \"synced\"\n          | \"partial\"\n          | \"failed\"\n          | \"manual_only\"\n          | \"disabled\"\n          | \"stale\"\n          | undefined,\n        syncMode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n        lastDirection:\n          rec.last_direction === \"to_crm\" || rec.last_direction === \"from_crm\" || rec.last_direction === \"bidirectional\"\n            ? rec.last_direction\n            : undefined,\n        staffEmail: actorEmail,\n        source: \"admin-bridge\",\n      }),\n      last_direction:\n        rec.last_direction === \"to_crm\" || rec.last_direction === \"from_crm\" || rec.last_direction === \"bidirectional\"\n          ? rec.last_direction\n          : undefined,\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"mapping\") {\n    const customerEmail = cleanText(rec.customer_email);\n    const mappingConnectionId = cleanText(rec.connection_id);\n    const mappingProviderConfigKey = cleanText(rec.provider_config_key);\n    if (!customerEmail) {\n      return correlatedJson(cid, { error: \"customer_email is required\" }, { status: 400 });\n    }\n    if (mappingConnectionId && mappingProviderConfigKey && !(await organizationConnectionExists(mappingProviderConfigKey, mappingConnectionId))) {\n      return correlatedJson(cid, { error: \"CRM connection is not available for this organization\" }, { status: 403 });\n    }\n\n    const row = await upsertCrmIntegrationMapping(sb, {\n      organization_id: organizationId,\n      customer_email: customerEmail,\n      medusa_customer_id: cleanText(rec.medusa_customer_id),\n      provider: \"nango\",\n      connection_id: cleanText(rec.connection_id),\n      external_contact_id: cleanText(rec.external_contact_id),\n      external_account_id: cleanText(rec.external_account_id),\n      sync_state: cleanText(rec.sync_state) as\n        | \"pending\"\n        | \"synced\"\n        | \"partial\"\n        | \"failed\"\n        | \"manual_only\"\n        | \"disabled\"\n        | \"stale\"\n        | undefined,\n      sync_mode: cleanText(rec.sync_mode) as \"automatic\" | \"manual\" | \"disabled\" | undefined,\n      metadata: {\n        source: \"admin-bridge\",\n        customer_name: cleanText(rec.customer_name),\n        notes: cleanText(rec.notes),\n      },\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  return correlatedJson(cid, { error: \"kind must be connection, record, or mapping\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/bridge:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_crm_bridgeRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_crm_bridgeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/nango:
    delete:
      operationId: delete_admin_crm_nango
      summary: "Delete Admin / Crm / Nango"
      description: "Admin / Crm / Nango. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization, organizationCanManageCrmConnections } from \"@/lib/staff-organization\";\nimport { configuredCrmIntegrations, nangoCrmErrorMessage, type CrmNangoConnection } from \"@/lib/crm-nango\";\nimport { buildNangoCrmConnectionMetadata, buildNangoCrmConnectionTags, upsertCrmIntegrationConnection } from \"@universal-music-store/platform-data\";\n\nexport const dynamic = \"force-dynamic\";\nconst saveSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), connection_id: z.string().trim().min(1).max(200) }).strict();\nconst disconnectSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), nango_connection_id: z.string().trim().min(1).max(200) }).strict();\n\nasync function organizationContext(cid: string, permission: \"crm:read\" | \"crm:write\") {\n  const staff = await requireStaffApiSession(permission);\n  if (!staff.ok) return { response: staff.response } as const;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return { response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return { response: correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 }) } as const;\n  return { staff, sup, organization } as const;\n}\n\nasync function get(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:read\");\n  if (\"response\" in context) return context.response;\n  const configured = configuredCrmIntegrations();\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  const identity = context.staff.session.user?.email?.trim().toLowerCase() ?? \"\";\n  if (!apiKey) return correlatedJson(cid, { data: [], integrations: configured, source: \"nango-not-configured\" });\n  const query = new URLSearchParams({ \"tags[end_user_id]\": identity, \"tags[organization_id]\": context.organization.id, limit: \"100\" });\n  const response = await fetch(`https://api.nango.dev/connections?${query}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const payload = await response.json().catch(() => ({}));\n  if (!response.ok) return correlatedJson(cid, { error: \"Nango connection status unavailable\" }, { status: 502 });\n  const allowed = new Set(configured.map((item) => item.id));\n  const data: CrmNangoConnection[] = (payload.connections ?? [])\n    .filter((row: Record<string, unknown>) => {\n      const tags = row.tags as Record<string, unknown> | undefined;\n      return allowed.has(String(row.provider_config_key)) && tags?.organization_id === context.organization.id;\n    })\n    .map((row: Record<string, unknown>) => ({\n      provider_config_key: String(row.provider_config_key),\n      nango_connection_id: String(row.connection_id),\n      provider: typeof row.provider === \"string\" ? row.provider : null,\n      active: true,\n      status: nangoCrmErrorMessage(row.errors) ? \"needs_attention\" : \"connected\",\n      error: nangoCrmErrorMessage(row.errors),\n      updated_at: typeof row.updated_at === \"string\" ? row.updated_at : new Date().toISOString(),\n    }));\n  return correlatedJson(cid, { data, integrations: configured, source: \"nango\" });\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, saveSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, connection_id: connectionId } = parsed.data;\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  const verification = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const remotePayload = await verification.json().catch(() => ({})) as Record<string, unknown> & { data?: Record<string, unknown> };\n  if (!verification.ok || String((remotePayload.data ?? remotePayload).connection_id ?? connectionId) !== connectionId) return correlatedJson(cid, { error: \"CRM provider connection could not be verified\" }, { status: 409 });\n  const actorEmail = context.staff.session.user?.email?.trim().toLowerCase() ?? null;\n  const row = await upsertCrmIntegrationConnection(context.sup.client, {\n    provider_config_key: providerConfigKey,\n    connection_id: connectionId,\n    connection_name: null,\n    organization_id: context.organization.id,\n    staff_email: actorEmail,\n    active: true,\n    metadata: { ...(buildNangoCrmConnectionMetadata({ providerConfigKey, organizationId: context.organization.id, syncScope: \"organization\", staffEmail: actorEmail ?? undefined, source: \"nango-connect\" })), provider: (remotePayload.data ?? remotePayload).provider ?? null },\n    sync_scope: \"organization\",\n    tags: buildNangoCrmConnectionTags({ endUserId: actorEmail ?? connectionId, endUserEmail: actorEmail ?? \"\", organizationId: context.organization.id, staffEmail: actorEmail ?? undefined }),\n  });\n  return correlatedJson(cid, { data: row }, { status: 201 });\n}\n\nasync function del(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, nango_connection_id: connectionId } = parsed.data;\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const remote = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { method: \"DELETE\", headers: { Authorization: `Bearer ${apiKey}` } });\n  if (!remote.ok && remote.status !== 404) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  const { error } = await context.sup.client.from(\"crm_nango_connections\").update({ active: false }).eq(\"organization_id\", context.organization.id).eq(\"provider_config_key\", providerConfigKey).eq(\"connection_id\", connectionId);\n  if (error) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const GET = get;\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/nango:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/nango:DELETE\", del);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_crm_nangoRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_crm_nangoResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_crm_nango
      summary: "Read Admin / Crm / Nango"
      description: "Admin / Crm / Nango. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization, organizationCanManageCrmConnections } from \"@/lib/staff-organization\";\nimport { configuredCrmIntegrations, nangoCrmErrorMessage, type CrmNangoConnection } from \"@/lib/crm-nango\";\nimport { buildNangoCrmConnectionMetadata, buildNangoCrmConnectionTags, upsertCrmIntegrationConnection } from \"@universal-music-store/platform-data\";\n\nexport const dynamic = \"force-dynamic\";\nconst saveSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), connection_id: z.string().trim().min(1).max(200) }).strict();\nconst disconnectSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), nango_connection_id: z.string().trim().min(1).max(200) }).strict();\n\nasync function organizationContext(cid: string, permission: \"crm:read\" | \"crm:write\") {\n  const staff = await requireStaffApiSession(permission);\n  if (!staff.ok) return { response: staff.response } as const;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return { response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return { response: correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 }) } as const;\n  return { staff, sup, organization } as const;\n}\n\nasync function get(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:read\");\n  if (\"response\" in context) return context.response;\n  const configured = configuredCrmIntegrations();\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  const identity = context.staff.session.user?.email?.trim().toLowerCase() ?? \"\";\n  if (!apiKey) return correlatedJson(cid, { data: [], integrations: configured, source: \"nango-not-configured\" });\n  const query = new URLSearchParams({ \"tags[end_user_id]\": identity, \"tags[organization_id]\": context.organization.id, limit: \"100\" });\n  const response = await fetch(`https://api.nango.dev/connections?${query}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const payload = await response.json().catch(() => ({}));\n  if (!response.ok) return correlatedJson(cid, { error: \"Nango connection status unavailable\" }, { status: 502 });\n  const allowed = new Set(configured.map((item) => item.id));\n  const data: CrmNangoConnection[] = (payload.connections ?? [])\n    .filter((row: Record<string, unknown>) => {\n      const tags = row.tags as Record<string, unknown> | undefined;\n      return allowed.has(String(row.provider_config_key)) && tags?.organization_id === context.organization.id;\n    })\n    .map((row: Record<string, unknown>) => ({\n      provider_config_key: String(row.provider_config_key),\n      nango_connection_id: String(row.connection_id),\n      provider: typeof row.provider === \"string\" ? row.provider : null,\n      active: true,\n      status: nangoCrmErrorMessage(row.errors) ? \"needs_attention\" : \"connected\",\n      error: nangoCrmErrorMessage(row.errors),\n      updated_at: typeof row.updated_at === \"string\" ? row.updated_at : new Date().toISOString(),\n    }));\n  return correlatedJson(cid, { data, integrations: configured, source: \"nango\" });\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, saveSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, connection_id: connectionId } = parsed.data;\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  const verification = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const remotePayload = await verification.json().catch(() => ({})) as Record<string, unknown> & { data?: Record<string, unknown> };\n  if (!verification.ok || String((remotePayload.data ?? remotePayload).connection_id ?? connectionId) !== connectionId) return correlatedJson(cid, { error: \"CRM provider connection could not be verified\" }, { status: 409 });\n  const actorEmail = context.staff.session.user?.email?.trim().toLowerCase() ?? null;\n  const row = await upsertCrmIntegrationConnection(context.sup.client, {\n    provider_config_key: providerConfigKey,\n    connection_id: connectionId,\n    connection_name: null,\n    organization_id: context.organization.id,\n    staff_email: actorEmail,\n    active: true,\n    metadata: { ...(buildNangoCrmConnectionMetadata({ providerConfigKey, organizationId: context.organization.id, syncScope: \"organization\", staffEmail: actorEmail ?? undefined, source: \"nango-connect\" })), provider: (remotePayload.data ?? remotePayload).provider ?? null },\n    sync_scope: \"organization\",\n    tags: buildNangoCrmConnectionTags({ endUserId: actorEmail ?? connectionId, endUserEmail: actorEmail ?? \"\", organizationId: context.organization.id, staffEmail: actorEmail ?? undefined }),\n  });\n  return correlatedJson(cid, { data: row }, { status: 201 });\n}\n\nasync function del(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, nango_connection_id: connectionId } = parsed.data;\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const remote = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { method: \"DELETE\", headers: { Authorization: `Bearer ${apiKey}` } });\n  if (!remote.ok && remote.status !== 404) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  const { error } = await context.sup.client.from(\"crm_nango_connections\").update({ active: false }).eq(\"organization_id\", context.organization.id).eq(\"provider_config_key\", providerConfigKey).eq(\"connection_id\", connectionId);\n  if (error) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const GET = get;\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/nango:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/nango:DELETE\", del);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_crm_nangoResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_crm_nango
      summary: "Create or execute Admin / Crm / Nango"
      description: "Admin / Crm / Nango. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization, organizationCanManageCrmConnections } from \"@/lib/staff-organization\";\nimport { configuredCrmIntegrations, nangoCrmErrorMessage, type CrmNangoConnection } from \"@/lib/crm-nango\";\nimport { buildNangoCrmConnectionMetadata, buildNangoCrmConnectionTags, upsertCrmIntegrationConnection } from \"@universal-music-store/platform-data\";\n\nexport const dynamic = \"force-dynamic\";\nconst saveSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), connection_id: z.string().trim().min(1).max(200) }).strict();\nconst disconnectSchema = z.object({ provider_config_key: z.string().trim().min(1).max(100), nango_connection_id: z.string().trim().min(1).max(200) }).strict();\n\nasync function organizationContext(cid: string, permission: \"crm:read\" | \"crm:write\") {\n  const staff = await requireStaffApiSession(permission);\n  if (!staff.ok) return { response: staff.response } as const;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return { response: sup.response } as const;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return { response: correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 }) } as const;\n  return { staff, sup, organization } as const;\n}\n\nasync function get(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:read\");\n  if (\"response\" in context) return context.response;\n  const configured = configuredCrmIntegrations();\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  const identity = context.staff.session.user?.email?.trim().toLowerCase() ?? \"\";\n  if (!apiKey) return correlatedJson(cid, { data: [], integrations: configured, source: \"nango-not-configured\" });\n  const query = new URLSearchParams({ \"tags[end_user_id]\": identity, \"tags[organization_id]\": context.organization.id, limit: \"100\" });\n  const response = await fetch(`https://api.nango.dev/connections?${query}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const payload = await response.json().catch(() => ({}));\n  if (!response.ok) return correlatedJson(cid, { error: \"Nango connection status unavailable\" }, { status: 502 });\n  const allowed = new Set(configured.map((item) => item.id));\n  const data: CrmNangoConnection[] = (payload.connections ?? [])\n    .filter((row: Record<string, unknown>) => {\n      const tags = row.tags as Record<string, unknown> | undefined;\n      return allowed.has(String(row.provider_config_key)) && tags?.organization_id === context.organization.id;\n    })\n    .map((row: Record<string, unknown>) => ({\n      provider_config_key: String(row.provider_config_key),\n      nango_connection_id: String(row.connection_id),\n      provider: typeof row.provider === \"string\" ? row.provider : null,\n      active: true,\n      status: nangoCrmErrorMessage(row.errors) ? \"needs_attention\" : \"connected\",\n      error: nangoCrmErrorMessage(row.errors),\n      updated_at: typeof row.updated_at === \"string\" ? row.updated_at : new Date().toISOString(),\n    }));\n  return correlatedJson(cid, { data, integrations: configured, source: \"nango\" });\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, saveSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, connection_id: connectionId } = parsed.data;\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  const verification = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" });\n  const remotePayload = await verification.json().catch(() => ({})) as Record<string, unknown> & { data?: Record<string, unknown> };\n  if (!verification.ok || String((remotePayload.data ?? remotePayload).connection_id ?? connectionId) !== connectionId) return correlatedJson(cid, { error: \"CRM provider connection could not be verified\" }, { status: 409 });\n  const actorEmail = context.staff.session.user?.email?.trim().toLowerCase() ?? null;\n  const row = await upsertCrmIntegrationConnection(context.sup.client, {\n    provider_config_key: providerConfigKey,\n    connection_id: connectionId,\n    connection_name: null,\n    organization_id: context.organization.id,\n    staff_email: actorEmail,\n    active: true,\n    metadata: { ...(buildNangoCrmConnectionMetadata({ providerConfigKey, organizationId: context.organization.id, syncScope: \"organization\", staffEmail: actorEmail ?? undefined, source: \"nango-connect\" })), provider: (remotePayload.data ?? remotePayload).provider ?? null },\n    sync_scope: \"organization\",\n    tags: buildNangoCrmConnectionTags({ endUserId: actorEmail ?? connectionId, endUserEmail: actorEmail ?? \"\", organizationId: context.organization.id, staffEmail: actorEmail ?? undefined }),\n  });\n  return correlatedJson(cid, { data: row }, { status: 201 });\n}\n\nasync function del(request: Request) {\n  const cid = getCorrelationId(request);\n  const context = await organizationContext(cid, \"crm:write\");\n  if (\"response\" in context) return context.response;\n  if (!organizationCanManageCrmConnections(context.organization.role)) return correlatedJson(cid, { error: \"Only the organization owner can manage CRM connections\" }, { status: 403 });\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { provider_config_key: providerConfigKey, nango_connection_id: connectionId } = parsed.data;\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  if (!configuredCrmIntegrations().some((item) => item.id === providerConfigKey)) return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  const remote = await fetch(`https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`, { method: \"DELETE\", headers: { Authorization: `Bearer ${apiKey}` } });\n  if (!remote.ok && remote.status !== 404) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  const { error } = await context.sup.client.from(\"crm_nango_connections\").update({ active: false }).eq(\"organization_id\", context.organization.id).eq(\"provider_config_key\", providerConfigKey).eq(\"connection_id\", connectionId);\n  if (error) return correlatedJson(cid, { error: \"Unable to disconnect CRM provider\" }, { status: 502 });\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const GET = get;\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/nango:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/nango:DELETE\", del);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_crm_nangoRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_crm_nangoResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/nango/connect-session:
    post:
      operationId: post_admin_crm_nango_connect_session
      summary: "Create or execute Admin / Crm / Nango / Connect Session"
      description: "Admin / Crm / Nango / Connect Session. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization, organizationCanManageCrmConnections } from \"@/lib/staff-organization\";\nimport { configuredCrmIntegrations } from \"@/lib/crm-nango\";\n\nconst schema = z.object({ integration_id: z.string().trim().min(1).max(100) }).strict();\n\nasync function post(request: NextRequest) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"crm:write\");\n  if (!staff.ok) return staff.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization || !organizationCanManageCrmConnections(organization.role)) {\n    return correlatedJson(cid, { error: \"Only the organization owner can connect CRM providers\" }, { status: 403 });\n  }\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return correlatedJson(cid, { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" }, { status: 503 });\n  const parsed = await parseAdminJson(request, schema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const integrationId = parsed.data.integration_id;\n  if (!configuredCrmIntegrations().some((item) => item.id === integrationId)) {\n    return correlatedJson(cid, { error: \"CRM integration is not configured for this server\" }, { status: 400 });\n  }\n  const identity = staff.session.user?.email?.trim().toLowerCase();\n  if (!identity) return correlatedJson(cid, { error: \"Authenticated owner identity is required\" }, { status: 400 });\n  const response = await fetch(\"https://api.nango.dev/connect/sessions\", {\n    method: \"POST\",\n    headers: { Authorization: `Bearer ${apiKey}`, \"Content-Type\": \"application/json\" },\n    body: JSON.stringify({\n      allowed_integrations: [integrationId],\n      tags: { end_user_id: identity, end_user_email: identity, organization_id: organization.id, surface: \"crm\" },\n    }),\n  });\n  const payload = await response.json().catch(() => ({}));\n  if (!response.ok) return correlatedJson(cid, { error: \"Nango Connect session creation failed\" }, { status: 502 });\n  return correlatedJson(cid, { data: { session_token: payload.data?.token ?? payload.token ?? null, integration_id: integrationId } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/nango/connect-session:POST\", post);\n"
      x-permission: "crm:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_crm_nango_connect_sessionRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_crm_nango_connect_sessionResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/notes:
    get:
      operationId: get_admin_crm_notes
      summary: "Read Admin / Crm / Notes"
      description: "Admin / Crm / Notes. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\r\nexport const dynamic = \"force-dynamic\";\n\nconst noteSchema = z.object({\n  customer_email: z.string().trim().toLowerCase().email().max(320),\n  note_body: z.string().trim().min(1).max(4000),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\r\n\r\n  const { searchParams } = new URL(req.url);\r\n  const emailResult = noteSchema.shape.customer_email.safeParse(searchParams.get(\"customer_email\") ?? \"\");\n  if (!emailResult.success) {\n    return correlatedJson(cid, { error: \"customer_email is required\" }, { status: 400 });\n  }\n  const customerEmail = emailResult.data;\n\r\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const { data, error } = await sup.client\n    .from(\"staff_customer_notes\")\r\n    .select(\"id,note_body,author_email,created_at,is_deleted\")\r\n    .eq(\"customer_email\", customerEmail)\n    .eq(\"organization_id\", organization.id)\n    .eq(\"is_deleted\", false)\r\n    .order(\"created_at\", { ascending: false })\r\n    .limit(100);\r\n\r\n  if (error) {\r\n    return correlatedJson(cid, { error: \"Unable to fetch notes\" }, { status: 500 });\r\n  }\r\n\r\n  return correlatedJson(cid, { data: data ?? [] });\r\n}\r\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\r\n\r\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = await parseAdminJson(req, noteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { customer_email: customerEmail, note_body: noteBody } = parsed.data;\n  const authorEmail = session.user.email.trim().toLowerCase();\n\n  const { data, error } = await sup.client\n    .from(\"staff_customer_notes\")\n    .insert({ organization_id: organization.id, customer_email: customerEmail, note_body: noteBody, author_email: authorEmail })\n    .select(\"id,note_body,author_email,created_at\")\r\n    .single();\r\n\r\n  if (error) {\r\n    return correlatedJson(cid, { error: \"Unable to save note\" }, { status: 500 });\r\n  }\r\n\r\n  await insertStaffAuditLog(sup.client, {\r\n    actorEmail: authorEmail,\r\n    action: \"crm.note.create\",\r\n    resource: `customer:${customerEmail}`,\r\n    details: { noteId: data.id },\r\n  });\r\n\r\n  return correlatedJson(cid, { data }, { status: 201 });\r\n}\r\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/notes:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_crm_notesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_crm_notes
      summary: "Create or execute Admin / Crm / Notes"
      description: "Admin / Crm / Notes. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\r\nexport const dynamic = \"force-dynamic\";\n\nconst noteSchema = z.object({\n  customer_email: z.string().trim().toLowerCase().email().max(320),\n  note_body: z.string().trim().min(1).max(4000),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\r\n\r\n  const { searchParams } = new URL(req.url);\r\n  const emailResult = noteSchema.shape.customer_email.safeParse(searchParams.get(\"customer_email\") ?? \"\");\n  if (!emailResult.success) {\n    return correlatedJson(cid, { error: \"customer_email is required\" }, { status: 400 });\n  }\n  const customerEmail = emailResult.data;\n\r\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const { data, error } = await sup.client\n    .from(\"staff_customer_notes\")\r\n    .select(\"id,note_body,author_email,created_at,is_deleted\")\r\n    .eq(\"customer_email\", customerEmail)\n    .eq(\"organization_id\", organization.id)\n    .eq(\"is_deleted\", false)\r\n    .order(\"created_at\", { ascending: false })\r\n    .limit(100);\r\n\r\n  if (error) {\r\n    return correlatedJson(cid, { error: \"Unable to fetch notes\" }, { status: 500 });\r\n  }\r\n\r\n  return correlatedJson(cid, { data: data ?? [] });\r\n}\r\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\r\n\r\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = await parseAdminJson(req, noteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { customer_email: customerEmail, note_body: noteBody } = parsed.data;\n  const authorEmail = session.user.email.trim().toLowerCase();\n\n  const { data, error } = await sup.client\n    .from(\"staff_customer_notes\")\n    .insert({ organization_id: organization.id, customer_email: customerEmail, note_body: noteBody, author_email: authorEmail })\n    .select(\"id,note_body,author_email,created_at\")\r\n    .single();\r\n\r\n  if (error) {\r\n    return correlatedJson(cid, { error: \"Unable to save note\" }, { status: 500 });\r\n  }\r\n\r\n  await insertStaffAuditLog(sup.client, {\r\n    actorEmail: authorEmail,\r\n    action: \"crm.note.create\",\r\n    resource: `customer:${customerEmail}`,\r\n    details: { noteId: data.id },\r\n  });\r\n\r\n  return correlatedJson(cid, { data }, { status: 201 });\r\n}\r\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/notes:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_crm_notesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_crm_notesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/notes/{id}:
    delete:
      operationId: delete_admin_crm_notes_id
      summary: "Delete Admin / Crm / Notes / Id"
      description: "Admin / Crm / Notes / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\r\ntype RouteCtx = { params: Promise<{ id: string }> };\r\n\r\nasync function deleteHandler(req: NextRequest, ctx: RouteCtx) {\n  const cid = getCorrelationId(req);\n  const { id } = await ctx.params;\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\r\n\r\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const { data: existing, error: fetchErr } = await sup.client\n    .from(\"staff_customer_notes\")\r\n    .select(\"id,customer_email,author_email\")\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .single();\r\n\r\n  if (fetchErr || !existing) {\r\n    return correlatedJson(cid, { error: \"Note not found\" }, { status: 404 });\r\n  }\r\n\r\n  const { error } = await sup.client\n    .from(\"staff_customer_notes\")\n    .update({ is_deleted: true })\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id);\n\r\n  if (error) {\r\n    return correlatedJson(cid, { error: \"Unable to delete note\" }, { status: 500 });\r\n  }\r\n\r\n  await insertStaffAuditLog(sup.client, {\r\n    actorEmail: session.user.email?.trim() ?? \"unknown\",\r\n    action: \"crm.note.delete\",\r\n    resource: `customer:${existing.customer_email}`,\r\n    details: { noteId: id },\r\n  });\r\n\r\n  return correlatedJson(cid, { ok: true });\r\n}\r\n\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/notes/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_crm_notes_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_crm_notes_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/crm/operations:
    delete:
      operationId: delete_admin_crm_operations
      summary: "Delete Admin / Crm / Operations"
      description: "Admin / Crm / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst email = z.string().trim().toLowerCase().email().max(320);\nconst metadata = z.record(z.string(), z.unknown()).default({});\nconst isoDate = z.string().datetime({ offset: true });\nconst base = z.object({ kind: z.enum([\"activity\", \"deal\", \"goal\"]) }).strict();\nconst activitySchema = base.extend({\n  kind: z.literal(\"activity\"),\n  customer_email: email,\n  subject: z.string().trim().min(1).max(240),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]),\n  body: z.string().max(10_000).nullable().optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().default(false),\n  metadata,\n});\nconst dealSchema = base.extend({\n  kind: z.literal(\"deal\"),\n  customer_email: email,\n  title: z.string().trim().min(1).max(240),\n  stage: z.string().trim().min(1).max(40).default(\"qualified\"),\n  value: z.number().finite().min(0).max(100_000_000).default(0),\n  probability: z.number().finite().min(0).max(1).default(0.25),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n  metadata,\n});\nconst goalSchema = base.extend({\n  kind: z.literal(\"goal\"),\n  period_start: z.string().date(),\n  period_end: z.string().date(),\n  target_value: z.number().finite().min(0).max(100_000_000),\n  target_deals: z.number().int().min(0).max(1_000_000).default(0),\n  metadata,\n});\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  activitySchema,\n  dealSchema,\n  goalSchema,\n]);\nconst idSchema = z.string().uuid();\nconst dealPatchSchema = z.object({\n  kind: z.literal(\"deal\"),\n  id: idSchema,\n  title: z.string().trim().min(1).max(240).optional(),\n  stage: z.string().trim().min(1).max(40).optional(),\n  value: z.number().finite().min(0).max(100_000_000).optional(),\n  probability: z.number().finite().min(0).max(1).optional(),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n}).strict();\nconst activityPatchSchema = z.object({\n  kind: z.literal(\"activity\"),\n  id: idSchema,\n  subject: z.string().trim().min(1).max(240).optional(),\n  body: z.string().max(10_000).nullable().optional(),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]).optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().optional(),\n}).strict();\nconst patchSchema = z.discriminatedUnion(\"kind\", [dealPatchSchema, activityPatchSchema]);\nconst deleteSchema = z.object({ kind: z.enum([\"deal\", \"activity\"]), id: idSchema }).strict();\n\nfunction unauthorized(cid: string, status: 401 | 403) {\n  return correlatedJson(\n    cid,\n    { error: status === 401 ? \"Unauthorized\" : \"Forbidden\" },\n    { status },\n  );\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:read\")) return unauthorized(cid, 403);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const [activities, deals, goals] = await Promise.all([\n    sup.client\n      .from(\"crm_activities\")\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"occurred_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_pipeline_deals\")\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"updated_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_goals\")\n      .select(\n        \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"period_start\", { ascending: false })\n      .limit(100),\n  ]);\n  if (\n    [activities, deals, goals].some(\n      (result) =>\n        result.error &&\n        !/relation .* does not exist/i.test(result.error.message),\n    )\n  ) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load CRM data\", code: \"CRM_READ_FAILED\" },\n      { status: 502 },\n    );\n  }\n  return correlatedJson(cid, {\n    data: {\n      activities: activities.data ?? [],\n      deals: deals.data ?? [],\n      goals: goals.data ?? [],\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, operationSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const actor = session.user.email.trim().toLowerCase();\n  const body = parsed.data;\n  let result;\n  if (body.kind === \"activity\") {\n    result = await sup.client\n      .from(\"crm_activities\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        activity_type: body.activity_type,\n        subject: body.subject,\n        body: body.body ?? null,\n        owner_email: actor,\n        occurred_at: body.occurred_at ?? new Date().toISOString(),\n        due_at: body.due_at ?? null,\n        completed_at: body.completed ? new Date().toISOString() : null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else if (body.kind === \"deal\") {\n    result = await sup.client\n      .from(\"crm_pipeline_deals\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        title: body.title,\n        stage: body.stage,\n        value: body.value,\n        probability: body.probability,\n        owner_email: actor,\n        expected_close_at: body.expected_close_at ?? null,\n        source: body.source ?? null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else {\n    if (body.period_end < body.period_start)\n      return correlatedJson(\n        cid,\n        { error: \"period_end must not precede period_start\" },\n        { status: 400 },\n      );\n    const goalValues = {\n      organization_id: organization.id,\n      owner_email: actor,\n      period_start: body.period_start,\n      period_end: body.period_end,\n      target_value: body.target_value,\n      target_deals: body.target_deals,\n      metadata: body.metadata,\n    };\n    const existingGoal = await sup.client\n      .from(\"crm_goals\")\n      .select(\"id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"owner_email\", actor)\n      .eq(\"period_start\", body.period_start)\n      .eq(\"period_end\", body.period_end)\n      .maybeSingle();\n    if (existingGoal.error)\n      return correlatedJson(\n        cid,\n        { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n        { status: 502 },\n      );\n    result = existingGoal.data?.id\n      ? await sup.client\n          .from(\"crm_goals\")\n          .update(goalValues)\n          .eq(\"id\", existingGoal.data.id)\n          .eq(\"organization_id\", organization.id)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single()\n      : await sup.client\n          .from(\"crm_goals\")\n          .insert(goalValues)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single();\n  }\n  if (result.error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n      { status: 502 },\n    );\n  return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/operations:POST\", post);\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, patchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const body = parsed.data;\n  const changedFields = body.kind === \"deal\"\n    ? [body.title, body.stage, body.value, body.probability, body.expected_close_at, body.source]\n    : [body.subject, body.body, body.activity_type, body.occurred_at, body.due_at, body.completed];\n  if (changedFields.every((value) => value === undefined)) {\n    return correlatedJson(cid, { error: \"At least one field is required\" }, { status: 400 });\n  }\n  const table = body.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const values: Record<string, unknown> = {};\n  if (body.kind === \"deal\") {\n    for (const key of [\"title\", \"stage\", \"value\", \"probability\", \"expected_close_at\", \"source\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n  } else {\n    for (const key of [\"subject\", \"body\", \"activity_type\", \"occurred_at\", \"due_at\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n    if (body.completed !== undefined) values.completed_at = body.completed ? new Date().toISOString() : null;\n  }\n  const result = await sup.client\n    .from(table)\n    .update(values)\n    .eq(\"id\", body.id)\n    .eq(\"organization_id\", organization.id)\n    .select(body.kind === \"deal\"\n      ? \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\"\n      : \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to update CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: body.kind === \"deal\" ? \"crm.deal.update\" : \"crm.activity.update\",\n    resource: `${body.kind}:${body.id}`,\n    details: { fields: Object.keys(values) },\n  });\n  return correlatedJson(cid, { data: result.data });\n}\n\nasync function remove(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, deleteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const table = parsed.data.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const result = await sup.client\n    .from(table)\n    .delete()\n    .eq(\"id\", parsed.data.id)\n    .eq(\"organization_id\", organization.id)\n    .select(\"id\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to delete CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: `crm.${parsed.data.kind}.delete`,\n    resource: `${parsed.data.kind}:${parsed.data.id}`,\n    details: {},\n  });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/crm/operations:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/operations:DELETE\", remove);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_crm_operationsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_crm_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_crm_operations
      summary: "Read Admin / Crm / Operations"
      description: "Admin / Crm / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst email = z.string().trim().toLowerCase().email().max(320);\nconst metadata = z.record(z.string(), z.unknown()).default({});\nconst isoDate = z.string().datetime({ offset: true });\nconst base = z.object({ kind: z.enum([\"activity\", \"deal\", \"goal\"]) }).strict();\nconst activitySchema = base.extend({\n  kind: z.literal(\"activity\"),\n  customer_email: email,\n  subject: z.string().trim().min(1).max(240),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]),\n  body: z.string().max(10_000).nullable().optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().default(false),\n  metadata,\n});\nconst dealSchema = base.extend({\n  kind: z.literal(\"deal\"),\n  customer_email: email,\n  title: z.string().trim().min(1).max(240),\n  stage: z.string().trim().min(1).max(40).default(\"qualified\"),\n  value: z.number().finite().min(0).max(100_000_000).default(0),\n  probability: z.number().finite().min(0).max(1).default(0.25),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n  metadata,\n});\nconst goalSchema = base.extend({\n  kind: z.literal(\"goal\"),\n  period_start: z.string().date(),\n  period_end: z.string().date(),\n  target_value: z.number().finite().min(0).max(100_000_000),\n  target_deals: z.number().int().min(0).max(1_000_000).default(0),\n  metadata,\n});\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  activitySchema,\n  dealSchema,\n  goalSchema,\n]);\nconst idSchema = z.string().uuid();\nconst dealPatchSchema = z.object({\n  kind: z.literal(\"deal\"),\n  id: idSchema,\n  title: z.string().trim().min(1).max(240).optional(),\n  stage: z.string().trim().min(1).max(40).optional(),\n  value: z.number().finite().min(0).max(100_000_000).optional(),\n  probability: z.number().finite().min(0).max(1).optional(),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n}).strict();\nconst activityPatchSchema = z.object({\n  kind: z.literal(\"activity\"),\n  id: idSchema,\n  subject: z.string().trim().min(1).max(240).optional(),\n  body: z.string().max(10_000).nullable().optional(),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]).optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().optional(),\n}).strict();\nconst patchSchema = z.discriminatedUnion(\"kind\", [dealPatchSchema, activityPatchSchema]);\nconst deleteSchema = z.object({ kind: z.enum([\"deal\", \"activity\"]), id: idSchema }).strict();\n\nfunction unauthorized(cid: string, status: 401 | 403) {\n  return correlatedJson(\n    cid,\n    { error: status === 401 ? \"Unauthorized\" : \"Forbidden\" },\n    { status },\n  );\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:read\")) return unauthorized(cid, 403);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const [activities, deals, goals] = await Promise.all([\n    sup.client\n      .from(\"crm_activities\")\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"occurred_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_pipeline_deals\")\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"updated_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_goals\")\n      .select(\n        \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"period_start\", { ascending: false })\n      .limit(100),\n  ]);\n  if (\n    [activities, deals, goals].some(\n      (result) =>\n        result.error &&\n        !/relation .* does not exist/i.test(result.error.message),\n    )\n  ) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load CRM data\", code: \"CRM_READ_FAILED\" },\n      { status: 502 },\n    );\n  }\n  return correlatedJson(cid, {\n    data: {\n      activities: activities.data ?? [],\n      deals: deals.data ?? [],\n      goals: goals.data ?? [],\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, operationSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const actor = session.user.email.trim().toLowerCase();\n  const body = parsed.data;\n  let result;\n  if (body.kind === \"activity\") {\n    result = await sup.client\n      .from(\"crm_activities\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        activity_type: body.activity_type,\n        subject: body.subject,\n        body: body.body ?? null,\n        owner_email: actor,\n        occurred_at: body.occurred_at ?? new Date().toISOString(),\n        due_at: body.due_at ?? null,\n        completed_at: body.completed ? new Date().toISOString() : null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else if (body.kind === \"deal\") {\n    result = await sup.client\n      .from(\"crm_pipeline_deals\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        title: body.title,\n        stage: body.stage,\n        value: body.value,\n        probability: body.probability,\n        owner_email: actor,\n        expected_close_at: body.expected_close_at ?? null,\n        source: body.source ?? null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else {\n    if (body.period_end < body.period_start)\n      return correlatedJson(\n        cid,\n        { error: \"period_end must not precede period_start\" },\n        { status: 400 },\n      );\n    const goalValues = {\n      organization_id: organization.id,\n      owner_email: actor,\n      period_start: body.period_start,\n      period_end: body.period_end,\n      target_value: body.target_value,\n      target_deals: body.target_deals,\n      metadata: body.metadata,\n    };\n    const existingGoal = await sup.client\n      .from(\"crm_goals\")\n      .select(\"id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"owner_email\", actor)\n      .eq(\"period_start\", body.period_start)\n      .eq(\"period_end\", body.period_end)\n      .maybeSingle();\n    if (existingGoal.error)\n      return correlatedJson(\n        cid,\n        { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n        { status: 502 },\n      );\n    result = existingGoal.data?.id\n      ? await sup.client\n          .from(\"crm_goals\")\n          .update(goalValues)\n          .eq(\"id\", existingGoal.data.id)\n          .eq(\"organization_id\", organization.id)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single()\n      : await sup.client\n          .from(\"crm_goals\")\n          .insert(goalValues)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single();\n  }\n  if (result.error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n      { status: 502 },\n    );\n  return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/operations:POST\", post);\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, patchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const body = parsed.data;\n  const changedFields = body.kind === \"deal\"\n    ? [body.title, body.stage, body.value, body.probability, body.expected_close_at, body.source]\n    : [body.subject, body.body, body.activity_type, body.occurred_at, body.due_at, body.completed];\n  if (changedFields.every((value) => value === undefined)) {\n    return correlatedJson(cid, { error: \"At least one field is required\" }, { status: 400 });\n  }\n  const table = body.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const values: Record<string, unknown> = {};\n  if (body.kind === \"deal\") {\n    for (const key of [\"title\", \"stage\", \"value\", \"probability\", \"expected_close_at\", \"source\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n  } else {\n    for (const key of [\"subject\", \"body\", \"activity_type\", \"occurred_at\", \"due_at\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n    if (body.completed !== undefined) values.completed_at = body.completed ? new Date().toISOString() : null;\n  }\n  const result = await sup.client\n    .from(table)\n    .update(values)\n    .eq(\"id\", body.id)\n    .eq(\"organization_id\", organization.id)\n    .select(body.kind === \"deal\"\n      ? \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\"\n      : \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to update CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: body.kind === \"deal\" ? \"crm.deal.update\" : \"crm.activity.update\",\n    resource: `${body.kind}:${body.id}`,\n    details: { fields: Object.keys(values) },\n  });\n  return correlatedJson(cid, { data: result.data });\n}\n\nasync function remove(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, deleteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const table = parsed.data.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const result = await sup.client\n    .from(table)\n    .delete()\n    .eq(\"id\", parsed.data.id)\n    .eq(\"organization_id\", organization.id)\n    .select(\"id\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to delete CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: `crm.${parsed.data.kind}.delete`,\n    resource: `${parsed.data.kind}:${parsed.data.id}`,\n    details: {},\n  });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/crm/operations:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/operations:DELETE\", remove);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_crm_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_crm_operations
      summary: "Update Admin / Crm / Operations"
      description: "Admin / Crm / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst email = z.string().trim().toLowerCase().email().max(320);\nconst metadata = z.record(z.string(), z.unknown()).default({});\nconst isoDate = z.string().datetime({ offset: true });\nconst base = z.object({ kind: z.enum([\"activity\", \"deal\", \"goal\"]) }).strict();\nconst activitySchema = base.extend({\n  kind: z.literal(\"activity\"),\n  customer_email: email,\n  subject: z.string().trim().min(1).max(240),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]),\n  body: z.string().max(10_000).nullable().optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().default(false),\n  metadata,\n});\nconst dealSchema = base.extend({\n  kind: z.literal(\"deal\"),\n  customer_email: email,\n  title: z.string().trim().min(1).max(240),\n  stage: z.string().trim().min(1).max(40).default(\"qualified\"),\n  value: z.number().finite().min(0).max(100_000_000).default(0),\n  probability: z.number().finite().min(0).max(1).default(0.25),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n  metadata,\n});\nconst goalSchema = base.extend({\n  kind: z.literal(\"goal\"),\n  period_start: z.string().date(),\n  period_end: z.string().date(),\n  target_value: z.number().finite().min(0).max(100_000_000),\n  target_deals: z.number().int().min(0).max(1_000_000).default(0),\n  metadata,\n});\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  activitySchema,\n  dealSchema,\n  goalSchema,\n]);\nconst idSchema = z.string().uuid();\nconst dealPatchSchema = z.object({\n  kind: z.literal(\"deal\"),\n  id: idSchema,\n  title: z.string().trim().min(1).max(240).optional(),\n  stage: z.string().trim().min(1).max(40).optional(),\n  value: z.number().finite().min(0).max(100_000_000).optional(),\n  probability: z.number().finite().min(0).max(1).optional(),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n}).strict();\nconst activityPatchSchema = z.object({\n  kind: z.literal(\"activity\"),\n  id: idSchema,\n  subject: z.string().trim().min(1).max(240).optional(),\n  body: z.string().max(10_000).nullable().optional(),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]).optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().optional(),\n}).strict();\nconst patchSchema = z.discriminatedUnion(\"kind\", [dealPatchSchema, activityPatchSchema]);\nconst deleteSchema = z.object({ kind: z.enum([\"deal\", \"activity\"]), id: idSchema }).strict();\n\nfunction unauthorized(cid: string, status: 401 | 403) {\n  return correlatedJson(\n    cid,\n    { error: status === 401 ? \"Unauthorized\" : \"Forbidden\" },\n    { status },\n  );\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:read\")) return unauthorized(cid, 403);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const [activities, deals, goals] = await Promise.all([\n    sup.client\n      .from(\"crm_activities\")\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"occurred_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_pipeline_deals\")\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"updated_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_goals\")\n      .select(\n        \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"period_start\", { ascending: false })\n      .limit(100),\n  ]);\n  if (\n    [activities, deals, goals].some(\n      (result) =>\n        result.error &&\n        !/relation .* does not exist/i.test(result.error.message),\n    )\n  ) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load CRM data\", code: \"CRM_READ_FAILED\" },\n      { status: 502 },\n    );\n  }\n  return correlatedJson(cid, {\n    data: {\n      activities: activities.data ?? [],\n      deals: deals.data ?? [],\n      goals: goals.data ?? [],\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, operationSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const actor = session.user.email.trim().toLowerCase();\n  const body = parsed.data;\n  let result;\n  if (body.kind === \"activity\") {\n    result = await sup.client\n      .from(\"crm_activities\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        activity_type: body.activity_type,\n        subject: body.subject,\n        body: body.body ?? null,\n        owner_email: actor,\n        occurred_at: body.occurred_at ?? new Date().toISOString(),\n        due_at: body.due_at ?? null,\n        completed_at: body.completed ? new Date().toISOString() : null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else if (body.kind === \"deal\") {\n    result = await sup.client\n      .from(\"crm_pipeline_deals\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        title: body.title,\n        stage: body.stage,\n        value: body.value,\n        probability: body.probability,\n        owner_email: actor,\n        expected_close_at: body.expected_close_at ?? null,\n        source: body.source ?? null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else {\n    if (body.period_end < body.period_start)\n      return correlatedJson(\n        cid,\n        { error: \"period_end must not precede period_start\" },\n        { status: 400 },\n      );\n    const goalValues = {\n      organization_id: organization.id,\n      owner_email: actor,\n      period_start: body.period_start,\n      period_end: body.period_end,\n      target_value: body.target_value,\n      target_deals: body.target_deals,\n      metadata: body.metadata,\n    };\n    const existingGoal = await sup.client\n      .from(\"crm_goals\")\n      .select(\"id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"owner_email\", actor)\n      .eq(\"period_start\", body.period_start)\n      .eq(\"period_end\", body.period_end)\n      .maybeSingle();\n    if (existingGoal.error)\n      return correlatedJson(\n        cid,\n        { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n        { status: 502 },\n      );\n    result = existingGoal.data?.id\n      ? await sup.client\n          .from(\"crm_goals\")\n          .update(goalValues)\n          .eq(\"id\", existingGoal.data.id)\n          .eq(\"organization_id\", organization.id)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single()\n      : await sup.client\n          .from(\"crm_goals\")\n          .insert(goalValues)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single();\n  }\n  if (result.error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n      { status: 502 },\n    );\n  return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/operations:POST\", post);\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, patchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const body = parsed.data;\n  const changedFields = body.kind === \"deal\"\n    ? [body.title, body.stage, body.value, body.probability, body.expected_close_at, body.source]\n    : [body.subject, body.body, body.activity_type, body.occurred_at, body.due_at, body.completed];\n  if (changedFields.every((value) => value === undefined)) {\n    return correlatedJson(cid, { error: \"At least one field is required\" }, { status: 400 });\n  }\n  const table = body.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const values: Record<string, unknown> = {};\n  if (body.kind === \"deal\") {\n    for (const key of [\"title\", \"stage\", \"value\", \"probability\", \"expected_close_at\", \"source\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n  } else {\n    for (const key of [\"subject\", \"body\", \"activity_type\", \"occurred_at\", \"due_at\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n    if (body.completed !== undefined) values.completed_at = body.completed ? new Date().toISOString() : null;\n  }\n  const result = await sup.client\n    .from(table)\n    .update(values)\n    .eq(\"id\", body.id)\n    .eq(\"organization_id\", organization.id)\n    .select(body.kind === \"deal\"\n      ? \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\"\n      : \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to update CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: body.kind === \"deal\" ? \"crm.deal.update\" : \"crm.activity.update\",\n    resource: `${body.kind}:${body.id}`,\n    details: { fields: Object.keys(values) },\n  });\n  return correlatedJson(cid, { data: result.data });\n}\n\nasync function remove(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, deleteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const table = parsed.data.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const result = await sup.client\n    .from(table)\n    .delete()\n    .eq(\"id\", parsed.data.id)\n    .eq(\"organization_id\", organization.id)\n    .select(\"id\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to delete CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: `crm.${parsed.data.kind}.delete`,\n    resource: `${parsed.data.kind}:${parsed.data.id}`,\n    details: {},\n  });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/crm/operations:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/operations:DELETE\", remove);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_crm_operationsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_crm_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_crm_operations
      summary: "Create or execute Admin / Crm / Operations"
      description: "Admin / Crm / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Crm"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst email = z.string().trim().toLowerCase().email().max(320);\nconst metadata = z.record(z.string(), z.unknown()).default({});\nconst isoDate = z.string().datetime({ offset: true });\nconst base = z.object({ kind: z.enum([\"activity\", \"deal\", \"goal\"]) }).strict();\nconst activitySchema = base.extend({\n  kind: z.literal(\"activity\"),\n  customer_email: email,\n  subject: z.string().trim().min(1).max(240),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]),\n  body: z.string().max(10_000).nullable().optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().default(false),\n  metadata,\n});\nconst dealSchema = base.extend({\n  kind: z.literal(\"deal\"),\n  customer_email: email,\n  title: z.string().trim().min(1).max(240),\n  stage: z.string().trim().min(1).max(40).default(\"qualified\"),\n  value: z.number().finite().min(0).max(100_000_000).default(0),\n  probability: z.number().finite().min(0).max(1).default(0.25),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n  metadata,\n});\nconst goalSchema = base.extend({\n  kind: z.literal(\"goal\"),\n  period_start: z.string().date(),\n  period_end: z.string().date(),\n  target_value: z.number().finite().min(0).max(100_000_000),\n  target_deals: z.number().int().min(0).max(1_000_000).default(0),\n  metadata,\n});\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  activitySchema,\n  dealSchema,\n  goalSchema,\n]);\nconst idSchema = z.string().uuid();\nconst dealPatchSchema = z.object({\n  kind: z.literal(\"deal\"),\n  id: idSchema,\n  title: z.string().trim().min(1).max(240).optional(),\n  stage: z.string().trim().min(1).max(40).optional(),\n  value: z.number().finite().min(0).max(100_000_000).optional(),\n  probability: z.number().finite().min(0).max(1).optional(),\n  expected_close_at: isoDate.nullable().optional(),\n  source: z.string().trim().max(120).nullable().optional(),\n}).strict();\nconst activityPatchSchema = z.object({\n  kind: z.literal(\"activity\"),\n  id: idSchema,\n  subject: z.string().trim().min(1).max(240).optional(),\n  body: z.string().max(10_000).nullable().optional(),\n  activity_type: z.enum([\"email\", \"call\", \"meeting\", \"note\", \"task\"]).optional(),\n  occurred_at: isoDate.optional(),\n  due_at: isoDate.nullable().optional(),\n  completed: z.boolean().optional(),\n}).strict();\nconst patchSchema = z.discriminatedUnion(\"kind\", [dealPatchSchema, activityPatchSchema]);\nconst deleteSchema = z.object({ kind: z.enum([\"deal\", \"activity\"]), id: idSchema }).strict();\n\nfunction unauthorized(cid: string, status: 401 | 403) {\n  return correlatedJson(\n    cid,\n    { error: status === 401 ? \"Unauthorized\" : \"Forbidden\" },\n    { status },\n  );\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:read\")) return unauthorized(cid, 403);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const [activities, deals, goals] = await Promise.all([\n    sup.client\n      .from(\"crm_activities\")\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"occurred_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_pipeline_deals\")\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"updated_at\", { ascending: false })\n      .limit(200),\n    sup.client\n      .from(\"crm_goals\")\n      .select(\n        \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n      )\n      .eq(\"organization_id\", organization.id)\n      .order(\"period_start\", { ascending: false })\n      .limit(100),\n  ]);\n  if (\n    [activities, deals, goals].some(\n      (result) =>\n        result.error &&\n        !/relation .* does not exist/i.test(result.error.message),\n    )\n  ) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load CRM data\", code: \"CRM_READ_FAILED\" },\n      { status: 502 },\n    );\n  }\n  return correlatedJson(cid, {\n    data: {\n      activities: activities.data ?? [],\n      deals: deals.data ?? [],\n      goals: goals.data ?? [],\n    },\n  });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, operationSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const actor = session.user.email.trim().toLowerCase();\n  const body = parsed.data;\n  let result;\n  if (body.kind === \"activity\") {\n    result = await sup.client\n      .from(\"crm_activities\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        activity_type: body.activity_type,\n        subject: body.subject,\n        body: body.body ?? null,\n        owner_email: actor,\n        occurred_at: body.occurred_at ?? new Date().toISOString(),\n        due_at: body.due_at ?? null,\n        completed_at: body.completed ? new Date().toISOString() : null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else if (body.kind === \"deal\") {\n    result = await sup.client\n      .from(\"crm_pipeline_deals\")\n      .insert({\n        organization_id: organization.id,\n        customer_email: body.customer_email,\n        title: body.title,\n        stage: body.stage,\n        value: body.value,\n        probability: body.probability,\n        owner_email: actor,\n        expected_close_at: body.expected_close_at ?? null,\n        source: body.source ?? null,\n        metadata: body.metadata,\n      })\n      .select(\n        \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\",\n      )\n      .single();\n  } else {\n    if (body.period_end < body.period_start)\n      return correlatedJson(\n        cid,\n        { error: \"period_end must not precede period_start\" },\n        { status: 400 },\n      );\n    const goalValues = {\n      organization_id: organization.id,\n      owner_email: actor,\n      period_start: body.period_start,\n      period_end: body.period_end,\n      target_value: body.target_value,\n      target_deals: body.target_deals,\n      metadata: body.metadata,\n    };\n    const existingGoal = await sup.client\n      .from(\"crm_goals\")\n      .select(\"id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"owner_email\", actor)\n      .eq(\"period_start\", body.period_start)\n      .eq(\"period_end\", body.period_end)\n      .maybeSingle();\n    if (existingGoal.error)\n      return correlatedJson(\n        cid,\n        { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n        { status: 502 },\n      );\n    result = existingGoal.data?.id\n      ? await sup.client\n          .from(\"crm_goals\")\n          .update(goalValues)\n          .eq(\"id\", existingGoal.data.id)\n          .eq(\"organization_id\", organization.id)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single()\n      : await sup.client\n          .from(\"crm_goals\")\n          .insert(goalValues)\n          .select(\n            \"id,owner_email,period_start,period_end,target_value,target_deals,metadata,created_at,updated_at\",\n          )\n          .single();\n  }\n  if (result.error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to save CRM record\", code: \"CRM_WRITE_FAILED\" },\n      { status: 502 },\n    );\n  return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/crm/operations:POST\", post);\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, patchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const body = parsed.data;\n  const changedFields = body.kind === \"deal\"\n    ? [body.title, body.stage, body.value, body.probability, body.expected_close_at, body.source]\n    : [body.subject, body.body, body.activity_type, body.occurred_at, body.due_at, body.completed];\n  if (changedFields.every((value) => value === undefined)) {\n    return correlatedJson(cid, { error: \"At least one field is required\" }, { status: 400 });\n  }\n  const table = body.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const values: Record<string, unknown> = {};\n  if (body.kind === \"deal\") {\n    for (const key of [\"title\", \"stage\", \"value\", \"probability\", \"expected_close_at\", \"source\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n  } else {\n    for (const key of [\"subject\", \"body\", \"activity_type\", \"occurred_at\", \"due_at\"] as const) {\n      if (body[key] !== undefined) values[key] = body[key];\n    }\n    if (body.completed !== undefined) values.completed_at = body.completed ? new Date().toISOString() : null;\n  }\n  const result = await sup.client\n    .from(table)\n    .update(values)\n    .eq(\"id\", body.id)\n    .eq(\"organization_id\", organization.id)\n    .select(body.kind === \"deal\"\n      ? \"id,customer_email,title,stage,value,probability,owner_email,expected_close_at,source,metadata,created_at,updated_at\"\n      : \"id,customer_email,activity_type,subject,body,owner_email,occurred_at,due_at,completed_at,metadata,created_at,updated_at\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to update CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: body.kind === \"deal\" ? \"crm.deal.update\" : \"crm.activity.update\",\n    resource: `${body.kind}:${body.id}`,\n    details: { fields: Object.keys(values) },\n  });\n  return correlatedJson(cid, { data: result.data });\n}\n\nasync function remove(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) return unauthorized(cid, 401);\n  if (!staffSessionAllows(session, \"crm:write\")) return unauthorized(cid, 403);\n  const parsed = await parseAdminJson(req, deleteSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const table = parsed.data.kind === \"deal\" ? \"crm_pipeline_deals\" : \"crm_activities\";\n  const result = await sup.client\n    .from(table)\n    .delete()\n    .eq(\"id\", parsed.data.id)\n    .eq(\"organization_id\", organization.id)\n    .select(\"id\")\n    .maybeSingle();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to delete CRM record\", code: \"CRM_WRITE_FAILED\" }, { status: 502 });\n  if (!result.data) return correlatedJson(cid, { error: \"CRM record not found\" }, { status: 404 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email.trim().toLowerCase(),\n    action: `crm.${parsed.data.kind}.delete`,\n    resource: `${parsed.data.kind}:${parsed.data.id}`,\n    details: {},\n  });\n  return correlatedJson(cid, { ok: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/crm/operations:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/crm/operations:DELETE\", remove);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_crm_operationsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_crm_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/delivery-logistics:
    get:
      operationId: get_admin_delivery_logistics
      summary: "Read Admin / Delivery Logistics"
      description: "Admin / Delivery Logistics. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Delivery Logistics"]
      x-source: "import { NextResponse } from \"next/server\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getDeliveryLogisticsOverview } from \"@/lib/delivery-logistics\";\n\nexport async function GET() {\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n\n  const overview = await getDeliveryLogisticsOverview();\n  return NextResponse.json({\n    ok: true,\n    overview,\n  });\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_delivery_logisticsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/delivery-logistics/operations:
    get:
      operationId: get_admin_delivery_logistics_operations
      summary: "Read Admin / Delivery Logistics / Operations"
      description: "Admin / Delivery Logistics / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Delivery Logistics"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { geocodeAddress, optimizeRouteWithProvider } from \"@/lib/logistics-provider-client\";\nimport { z } from \"zod\";\nimport { assertDeliveryGeofence, calculateDriverEarnings, verifyDeliveryProof } from \"@universal-music-store/platform-data\";\n\nfunction text(value: unknown): string | null { return typeof value === \"string\" && value.trim() ? value.trim() : null; }\nconst geoPointSchema = z.object({ latitude: z.number().finite().min(-90).max(90), longitude: z.number().finite().min(-180).max(180) }).strict();\nconst baseSchema = { shipment_id: z.string().trim().min(1).max(160) };\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  z.object({ kind: z.literal(\"courier\"), slug: z.string().trim().min(1).max(80), label: z.string().trim().min(1).max(160), phone: z.string().trim().max(40).optional(), status: z.enum([\"available\", \"assigned\", \"offline\", \"suspended\"]).optional(), latitude: z.number().finite().min(-90).max(90).optional(), longitude: z.number().finite().min(-180).max(180).optional(), rating: z.number().finite().min(0).max(5).optional(), max_weight_kg: z.number().finite().nonnegative().max(10000).optional(), max_volume_cm3: z.number().finite().nonnegative().max(100000000).optional(), cash_limit: z.number().finite().nonnegative().max(100000000).optional(), metadata: z.record(z.string().max(80), z.unknown()).default({}) }).strict(),\n  z.object({ kind: z.literal(\"telemetry\"), ...baseSchema, courier_id: z.string().trim().max(160).optional(), latitude: z.number().finite().min(-90).max(90), longitude: z.number().finite().min(-180).max(180), speed_kph: z.number().finite().nonnegative().max(500).optional(), heading: z.number().finite().min(0).max(360).optional(), captured_at: z.string().datetime().optional() }).strict(),\n  z.object({ kind: z.literal(\"proof\"), ...baseSchema, method: z.enum([\"signature\", \"photo\", \"otp\", \"contactless\"]), recipient_name: z.string().trim().max(160).optional(), photo_url: z.string().url().max(2048).optional(), signature: z.string().max(100000).optional(), contact_log: z.string().trim().max(2000).optional(), otp_digest: z.string().max(256).optional(), otp: z.string().trim().max(32).optional(), expected_otp: z.string().trim().max(32).optional(), latitude: z.number().finite().min(-90).max(90).optional(), longitude: z.number().finite().min(-180).max(180).optional(), destination: geoPointSchema.optional(), verified: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"exception\"), ...baseSchema, exception_type: z.string().trim().min(1).max(80), severity: z.enum([\"low\", \"medium\", \"high\", \"critical\"]).optional(), details: z.string().trim().min(1).max(4000) }).strict(),\n  z.object({ kind: z.literal(\"settlement\"), ...baseSchema, courier_id: z.string().trim().max(160).optional(), delivery_fee: z.number().finite().nonnegative().max(100000000).default(0), driver_earnings: z.number().finite().nonnegative().max(100000000).default(0), tolls: z.number().finite().nonnegative().max(100000000).default(0), tip: z.number().finite().nonnegative().max(100000000).default(0), cod_collected: z.number().finite().nonnegative().max(100000000).default(0), remitted: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"geocode\"), address: z.string().trim().min(5).max(500) }).strict(),\n  z.object({ kind: z.literal(\"route\"), points: z.array(geoPointSchema).min(1).max(100) }).strict(),\n]);\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req); const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"dashboard:read\")) return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const tenantHeader = req.headers.get(\"x-tenant-key\")?.trim();\n  if (process.env.NODE_ENV === \"production\" && !tenantHeader) return correlatedJson(cid, { error: \"Tenant scope is required\" }, { status: 400 });\n  const tenantKey = tenantHeader || \"default\";\n  const [couriers, exceptions] = await Promise.all([\n    sup.client.from(\"delivery_logistics_couriers\").select(\"*\").eq(\"tenant_key\", tenantKey).order(\"updated_at\", { ascending: false }).limit(200),\n    sup.client.from(\"delivery_logistics_exceptions\").select(\"*\").eq(\"tenant_key\", tenantKey).is(\"resolved_at\", null).order(\"created_at\", { ascending: false }).limit(200),\n  ]);\n  for (const result of [couriers, exceptions]) if (result.error && !/relation .* does not exist/i.test(result.error.message)) throw result.error;\n  return correlatedJson(cid, { data: { couriers: couriers.data ?? [], openExceptions: exceptions.data ?? [] } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req); const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"orders:write\")) return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  let body: unknown; try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = operationSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid delivery operation payload\" }, { status: 400 });\n  const rec = parsed.data; const kind = rec.kind; const actor = session.user.email?.trim().toLowerCase() ?? \"system\";\n  if (kind === \"geocode\") {\n    try { return correlatedJson(cid, { data: await geocodeAddress(rec.address) }); } catch { return correlatedJson(cid, { error: \"Geocoding provider unavailable\" }, { status: 503 }); }\n  }\n  if (kind === \"route\") {\n    try { return correlatedJson(cid, { data: await optimizeRouteWithProvider(rec.points) }); } catch { return correlatedJson(cid, { error: \"Routing provider unavailable\" }, { status: 503 }); }\n  }\n  const tenantHeader = req.headers.get(\"x-tenant-key\")?.trim();\n  if (process.env.NODE_ENV === \"production\" && !tenantHeader) return correlatedJson(cid, { error: \"Tenant scope is required\" }, { status: 400 });\n  const tenantKey = tenantHeader || \"default\";\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  if (kind === \"courier\") {\n    const slug = text(rec.slug); const label = text(rec.label);\n    if (!slug || !label) return correlatedJson(cid, { error: \"slug and label are required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_couriers\").upsert({ tenant_key: tenantKey, slug, label, phone: text(rec.phone), status: rec.status ?? \"available\", latitude: rec.latitude ?? null, longitude: rec.longitude ?? null, rating: rec.rating ?? 0, max_weight_kg: rec.max_weight_kg ?? 0, max_volume_cm3: rec.max_volume_cm3 ?? 0, cash_limit: rec.cash_limit ?? 10000, metadata: rec.metadata }, { onConflict: \"tenant_key,slug\" }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  const shipmentId = text(rec.shipment_id); if (!shipmentId) return correlatedJson(cid, { error: \"shipment_id is required\" }, { status: 400 });\n  if (kind === \"telemetry\") {\n    if (typeof rec.latitude !== \"number\" || typeof rec.longitude !== \"number\") return correlatedJson(cid, { error: \"latitude and longitude are required\" }, { status: 400 });\n    const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n    if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_telemetry\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, courier_id: text(rec.courier_id), latitude: rec.latitude, longitude: rec.longitude, speed_kph: rec.speed_kph ?? null, heading: rec.heading ?? null, captured_at: rec.captured_at ?? new Date().toISOString(), idempotency_key: idempotencyKey }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"proof\") {\n    const method = rec.method;\n    const shipment = await sup.client.from(\"delivery_logistics_shipments\").select(\"destination_address,geocoded_destination\").eq(\"id\", shipmentId).eq(\"tenant_key\", tenantKey).maybeSingle();\n    const storedDestination = shipment.data?.geocoded_destination && typeof shipment.data.geocoded_destination === \"object\" && \"latitude\" in shipment.data.geocoded_destination && \"longitude\" in shipment.data.geocoded_destination ? shipment.data.geocoded_destination as { latitude: number; longitude: number } : null;\n    const destination = rec.destination ?? storedDestination ?? undefined;\n    const proofCheck = verifyDeliveryProof({ method, recipientName: rec.recipient_name, otp: rec.otp, expectedOtp: rec.expected_otp, latitude: rec.latitude, longitude: rec.longitude, destination, photoUrl: rec.photo_url, signature: rec.signature, contactLog: rec.contact_log });\n    if (rec.verified && destination && typeof rec.latitude === \"number\" && typeof rec.longitude === \"number\") {\n      try { assertDeliveryGeofence(destination, { latitude: rec.latitude, longitude: rec.longitude }); } catch { return correlatedJson(cid, { error: \"Delivery proof is outside the destination geofence\" }, { status: 400 }); }\n    }\n    if (rec.verified && !proofCheck.valid) return correlatedJson(cid, { error: \"Delivery proof verification failed\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_proofs\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, method, recipient_name: text(rec.recipient_name), photo_url: text(rec.photo_url), signature: text(rec.signature), contact_log: text(rec.contact_log), otp_digest: text(rec.otp_digest), latitude: rec.latitude ?? null, longitude: rec.longitude ?? null, verified: rec.verified, verified_at: rec.verified ? new Date().toISOString() : null, created_by_email: actor }).select(\"*\").single();\n    if (result.error) throw result.error;\n    if (rec.verified) await sup.client.from(\"delivery_logistics_shipments\").update({ status: \"delivered\", proof_of_delivery: { proof_id: result.data.id, method, verified_at: result.data.verified_at } }).eq(\"id\", shipmentId).eq(\"tenant_key\", tenantKey);\n    return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"exception\") {\n    const exceptionType = text(rec.exception_type); const details = text(rec.details);\n    if (!exceptionType || !details) return correlatedJson(cid, { error: \"exception_type and details are required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_exceptions\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, exception_type: exceptionType, severity: rec.severity ?? \"medium\", details, created_by_email: actor }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"settlement\") {\n    if (rec.remitted && !rec.courier_id) return correlatedJson(cid, { error: \"courier_id is required for remittance\" }, { status: 400 });\n    const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n    if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n    const driverEarnings = calculateDriverEarnings({ deliveryFee: rec.delivery_fee, tip: rec.tip, tolls: rec.tolls, commissionRate: 0 });\n    const result = await sup.client.from(\"delivery_logistics_settlements\").upsert({ tenant_key: tenantKey, shipment_id: shipmentId, courier_id: text(rec.courier_id), delivery_fee: rec.delivery_fee, driver_earnings: driverEarnings, tolls: rec.tolls, tip: rec.tip, cod_collected: rec.cod_collected, remitted_at: rec.remitted ? new Date().toISOString() : null, remitted_by_email: rec.remitted ? actor : null }, { onConflict: \"tenant_key,shipment_id\" }).select(\"*\").single();\n    if (result.error) throw result.error;\n    if (rec.courier_id) {\n      const courierQuery = /^[0-9a-f-]{36}$/i.test(rec.courier_id) ? sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"id\", rec.courier_id).eq(\"tenant_key\", tenantKey) : sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"slug\", rec.courier_id).eq(\"tenant_key\", tenantKey);\n      const courier = await courierQuery.maybeSingle();\n      if (courier.data?.id) {\n        const earning = await sup.client.from(\"delivery_driver_earnings\").upsert({ tenant_key: tenantKey, courier_id: courier.data.id, shipment_id: shipmentId, delivery_fee: rec.delivery_fee, tip: rec.tip, tolls: rec.tolls, commission_rate: 0, net_earnings: driverEarnings, status: rec.remitted ? \"paid\" : \"pending\" }, { onConflict: \"tenant_key,courier_id,shipment_id\" }).select(\"*\").single();\n        if (earning.error) return correlatedJson(cid, { error: \"Unable to persist driver earnings\" }, { status: 503 });\n        if (rec.cod_collected > 0) {\n          const cash = await sup.client.from(\"delivery_courier_cash_ledger\").insert({ tenant_key: tenantKey, courier_id: courier.data.id, shipment_id: shipmentId, amount: rec.cod_collected, direction: rec.remitted ? \"remit\" : \"collect\", idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n          if (cash.error && !/duplicate key/i.test(cash.error.message)) return correlatedJson(cid, { error: \"Unable to persist COD custody\" }, { status: 503 });\n        }\n        return correlatedJson(cid, { data: { settlement: result.data, earnings: earning.data } }, { status: 201 });\n      }\n    }\n    return correlatedJson(cid, { data: { settlement: result.data } }, { status: 201 });\n  }\n  return correlatedJson(cid, { error: \"kind must be courier, telemetry, proof, exception, or settlement\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/delivery-logistics/operations:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_delivery_logistics_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_delivery_logistics_operations
      summary: "Create or execute Admin / Delivery Logistics / Operations"
      description: "Admin / Delivery Logistics / Operations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Delivery Logistics"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { geocodeAddress, optimizeRouteWithProvider } from \"@/lib/logistics-provider-client\";\nimport { z } from \"zod\";\nimport { assertDeliveryGeofence, calculateDriverEarnings, verifyDeliveryProof } from \"@universal-music-store/platform-data\";\n\nfunction text(value: unknown): string | null { return typeof value === \"string\" && value.trim() ? value.trim() : null; }\nconst geoPointSchema = z.object({ latitude: z.number().finite().min(-90).max(90), longitude: z.number().finite().min(-180).max(180) }).strict();\nconst baseSchema = { shipment_id: z.string().trim().min(1).max(160) };\nconst operationSchema = z.discriminatedUnion(\"kind\", [\n  z.object({ kind: z.literal(\"courier\"), slug: z.string().trim().min(1).max(80), label: z.string().trim().min(1).max(160), phone: z.string().trim().max(40).optional(), status: z.enum([\"available\", \"assigned\", \"offline\", \"suspended\"]).optional(), latitude: z.number().finite().min(-90).max(90).optional(), longitude: z.number().finite().min(-180).max(180).optional(), rating: z.number().finite().min(0).max(5).optional(), max_weight_kg: z.number().finite().nonnegative().max(10000).optional(), max_volume_cm3: z.number().finite().nonnegative().max(100000000).optional(), cash_limit: z.number().finite().nonnegative().max(100000000).optional(), metadata: z.record(z.string().max(80), z.unknown()).default({}) }).strict(),\n  z.object({ kind: z.literal(\"telemetry\"), ...baseSchema, courier_id: z.string().trim().max(160).optional(), latitude: z.number().finite().min(-90).max(90), longitude: z.number().finite().min(-180).max(180), speed_kph: z.number().finite().nonnegative().max(500).optional(), heading: z.number().finite().min(0).max(360).optional(), captured_at: z.string().datetime().optional() }).strict(),\n  z.object({ kind: z.literal(\"proof\"), ...baseSchema, method: z.enum([\"signature\", \"photo\", \"otp\", \"contactless\"]), recipient_name: z.string().trim().max(160).optional(), photo_url: z.string().url().max(2048).optional(), signature: z.string().max(100000).optional(), contact_log: z.string().trim().max(2000).optional(), otp_digest: z.string().max(256).optional(), otp: z.string().trim().max(32).optional(), expected_otp: z.string().trim().max(32).optional(), latitude: z.number().finite().min(-90).max(90).optional(), longitude: z.number().finite().min(-180).max(180).optional(), destination: geoPointSchema.optional(), verified: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"exception\"), ...baseSchema, exception_type: z.string().trim().min(1).max(80), severity: z.enum([\"low\", \"medium\", \"high\", \"critical\"]).optional(), details: z.string().trim().min(1).max(4000) }).strict(),\n  z.object({ kind: z.literal(\"settlement\"), ...baseSchema, courier_id: z.string().trim().max(160).optional(), delivery_fee: z.number().finite().nonnegative().max(100000000).default(0), driver_earnings: z.number().finite().nonnegative().max(100000000).default(0), tolls: z.number().finite().nonnegative().max(100000000).default(0), tip: z.number().finite().nonnegative().max(100000000).default(0), cod_collected: z.number().finite().nonnegative().max(100000000).default(0), remitted: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"geocode\"), address: z.string().trim().min(5).max(500) }).strict(),\n  z.object({ kind: z.literal(\"route\"), points: z.array(geoPointSchema).min(1).max(100) }).strict(),\n]);\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req); const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"dashboard:read\")) return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const tenantHeader = req.headers.get(\"x-tenant-key\")?.trim();\n  if (process.env.NODE_ENV === \"production\" && !tenantHeader) return correlatedJson(cid, { error: \"Tenant scope is required\" }, { status: 400 });\n  const tenantKey = tenantHeader || \"default\";\n  const [couriers, exceptions] = await Promise.all([\n    sup.client.from(\"delivery_logistics_couriers\").select(\"*\").eq(\"tenant_key\", tenantKey).order(\"updated_at\", { ascending: false }).limit(200),\n    sup.client.from(\"delivery_logistics_exceptions\").select(\"*\").eq(\"tenant_key\", tenantKey).is(\"resolved_at\", null).order(\"created_at\", { ascending: false }).limit(200),\n  ]);\n  for (const result of [couriers, exceptions]) if (result.error && !/relation .* does not exist/i.test(result.error.message)) throw result.error;\n  return correlatedJson(cid, { data: { couriers: couriers.data ?? [], openExceptions: exceptions.data ?? [] } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req); const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"orders:write\")) return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  let body: unknown; try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = operationSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid delivery operation payload\" }, { status: 400 });\n  const rec = parsed.data; const kind = rec.kind; const actor = session.user.email?.trim().toLowerCase() ?? \"system\";\n  if (kind === \"geocode\") {\n    try { return correlatedJson(cid, { data: await geocodeAddress(rec.address) }); } catch { return correlatedJson(cid, { error: \"Geocoding provider unavailable\" }, { status: 503 }); }\n  }\n  if (kind === \"route\") {\n    try { return correlatedJson(cid, { data: await optimizeRouteWithProvider(rec.points) }); } catch { return correlatedJson(cid, { error: \"Routing provider unavailable\" }, { status: 503 }); }\n  }\n  const tenantHeader = req.headers.get(\"x-tenant-key\")?.trim();\n  if (process.env.NODE_ENV === \"production\" && !tenantHeader) return correlatedJson(cid, { error: \"Tenant scope is required\" }, { status: 400 });\n  const tenantKey = tenantHeader || \"default\";\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  if (kind === \"courier\") {\n    const slug = text(rec.slug); const label = text(rec.label);\n    if (!slug || !label) return correlatedJson(cid, { error: \"slug and label are required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_couriers\").upsert({ tenant_key: tenantKey, slug, label, phone: text(rec.phone), status: rec.status ?? \"available\", latitude: rec.latitude ?? null, longitude: rec.longitude ?? null, rating: rec.rating ?? 0, max_weight_kg: rec.max_weight_kg ?? 0, max_volume_cm3: rec.max_volume_cm3 ?? 0, cash_limit: rec.cash_limit ?? 10000, metadata: rec.metadata }, { onConflict: \"tenant_key,slug\" }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  const shipmentId = text(rec.shipment_id); if (!shipmentId) return correlatedJson(cid, { error: \"shipment_id is required\" }, { status: 400 });\n  if (kind === \"telemetry\") {\n    if (typeof rec.latitude !== \"number\" || typeof rec.longitude !== \"number\") return correlatedJson(cid, { error: \"latitude and longitude are required\" }, { status: 400 });\n    const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n    if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_telemetry\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, courier_id: text(rec.courier_id), latitude: rec.latitude, longitude: rec.longitude, speed_kph: rec.speed_kph ?? null, heading: rec.heading ?? null, captured_at: rec.captured_at ?? new Date().toISOString(), idempotency_key: idempotencyKey }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"proof\") {\n    const method = rec.method;\n    const shipment = await sup.client.from(\"delivery_logistics_shipments\").select(\"destination_address,geocoded_destination\").eq(\"id\", shipmentId).eq(\"tenant_key\", tenantKey).maybeSingle();\n    const storedDestination = shipment.data?.geocoded_destination && typeof shipment.data.geocoded_destination === \"object\" && \"latitude\" in shipment.data.geocoded_destination && \"longitude\" in shipment.data.geocoded_destination ? shipment.data.geocoded_destination as { latitude: number; longitude: number } : null;\n    const destination = rec.destination ?? storedDestination ?? undefined;\n    const proofCheck = verifyDeliveryProof({ method, recipientName: rec.recipient_name, otp: rec.otp, expectedOtp: rec.expected_otp, latitude: rec.latitude, longitude: rec.longitude, destination, photoUrl: rec.photo_url, signature: rec.signature, contactLog: rec.contact_log });\n    if (rec.verified && destination && typeof rec.latitude === \"number\" && typeof rec.longitude === \"number\") {\n      try { assertDeliveryGeofence(destination, { latitude: rec.latitude, longitude: rec.longitude }); } catch { return correlatedJson(cid, { error: \"Delivery proof is outside the destination geofence\" }, { status: 400 }); }\n    }\n    if (rec.verified && !proofCheck.valid) return correlatedJson(cid, { error: \"Delivery proof verification failed\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_proofs\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, method, recipient_name: text(rec.recipient_name), photo_url: text(rec.photo_url), signature: text(rec.signature), contact_log: text(rec.contact_log), otp_digest: text(rec.otp_digest), latitude: rec.latitude ?? null, longitude: rec.longitude ?? null, verified: rec.verified, verified_at: rec.verified ? new Date().toISOString() : null, created_by_email: actor }).select(\"*\").single();\n    if (result.error) throw result.error;\n    if (rec.verified) await sup.client.from(\"delivery_logistics_shipments\").update({ status: \"delivered\", proof_of_delivery: { proof_id: result.data.id, method, verified_at: result.data.verified_at } }).eq(\"id\", shipmentId).eq(\"tenant_key\", tenantKey);\n    return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"exception\") {\n    const exceptionType = text(rec.exception_type); const details = text(rec.details);\n    if (!exceptionType || !details) return correlatedJson(cid, { error: \"exception_type and details are required\" }, { status: 400 });\n    const result = await sup.client.from(\"delivery_logistics_exceptions\").insert({ tenant_key: tenantKey, shipment_id: shipmentId, exception_type: exceptionType, severity: rec.severity ?? \"medium\", details, created_by_email: actor }).select(\"*\").single();\n    if (result.error) throw result.error; return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (kind === \"settlement\") {\n    if (rec.remitted && !rec.courier_id) return correlatedJson(cid, { error: \"courier_id is required for remittance\" }, { status: 400 });\n    const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n    if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n    const driverEarnings = calculateDriverEarnings({ deliveryFee: rec.delivery_fee, tip: rec.tip, tolls: rec.tolls, commissionRate: 0 });\n    const result = await sup.client.from(\"delivery_logistics_settlements\").upsert({ tenant_key: tenantKey, shipment_id: shipmentId, courier_id: text(rec.courier_id), delivery_fee: rec.delivery_fee, driver_earnings: driverEarnings, tolls: rec.tolls, tip: rec.tip, cod_collected: rec.cod_collected, remitted_at: rec.remitted ? new Date().toISOString() : null, remitted_by_email: rec.remitted ? actor : null }, { onConflict: \"tenant_key,shipment_id\" }).select(\"*\").single();\n    if (result.error) throw result.error;\n    if (rec.courier_id) {\n      const courierQuery = /^[0-9a-f-]{36}$/i.test(rec.courier_id) ? sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"id\", rec.courier_id).eq(\"tenant_key\", tenantKey) : sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"slug\", rec.courier_id).eq(\"tenant_key\", tenantKey);\n      const courier = await courierQuery.maybeSingle();\n      if (courier.data?.id) {\n        const earning = await sup.client.from(\"delivery_driver_earnings\").upsert({ tenant_key: tenantKey, courier_id: courier.data.id, shipment_id: shipmentId, delivery_fee: rec.delivery_fee, tip: rec.tip, tolls: rec.tolls, commission_rate: 0, net_earnings: driverEarnings, status: rec.remitted ? \"paid\" : \"pending\" }, { onConflict: \"tenant_key,courier_id,shipment_id\" }).select(\"*\").single();\n        if (earning.error) return correlatedJson(cid, { error: \"Unable to persist driver earnings\" }, { status: 503 });\n        if (rec.cod_collected > 0) {\n          const cash = await sup.client.from(\"delivery_courier_cash_ledger\").insert({ tenant_key: tenantKey, courier_id: courier.data.id, shipment_id: shipmentId, amount: rec.cod_collected, direction: rec.remitted ? \"remit\" : \"collect\", idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n          if (cash.error && !/duplicate key/i.test(cash.error.message)) return correlatedJson(cid, { error: \"Unable to persist COD custody\" }, { status: 503 });\n        }\n        return correlatedJson(cid, { data: { settlement: result.data, earnings: earning.data } }, { status: 201 });\n      }\n    }\n    return correlatedJson(cid, { data: { settlement: result.data } }, { status: 201 });\n  }\n  return correlatedJson(cid, { error: \"kind must be courier, telemetry, proof, exception, or settlement\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/delivery-logistics/operations:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_delivery_logistics_operationsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_delivery_logistics_operationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/delivery-logistics/shipments:
    get:
      operationId: get_admin_delivery_logistics_shipments
      summary: "Read Admin / Delivery Logistics / Shipments"
      description: "Admin / Delivery Logistics / Shipments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Delivery Logistics"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  appendDeliveryLogisticsEvent,\n  listDeliveryLogisticsEvents,\n  listDeliveryLogisticsShipments,\n  upsertDeliveryLogisticsShipment,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nfunction cleanText(value: unknown): string | null {\n  return typeof value === \"string\" && value.trim() ? value.trim() : null;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"dashboard:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const [shipments, events] = await Promise.all([\n    listDeliveryLogisticsShipments(sup.client, { limit: 50 }),\n    listDeliveryLogisticsEvents(sup.client, { limit: 50 }),\n  ]);\n  return correlatedJson(cid, { data: { shipments, events } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"orders:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n\n  const rec = body as Record<string, unknown>;\n  const kind = cleanText(rec.kind);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const actorEmail = session.user.email?.trim().toLowerCase() ?? null;\n\n  if (kind === \"shipment\") {\n    const orderId = cleanText(rec.order_id);\n    const customerEmail = cleanText(rec.customer_email);\n    if (!orderId || !customerEmail) {\n      return correlatedJson(cid, { error: \"order_id and customer_email are required\" }, { status: 400 });\n    }\n\n    const row = await upsertDeliveryLogisticsShipment(sb, {\n      order_id: orderId,\n      order_display_id: cleanText(rec.order_display_id),\n      customer_email: customerEmail,\n      branch_id: cleanText(rec.branch_id),\n      courier_slug: cleanText(rec.courier_slug),\n      courier_label: cleanText(rec.courier_label),\n      status:\n        rec.status === \"assigned\" ||\n        rec.status === \"in_transit\" ||\n        rec.status === \"delivered\" ||\n        rec.status === \"returned\" ||\n        rec.status === \"cancelled\"\n          ? rec.status\n          : \"planned\",\n      origin_address:\n        rec.origin_address && typeof rec.origin_address === \"object\" && !Array.isArray(rec.origin_address)\n          ? (rec.origin_address as Record<string, unknown>)\n          : {},\n      destination_address:\n        rec.destination_address && typeof rec.destination_address === \"object\" && !Array.isArray(rec.destination_address)\n          ? (rec.destination_address as Record<string, unknown>)\n          : {},\n      geocoded_destination:\n        rec.geocoded_destination && typeof rec.geocoded_destination === \"object\" && !Array.isArray(rec.geocoded_destination)\n          ? (rec.geocoded_destination as Record<string, unknown>)\n          : {},\n      sla_code: cleanText(rec.sla_code),\n      sla_label: cleanText(rec.sla_label),\n      package_dimensions:\n        rec.package_dimensions && typeof rec.package_dimensions === \"object\" && !Array.isArray(rec.package_dimensions)\n          ? (rec.package_dimensions as Record<string, unknown>)\n          : {},\n      hazard_flags: Array.isArray(rec.hazard_flags)\n        ? rec.hazard_flags.map((value) => String(value))\n        : [],\n      route_metadata:\n        rec.route_metadata && typeof rec.route_metadata === \"object\" && !Array.isArray(rec.route_metadata)\n          ? (rec.route_metadata as Record<string, unknown>)\n          : {},\n      tracking_url: cleanText(rec.tracking_url),\n      tracking_status: cleanText(rec.tracking_status),\n      proof_of_delivery:\n        rec.proof_of_delivery && typeof rec.proof_of_delivery === \"object\" && !Array.isArray(rec.proof_of_delivery)\n          ? (rec.proof_of_delivery as Record<string, unknown>)\n          : {},\n      cod_amount: typeof rec.cod_amount === \"number\" ? rec.cod_amount : null,\n      driver_cash_balance: typeof rec.driver_cash_balance === \"number\" ? rec.driver_cash_balance : null,\n      settlement_status:\n        rec.settlement_status === \"held\" ||\n        rec.settlement_status === \"reconciled\" ||\n        rec.settlement_status === \"remitted\" ||\n        rec.settlement_status === \"none\"\n          ? rec.settlement_status\n          : \"pending\",\n      pricing:\n        rec.pricing && typeof rec.pricing === \"object\" && !Array.isArray(rec.pricing)\n          ? (rec.pricing as Record<string, unknown>)\n          : {},\n      metadata:\n        rec.metadata && typeof rec.metadata === \"object\" && !Array.isArray(rec.metadata)\n          ? (rec.metadata as Record<string, unknown>)\n          : {},\n      last_event_at: cleanText(rec.last_event_at),\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"event\") {\n    const shipmentId = cleanText(rec.shipment_id);\n    const eventType = cleanText(rec.event_type);\n    if (!shipmentId || !eventType) {\n      return correlatedJson(cid, { error: \"shipment_id and event_type are required\" }, { status: 400 });\n    }\n    const event = await appendDeliveryLogisticsEvent(sb, {\n      shipment_id: shipmentId,\n      event_type: eventType,\n      event_status: cleanText(rec.event_status),\n      event_payload:\n        rec.event_payload && typeof rec.event_payload === \"object\" && !Array.isArray(rec.event_payload)\n          ? (rec.event_payload as Record<string, unknown>)\n          : {},\n      occurred_at: cleanText(rec.occurred_at) ?? undefined,\n      created_by_email: actorEmail,\n    });\n    return correlatedJson(cid, { data: event }, { status: 201 });\n  }\n\n  return correlatedJson(cid, { error: \"kind must be shipment or event\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/delivery-logistics/shipments:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_delivery_logistics_shipmentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_delivery_logistics_shipments
      summary: "Create or execute Admin / Delivery Logistics / Shipments"
      description: "Admin / Delivery Logistics / Shipments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Delivery Logistics"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  appendDeliveryLogisticsEvent,\n  listDeliveryLogisticsEvents,\n  listDeliveryLogisticsShipments,\n  upsertDeliveryLogisticsShipment,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nfunction cleanText(value: unknown): string | null {\n  return typeof value === \"string\" && value.trim() ? value.trim() : null;\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"dashboard:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const [shipments, events] = await Promise.all([\n    listDeliveryLogisticsShipments(sup.client, { limit: 50 }),\n    listDeliveryLogisticsEvents(sup.client, { limit: 50 }),\n  ]);\n  return correlatedJson(cid, { data: { shipments, events } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"orders:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n\n  const rec = body as Record<string, unknown>;\n  const kind = cleanText(rec.kind);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const actorEmail = session.user.email?.trim().toLowerCase() ?? null;\n\n  if (kind === \"shipment\") {\n    const orderId = cleanText(rec.order_id);\n    const customerEmail = cleanText(rec.customer_email);\n    if (!orderId || !customerEmail) {\n      return correlatedJson(cid, { error: \"order_id and customer_email are required\" }, { status: 400 });\n    }\n\n    const row = await upsertDeliveryLogisticsShipment(sb, {\n      order_id: orderId,\n      order_display_id: cleanText(rec.order_display_id),\n      customer_email: customerEmail,\n      branch_id: cleanText(rec.branch_id),\n      courier_slug: cleanText(rec.courier_slug),\n      courier_label: cleanText(rec.courier_label),\n      status:\n        rec.status === \"assigned\" ||\n        rec.status === \"in_transit\" ||\n        rec.status === \"delivered\" ||\n        rec.status === \"returned\" ||\n        rec.status === \"cancelled\"\n          ? rec.status\n          : \"planned\",\n      origin_address:\n        rec.origin_address && typeof rec.origin_address === \"object\" && !Array.isArray(rec.origin_address)\n          ? (rec.origin_address as Record<string, unknown>)\n          : {},\n      destination_address:\n        rec.destination_address && typeof rec.destination_address === \"object\" && !Array.isArray(rec.destination_address)\n          ? (rec.destination_address as Record<string, unknown>)\n          : {},\n      geocoded_destination:\n        rec.geocoded_destination && typeof rec.geocoded_destination === \"object\" && !Array.isArray(rec.geocoded_destination)\n          ? (rec.geocoded_destination as Record<string, unknown>)\n          : {},\n      sla_code: cleanText(rec.sla_code),\n      sla_label: cleanText(rec.sla_label),\n      package_dimensions:\n        rec.package_dimensions && typeof rec.package_dimensions === \"object\" && !Array.isArray(rec.package_dimensions)\n          ? (rec.package_dimensions as Record<string, unknown>)\n          : {},\n      hazard_flags: Array.isArray(rec.hazard_flags)\n        ? rec.hazard_flags.map((value) => String(value))\n        : [],\n      route_metadata:\n        rec.route_metadata && typeof rec.route_metadata === \"object\" && !Array.isArray(rec.route_metadata)\n          ? (rec.route_metadata as Record<string, unknown>)\n          : {},\n      tracking_url: cleanText(rec.tracking_url),\n      tracking_status: cleanText(rec.tracking_status),\n      proof_of_delivery:\n        rec.proof_of_delivery && typeof rec.proof_of_delivery === \"object\" && !Array.isArray(rec.proof_of_delivery)\n          ? (rec.proof_of_delivery as Record<string, unknown>)\n          : {},\n      cod_amount: typeof rec.cod_amount === \"number\" ? rec.cod_amount : null,\n      driver_cash_balance: typeof rec.driver_cash_balance === \"number\" ? rec.driver_cash_balance : null,\n      settlement_status:\n        rec.settlement_status === \"held\" ||\n        rec.settlement_status === \"reconciled\" ||\n        rec.settlement_status === \"remitted\" ||\n        rec.settlement_status === \"none\"\n          ? rec.settlement_status\n          : \"pending\",\n      pricing:\n        rec.pricing && typeof rec.pricing === \"object\" && !Array.isArray(rec.pricing)\n          ? (rec.pricing as Record<string, unknown>)\n          : {},\n      metadata:\n        rec.metadata && typeof rec.metadata === \"object\" && !Array.isArray(rec.metadata)\n          ? (rec.metadata as Record<string, unknown>)\n          : {},\n      last_event_at: cleanText(rec.last_event_at),\n      created_by_email: actorEmail,\n      updated_by_email: actorEmail,\n    });\n\n    return correlatedJson(cid, { data: row }, { status: 201 });\n  }\n\n  if (kind === \"event\") {\n    const shipmentId = cleanText(rec.shipment_id);\n    const eventType = cleanText(rec.event_type);\n    if (!shipmentId || !eventType) {\n      return correlatedJson(cid, { error: \"shipment_id and event_type are required\" }, { status: 400 });\n    }\n    const event = await appendDeliveryLogisticsEvent(sb, {\n      shipment_id: shipmentId,\n      event_type: eventType,\n      event_status: cleanText(rec.event_status),\n      event_payload:\n        rec.event_payload && typeof rec.event_payload === \"object\" && !Array.isArray(rec.event_payload)\n          ? (rec.event_payload as Record<string, unknown>)\n          : {},\n      occurred_at: cleanText(rec.occurred_at) ?? undefined,\n      created_by_email: actorEmail,\n    });\n    return correlatedJson(cid, { data: event }, { status: 201 });\n  }\n\n  return correlatedJson(cid, { error: \"kind must be shipment or event\" }, { status: 400 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/delivery-logistics/shipments:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_delivery_logistics_shipmentsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_delivery_logistics_shipmentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/devices:
    get:
      operationId: get_admin_devices
      summary: "Read Admin / Devices"
      description: "Admin / Devices. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Devices"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listDevices, upsertDevice } from \"@universal-music-store/platform-data\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst deviceSchema = z.object({\n  id: z.string().uuid().optional(),\n  name: z.string().trim().min(1).max(120),\n  type: z.enum([\"terminal\", \"printer\", \"kds\", \"scanner\"]).default(\"terminal\"),\n  ip_address: z.string().trim().max(64).optional(),\n  is_active: z.boolean().optional(),\n  config: z.record(z.string(), z.unknown()).optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"devices:manage\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listDevices(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"devices:manage\");\n  if (!auth.ok) return auth.response;\n  const parsed = await parseAdminJson(req, deviceSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const device = await upsertDevice(sup.client, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: device }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/devices:POST\", post);\n"
      x-permission: "devices:manage"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_devicesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_devices
      summary: "Create or execute Admin / Devices"
      description: "Admin / Devices. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Devices"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { listDevices, upsertDevice } from \"@universal-music-store/platform-data\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst deviceSchema = z.object({\n  id: z.string().uuid().optional(),\n  name: z.string().trim().min(1).max(120),\n  type: z.enum([\"terminal\", \"printer\", \"kds\", \"scanner\"]).default(\"terminal\"),\n  ip_address: z.string().trim().max(64).optional(),\n  is_active: z.boolean().optional(),\n  config: z.record(z.string(), z.unknown()).optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"devices:manage\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listDevices(sup.client, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"devices:manage\");\n  if (!auth.ok) return auth.response;\n  const parsed = await parseAdminJson(req, deviceSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const device = await upsertDevice(sup.client, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: device }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/devices:POST\", post);\n"
      x-permission: "devices:manage"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_devicesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_devicesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/devices/{id}:
    patch:
      operationId: patch_admin_devices_id
      summary: "Update Admin / Devices / Id"
      description: "Admin / Devices / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Devices"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { updateDevice } from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst devicePatchSchema = z.object({\n  ip_address: z.string().trim().max(64).nullable().optional(),\n  config: z.record(z.string(), z.unknown()).optional(),\n  is_active: z.boolean().optional(),\n}).strict();\n\nasync function patch(\n  req: NextRequest,\n  ctx: { params: Promise<{ id: string }> },\n) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"devices:manage\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  if (!id) {\n    return correlatedJson(cid, { error: \"Missing id\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const parsed = await parseAdminJson(req, devicePatchSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const device = await updateDevice(sup.client, id, parsed.data, organization.id);\n  return correlatedJson(cid, { data: device });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/devices/[id]:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_devices_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_devices_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/employees:
    get:
      operationId: get_admin_employees
      summary: "Read Admin / Employees"
      description: "Admin / Employees. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listEmployees,\n  createEmployee,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"employees:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const activeOnly = req.nextUrl.searchParams.get(\"active\") === \"true\";\n  const data = await listEmployees(sb, { activeOnly });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"employees:write\");\n  if (!auth.ok) return auth.response;\n  const body = await req.json();\n  if (!body.full_name) {\n    return correlatedJson(cid, { error: \"full_name is required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const employee = await createEmployee(sb, body);\n  return correlatedJson(cid, { data: employee }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/employees:POST\", post);\n"
      x-permission: "employees:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_employeesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_employees
      summary: "Create or execute Admin / Employees"
      description: "Admin / Employees. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listEmployees,\n  createEmployee,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"employees:read\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const activeOnly = req.nextUrl.searchParams.get(\"active\") === \"true\";\n  const data = await listEmployees(sb, { activeOnly });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"employees:write\");\n  if (!auth.ok) return auth.response;\n  const body = await req.json();\n  if (!body.full_name) {\n    return correlatedJson(cid, { error: \"full_name is required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const employee = await createEmployee(sb, body);\n  return correlatedJson(cid, { data: employee }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/employees:POST\", post);\n"
      x-permission: "employees:read"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_employeesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_employeesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/employees/{id}:
    delete:
      operationId: delete_admin_employees_id
      summary: "Delete Admin / Employees / Id"
      description: "Admin / Employees / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getEmployee,\n  updateEmployee,\n  deleteEmployee,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await getEmployee(sb, id);\n  if (!emp) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function patch(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const body = await req.json();\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await updateEmployee(sb, id, body);\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  try {\n    await deleteEmployee(sb, id);\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : String(e);\n    const isFk =\n      /foreign key|violates|23503/i.test(msg) ||\n      msg.toLowerCase().includes(\"referenced\");\n    return correlatedError(\n      cid,\n      isFk ? 409 : 502,\n      isFk\n        ? \"This employee cannot be deleted while POS shifts, voids, or other records still reference them. Deactivate the employee instead, or remove related records first.\"\n        : \"Unable to delete employee.\",\n      isFk ? \"CONFLICT\" : \"INTERNAL_ERROR\",\n    );\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/employees/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/employees/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_employees_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_employees_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_employees_id
      summary: "Read Admin / Employees / Id"
      description: "Admin / Employees / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getEmployee,\n  updateEmployee,\n  deleteEmployee,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await getEmployee(sb, id);\n  if (!emp) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function patch(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const body = await req.json();\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await updateEmployee(sb, id, body);\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  try {\n    await deleteEmployee(sb, id);\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : String(e);\n    const isFk =\n      /foreign key|violates|23503/i.test(msg) ||\n      msg.toLowerCase().includes(\"referenced\");\n    return correlatedError(\n      cid,\n      isFk ? 409 : 502,\n      isFk\n        ? \"This employee cannot be deleted while POS shifts, voids, or other records still reference them. Deactivate the employee instead, or remove related records first.\"\n        : \"Unable to delete employee.\",\n      isFk ? \"CONFLICT\" : \"INTERNAL_ERROR\",\n    );\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/employees/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/employees/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_employees_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_employees_id
      summary: "Update Admin / Employees / Id"
      description: "Admin / Employees / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getEmployee,\n  updateEmployee,\n  deleteEmployee,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson } from \"@/lib/staff-api-response\";\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await getEmployee(sb, id);\n  if (!emp) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function patch(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const body = await req.json();\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const emp = await updateEmployee(sb, id, body);\n  return correlatedJson(cid, { data: emp });\n}\n\nasync function deleteHandler(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  try {\n    await deleteEmployee(sb, id);\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : String(e);\n    const isFk =\n      /foreign key|violates|23503/i.test(msg) ||\n      msg.toLowerCase().includes(\"referenced\");\n    return correlatedError(\n      cid,\n      isFk ? 409 : 502,\n      isFk\n        ? \"This employee cannot be deleted while POS shifts, voids, or other records still reference them. Deactivate the employee instead, or remove related records first.\"\n        : \"Unable to delete employee.\",\n      isFk ? \"CONFLICT\" : \"INTERNAL_ERROR\",\n    );\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/employees/[id]:PATCH\", patch);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/employees/[id]:DELETE\", deleteHandler);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_employees_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_employees_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/employees/{id}/pin:
    post:
      operationId: post_admin_employees_id_pin
      summary: "Create or execute Admin / Employees / Id / Pin"
      description: "Admin / Employees / Id / Pin. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  setEmployeePin,\n  verifyEmployeePin,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, stepUpRequired } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst pinSchema = z.object({ pin: z.string().regex(/^\\d{4,8}$/) }).strict();\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nasync function put(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (!stepUpRequired(\"employees.pin.write\", req)) {\n    return correlatedJson(cid, { error: \"Step-up authentication required\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, pinSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { pin } = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  await setEmployeePin(sb, id, pin);\n  return correlatedJson(cid, { success: true });\n}\n\nasync function post(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, pinSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { pin } = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const valid = await verifyEmployeePin(sb, id, pin);\n  return correlatedJson(cid, { valid });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/employees/[id]/pin:PUT\", put);\nexport const POST = withAdminMutationIdempotency(\"/admin/employees/[id]/pin:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_employees_id_pinRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_employees_id_pinResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_employees_id_pin
      summary: "Update Admin / Employees / Id / Pin"
      description: "Admin / Employees / Id / Pin. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Employees"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  setEmployeePin,\n  verifyEmployeePin,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, stepUpRequired } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst pinSchema = z.object({ pin: z.string().regex(/^\\d{4,8}$/) }).strict();\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nasync function put(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (!stepUpRequired(\"employees.pin.write\", req)) {\n    return correlatedJson(cid, { error: \"Step-up authentication required\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, pinSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { pin } = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  await setEmployeePin(sb, id, pin);\n  return correlatedJson(cid, { success: true });\n}\n\nasync function post(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, pinSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const { pin } = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const valid = await verifyEmployeePin(sb, id, pin);\n  return correlatedJson(cid, { valid });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/employees/[id]/pin:PUT\", put);\nexport const POST = withAdminMutationIdempotency(\"/admin/employees/[id]/pin:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_employees_id_pinRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_employees_id_pinResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/feature-mappings:
    get:
      operationId: get_admin_feature_mappings
      summary: "Read Admin / Feature Mappings"
      description: "Admin / Feature Mappings. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Feature Mappings"]
      x-source: "import { PLATFORM_FEATURE_MAPPINGS, buildPlatformFeatureMappingMetadata } from \"@universal-music-store/platform-data\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { NextRequest } from \"next/server\";\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"dashboard:read\")) return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  return correlatedJson(correlationId, {\n    data: buildPlatformFeatureMappingMetadata(),\n    generatedAt: new Date().toISOString(),\n    count: PLATFORM_FEATURE_MAPPINGS.length,\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_feature_mappingsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/integration-health:
    get:
      operationId: get_admin_integration_health
      summary: "Read Admin / Integration Health"
      description: "Admin / Integration Health. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Integration Health"]
      x-source: "import { NextResponse } from \"next/server\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport type IntegrationHealthEntry = {\n  provider: string;\n  status: \"healthy\" | \"degraded\" | \"down\" | \"unconfigured\";\n  sdkVersion: string | null;\n  lastWebhookAt: string | null;\n  webhookStatus: \"unknown\" | \"healthy\" | \"failing\";\n  envPresent: boolean;\n  note: string;\n};\n\nconst PROVIDER_ENV_KEYS: Record<string, string[]> = {\n  stripe: [\"STRIPE_API_KEY\"],\n  paypal: [\"PAYPAL_CLIENT_ID\", \"PAYPAL_CLIENT_SECRET\"],\n  paymongo: [\"PAYMONGO_SECRET_KEY\"],\n  maya: [\"MAYA_SECRET_KEY\"],\n  aftership: [\"AFTERSHIP_API_KEY\"],\n  nango: [\"NANGO_API_KEY\"],\n  pancake_pos: [\"PANCAKE_POS_API_KEY\"],\n};\n\nconst SDK_VERSIONS: Record<string, string> = {\n  stripe:\n    \"stripe (Checkout Session provider in apps/medusa/src/modules/stripe-checkout-payment)\",\n  paypal: \"@paypal/paypal-server-sdk\",\n  paymongo: \"typed REST wrapper (no official Node SDK)\",\n  maya: \"typed REST wrapper (no official Node SDK)\",\n  aftership: \"@aftership/tracking-sdk\",\n  nango: \"@nangohq/node\",\n  pancake_pos: \"Pancake POS Open API (server-side REST)\",\n};\n\nexport async function GET(request: Request) {\n  const auth = await requireStaffApiSession(\"settings:read\");\n  if (!auth.ok) return auth.response;\n\n  const supabase = adminSupabaseOr503(getCorrelationId(request));\n  if (\"response\" in supabase) return supabase.response;\n  const organization = await resolveStaffOrganization(supabase.client, auth.session.user?.email);\n  const { data: connections } = organization\n    ? await supabase.client\n        .from(\"payment_nango_connections\")\n        .select(\"provider\")\n        .eq(\"organization_id\", organization.id)\n        .eq(\"active\", true)\n    : { data: [] as Array<{ provider: string | null }> };\n  const connectedProviders = new Set((connections ?? []).map((item) => item.provider));\n  const providerConfigured = (provider: string, envKeys: string[]) => {\n    if (provider === \"stripe\") {\n      return connectedProviders.has(\"stripe\");\n    }\n    if (provider === \"paypal\") {\n      return connectedProviders.has(\"paypal\");\n    }\n    return envKeys.every((k) => Boolean(process.env[k]?.trim()));\n  };\n\n  const entries: IntegrationHealthEntry[] = Object.keys(PROVIDER_ENV_KEYS).map(\n    (provider) => {\n      const envKeys = PROVIDER_ENV_KEYS[provider] ?? [];\n      const envPresent = providerConfigured(provider, envKeys);\n\n      const status: IntegrationHealthEntry[\"status\"] = envPresent\n        ? \"healthy\"\n        : \"unconfigured\";\n      const note = envPresent\n        ? provider === \"stripe\" || provider === \"paypal\"\n          ? \"Organization-scoped Nango merchant connection is active.\"\n          : \"Env keys present for this app process (Medusa may use its own env).\"\n        : `Missing env: ${envKeys.join(\", \")}`;\n\n      return {\n        provider,\n        status,\n        sdkVersion: SDK_VERSIONS[provider] ?? null,\n        lastWebhookAt: null,\n        webhookStatus: \"unknown\" as const,\n        envPresent,\n        note,\n      };\n    },\n  );\n\n  return NextResponse.json({ entries });\n}\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_integration_healthResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/integrations/pancake:
    get:
      operationId: get_admin_integrations_pancake
      summary: "Read Admin / Integrations / Pancake"
      description: "Admin / Integrations / Pancake. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Integrations"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { POS_FEATURE_MAPPINGS } from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedError, correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport {\n  isPancakeConfigured,\n  listPancakeResource,\n  listPancakeShops,\n  PancakeApiError,\n  type PancakeResource,\n} from \"@/lib/pancake-client\";\n\nconst RESOURCES = new Set<PancakeResource>([\n  \"orders\", \"customers\", \"products\", \"warehouses\", \"inventory_histories\",\n  \"order_source\", \"order_tags\", \"e_invoices\", \"employees\", \"analytics_sale\",\n]);\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n\n  const resource = req.nextUrl.searchParams.get(\"resource\") ?? \"shops\";\n  if (!isPancakeConfigured()) {\n    return correlatedJson(correlationId, {\n      configured: false,\n      provider: \"pancake_pos\",\n      resource,\n      data: resource === \"feature-mappings\" ? POS_FEATURE_MAPPINGS : [],\n      message: \"Connect Pancake by setting PANCAKE_POS_API_KEY on the admin server.\",\n    });\n  }\n\n  try {\n    if (resource === \"feature-mappings\") {\n      return correlatedJson(correlationId, { configured: true, provider: \"pancake_pos\", resource, data: POS_FEATURE_MAPPINGS });\n    }\n    if (resource === \"shops\") {\n      return correlatedJson(correlationId, { configured: true, provider: \"pancake_pos\", resource, data: await listPancakeShops() });\n    }\n    if (!RESOURCES.has(resource as PancakeResource)) {\n      return correlatedError(correlationId, 400, \"Unsupported Pancake resource\", \"BAD_REQUEST\");\n    }\n    const shopId = req.nextUrl.searchParams.get(\"shopId\")?.trim();\n    if (!shopId) return correlatedError(correlationId, 400, \"shopId is required\", \"BAD_REQUEST\");\n    const data = await listPancakeResource(resource as PancakeResource, shopId, req.nextUrl.searchParams);\n    return correlatedJson(correlationId, { configured: true, provider: \"pancake_pos\", resource, shopId, data });\n  } catch (error) {\n    const status = error instanceof PancakeApiError ? error.status : 502;\n    return correlatedError(correlationId, status === 503 ? 503 : 502, error instanceof Error ? error.message : \"Pancake request failed\", status === 503 ? \"SERVICE_UNAVAILABLE\" : \"INTERNAL_ERROR\");\n  }\n}\n"
      x-permission: "settings:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_integrations_pancakeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory:
    get:
      operationId: get_admin_inventory
      summary: "Read Admin / Inventory"
      description: "Admin / Inventory. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import {\n  fetchMedusaInventoryPage,\n} from \"@/lib/medusa-inventory-bridge\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { requireStaffSessionWithPermission } from \"@/lib/requireStaffSession\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst ALLOWED_PAGE_SIZES = new Set([25, 50, 100]);\n\nfunction parseInventoryQuery(req: Request): { page: number; pageSize: number } {\n  const url = new URL(req.url);\n  const pageRaw = Number(url.searchParams.get(\"page\") ?? \"1\");\n  const page = Number.isFinite(pageRaw) && pageRaw >= 1 ? Math.floor(pageRaw) : 1;\n  const sizeRaw = Number(url.searchParams.get(\"pageSize\") ?? \"25\");\n  const pageSize = ALLOWED_PAGE_SIZES.has(sizeRaw) ? sizeRaw : 25;\n  return { page, pageSize };\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffSessionWithPermission(\"inventory:read\");\n  if (!staff.ok) {\n    logAdminApiEvent({\n      route: \"GET /api/admin/inventory\",\n      correlationId,\n      phase: \"error\",\n      detail: { code: \"unauthorized\" },\n    });\n    return tagResponse(staff.response, correlationId);\n  }\n\n  const { page, pageSize } = parseInventoryQuery(req);\n  const offset = (page - 1) * pageSize;\n\n  logAdminApiEvent({\n    route: \"GET /api/admin/inventory\",\n    correlationId,\n    phase: \"start\",\n    detail: { page, pageSize },\n  });\n\n  const result = await fetchMedusaInventoryPage({ limit: pageSize, offset });\n  logAdminApiEvent({\n    route: \"GET /api/admin/inventory\",\n    correlationId,\n    phase: \"ok\",\n    detail: { rowCount: result.rows.length, total: result.total },\n  });\n\n  return correlatedJson(correlationId, {\n    rows: result.rows,\n    page,\n    pageSize,\n    total: result.total,\n  });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventoryResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/adjust:
    post:
      operationId: post_admin_inventory_adjust
      summary: "Create or execute Admin / Inventory / Adjust"
      description: "Admin / Inventory / Adjust. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { z } from \"zod\";\nimport {\n  applyVariantStockedQuantity,\n  fetchVariantStockedQuantity,\n} from \"@/lib/medusa-catalog-inventory-stock\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport {\n  parseAdminJson,\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n} from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { recordInventoryMovementAudit } from \"@/lib/inventory-movement-audit\";\n\nconst schema = z\n  .object({\n    productId: z.string().trim().min(1).max(200),\n    variantId: z.string().trim().min(1).max(200),\n    stockedQuantity: z.number().int().min(0).max(1_000_000).optional(),\n    delta: z.number().int().min(-1_000_000).max(1_000_000).optional(),\n    expectedStockedQuantity: z.number().int().min(0).max(1_000_000).optional(),\n    locationId: z.string().trim().min(1).max(200).optional(),\n    reason: z\n      .enum([\n        \"receive\",\n        \"count\",\n        \"damage\",\n        \"loss\",\n        \"return\",\n        \"correction\",\n        \"transfer\",\n      ])\n      .default(\"correction\"),\n  })\n  .strict()\n  .superRefine((value, ctx) => {\n    if ((value.stockedQuantity == null) === (value.delta == null)) {\n      ctx.addIssue({\n        code: z.ZodIssueCode.custom,\n        message: \"Provide exactly one of stockedQuantity or delta\",\n      });\n    }\n  });\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  if (!staffSessionAllows(session, \"inventory:write\"))\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey)\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency-Key is required\" },\n      { status: 400 },\n    );\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${session.user.email.toLowerCase()}`,\n    actionKey: `inventory.adjust:${parsed.data.variantId}`,\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\")\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency key is already in use\" },\n      { status: 409 },\n    );\n  if (claim.kind !== \"claimed\")\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency service unavailable\" },\n      { status: 503 },\n    );\n  const before = await fetchVariantStockedQuantity(\n    parsed.data.variantId,\n    parsed.data.locationId,\n  );\n  if (before == null) {\n    const body = { error: \"Inventory quantity is unavailable\" };\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n  if (\n    parsed.data.expectedStockedQuantity != null &&\n    parsed.data.expectedStockedQuantity !== before\n  ) {\n    const body = { error: \"Inventory changed; reload before adjusting\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const stockedQuantity =\n    parsed.data.delta != null\n      ? before + parsed.data.delta\n      : parsed.data.stockedQuantity!;\n  if (stockedQuantity < 0 || stockedQuantity > 1_000_000) {\n    const body = { error: \"Inventory quantity is out of range\" };\n    await completeAdminIdempotency(sup.client, claim.id, 400, body);\n    return correlatedJson(correlationId, body, { status: 400 });\n  }\n  const result = await applyVariantStockedQuantity({\n    ...parsed.data,\n    stockedQuantity,\n  });\n  if (!result.ok) {\n    const body = { error: \"Unable to update inventory\" };\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n  await recordInventoryMovementAudit(sup.client, {\n    actorEmail: session.user.email,\n    reason: parsed.data.reason ?? \"correction\",\n    referenceType: \"inventory_adjustment\",\n    referenceId: idempotencyKey,\n    productId: parsed.data.productId,\n    variantId: parsed.data.variantId,\n    inventoryItemId: null,\n    locationId: parsed.data.locationId ?? \"default\",\n    quantityBefore: before,\n    quantityAfter: stockedQuantity,\n    correlationId,\n    organizationId: organization.id,\n  });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email,\n    action: \"inventory.adjust\",\n    resource: \"inventory_variant\",\n    resourceId: parsed.data.variantId,\n    details: {\n      organization_id: organization.id,\n      product_id: parsed.data.productId,\n      reason: parsed.data.reason,\n      location_id: parsed.data.locationId ?? null,\n      stocked_quantity: stockedQuantity,\n      delta: stockedQuantity - before,\n      before,\n    },\n  });\n  const body = {\n    data: {\n      variantId: parsed.data.variantId,\n      stockedQuantity,\n      delta: stockedQuantity - before,\n      reason: parsed.data.reason,\n    },\n  };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_adjustRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_adjustResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/cycle-counts:
    get:
      operationId: get_admin_inventory_cycle_counts
      summary: "Read Admin / Inventory / Cycle Counts"
      description: "Admin / Inventory / Cycle Counts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z.object({ productId: z.string().trim().min(1).max(200), variantId: z.string().trim().min(1).max(200) }).strict();\nconst createSchema = z.object({ locationId: z.string().trim().min(1).max(200), lines: z.array(lineSchema).min(1).max(500) }).strict().superRefine((value, ctx) => { if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" }); });\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory cycle counts\", code: \"INVENTORY_CYCLE_COUNTS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.cycle_count.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const expected: Array<{ productId: string; variantId: string; expectedQuantity: number }> = [];\n  for (const line of parsed.data.lines) {\n    const quantity = await fetchVariantStockedQuantity(line.variantId, parsed.data.locationId);\n    if (quantity == null) {\n      const body = { error: \"Inventory quantity is unavailable\", code: \"INVENTORY_READ_FAILED\" };\n      await completeAdminIdempotency(sup.client, claim.id, 502, body);\n      return correlatedJson(correlationId, body, { status: 502 });\n    }\n    expected.push({ ...line, expectedQuantity: quantity });\n  }\n  const { data: count, error } = await sup.client.from(\"inventory_cycle_counts\").insert({ organization_id: organization.id, location_id: parsed.data.locationId, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !count) {\n    const body = { error: \"Unable to create inventory cycle count\", code: \"INVENTORY_CYCLE_COUNT_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_cycle_count_lines\").insert(expected.map((line) => ({ cycle_count_id: count.id, product_id: line.productId, variant_id: line.variantId, expected_quantity: line.expectedQuantity })));\n  if (linesError) {\n    const body = { error: \"Unable to persist inventory cycle count lines\", code: \"INVENTORY_CYCLE_COUNT_CREATE_FAILED\" };\n    await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", count.id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision);\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...count, inventory_cycle_count_lines: expected.map((line) => ({ ...line, countedQuantity: null })) }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_cycle_countsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_cycle_counts
      summary: "Create or execute Admin / Inventory / Cycle Counts"
      description: "Admin / Inventory / Cycle Counts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z.object({ productId: z.string().trim().min(1).max(200), variantId: z.string().trim().min(1).max(200) }).strict();\nconst createSchema = z.object({ locationId: z.string().trim().min(1).max(200), lines: z.array(lineSchema).min(1).max(500) }).strict().superRefine((value, ctx) => { if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" }); });\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory cycle counts\", code: \"INVENTORY_CYCLE_COUNTS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.cycle_count.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const expected: Array<{ productId: string; variantId: string; expectedQuantity: number }> = [];\n  for (const line of parsed.data.lines) {\n    const quantity = await fetchVariantStockedQuantity(line.variantId, parsed.data.locationId);\n    if (quantity == null) {\n      const body = { error: \"Inventory quantity is unavailable\", code: \"INVENTORY_READ_FAILED\" };\n      await completeAdminIdempotency(sup.client, claim.id, 502, body);\n      return correlatedJson(correlationId, body, { status: 502 });\n    }\n    expected.push({ ...line, expectedQuantity: quantity });\n  }\n  const { data: count, error } = await sup.client.from(\"inventory_cycle_counts\").insert({ organization_id: organization.id, location_id: parsed.data.locationId, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !count) {\n    const body = { error: \"Unable to create inventory cycle count\", code: \"INVENTORY_CYCLE_COUNT_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_cycle_count_lines\").insert(expected.map((line) => ({ cycle_count_id: count.id, product_id: line.productId, variant_id: line.variantId, expected_quantity: line.expectedQuantity })));\n  if (linesError) {\n    const body = { error: \"Unable to persist inventory cycle count lines\", code: \"INVENTORY_CYCLE_COUNT_CREATE_FAILED\" };\n    await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", count.id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision);\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...count, inventory_cycle_count_lines: expected.map((line) => ({ ...line, countedQuantity: null })) }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_cycle_countsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_cycle_countsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/cycle-counts/{id}:
    get:
      operationId: get_admin_inventory_cycle_counts_id
      summary: "Read Admin / Inventory / Cycle Counts / Id"
      description: "Admin / Inventory / Cycle Counts / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst bodySchema = z.object({\n  action: z.enum([\"record\", \"complete\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n  lines: z.array(z.object({ lineId: z.string().uuid(), countedQuantity: z.number().int().nonnegative().max(1_000_000) }).strict()).max(500).optional(),\n}).strict().superRefine((value, ctx) => {\n  if (value.action === \"record\" && (!value.lines || value.lines.length === 0)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"Counted lines are required\" });\n  if (value.lines && new Set(value.lines.map((line) => line.lineId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A line may only be recorded once\" });\n});\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory cycle count\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory cycle count not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, bodySchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.cycle_count.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory cycle count not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const count = loaded.data as { id: string; status: string; revision: number; location_id: string; inventory_cycle_count_lines?: Array<{ id: string; product_id: string; variant_id: string; expected_quantity: number; counted_quantity: number | null }> };\n  if (count.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Cycle count changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action === \"cancel\") {\n    if (![\"open\"].includes(count.status)) {\n      const body = { error: \"Only open cycle counts can be cancelled\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const update = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"cancelled\", revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n  if (parsed.data.action === \"record\") {\n    if (count.status !== \"open\") {\n      const body = { error: \"Only open cycle counts can record quantities\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const lineById = new Map((count.inventory_cycle_count_lines ?? []).map((line) => [line.id, line]));\n    const lines = parsed.data.lines ?? [];\n    if (lines.some((line) => !lineById.has(line.lineId))) {\n      const body = { error: \"Cycle count line does not belong to this count\", code: \"INVENTORY_CYCLE_COUNT_LINE_INVALID\" };\n      await completeAdminIdempotency(sup.client, claim.id, 400, body);\n      return correlatedJson(correlationId, body, { status: 400 });\n    }\n    const recorded: Array<{ lineId: string; previous: number | null; next: number }> = [];\n    for (const line of lines) {\n      const previous = lineById.get(line.lineId)!.counted_quantity;\n      const update = previous == null\n        ? await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: line.countedQuantity }).eq(\"id\", line.lineId).eq(\"cycle_count_id\", id).is(\"counted_quantity\", null).select(\"id\").single()\n        : await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: line.countedQuantity }).eq(\"id\", line.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", previous).select(\"id\").single();\n      if (update.error || !update.data) {\n        for (const changed of recorded) {\n          const rollback = sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: changed.previous }).eq(\"id\", changed.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", changed.next);\n          await rollback;\n        }\n        const body = { error: \"Cycle count changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n        await completeAdminIdempotency(sup.client, claim.id, 409, body);\n        return correlatedJson(correlationId, body, { status: 409 });\n      }\n      recorded.push({ lineId: line.lineId, previous, next: line.countedQuantity });\n    }\n    const update = await sup.client.from(\"inventory_cycle_counts\").update({ revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      for (const changed of recorded) {\n        await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: changed.previous }).eq(\"id\", changed.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", changed.next);\n      }\n      const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.cycle_count.record\", resource: \"inventory_cycle_count\", resourceId: id, details: { organization_id: organization.id, lines: lines.length } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n  if (count.status !== \"open\") {\n    const body = { error: \"Only open cycle counts can be completed\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const lines = count.inventory_cycle_count_lines ?? [];\n  if (lines.length === 0 || lines.some((line) => line.counted_quantity == null)) {\n    const body = { error: \"Every cycle count line must be recorded before completion\", code: \"INVENTORY_CYCLE_COUNT_INCOMPLETE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 400, body);\n    return correlatedJson(correlationId, body, { status: 400 });\n  }\n  const processing = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"processing\", revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  for (const line of lines) {\n    const current = await fetchVariantStockedQuantity(line.variant_id, count.location_id);\n    if (current == null || current !== line.expected_quantity) {\n      const body = { error: \"Inventory changed since the count started; create a new count\", code: \"INVENTORY_CYCLE_COUNT_STALE\" };\n      await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: count.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: count.location_id, quantityBefore: current, quantityAfter: line.counted_quantity! });\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_cycle_count\", referenceId: id });\n  if (!applied.ok) {\n    await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: applied.code, failure_message: applied.message, revision: count.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const completed = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"completed\", revision: count.revision + 2, completed_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Cycle count applied but its lifecycle record could not be finalized\", code: \"INVENTORY_CYCLE_COUNT_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.cycle_count.complete\", resource: \"inventory_cycle_count\", resourceId: id, details: { organization_id: organization.id, lines: lines.length } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_cycle_counts_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_cycle_counts_id
      summary: "Create or execute Admin / Inventory / Cycle Counts / Id"
      description: "Admin / Inventory / Cycle Counts / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst bodySchema = z.object({\n  action: z.enum([\"record\", \"complete\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n  lines: z.array(z.object({ lineId: z.string().uuid(), countedQuantity: z.number().int().nonnegative().max(1_000_000) }).strict()).max(500).optional(),\n}).strict().superRefine((value, ctx) => {\n  if (value.action === \"record\" && (!value.lines || value.lines.length === 0)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"Counted lines are required\" });\n  if (value.lines && new Set(value.lines.map((line) => line.lineId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A line may only be recorded once\" });\n});\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory cycle count\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory cycle count not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, bodySchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.cycle_count.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await sup.client.from(\"inventory_cycle_counts\").select(\"*,inventory_cycle_count_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory cycle count not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const count = loaded.data as { id: string; status: string; revision: number; location_id: string; inventory_cycle_count_lines?: Array<{ id: string; product_id: string; variant_id: string; expected_quantity: number; counted_quantity: number | null }> };\n  if (count.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Cycle count changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action === \"cancel\") {\n    if (![\"open\"].includes(count.status)) {\n      const body = { error: \"Only open cycle counts can be cancelled\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const update = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"cancelled\", revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n  if (parsed.data.action === \"record\") {\n    if (count.status !== \"open\") {\n      const body = { error: \"Only open cycle counts can record quantities\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    const lineById = new Map((count.inventory_cycle_count_lines ?? []).map((line) => [line.id, line]));\n    const lines = parsed.data.lines ?? [];\n    if (lines.some((line) => !lineById.has(line.lineId))) {\n      const body = { error: \"Cycle count line does not belong to this count\", code: \"INVENTORY_CYCLE_COUNT_LINE_INVALID\" };\n      await completeAdminIdempotency(sup.client, claim.id, 400, body);\n      return correlatedJson(correlationId, body, { status: 400 });\n    }\n    const recorded: Array<{ lineId: string; previous: number | null; next: number }> = [];\n    for (const line of lines) {\n      const previous = lineById.get(line.lineId)!.counted_quantity;\n      const update = previous == null\n        ? await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: line.countedQuantity }).eq(\"id\", line.lineId).eq(\"cycle_count_id\", id).is(\"counted_quantity\", null).select(\"id\").single()\n        : await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: line.countedQuantity }).eq(\"id\", line.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", previous).select(\"id\").single();\n      if (update.error || !update.data) {\n        for (const changed of recorded) {\n          const rollback = sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: changed.previous }).eq(\"id\", changed.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", changed.next);\n          await rollback;\n        }\n        const body = { error: \"Cycle count changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n        await completeAdminIdempotency(sup.client, claim.id, 409, body);\n        return correlatedJson(correlationId, body, { status: 409 });\n      }\n      recorded.push({ lineId: line.lineId, previous, next: line.countedQuantity });\n    }\n    const update = await sup.client.from(\"inventory_cycle_counts\").update({ revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      for (const changed of recorded) {\n        await sup.client.from(\"inventory_cycle_count_lines\").update({ counted_quantity: changed.previous }).eq(\"id\", changed.lineId).eq(\"cycle_count_id\", id).eq(\"counted_quantity\", changed.next);\n      }\n      const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.cycle_count.record\", resource: \"inventory_cycle_count\", resourceId: id, details: { organization_id: organization.id, lines: lines.length } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n  if (count.status !== \"open\") {\n    const body = { error: \"Only open cycle counts can be completed\", code: \"INVENTORY_CYCLE_COUNT_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const lines = count.inventory_cycle_count_lines ?? [];\n  if (lines.length === 0 || lines.some((line) => line.counted_quantity == null)) {\n    const body = { error: \"Every cycle count line must be recorded before completion\", code: \"INVENTORY_CYCLE_COUNT_INCOMPLETE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 400, body);\n    return correlatedJson(correlationId, body, { status: 400 });\n  }\n  const processing = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"processing\", revision: count.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", count.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Cycle count was changed; refresh and retry\", code: \"INVENTORY_CYCLE_COUNT_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  for (const line of lines) {\n    const current = await fetchVariantStockedQuantity(line.variant_id, count.location_id);\n    if (current == null || current !== line.expected_quantity) {\n      const body = { error: \"Inventory changed since the count started; create a new count\", code: \"INVENTORY_CYCLE_COUNT_STALE\" };\n      await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: count.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: count.location_id, quantityBefore: current, quantityAfter: line.counted_quantity! });\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_cycle_count\", referenceId: id });\n  if (!applied.ok) {\n    await sup.client.from(\"inventory_cycle_counts\").update({ status: \"failed\", failure_code: applied.code, failure_message: applied.message, revision: count.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const completed = await sup.client.from(\"inventory_cycle_counts\").update({ status: \"completed\", revision: count.revision + 2, completed_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Cycle count applied but its lifecycle record could not be finalized\", code: \"INVENTORY_CYCLE_COUNT_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.cycle_count.complete\", resource: \"inventory_cycle_count\", resourceId: id, details: { organization_id: organization.id, lines: lines.length } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_cycle_counts_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_cycle_counts_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/ledger:
    get:
      operationId: get_admin_inventory_ledger
      summary: "Read Admin / Inventory / Ledger"
      description: "Admin / Inventory / Ledger. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const url = new URL(request.url);\n  const variantId = url.searchParams.get(\"variant_id\")?.trim();\n  const productId = url.searchParams.get(\"product_id\")?.trim();\n  const limit = Math.min(\n    200,\n    Math.max(1, Number(url.searchParams.get(\"limit\")) || 50),\n  );\n  let query = sup.client\n    .from(\"staff_catalog_inventory_audit\")\n    .select(\n      \"id,created_at,actor_email,reason,reference_type,reference_id,product_id,variant_id,location_id,quantity_before,quantity_after,quantity_delta,correlation_id\",\n    )\n    .eq(\"organization_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(limit);\n  if (variantId) query = query.eq(\"variant_id\", variantId);\n  if (productId) query = query.eq(\"product_id\", productId);\n  const { data, error } = await query;\n  if (error)\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Unable to load inventory ledger\",\n        code: \"INVENTORY_LEDGER_FAILED\",\n      },\n      { status: 502 },\n    );\n  return correlatedJson(correlationId, {\n    data: data ?? [],\n    organization_id: organization.id,\n  });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_ledgerResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/purchase-orders:
    get:
      operationId: get_admin_inventory_purchase_orders
      summary: "Read Admin / Inventory / Purchase Orders"
      description: "Admin / Inventory / Purchase Orders. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z.object({ productId: z.string().trim().min(1).max(200), variantId: z.string().trim().min(1).max(200), orderedQuantity: z.number().int().positive().max(1_000_000), unitCostMinor: z.number().int().nonnegative().max(1_000_000_000) }).strict();\nconst createSchema = z.object({ supplierName: z.string().trim().min(1).max(200), destinationLocationId: z.string().trim().min(1).max(200), currencyCode: z.literal(\"PHP\").default(\"PHP\"), lines: z.array(lineSchema).min(1).max(100) }).strict().superRefine((value, ctx) => { if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" }); });\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory purchase orders\", code: \"INVENTORY_PURCHASE_ORDERS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.purchase_order.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const { data: purchaseOrder, error } = await sup.client.from(\"inventory_purchase_orders\").insert({ organization_id: organization.id, supplier_name: parsed.data.supplierName, destination_location_id: parsed.data.destinationLocationId, currency_code: parsed.data.currencyCode, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !purchaseOrder) {\n    const body = { error: \"Unable to create inventory purchase order\", code: \"INVENTORY_PURCHASE_ORDER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_purchase_order_lines\").insert(parsed.data.lines.map((line) => ({ purchase_order_id: purchaseOrder.id, product_id: line.productId, variant_id: line.variantId, ordered_quantity: line.orderedQuantity, unit_cost_minor: line.unitCostMinor })));\n  if (linesError) {\n    await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: \"LINE_CREATE_FAILED\", failure_message: \"Unable to persist purchase order lines\", revision: purchaseOrder.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", purchaseOrder.id).eq(\"organization_id\", organization.id).eq(\"revision\", purchaseOrder.revision);\n    const body = { error: \"Unable to create inventory purchase order\", code: \"INVENTORY_PURCHASE_ORDER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...purchaseOrder, inventory_purchase_order_lines: parsed.data.lines.map((line) => ({ ...line, receivedQuantity: 0 })) }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_purchase_ordersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_purchase_orders
      summary: "Create or execute Admin / Inventory / Purchase Orders"
      description: "Admin / Inventory / Purchase Orders. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z.object({ productId: z.string().trim().min(1).max(200), variantId: z.string().trim().min(1).max(200), orderedQuantity: z.number().int().positive().max(1_000_000), unitCostMinor: z.number().int().nonnegative().max(1_000_000_000) }).strict();\nconst createSchema = z.object({ supplierName: z.string().trim().min(1).max(200), destinationLocationId: z.string().trim().min(1).max(200), currencyCode: z.literal(\"PHP\").default(\"PHP\"), lines: z.array(lineSchema).min(1).max(100) }).strict().superRefine((value, ctx) => { if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" }); });\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory purchase orders\", code: \"INVENTORY_PURCHASE_ORDERS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.purchase_order.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const { data: purchaseOrder, error } = await sup.client.from(\"inventory_purchase_orders\").insert({ organization_id: organization.id, supplier_name: parsed.data.supplierName, destination_location_id: parsed.data.destinationLocationId, currency_code: parsed.data.currencyCode, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !purchaseOrder) {\n    const body = { error: \"Unable to create inventory purchase order\", code: \"INVENTORY_PURCHASE_ORDER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_purchase_order_lines\").insert(parsed.data.lines.map((line) => ({ purchase_order_id: purchaseOrder.id, product_id: line.productId, variant_id: line.variantId, ordered_quantity: line.orderedQuantity, unit_cost_minor: line.unitCostMinor })));\n  if (linesError) {\n    await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: \"LINE_CREATE_FAILED\", failure_message: \"Unable to persist purchase order lines\", revision: purchaseOrder.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", purchaseOrder.id).eq(\"organization_id\", organization.id).eq(\"revision\", purchaseOrder.revision);\n    const body = { error: \"Unable to create inventory purchase order\", code: \"INVENTORY_PURCHASE_ORDER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...purchaseOrder, inventory_purchase_order_lines: parsed.data.lines.map((line) => ({ ...line, receivedQuantity: 0 })) }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_purchase_ordersRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_purchase_ordersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/purchase-orders/{id}:
    get:
      operationId: get_admin_inventory_purchase_orders_id
      summary: "Read Admin / Inventory / Purchase Orders / Id"
      description: "Admin / Inventory / Purchase Orders / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst bodySchema = z.object({\n  action: z.enum([\"submit\", \"receive\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n  lines: z.array(z.object({ lineId: z.string().uuid(), quantity: z.number().int().positive().max(1_000_000) }).strict()).max(100).optional(),\n}).strict().superRefine((value, ctx) => {\n  if (value.action === \"receive\" && (!value.lines || value.lines.length === 0)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"Receive lines are required\" });\n  if (value.lines && new Set(value.lines.map((line) => line.lineId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A line may only be received once\" });\n});\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory purchase order\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory purchase order not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, bodySchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.purchase_order.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory purchase order not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const po = loaded.data as { id: string; status: string; revision: number; destination_location_id: string; inventory_purchase_order_lines?: Array<{ id: string; product_id: string; variant_id: string; ordered_quantity: number; received_quantity: number }> };\n  if (po.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Purchase order changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const allowed: Record<string, string[]> = { submit: [\"draft\"], cancel: [\"draft\", \"submitted\", \"partially_received\"], receive: [\"submitted\", \"partially_received\"] };\n  if (!allowed[parsed.data.action]?.includes(po.status)) {\n    const body = { error: `Cannot ${parsed.data.action} a purchase order in its current state`, code: \"INVENTORY_PURCHASE_ORDER_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action !== \"receive\") {\n    const next = parsed.data.action === \"submit\" ? \"submitted\" : \"cancelled\";\n    const update = await sup.client.from(\"inventory_purchase_orders\").update({ status: next, revision: po.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", po.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Purchase order was changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: `inventory.purchase_order.${parsed.data.action}`, resource: \"inventory_purchase_order\", resourceId: id, details: { organization_id: organization.id, status: next } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n\n  const incoming = new Map((parsed.data.lines ?? []).map((line) => [line.lineId, line.quantity]));\n  const updates = (po.inventory_purchase_order_lines ?? []).filter((line) => incoming.has(line.id)).map((line) => ({ line, quantity: incoming.get(line.id)! }));\n  if (updates.length === 0 || updates.some(({ line, quantity }) => line.received_quantity + quantity > line.ordered_quantity)) {\n    const body = { error: \"Received quantities exceed the purchase order\", code: \"INVENTORY_RECEIPT_QUANTITY_INVALID\" };\n    await completeAdminIdempotency(sup.client, claim.id, 400, body);\n    return correlatedJson(correlationId, body, { status: 400 });\n  }\n  const processing = await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receiving\", revision: po.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", po.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Purchase order was changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  const updatedLines: Array<{ lineId: string; previous: number; next: number }> = [];\n  for (const { line, quantity } of updates) {\n    const current = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, po.destination_location_id);\n    if (current == null) {\n      const body = { error: \"Inventory quantity is unavailable\", code: \"INVENTORY_READ_FAILED\" };\n      await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: body.code, failure_message: body.error, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n      await completeAdminIdempotency(sup.client, claim.id, 502, body);\n      return correlatedJson(correlationId, body, { status: 502 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: po.destination_location_id, quantityBefore: current, quantityAfter: current + quantity });\n    updatedLines.push({ lineId: line.id, previous: line.received_quantity, next: line.received_quantity + quantity });\n  }\n  for (const line of updatedLines) {\n    const update = await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: line.next }).eq(\"id\", line.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", line.previous).select(\"id\").single();\n    if (update.error || !update.data) {\n      for (const previous of updatedLines) {\n        if (previous.lineId === line.lineId) break;\n        await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: previous.previous }).eq(\"id\", previous.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", previous.next);\n      }\n      const body = { error: \"Unable to persist received quantities\", code: \"INVENTORY_RECEIPT_PERSIST_FAILED\" };\n      await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: body.code, failure_message: body.error, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n      await completeAdminIdempotency(sup.client, claim.id, 503, body);\n      return correlatedJson(correlationId, body, { status: 503 });\n    }\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_purchase_order_receipt\", referenceId: id });\n  if (!applied.ok) {\n    for (const line of updatedLines) await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: line.previous }).eq(\"id\", line.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", line.next);\n    await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: applied.code, failure_message: applied.message, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const allReceived = (po.inventory_purchase_order_lines ?? []).every((line) => line.received_quantity + (incoming.get(line.id) ?? 0) === line.ordered_quantity);\n  const completed = await sup.client.from(\"inventory_purchase_orders\").update({ status: allReceived ? \"received\" : \"partially_received\", revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Purchase receipt applied but its lifecycle record could not be finalized\", code: \"INVENTORY_RECEIPT_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.purchase_order.receive\", resource: \"inventory_purchase_order\", resourceId: id, details: { organization_id: organization.id, received_lines: updatedLines.length } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_purchase_orders_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_purchase_orders_id
      summary: "Create or execute Admin / Inventory / Purchase Orders / Id"
      description: "Admin / Inventory / Purchase Orders / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst bodySchema = z.object({\n  action: z.enum([\"submit\", \"receive\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n  lines: z.array(z.object({ lineId: z.string().uuid(), quantity: z.number().int().positive().max(1_000_000) }).strict()).max(100).optional(),\n}).strict().superRefine((value, ctx) => {\n  if (value.action === \"receive\" && (!value.lines || value.lines.length === 0)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"Receive lines are required\" });\n  if (value.lines && new Set(value.lines.map((line) => line.lineId)).size !== value.lines.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A line may only be received once\" });\n});\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory purchase order\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory purchase order not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, bodySchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.purchase_order.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await sup.client.from(\"inventory_purchase_orders\").select(\"*,inventory_purchase_order_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory purchase order not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const po = loaded.data as { id: string; status: string; revision: number; destination_location_id: string; inventory_purchase_order_lines?: Array<{ id: string; product_id: string; variant_id: string; ordered_quantity: number; received_quantity: number }> };\n  if (po.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Purchase order changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const allowed: Record<string, string[]> = { submit: [\"draft\"], cancel: [\"draft\", \"submitted\", \"partially_received\"], receive: [\"submitted\", \"partially_received\"] };\n  if (!allowed[parsed.data.action]?.includes(po.status)) {\n    const body = { error: `Cannot ${parsed.data.action} a purchase order in its current state`, code: \"INVENTORY_PURCHASE_ORDER_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action !== \"receive\") {\n    const next = parsed.data.action === \"submit\" ? \"submitted\" : \"cancelled\";\n    const update = await sup.client.from(\"inventory_purchase_orders\").update({ status: next, revision: po.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", po.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Purchase order was changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: `inventory.purchase_order.${parsed.data.action}`, resource: \"inventory_purchase_order\", resourceId: id, details: { organization_id: organization.id, status: next } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n\n  const incoming = new Map((parsed.data.lines ?? []).map((line) => [line.lineId, line.quantity]));\n  const updates = (po.inventory_purchase_order_lines ?? []).filter((line) => incoming.has(line.id)).map((line) => ({ line, quantity: incoming.get(line.id)! }));\n  if (updates.length === 0 || updates.some(({ line, quantity }) => line.received_quantity + quantity > line.ordered_quantity)) {\n    const body = { error: \"Received quantities exceed the purchase order\", code: \"INVENTORY_RECEIPT_QUANTITY_INVALID\" };\n    await completeAdminIdempotency(sup.client, claim.id, 400, body);\n    return correlatedJson(correlationId, body, { status: 400 });\n  }\n  const processing = await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receiving\", revision: po.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", po.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Purchase order was changed; refresh and retry\", code: \"INVENTORY_PURCHASE_ORDER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  const updatedLines: Array<{ lineId: string; previous: number; next: number }> = [];\n  for (const { line, quantity } of updates) {\n    const current = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, po.destination_location_id);\n    if (current == null) {\n      const body = { error: \"Inventory quantity is unavailable\", code: \"INVENTORY_READ_FAILED\" };\n      await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: body.code, failure_message: body.error, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n      await completeAdminIdempotency(sup.client, claim.id, 502, body);\n      return correlatedJson(correlationId, body, { status: 502 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: po.destination_location_id, quantityBefore: current, quantityAfter: current + quantity });\n    updatedLines.push({ lineId: line.id, previous: line.received_quantity, next: line.received_quantity + quantity });\n  }\n  for (const line of updatedLines) {\n    const update = await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: line.next }).eq(\"id\", line.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", line.previous).select(\"id\").single();\n    if (update.error || !update.data) {\n      for (const previous of updatedLines) {\n        if (previous.lineId === line.lineId) break;\n        await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: previous.previous }).eq(\"id\", previous.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", previous.next);\n      }\n      const body = { error: \"Unable to persist received quantities\", code: \"INVENTORY_RECEIPT_PERSIST_FAILED\" };\n      await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: body.code, failure_message: body.error, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n      await completeAdminIdempotency(sup.client, claim.id, 503, body);\n      return correlatedJson(correlationId, body, { status: 503 });\n    }\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_purchase_order_receipt\", referenceId: id });\n  if (!applied.ok) {\n    for (const line of updatedLines) await sup.client.from(\"inventory_purchase_order_lines\").update({ received_quantity: line.previous }).eq(\"id\", line.lineId).eq(\"purchase_order_id\", id).eq(\"received_quantity\", line.next);\n    await sup.client.from(\"inventory_purchase_orders\").update({ status: \"receive_failed\", failure_code: applied.code, failure_message: applied.message, revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const allReceived = (po.inventory_purchase_order_lines ?? []).every((line) => line.received_quantity + (incoming.get(line.id) ?? 0) === line.ordered_quantity);\n  const completed = await sup.client.from(\"inventory_purchase_orders\").update({ status: allReceived ? \"received\" : \"partially_received\", revision: po.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"receiving\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Purchase receipt applied but its lifecycle record could not be finalized\", code: \"INVENTORY_RECEIPT_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.purchase_order.receive\", resource: \"inventory_purchase_order\", resourceId: id, details: { organization_id: organization.id, received_lines: updatedLines.length } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_purchase_orders_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_purchase_orders_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/reservations:
    get:
      operationId: get_admin_inventory_reservations
      summary: "Read Admin / Inventory / Reservations"
      description: "Admin / Inventory / Reservations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport {\n  reserveInventory,\n  type InventoryReservationRow,\n} from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst reserveSchema = z\n  .object({\n    locationId: z.string().trim().min(1).max(200).default(\"default\"),\n    inventoryItemId: z.string().trim().min(1).max(200),\n    quantity: z.number().int().positive().max(1_000_000),\n    referenceType: z.string().trim().max(100).optional(),\n    referenceId: z.string().trim().max(200).optional(),\n    metadata: z.record(z.string(), z.unknown()).optional(),\n  })\n  .strict();\n\nfunction row(row: InventoryReservationRow) {\n  return {\n    id: row.id,\n    tenantId: row.tenant_id,\n    locationId: row.location_id,\n    inventoryItemId: row.inventory_item_id,\n    quantity: row.quantity,\n    status: row.status,\n    referenceType: row.reference_type,\n    referenceId: row.reference_id,\n    medusaReservationId: row.medusa_reservation_id,\n    reservedAt: row.reserved_at,\n    releasedAt: row.released_at,\n    committedAt: row.committed_at,\n  };\n}\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization) {\n    return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  }\n  const url = new URL(request.url);\n  const limit = Math.min(200, Math.max(1, Number(url.searchParams.get(\"limit\")) || 50));\n  let query = sup.client\n    .from(\"inventory_reservations\")\n    .select(\"id,tenant_id,location_id,inventory_item_id,quantity,status,reference_type,reference_id,medusa_reservation_id,reserved_at,released_at,committed_at\")\n    .eq(\"tenant_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(limit);\n  const status = url.searchParams.get(\"status\")?.trim();\n  const inventoryItemId = url.searchParams.get(\"inventoryItemId\")?.trim();\n  if (status) query = query.eq(\"status\", status);\n  if (inventoryItemId) query = query.eq(\"inventory_item_id\", inventoryItemId);\n  const { data, error } = await query;\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory reservations\", code: \"INVENTORY_RESERVATIONS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nasync function post(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = request.headers.get(\"Idempotency-Key\")?.trim();\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const body = await request.json().catch(() => null);\n  const parsed = reserveSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(correlationId, { error: \"Invalid reservation payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const availableQuantity = await fetchVariantStockedQuantity(parsed.data.inventoryItemId);\n  if (availableQuantity == null) return correlatedJson(correlationId, { error: \"Inventory quantity is unavailable\" }, { status: 502 });\n  try {\n    const reservation = await reserveInventory(sup.client, {\n      tenantId: organization.id,\n      locationId: parsed.data.locationId,\n      inventoryItemId: parsed.data.inventoryItemId,\n      quantity: parsed.data.quantity,\n      availableQuantity,\n      idempotencyKey,\n      referenceType: parsed.data.referenceType,\n      referenceId: parsed.data.referenceId,\n      metadata: parsed.data.metadata,\n    });\n    return correlatedJson(correlationId, { data: row(reservation) }, { status: 201 });\n  } catch {\n    return correlatedJson(correlationId, { error: \"Unable to reserve inventory\", code: \"INVENTORY_RESERVATION_FAILED\" }, { status: 409 });\n  }\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/inventory/reservations:POST\", post);\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_reservationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_reservations
      summary: "Create or execute Admin / Inventory / Reservations"
      description: "Admin / Inventory / Reservations. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport {\n  reserveInventory,\n  type InventoryReservationRow,\n} from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { fetchVariantStockedQuantity } from \"@/lib/medusa-catalog-inventory-stock\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst reserveSchema = z\n  .object({\n    locationId: z.string().trim().min(1).max(200).default(\"default\"),\n    inventoryItemId: z.string().trim().min(1).max(200),\n    quantity: z.number().int().positive().max(1_000_000),\n    referenceType: z.string().trim().max(100).optional(),\n    referenceId: z.string().trim().max(200).optional(),\n    metadata: z.record(z.string(), z.unknown()).optional(),\n  })\n  .strict();\n\nfunction row(row: InventoryReservationRow) {\n  return {\n    id: row.id,\n    tenantId: row.tenant_id,\n    locationId: row.location_id,\n    inventoryItemId: row.inventory_item_id,\n    quantity: row.quantity,\n    status: row.status,\n    referenceType: row.reference_type,\n    referenceId: row.reference_id,\n    medusaReservationId: row.medusa_reservation_id,\n    reservedAt: row.reserved_at,\n    releasedAt: row.released_at,\n    committedAt: row.committed_at,\n  };\n}\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization) {\n    return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  }\n  const url = new URL(request.url);\n  const limit = Math.min(200, Math.max(1, Number(url.searchParams.get(\"limit\")) || 50));\n  let query = sup.client\n    .from(\"inventory_reservations\")\n    .select(\"id,tenant_id,location_id,inventory_item_id,quantity,status,reference_type,reference_id,medusa_reservation_id,reserved_at,released_at,committed_at\")\n    .eq(\"tenant_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(limit);\n  const status = url.searchParams.get(\"status\")?.trim();\n  const inventoryItemId = url.searchParams.get(\"inventoryItemId\")?.trim();\n  if (status) query = query.eq(\"status\", status);\n  if (inventoryItemId) query = query.eq(\"inventory_item_id\", inventoryItemId);\n  const { data, error } = await query;\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory reservations\", code: \"INVENTORY_RESERVATIONS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nasync function post(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = request.headers.get(\"Idempotency-Key\")?.trim();\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const body = await request.json().catch(() => null);\n  const parsed = reserveSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(correlationId, { error: \"Invalid reservation payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const availableQuantity = await fetchVariantStockedQuantity(parsed.data.inventoryItemId);\n  if (availableQuantity == null) return correlatedJson(correlationId, { error: \"Inventory quantity is unavailable\" }, { status: 502 });\n  try {\n    const reservation = await reserveInventory(sup.client, {\n      tenantId: organization.id,\n      locationId: parsed.data.locationId,\n      inventoryItemId: parsed.data.inventoryItemId,\n      quantity: parsed.data.quantity,\n      availableQuantity,\n      idempotencyKey,\n      referenceType: parsed.data.referenceType,\n      referenceId: parsed.data.referenceId,\n      metadata: parsed.data.metadata,\n    });\n    return correlatedJson(correlationId, { data: row(reservation) }, { status: 201 });\n  } catch {\n    return correlatedJson(correlationId, { error: \"Unable to reserve inventory\", code: \"INVENTORY_RESERVATION_FAILED\" }, { status: 409 });\n  }\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/inventory/reservations:POST\", post);\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_reservationsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_reservationsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/reservations/{id}:
    post:
      operationId: post_admin_inventory_reservations_id
      summary: "Create or execute Admin / Inventory / Reservations / Id"
      description: "Admin / Inventory / Reservations / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport {\n  attachMedusaInventoryReservation,\n  closeMedusaInventoryReservation,\n  commitInventoryReservation,\n  releaseInventoryReservation,\n} from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst bodySchema = z\n  .object({\n    operation: z.enum([\"release\", \"commit\", \"attach_medusa\", \"close_medusa\"]),\n    medusaReservationId: z.string().trim().min(1).max(200).optional(),\n  })\n  .strict()\n  .superRefine((value, context) => {\n    if (value.operation === \"attach_medusa\" && !value.medusaReservationId) {\n      context.addIssue({ code: z.ZodIssueCode.custom, message: \"medusaReservationId is required\" });\n    }\n  });\n\ntype Params = { params: Promise<{ id: string }> };\n\nasync function post(request: Request, context: Params) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = request.headers.get(\"Idempotency-Key\")?.trim();\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = bodySchema.safeParse(await request.json().catch(() => null));\n  if (!parsed.success) return correlatedJson(correlationId, { error: \"Invalid reservation operation\" }, { status: 400 });\n  const { id } = await context.params;\n  if (!id?.trim()) return correlatedJson(correlationId, { error: \"Reservation not found\" }, { status: 404 });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  try {\n    const input = { tenantId: organization.id, reservationId: id, idempotencyKey };\n    const reservation = parsed.data.operation === \"release\"\n      ? await releaseInventoryReservation(sup.client, input)\n      : parsed.data.operation === \"commit\"\n        ? await commitInventoryReservation(sup.client, input)\n        : parsed.data.operation === \"attach_medusa\"\n          ? await attachMedusaInventoryReservation(sup.client, { ...input, medusaReservationId: parsed.data.medusaReservationId! })\n          : await closeMedusaInventoryReservation(sup.client, input);\n    return correlatedJson(correlationId, { data: reservation }, { status: 200 });\n  } catch {\n    return correlatedJson(correlationId, { error: \"Unable to update inventory reservation\", code: \"INVENTORY_RESERVATION_OPERATION_FAILED\" }, { status: 409 });\n  }\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/inventory/reservations/[id]:POST\", post);\n"
      x-permission: "inventory:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_reservations_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_reservations_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/stream:
    get:
      operationId: get_admin_inventory_stream
      summary: "Read Admin / Inventory / Stream"
      description: "Admin / Inventory / Stream. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { fetchMedusaInventoryPage } from \"@/lib/medusa-inventory-bridge\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffSessionWithPermission } from \"@/lib/requireStaffSession\";\nimport { tagResponse } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\nexport const runtime = \"nodejs\";\n\nconst TICK_MS = 10_000;\n\nconst ALLOWED_PAGE_SIZES = new Set([25, 50, 100]);\n\nfunction parseInventoryQuery(req: Request): { page: number; pageSize: number } {\n  const url = new URL(req.url);\n  const pageRaw = Number(url.searchParams.get(\"page\") ?? \"1\");\n  const page = Number.isFinite(pageRaw) && pageRaw >= 1 ? Math.floor(pageRaw) : 1;\n  const sizeRaw = Number(url.searchParams.get(\"pageSize\") ?? \"25\");\n  const pageSize = ALLOWED_PAGE_SIZES.has(sizeRaw) ? sizeRaw : 25;\n  return { page, pageSize };\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffSessionWithPermission(\"inventory:read\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  const { page, pageSize } = parseInventoryQuery(req);\n  const offset = (page - 1) * pageSize;\n\n  logAdminApiEvent({\n    route: \"GET /api/admin/inventory/stream\",\n    correlationId,\n    phase: \"start\",\n    detail: { page, pageSize },\n  });\n\n  const encoder = new TextEncoder();\n  let closeStream = () => {};\n  const stream = new ReadableStream({\n    async start(controller) {\n      let closed = false;\n      let interval: ReturnType<typeof setInterval> | undefined;\n\n      const close = () => {\n        if (closed) return;\n        closed = true;\n        if (interval) clearInterval(interval);\n        try {\n          controller.close();\n        } catch {\n          // The consumer may have closed the stream before the abort event.\n        }\n      };\n      closeStream = close;\n\n      const send = async () => {\n        if (closed) return;\n        try {\n          const result = await fetchMedusaInventoryPage({\n            limit: pageSize,\n            offset,\n          });\n          if (closed) return;\n          controller.enqueue(\n            encoder.encode(\n              `data: ${JSON.stringify({\n                rows: result.rows,\n                page,\n                pageSize,\n                total: result.total,\n              })}\\n\\n`,\n            ),\n          );\n        } catch (e) {\n          if (closed) return;\n          const msg = e instanceof Error ? e.message : String(e);\n          try {\n            controller.enqueue(\n              encoder.encode(`event: error\\ndata: ${JSON.stringify({ error: msg })}\\n\\n`),\n            );\n          } catch {\n            close();\n          }\n        }\n      };\n\n      await send();\n      if (closed) return;\n      interval = setInterval(() => {\n        void send();\n      }, TICK_MS);\n\n      req.signal.addEventListener(\"abort\", () => {\n        close();\n      });\n    },\n    cancel() {\n      closeStream();\n    },\n  });\n\n  return new Response(stream, {\n    headers: {\n      \"Content-Type\": \"text/event-stream\",\n      \"Cache-Control\": \"no-cache, no-transform\",\n      Connection: \"keep-alive\",\n      \"x-request-id\": correlationId,\n    },\n  });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_streamResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/transfers:
    get:
      operationId: get_admin_inventory_transfers
      summary: "Read Admin / Inventory / Transfers"
      description: "Admin / Inventory / Transfers. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst lineSchema = z.object({\n  productId: z.string().trim().min(1).max(200),\n  variantId: z.string().trim().min(1).max(200),\n  quantity: z.number().int().positive().max(1_000_000),\n}).strict();\nconst createSchema = z.object({\n  sourceLocationId: z.string().trim().min(1).max(200),\n  destinationLocationId: z.string().trim().min(1).max(200),\n  lines: z.array(lineSchema).min(1).max(100),\n}).strict().superRefine((value, ctx) => {\n  if (value.sourceLocationId === value.destinationLocationId) {\n    ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"destinationLocationId\"], message: \"Locations must differ\" });\n  }\n  if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) {\n    ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" });\n  }\n});\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_transfers\").select(\"*,inventory_transfer_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory transfers\", code: \"INVENTORY_TRANSFERS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.transfer.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const { data: transfer, error } = await sup.client.from(\"inventory_transfers\").insert({ organization_id: organization.id, source_location_id: parsed.data.sourceLocationId, destination_location_id: parsed.data.destinationLocationId, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !transfer) {\n    const body = { error: \"Unable to create inventory transfer\", code: \"INVENTORY_TRANSFER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_transfer_lines\").insert(parsed.data.lines.map((line) => ({ transfer_id: transfer.id, product_id: line.productId, variant_id: line.variantId, quantity: line.quantity })));\n  if (linesError) {\n    await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: \"LINE_CREATE_FAILED\", failure_message: \"Unable to persist transfer lines\", revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", transfer.id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision);\n    const body = { error: \"Unable to create inventory transfer\", code: \"INVENTORY_TRANSFER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...transfer, inventory_transfer_lines: parsed.data.lines }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_transfersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_transfers
      summary: "Create or execute Admin / Inventory / Transfers"
      description: "Admin / Inventory / Transfers. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst lineSchema = z.object({\n  productId: z.string().trim().min(1).max(200),\n  variantId: z.string().trim().min(1).max(200),\n  quantity: z.number().int().positive().max(1_000_000),\n}).strict();\nconst createSchema = z.object({\n  sourceLocationId: z.string().trim().min(1).max(200),\n  destinationLocationId: z.string().trim().min(1).max(200),\n  lines: z.array(lineSchema).min(1).max(100),\n}).strict().superRefine((value, ctx) => {\n  if (value.sourceLocationId === value.destinationLocationId) {\n    ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"destinationLocationId\"], message: \"Locations must differ\" });\n  }\n  if (new Set(value.lines.map((line) => line.variantId)).size !== value.lines.length) {\n    ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"lines\"], message: \"A variant may only appear once\" });\n  }\n});\n\nexport async function GET(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const limit = Math.min(200, Math.max(1, Number(new URL(request.url).searchParams.get(\"limit\")) || 50));\n  const { data, error } = await sup.client.from(\"inventory_transfers\").select(\"*,inventory_transfer_lines(*)\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }).limit(limit);\n  if (error) return correlatedJson(correlationId, { error: \"Unable to load inventory transfers\", code: \"INVENTORY_TRANSFERS_FAILED\" }, { status: 502 });\n  return correlatedJson(correlationId, { data: data ?? [], organizationId: organization.id });\n}\n\nexport async function POST(request: Request) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, createSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: \"inventory.transfer.create\", idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const { data: transfer, error } = await sup.client.from(\"inventory_transfers\").insert({ organization_id: organization.id, source_location_id: parsed.data.sourceLocationId, destination_location_id: parsed.data.destinationLocationId, idempotency_key: idempotencyKey, created_by_email: actor }).select(\"*\").single();\n  if (error || !transfer) {\n    const body = { error: \"Unable to create inventory transfer\", code: \"INVENTORY_TRANSFER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const { error: linesError } = await sup.client.from(\"inventory_transfer_lines\").insert(parsed.data.lines.map((line) => ({ transfer_id: transfer.id, product_id: line.productId, variant_id: line.variantId, quantity: line.quantity })));\n  if (linesError) {\n    await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: \"LINE_CREATE_FAILED\", failure_message: \"Unable to persist transfer lines\", revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", transfer.id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision);\n    const body = { error: \"Unable to create inventory transfer\", code: \"INVENTORY_TRANSFER_CREATE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  const body = { data: { ...transfer, inventory_transfer_lines: parsed.data.lines }, organizationId: organization.id };\n  await completeAdminIdempotency(sup.client, claim.id, 201, body);\n  return correlatedJson(correlationId, body, { status: 201 });\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_transfersRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_transfersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/inventory/transfers/{id}:
    get:
      operationId: get_admin_inventory_transfers_id
      summary: "Read Admin / Inventory / Transfers / Id"
      description: "Admin / Inventory / Transfers / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { tryCreateSupabaseClient } from \"@universal-music-store/platform-data\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst actionSchema = z.object({\n  action: z.enum([\"approve\", \"ship\", \"complete\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n}).strict();\n\ntype AdminSupabaseClient = NonNullable<ReturnType<typeof tryCreateSupabaseClient>>;\n\nasync function loadTransfer(client: AdminSupabaseClient, id: string, organizationId: string) {\n  return client.from(\"inventory_transfers\").select(\"*,inventory_transfer_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organizationId).maybeSingle();\n}\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await loadTransfer(sup.client, id, organization.id);\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory transfer\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory transfer not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, actionSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.transfer.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await loadTransfer(sup.client, id, organization.id);\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory transfer not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const transfer = loaded.data as { id: string; status: string; revision: number; source_location_id: string; destination_location_id: string; inventory_transfer_lines?: Array<{ product_id: string; variant_id: string; quantity: number }> };\n  if (transfer.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Inventory transfer changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const transitions: Record<string, string[]> = { approve: [\"draft\"], ship: [\"approved\"], complete: [\"in_transit\"], cancel: [\"draft\", \"approved\", \"in_transit\"] };\n  if (!transitions[parsed.data.action]?.includes(transfer.status)) {\n    const body = { error: `Cannot ${parsed.data.action} a transfer in its current state`, code: \"INVENTORY_TRANSFER_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action !== \"complete\") {\n    const next = parsed.data.action === \"approve\" ? \"approved\" : parsed.data.action === \"ship\" ? \"in_transit\" : \"cancelled\";\n    const update = await sup.client.from(\"inventory_transfers\").update({ status: next, revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Inventory transfer was changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: `inventory.transfer.${parsed.data.action}`, resource: \"inventory_transfer\", resourceId: id, details: { organization_id: organization.id, status: next } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n\n  const processing = await sup.client.from(\"inventory_transfers\").update({ status: \"processing\", revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Inventory transfer was changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  for (const line of transfer.inventory_transfer_lines ?? []) {\n    const source = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, transfer.source_location_id);\n    const destination = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, transfer.destination_location_id);\n    if (source == null || destination == null || source < line.quantity) {\n      const body = { error: \"Unable to complete transfer because source inventory is unavailable or insufficient\", code: source != null && source < line.quantity ? \"INSUFFICIENT_INVENTORY\" : \"INVENTORY_READ_FAILED\" };\n      await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: transfer.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: transfer.source_location_id, quantityBefore: source, quantityAfter: source - line.quantity });\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: transfer.destination_location_id, quantityBefore: destination, quantityAfter: destination + line.quantity });\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_transfer\", referenceId: id });\n  if (!applied.ok) {\n    await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: applied.code, failure_message: applied.message, revision: transfer.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const completed = await sup.client.from(\"inventory_transfers\").update({ status: \"completed\", revision: transfer.revision + 2, completed_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Inventory transfer completed but its lifecycle record could not be finalized\", code: \"INVENTORY_TRANSFER_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.transfer.complete\", resource: \"inventory_transfer\", resourceId: id, details: { organization_id: organization.id } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_inventory_transfers_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_inventory_transfers_id
      summary: "Create or execute Admin / Inventory / Transfers / Id"
      description: "Admin / Inventory / Transfers / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Inventory"]
      x-source: "import { z } from \"zod\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { applyInventoryStockChanges, type InventoryStockChange } from \"@/lib/inventory-lifecycle\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { tryCreateSupabaseClient } from \"@universal-music-store/platform-data\";\n\nexport const dynamic = \"force-dynamic\";\ntype Context = { params: Promise<{ id: string }> };\nconst actionSchema = z.object({\n  action: z.enum([\"approve\", \"ship\", \"complete\", \"cancel\"]),\n  expectedRevision: z.number().int().positive(),\n}).strict();\n\ntype AdminSupabaseClient = NonNullable<ReturnType<typeof tryCreateSupabaseClient>>;\n\nasync function loadTransfer(client: AdminSupabaseClient, id: string, organizationId: string) {\n  return client.from(\"inventory_transfers\").select(\"*,inventory_transfer_lines(*)\").eq(\"id\", id).eq(\"organization_id\", organizationId).maybeSingle();\n}\n\nexport async function GET(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:read\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { id } = await context.params;\n  const result = await loadTransfer(sup.client, id, organization.id);\n  if (result.error) return correlatedJson(correlationId, { error: \"Unable to load inventory transfer\" }, { status: 502 });\n  if (!result.data) return correlatedJson(correlationId, { error: \"Inventory transfer not found\" }, { status: 404 });\n  return correlatedJson(correlationId, { data: result.data });\n}\n\nexport async function POST(request: Request, context: Context) {\n  const correlationId = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"inventory:write\");\n  if (!staff.ok) return tagResponse(staff.response, correlationId);\n  const idempotencyKey = getIdempotencyKey(request);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(request, actionSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { id } = await context.params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const actor = staff.session.user?.email?.trim().toLowerCase() ?? \"system\";\n  const claim = await claimAdminIdempotency(sup.client, { actorKey: `${organization.id}:${actor}`, actionKey: `inventory.transfer.${parsed.data.action}:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Idempotency key was already used with another request\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const loaded = await loadTransfer(sup.client, id, organization.id);\n  if (loaded.error || !loaded.data) {\n    const body = { error: \"Inventory transfer not found\" };\n    await completeAdminIdempotency(sup.client, claim.id, 404, body);\n    return correlatedJson(correlationId, body, { status: 404 });\n  }\n  const transfer = loaded.data as { id: string; status: string; revision: number; source_location_id: string; destination_location_id: string; inventory_transfer_lines?: Array<{ product_id: string; variant_id: string; quantity: number }> };\n  if (transfer.revision !== parsed.data.expectedRevision) {\n    const body = { error: \"Inventory transfer changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const transitions: Record<string, string[]> = { approve: [\"draft\"], ship: [\"approved\"], complete: [\"in_transit\"], cancel: [\"draft\", \"approved\", \"in_transit\"] };\n  if (!transitions[parsed.data.action]?.includes(transfer.status)) {\n    const body = { error: `Cannot ${parsed.data.action} a transfer in its current state`, code: \"INVENTORY_TRANSFER_INVALID_STATE\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  if (parsed.data.action !== \"complete\") {\n    const next = parsed.data.action === \"approve\" ? \"approved\" : parsed.data.action === \"ship\" ? \"in_transit\" : \"cancelled\";\n    const update = await sup.client.from(\"inventory_transfers\").update({ status: next, revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision).select(\"*\").single();\n    if (update.error || !update.data) {\n      const body = { error: \"Inventory transfer was changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    await insertStaffAuditLog(sup.client, { actorEmail: actor, action: `inventory.transfer.${parsed.data.action}`, resource: \"inventory_transfer\", resourceId: id, details: { organization_id: organization.id, status: next } });\n    const body = { data: update.data };\n    await completeAdminIdempotency(sup.client, claim.id, 200, body);\n    return correlatedJson(correlationId, body);\n  }\n\n  const processing = await sup.client.from(\"inventory_transfers\").update({ status: \"processing\", revision: transfer.revision + 1, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"revision\", transfer.revision).select(\"revision\").single();\n  if (processing.error || !processing.data) {\n    const body = { error: \"Inventory transfer was changed; refresh and retry\", code: \"INVENTORY_TRANSFER_CONFLICT\" };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const changes: InventoryStockChange[] = [];\n  for (const line of transfer.inventory_transfer_lines ?? []) {\n    const source = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, transfer.source_location_id);\n    const destination = await (await import(\"@/lib/medusa-catalog-inventory-stock\")).fetchVariantStockedQuantity(line.variant_id, transfer.destination_location_id);\n    if (source == null || destination == null || source < line.quantity) {\n      const body = { error: \"Unable to complete transfer because source inventory is unavailable or insufficient\", code: source != null && source < line.quantity ? \"INSUFFICIENT_INVENTORY\" : \"INVENTORY_READ_FAILED\" };\n      await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: body.code, failure_message: body.error, revision: transfer.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n      await completeAdminIdempotency(sup.client, claim.id, 409, body);\n      return correlatedJson(correlationId, body, { status: 409 });\n    }\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: transfer.source_location_id, quantityBefore: source, quantityAfter: source - line.quantity });\n    changes.push({ productId: line.product_id, variantId: line.variant_id, locationId: transfer.destination_location_id, quantityBefore: destination, quantityAfter: destination + line.quantity });\n  }\n  const applied = await applyInventoryStockChanges({ changes, client: sup.client, actorEmail: actor, correlationId, referenceType: \"inventory_transfer\", referenceId: id });\n  if (!applied.ok) {\n    await sup.client.from(\"inventory_transfers\").update({ status: \"failed\", failure_code: applied.code, failure_message: applied.message, revision: transfer.revision + 2, updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\");\n    const body = { error: applied.message, code: applied.code };\n    await completeAdminIdempotency(sup.client, claim.id, 409, body);\n    return correlatedJson(correlationId, body, { status: 409 });\n  }\n  const completed = await sup.client.from(\"inventory_transfers\").update({ status: \"completed\", revision: transfer.revision + 2, completed_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq(\"id\", id).eq(\"organization_id\", organization.id).eq(\"status\", \"processing\").select(\"*\").single();\n  if (completed.error || !completed.data) {\n    const body = { error: \"Inventory transfer completed but its lifecycle record could not be finalized\", code: \"INVENTORY_TRANSFER_FINALIZE_FAILED\" };\n    await completeAdminIdempotency(sup.client, claim.id, 503, body);\n    return correlatedJson(correlationId, body, { status: 503 });\n  }\n  await insertStaffAuditLog(sup.client, { actorEmail: actor, action: \"inventory.transfer.complete\", resource: \"inventory_transfer\", resourceId: id, details: { organization_id: organization.id } });\n  const body = { data: completed.data };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "inventory:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_inventory_transfers_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_inventory_transfers_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/invoices:
    get:
      operationId: get_admin_invoices
      summary: "Read Admin / Invoices"
      description: "Admin / Invoices. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Invoices"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { sendResendTransactionalEmail } from \"@universal-music-store/resend-mail\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n  parseAdminJson,\n} from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { fetchMedusaCustomerById } from \"@/lib/customers-bridge\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z\n  .object({\n    id: z.string().trim().min(1).max(80),\n    description: z.string().trim().min(1).max(240),\n    quantity: z.number().finite().int().min(1).max(10000),\n    unitPrice: z.number().finite().min(0).max(100000000),\n  })\n  .strict();\nconst detailsSchema = z\n  .object({\n    name: z.string().trim().min(1).max(160),\n    email: z.string().trim().email().max(320),\n    phone: z.string().trim().max(40),\n    website: z.string().trim().max(240),\n    addressLines: z.array(z.string().trim().max(160)).max(6),\n    taxId: z.string().trim().max(80),\n    issuerName: z.string().trim().max(160),\n  })\n  .strict();\nconst invoiceSchema = z\n  .object({\n    referenceNumber: z\n      .string()\n      .trim()\n      .regex(/^[A-Za-z0-9][A-Za-z0-9._-]{1,79}$/),\n    issuedDate: z.string().date(),\n    paymentDueDate: z.string().date(),\n    from: detailsSchema,\n    to: z\n      .object({\n        id: z.string().trim().min(1).max(80),\n        name: z.string().trim().min(1).max(160),\n        email: z.string().trim().email().max(320),\n        addressLines: z.array(z.string().trim().max(160)).max(6),\n        taxId: z.string().trim().max(80),\n      })\n      .strict(),\n    taxId: z.enum([\"gst\", \"vat\", \"service-tax\", \"none\"]),\n    discountType: z.enum([\"fixed\", \"percent\"]),\n    discountValue: z.number().finite().min(0).max(100000000),\n    items: z.array(lineSchema).min(1).max(100),\n  })\n  .strict()\n  .superRefine((value, ctx) => {\n    if (value.paymentDueDate < value.issuedDate) {\n      ctx.addIssue({\n        code: z.ZodIssueCode.custom,\n        path: [\"paymentDueDate\"],\n        message: \"Payment due date cannot precede issued date\",\n      });\n    }\n    if (value.discountType === \"percent\" && value.discountValue > 100) {\n      ctx.addIssue({\n        code: z.ZodIssueCode.custom,\n        path: [\"discountValue\"],\n        message: \"Percentage discount cannot exceed 100\",\n      });\n    }\n  });\nconst requestSchema = z\n  .object({\n    invoice: invoiceSchema,\n    mode: z.enum([\"draft\", \"send\"]).default(\"draft\"),\n  })\n  .strict();\n\nfunction totalOf(invoice: z.infer<typeof invoiceSchema>) {\n  const subtotal = invoice.items.reduce(\n    (sum, item) => sum + item.quantity * item.unitPrice,\n    0,\n  );\n  const discount =\n    invoice.discountType === \"percent\"\n      ? (subtotal * invoice.discountValue) / 100\n      : invoice.discountValue;\n  const taxable = Math.max(0, subtotal - Math.min(discount, subtotal));\n  const rate =\n    invoice.taxId === \"gst\"\n      ? 18\n      : invoice.taxId === \"vat\"\n        ? 12\n        : invoice.taxId === \"service-tax\"\n          ? 10\n          : 0;\n  return Number((taxable + (taxable * rate) / 100).toFixed(2));\n}\n\nfunction escapeHtml(value: string) {\n  return value.replace(\n    /[&<>\"']/g,\n    (char) =>\n      ({ \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\", \"'\": \"&#39;\" })[\n        char\n      ] ?? char,\n  );\n}\nfunction invoiceHtml(invoice: z.infer<typeof invoiceSchema>, total: number) {\n  const rows = invoice.items\n    .map(\n      (item) =>\n        `<tr><td>${escapeHtml(item.description)}</td><td>${item.quantity}</td><td>PHP ${item.unitPrice.toFixed(2)}</td><td>PHP ${(item.quantity * item.unitPrice).toFixed(2)}</td></tr>`,\n    )\n    .join(\"\");\n  return `<h1>Invoice ${escapeHtml(invoice.referenceNumber)}</h1><p>From ${escapeHtml(invoice.from.name)} to ${escapeHtml(invoice.to.name)}</p><p>Issued ${invoice.issuedDate}, due ${invoice.paymentDueDate}</p><table><thead><tr><th>Description</th><th>Qty</th><th>Unit price</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><p><strong>Total: PHP ${total.toFixed(2)}</strong></p>`;\n}\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"receipts:read\");\n  if (!staff.ok) return staff.response;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const { data, error } = await sup.client\n    .from(\"admin_invoices\")\n    .select(\n      \"id,reference_number,status,currency,total,recipient_email,sent_at,created_by,created_at,updated_at\",\n    )\n    .eq(\"organization_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(50);\n  if (error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to load invoices\" },\n      { status: 502 },\n    );\n  return correlatedJson(correlationId, { data: data ?? [] });\n}\n\nexport async function POST(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"receipts:send\");\n  if (!staff.ok) return staff.response;\n  const parsed = await parseAdminJson(req, requestSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const { invoice, mode } = parsed.data;\n  const total = totalOf(invoice);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const customer = await fetchMedusaCustomerById(invoice.to.id);\n  if (!customer?.email) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice recipient is unavailable\" },\n      { status: 404 },\n    );\n  }\n  if (customer.email.trim().toLowerCase() !== invoice.to.email.trim().toLowerCase()) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice recipient changed; reload the customer\" },\n      { status: 409 },\n    );\n  }\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey)\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency-Key is required for invoice mutations\" },\n      { status: 400 },\n    );\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email ?? \"local-admin@localhost\"}`,\n    actionKey: \"admin.invoice.create\",\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind !== \"claimed\")\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"Invoice mutation is already in progress or conflicts with a previous request\",\n      },\n      { status: 409 },\n    );\n  const idempotencyId = claim.id;\n  const canonicalName =\n    [customer.first_name, customer.last_name].filter(Boolean).join(\" \").trim() ||\n    customer.email;\n  const safeInvoice = {\n    ...invoice,\n    to: {\n      ...invoice.to,\n      id: customer.id,\n      name: canonicalName,\n      email: customer.email,\n    },\n  };\n  const { data, error } = await sup.client\n    .from(\"admin_invoices\")\n    .insert({\n      organization_id: organization.id,\n      reference_number: invoice.referenceNumber,\n      status: \"draft\",\n      currency: \"PHP\",\n      total,\n      recipient_email: customer.email,\n      payload: safeInvoice,\n      created_by: staff.session.user?.email ?? \"local-admin@localhost\",\n    })\n    .select(\n      \"id,reference_number,status,currency,total,recipient_email,sent_at,created_at\",\n    )\n    .single();\n  if (error || !data) {\n    const response = correlatedJson(\n      correlationId,\n      {\n        error:\n          error?.code === \"23505\"\n            ? \"Reference number already exists\"\n            : \"Unable to save invoice\",\n      },\n      { status: error?.code === \"23505\" ? 409 : 502 },\n    );\n    await completeAdminIdempotency(sup.client, idempotencyId, response.status, {\n      error:\n        error?.code === \"23505\"\n          ? \"Reference number already exists\"\n          : \"Unable to save invoice\",\n    });\n    return response;\n  }\n  let result = data;\n  if (mode === \"send\") {\n    const resendKey = process.env.RESEND_API_KEY?.trim();\n    if (!resendKey) {\n      const response = correlatedJson(\n        correlationId,\n        {\n          error: \"Invoice saved as draft; RESEND_API_KEY is not configured\",\n          data,\n        },\n        { status: 503 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        {\n          error: \"Invoice saved as draft; RESEND_API_KEY is not configured\",\n          data,\n        },\n      );\n      return response;\n    }\n    const sent = await sendResendTransactionalEmail({\n      apiKey: resendKey,\n      from:\n        process.env.RESEND_FROM_EMAIL?.trim() ||\n        process.env.RESEND_FROM?.trim() ||\n        \"noreply@universal-music-store.com\",\n      to: invoice.to.email,\n      subject: `Invoice ${invoice.referenceNumber}`,\n      html: invoiceHtml(safeInvoice, total),\n      tags: [{ name: \"type\", value: \"admin_invoice\" }],\n    });\n    if (!sent.ok) {\n      const response = correlatedJson(\n        correlationId,\n        { error: \"Invoice saved as draft; email delivery failed\", data },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        { error: \"Invoice saved as draft; email delivery failed\", data },\n      );\n      return response;\n    }\n    const updated = await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        status: \"sent\",\n        sent_at: new Date().toISOString(),\n        updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", data.id)\n      .select(\n        \"id,reference_number,status,currency,total,recipient_email,sent_at,created_at\",\n      )\n      .single();\n    if (updated.error || !updated.data) {\n      const response = correlatedJson(\n        correlationId,\n        {\n          error: \"Invoice email sent but invoice status could not be recorded\",\n        },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        {\n          error: \"Invoice email sent but invoice status could not be recorded\",\n        },\n      );\n      return response;\n    }\n    result = updated.data;\n  }\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staff.session.user?.email ?? \"local-admin@localhost\",\n    action: mode === \"send\" ? \"invoice.send\" : \"invoice.create\",\n    resource: \"admin_invoice\",\n    resourceId: String(data.id),\n    details: {\n      reference_number: invoice.referenceNumber,\n      total,\n      currency: \"PHP\",\n      status: result.status,\n    },\n  });\n  const response = correlatedJson(\n    correlationId,\n    { data: result },\n    { status: 201 },\n  );\n  await completeAdminIdempotency(sup.client, idempotencyId, response.status, {\n    data: result,\n  });\n  return response;\n}\n"
      x-permission: "receipts:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_invoicesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_invoices
      summary: "Create or execute Admin / Invoices"
      description: "Admin / Invoices. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Invoices"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { sendResendTransactionalEmail } from \"@universal-music-store/resend-mail\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n  parseAdminJson,\n} from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { fetchMedusaCustomerById } from \"@/lib/customers-bridge\";\n\nexport const dynamic = \"force-dynamic\";\nconst lineSchema = z\n  .object({\n    id: z.string().trim().min(1).max(80),\n    description: z.string().trim().min(1).max(240),\n    quantity: z.number().finite().int().min(1).max(10000),\n    unitPrice: z.number().finite().min(0).max(100000000),\n  })\n  .strict();\nconst detailsSchema = z\n  .object({\n    name: z.string().trim().min(1).max(160),\n    email: z.string().trim().email().max(320),\n    phone: z.string().trim().max(40),\n    website: z.string().trim().max(240),\n    addressLines: z.array(z.string().trim().max(160)).max(6),\n    taxId: z.string().trim().max(80),\n    issuerName: z.string().trim().max(160),\n  })\n  .strict();\nconst invoiceSchema = z\n  .object({\n    referenceNumber: z\n      .string()\n      .trim()\n      .regex(/^[A-Za-z0-9][A-Za-z0-9._-]{1,79}$/),\n    issuedDate: z.string().date(),\n    paymentDueDate: z.string().date(),\n    from: detailsSchema,\n    to: z\n      .object({\n        id: z.string().trim().min(1).max(80),\n        name: z.string().trim().min(1).max(160),\n        email: z.string().trim().email().max(320),\n        addressLines: z.array(z.string().trim().max(160)).max(6),\n        taxId: z.string().trim().max(80),\n      })\n      .strict(),\n    taxId: z.enum([\"gst\", \"vat\", \"service-tax\", \"none\"]),\n    discountType: z.enum([\"fixed\", \"percent\"]),\n    discountValue: z.number().finite().min(0).max(100000000),\n    items: z.array(lineSchema).min(1).max(100),\n  })\n  .strict()\n  .superRefine((value, ctx) => {\n    if (value.paymentDueDate < value.issuedDate) {\n      ctx.addIssue({\n        code: z.ZodIssueCode.custom,\n        path: [\"paymentDueDate\"],\n        message: \"Payment due date cannot precede issued date\",\n      });\n    }\n    if (value.discountType === \"percent\" && value.discountValue > 100) {\n      ctx.addIssue({\n        code: z.ZodIssueCode.custom,\n        path: [\"discountValue\"],\n        message: \"Percentage discount cannot exceed 100\",\n      });\n    }\n  });\nconst requestSchema = z\n  .object({\n    invoice: invoiceSchema,\n    mode: z.enum([\"draft\", \"send\"]).default(\"draft\"),\n  })\n  .strict();\n\nfunction totalOf(invoice: z.infer<typeof invoiceSchema>) {\n  const subtotal = invoice.items.reduce(\n    (sum, item) => sum + item.quantity * item.unitPrice,\n    0,\n  );\n  const discount =\n    invoice.discountType === \"percent\"\n      ? (subtotal * invoice.discountValue) / 100\n      : invoice.discountValue;\n  const taxable = Math.max(0, subtotal - Math.min(discount, subtotal));\n  const rate =\n    invoice.taxId === \"gst\"\n      ? 18\n      : invoice.taxId === \"vat\"\n        ? 12\n        : invoice.taxId === \"service-tax\"\n          ? 10\n          : 0;\n  return Number((taxable + (taxable * rate) / 100).toFixed(2));\n}\n\nfunction escapeHtml(value: string) {\n  return value.replace(\n    /[&<>\"']/g,\n    (char) =>\n      ({ \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\", \"'\": \"&#39;\" })[\n        char\n      ] ?? char,\n  );\n}\nfunction invoiceHtml(invoice: z.infer<typeof invoiceSchema>, total: number) {\n  const rows = invoice.items\n    .map(\n      (item) =>\n        `<tr><td>${escapeHtml(item.description)}</td><td>${item.quantity}</td><td>PHP ${item.unitPrice.toFixed(2)}</td><td>PHP ${(item.quantity * item.unitPrice).toFixed(2)}</td></tr>`,\n    )\n    .join(\"\");\n  return `<h1>Invoice ${escapeHtml(invoice.referenceNumber)}</h1><p>From ${escapeHtml(invoice.from.name)} to ${escapeHtml(invoice.to.name)}</p><p>Issued ${invoice.issuedDate}, due ${invoice.paymentDueDate}</p><table><thead><tr><th>Description</th><th>Qty</th><th>Unit price</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><p><strong>Total: PHP ${total.toFixed(2)}</strong></p>`;\n}\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"receipts:read\");\n  if (!staff.ok) return staff.response;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const { data, error } = await sup.client\n    .from(\"admin_invoices\")\n    .select(\n      \"id,reference_number,status,currency,total,recipient_email,sent_at,created_by,created_at,updated_at\",\n    )\n    .eq(\"organization_id\", organization.id)\n    .order(\"created_at\", { ascending: false })\n    .limit(50);\n  if (error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to load invoices\" },\n      { status: 502 },\n    );\n  return correlatedJson(correlationId, { data: data ?? [] });\n}\n\nexport async function POST(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"receipts:send\");\n  if (!staff.ok) return staff.response;\n  const parsed = await parseAdminJson(req, requestSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const { invoice, mode } = parsed.data;\n  const total = totalOf(invoice);\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const customer = await fetchMedusaCustomerById(invoice.to.id);\n  if (!customer?.email) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice recipient is unavailable\" },\n      { status: 404 },\n    );\n  }\n  if (customer.email.trim().toLowerCase() !== invoice.to.email.trim().toLowerCase()) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice recipient changed; reload the customer\" },\n      { status: 409 },\n    );\n  }\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey)\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency-Key is required for invoice mutations\" },\n      { status: 400 },\n    );\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email ?? \"local-admin@localhost\"}`,\n    actionKey: \"admin.invoice.create\",\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind !== \"claimed\")\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"Invoice mutation is already in progress or conflicts with a previous request\",\n      },\n      { status: 409 },\n    );\n  const idempotencyId = claim.id;\n  const canonicalName =\n    [customer.first_name, customer.last_name].filter(Boolean).join(\" \").trim() ||\n    customer.email;\n  const safeInvoice = {\n    ...invoice,\n    to: {\n      ...invoice.to,\n      id: customer.id,\n      name: canonicalName,\n      email: customer.email,\n    },\n  };\n  const { data, error } = await sup.client\n    .from(\"admin_invoices\")\n    .insert({\n      organization_id: organization.id,\n      reference_number: invoice.referenceNumber,\n      status: \"draft\",\n      currency: \"PHP\",\n      total,\n      recipient_email: customer.email,\n      payload: safeInvoice,\n      created_by: staff.session.user?.email ?? \"local-admin@localhost\",\n    })\n    .select(\n      \"id,reference_number,status,currency,total,recipient_email,sent_at,created_at\",\n    )\n    .single();\n  if (error || !data) {\n    const response = correlatedJson(\n      correlationId,\n      {\n        error:\n          error?.code === \"23505\"\n            ? \"Reference number already exists\"\n            : \"Unable to save invoice\",\n      },\n      { status: error?.code === \"23505\" ? 409 : 502 },\n    );\n    await completeAdminIdempotency(sup.client, idempotencyId, response.status, {\n      error:\n        error?.code === \"23505\"\n          ? \"Reference number already exists\"\n          : \"Unable to save invoice\",\n    });\n    return response;\n  }\n  let result = data;\n  if (mode === \"send\") {\n    const resendKey = process.env.RESEND_API_KEY?.trim();\n    if (!resendKey) {\n      const response = correlatedJson(\n        correlationId,\n        {\n          error: \"Invoice saved as draft; RESEND_API_KEY is not configured\",\n          data,\n        },\n        { status: 503 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        {\n          error: \"Invoice saved as draft; RESEND_API_KEY is not configured\",\n          data,\n        },\n      );\n      return response;\n    }\n    const sent = await sendResendTransactionalEmail({\n      apiKey: resendKey,\n      from:\n        process.env.RESEND_FROM_EMAIL?.trim() ||\n        process.env.RESEND_FROM?.trim() ||\n        \"noreply@universal-music-store.com\",\n      to: invoice.to.email,\n      subject: `Invoice ${invoice.referenceNumber}`,\n      html: invoiceHtml(safeInvoice, total),\n      tags: [{ name: \"type\", value: \"admin_invoice\" }],\n    });\n    if (!sent.ok) {\n      const response = correlatedJson(\n        correlationId,\n        { error: \"Invoice saved as draft; email delivery failed\", data },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        { error: \"Invoice saved as draft; email delivery failed\", data },\n      );\n      return response;\n    }\n    const updated = await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        status: \"sent\",\n        sent_at: new Date().toISOString(),\n        updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", data.id)\n      .select(\n        \"id,reference_number,status,currency,total,recipient_email,sent_at,created_at\",\n      )\n      .single();\n    if (updated.error || !updated.data) {\n      const response = correlatedJson(\n        correlationId,\n        {\n          error: \"Invoice email sent but invoice status could not be recorded\",\n        },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(\n        sup.client,\n        idempotencyId,\n        response.status,\n        {\n          error: \"Invoice email sent but invoice status could not be recorded\",\n        },\n      );\n      return response;\n    }\n    result = updated.data;\n  }\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staff.session.user?.email ?? \"local-admin@localhost\",\n    action: mode === \"send\" ? \"invoice.send\" : \"invoice.create\",\n    resource: \"admin_invoice\",\n    resourceId: String(data.id),\n    details: {\n      reference_number: invoice.referenceNumber,\n      total,\n      currency: \"PHP\",\n      status: result.status,\n    },\n  });\n  const response = correlatedJson(\n    correlationId,\n    { data: result },\n    { status: 201 },\n  );\n  await completeAdminIdempotency(sup.client, idempotencyId, response.status, {\n    data: result,\n  });\n  return response;\n}\n"
      x-permission: "receipts:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_invoicesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_invoicesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/invoices/{id}/lifecycle:
    post:
      operationId: post_admin_invoices_id_lifecycle
      summary: "Execute Admin / Invoices / Id / Lifecycle"
      description: "Tenant-scoped invoice lifecycle transition."
      tags: ["Admin / Invoices"]
      x-source: "apps/admin/src/app/api/admin/invoices/[id]/lifecycle/route.ts"
      x-permission: "receipts:send"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [action]
              properties:
                action:
                  type: string
                  enum: [retry, void, refund]
      responses:
        "200":
          description: Operation completed successfully.
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "404":
          description: Invoice not found.
        "409":
          description: Invalid lifecycle transition or replay conflict.
        "500":
          description: Internal operation failure.
  /admin/invoices/{id}/provider:
    post:
      operationId: post_admin_invoices_id_provider
      summary: "Create or execute Admin / Invoices / Id / Provider"
      description: "Admin / Invoices / Id / Provider. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Invoices"]
      x-source: "import { z } from \"zod\";\nimport { upsertPaymentProviderArtifact } from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n  parseAdminJson,\n} from \"@/lib/admin-api-security\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nconst schema = z\n  .object({\n    provider: z.enum([\"stripe\", \"paypal\", \"xendit\"]),\n    action: z.enum([\"create\", \"finalize\", \"send\", \"pay\", \"remind\", \"cancel\"]),\n  })\n  .strict();\n\ntype InvoicePayload = {\n  referenceNumber?: string;\n  paymentDueDate?: string;\n  to?: { name?: string; email?: string };\n  items?: Array<{\n    description?: string;\n    quantity?: number;\n    unitPrice?: number;\n  }>;\n};\n\nfunction money(value: unknown) {\n  const amount =\n    typeof value === \"number\" && Number.isFinite(value) ? value : 0;\n  return Math.round(amount * 100) / 100;\n}\n\nexport async function POST(\n  req: Request,\n  { params }: { params: Promise<{ id: string }> },\n) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"receipts:send\");\n  if (!staff.ok) return staff.response;\n  const parsed = await parseAdminJson(req, schema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey)\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency-Key is required\" },\n      { status: 400 },\n    );\n  const { id } = await params;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const invoiceResult = await sup.client\n    .from(\"admin_invoices\")\n    .select(\n      \"id,reference_number,status,total,currency,payload,provider,provider_external_id,provider_retry_count\",\n    )\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .maybeSingle();\n  if (invoiceResult.error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to load invoice\" },\n      { status: 502 },\n    );\n  if (!invoiceResult.data)\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice not found\" },\n      { status: 404 },\n    );\n  const invoice = invoiceResult.data;\n  if (parsed.data.provider === \"xendit\")\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"Xendit does not provide a native invoice resource; use a payment session\",\n        code: \"PROVIDER_CAPABILITY_UNAVAILABLE\",\n      },\n      { status: 409 },\n    );\n  if (\n    parsed.data.action !== \"create\" &&\n    !invoice.provider_external_id\n  )\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Create the provider invoice before performing this action\",\n        code: \"PROVIDER_INVOICE_NOT_CREATED\",\n      },\n      { status: 409 },\n    );\n  if (\n    invoice.provider_external_id &&\n    invoice.provider !== parsed.data.provider\n  )\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Invoice is connected to a different payment provider\",\n        code: \"PROVIDER_INVOICE_MISMATCH\",\n      },\n      { status: 409 },\n    );\n  if (\n    parsed.data.provider === \"paypal\" &&\n    [\"finalize\", \"pay\"].includes(parsed.data.action)\n  )\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"PayPal invoices use send, remind, or cancel actions\",\n        code: \"PROVIDER_CAPABILITY_UNAVAILABLE\",\n      },\n      { status: 409 },\n    );\n  if (\n    parsed.data.action === \"create\" &&\n    invoice.provider === parsed.data.provider &&\n    invoice.provider_external_id\n  ) {\n    return correlatedJson(correlationId, {\n      data: {\n        id: invoice.id,\n        reference_number: invoice.reference_number,\n        status: invoice.status,\n        provider: invoice.provider,\n        provider_external_id: invoice.provider_external_id,\n      },\n      replay: true,\n    });\n  }\n  const payload = (invoice.payload ?? {}) as InvoicePayload;\n  const connection = await sup.client\n    .from(\"payment_nango_connections\")\n    .select(\"nango_connection_id,provider_config_key\")\n    .eq(\"organization_id\", organization.id)\n    .eq(\"provider\", parsed.data.provider)\n    .eq(\"active\", true)\n    .order(\"updated_at\", { ascending: false })\n    .limit(1)\n    .maybeSingle();\n  if (connection.error || !connection.data?.nango_connection_id)\n    return correlatedJson(\n      correlationId,\n      {\n        error: `${parsed.data.provider} is not connected for this organization`,\n        code: \"PAYMENT_CONNECTION_REQUIRED\",\n      },\n      { status: 409 },\n    );\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken)\n    return correlatedJson(\n      correlationId,\n      { error: \"Payment operations are not configured\" },\n      { status: 503 },\n    );\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email?.trim().toLowerCase() ?? \"unknown\"}`,\n    actionKey: `invoice.provider:${id}:${parsed.data.provider}:${parsed.data.action}`,\n    idempotencyKey,\n    requestHash: getRequestHash({\n      invoiceId: id,\n      provider: parsed.data.provider,\n      action: parsed.data.action,\n      providerExternalId: invoice.provider_external_id,\n      total: invoice.total,\n    }),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\")\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency key is already in use\" },\n      { status: 409 },\n    );\n  if (claim.kind !== \"claimed\")\n    return correlatedJson(\n      correlationId,\n      { error: \"Idempotency service unavailable\" },\n      { status: 503 },\n    );\n  const finish = async (status: number, body: Record<string, unknown>) => {\n    await completeAdminIdempotency(sup.client, claim.id, status, body);\n    return correlatedJson(correlationId, body, { status });\n  };\n  await sup.client\n    .from(\"admin_invoices\")\n    .update({\n      provider: parsed.data.provider,\n      provider_retry_state: \"processing\",\n      provider_updated_at: new Date().toISOString(),\n    })\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id);\n  const total = money(invoice.total);\n  const items = Array.isArray(payload.items) ? payload.items : [];\n  const responsePayload =\n    parsed.data.provider === \"stripe\"\n      ? {\n          operation: \"invoice\",\n          customer_email: payload.to?.email,\n          invoice_id:\n            parsed.data.action === \"create\"\n              ? undefined\n              : (invoice.provider_external_id ?? undefined),\n          currency: invoice.currency,\n          items:\n            parsed.data.action === \"create\"\n              ? [\n                  {\n                    amount_minor: Math.round(total * 100),\n                    description: `Invoice ${invoiceResult.data.reference_number}`,\n                  },\n                ]\n              : undefined,\n          action:\n            parsed.data.action === \"create\" ? \"draft\" : parsed.data.action,\n          idempotency_key: idempotencyKey,\n        }\n      : {\n          operation: \"invoice\",\n          action:\n            parsed.data.action === \"create\" ? \"create\" : parsed.data.action,\n          invoice_id:\n            parsed.data.action === \"create\"\n              ? undefined\n              : (invoice.provider_external_id ?? undefined),\n          payload:\n            parsed.data.action === \"create\"\n              ? {\n                  detail: {\n                    invoice_number: invoice.reference_number,\n                    invoice_date: new Date().toISOString().slice(0, 10),\n                    currency_code: invoice.currency,\n                    note: `UVS invoice ${invoice.reference_number}`,\n                  },\n                  primary_recipients: [\n                    {\n                      billing_info: {\n                        email_address: payload.to?.email,\n                        name: {\n                          full_name: payload.to?.name ?? payload.to?.email,\n                        },\n                      },\n                    },\n                  ],\n                  items: items.map((item) => ({\n                    name: item.description,\n                    quantity: String(item.quantity ?? 1),\n                    unit_amount: {\n                      currency_code: invoice.currency,\n                      value: money(\n                        (item.unitPrice ?? 0) * (item.quantity ?? 1),\n                      ).toFixed(2),\n                    },\n                  })),\n                  configuration: {\n                    partial_payment: { allow_partial_payment: false },\n                  },\n                }\n              : {},\n          idempotency_key: idempotencyKey,\n        };\n  let providerResponse: Response;\n  try {\n    providerResponse = await medusaAdminFetch(\n      `/admin/payment-provider/${parsed.data.provider}`,\n      {\n        method: \"POST\",\n        headers: {\n          \"x-uvs-internal-token\": internalToken,\n          \"x-nango-connection-id\": connection.data.nango_connection_id,\n          ...(connection.data.provider_config_key\n            ? {\n                \"x-nango-provider-config-key\":\n                  connection.data.provider_config_key,\n              }\n            : {}),\n        },\n        body: JSON.stringify(responsePayload),\n      },\n    );\n  } catch {\n    await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        provider_retry_count: Number(invoice.provider_retry_count ?? 0) + 1,\n        provider_retry_state: \"retryable\",\n        provider_last_error: \"Provider operation unavailable\",\n        provider_updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id);\n    return finish(502, {\n      error: \"Provider invoice operation unavailable\",\n      code: \"PROVIDER_OPERATION_FAILED\",\n    });\n  }\n  const body = (await providerResponse\n    .json()\n    .catch(() => ({ error: \"Provider operation failed\" }))) as {\n    data?: { id?: string; status?: string; hosted_invoice_url?: string };\n    error?: string;\n  };\n  if (!providerResponse.ok) {\n    await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        provider: parsed.data.provider,\n        provider_last_error: body.error ?? \"Provider operation failed\",\n        provider_retry_count: Number(invoice.provider_retry_count ?? 0) + 1,\n        provider_retry_state:\n          providerResponse.status >= 500 ? \"retryable\" : \"permanent_failure\",\n        provider_updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id);\n    return finish(\n      providerResponse.status >= 500 ? 502 : providerResponse.status,\n      {\n        error: body.error ?? \"Provider invoice operation failed\",\n        code: \"PROVIDER_OPERATION_FAILED\",\n      },\n    );\n  }\n  const externalId = body.data?.id ?? invoice.provider_external_id;\n  if (!externalId) {\n    await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        provider_retry_count: Number(invoice.provider_retry_count ?? 0) + 1,\n        provider_retry_state: \"retryable\",\n        provider_last_error: \"Provider did not return an invoice id\",\n        provider_updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id);\n    return finish(502, {\n      error: \"Provider did not return an invoice id\",\n      code: \"PROVIDER_INVALID_RESPONSE\",\n    });\n  }\n  const artifactOk = await upsertPaymentProviderArtifact(sup.client, {\n    organization_id: organization.id,\n    merchant_identity: connection.data.nango_connection_id,\n    provider: parsed.data.provider,\n    artifact_type: \"invoice\",\n    external_id: externalId,\n    status: body.data?.status ?? parsed.data.action,\n    amount_minor: Math.round(total * 100),\n    currency: invoice.currency,\n    idempotency_key: idempotencyKey,\n    metadata: {\n      admin_invoice_id: id,\n      reference_number: invoice.reference_number,\n      hosted_invoice_url: body.data?.hosted_invoice_url ?? null,\n    },\n  });\n  if (!artifactOk) {\n    await sup.client\n      .from(\"admin_invoices\")\n      .update({\n        provider_retry_count: Number(invoice.provider_retry_count ?? 0) + 1,\n        provider_retry_state: \"retryable\",\n        provider_last_error: \"Provider invoice artifact could not be recorded\",\n        provider_updated_at: new Date().toISOString(),\n      })\n      .eq(\"id\", id)\n      .eq(\"organization_id\", organization.id);\n    return finish(502, {\n      error:\n        \"Provider invoice succeeded but its local artifact could not be recorded\",\n      code: \"LOCAL_ARTIFACT_WRITE_FAILED\",\n    });\n  }\n  const artifact = artifactOk\n    ? await sup.client\n        .from(\"payment_provider_artifacts\")\n        .select(\"id\")\n        .eq(\"organization_id\", organization.id)\n        .eq(\"provider\", parsed.data.provider)\n        .eq(\"artifact_type\", \"invoice\")\n        .eq(\"external_id\", externalId)\n        .maybeSingle()\n    : { data: null };\n  const updated = await sup.client\n    .from(\"admin_invoices\")\n    .update({\n      provider: parsed.data.provider,\n      provider_external_id: externalId,\n      provider_artifact_id: artifact.data?.id ?? null,\n      provider_status: body.data?.status ?? parsed.data.action,\n      provider_last_error: null,\n      provider_retry_count: 0,\n      provider_retry_state: \"idle\",\n      provider_updated_at: new Date().toISOString(),\n    })\n    .eq(\"id\", id)\n    .eq(\"organization_id\", organization.id)\n    .select(\n      \"id,reference_number,status,provider,provider_external_id,provider_artifact_id,provider_status,provider_retry_state,provider_updated_at\",\n    )\n    .single();\n  if (updated.error || !updated.data)\n    return finish(502, {\n      error: \"Provider invoice succeeded but local state could not be recorded\",\n    });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staff.session.user?.email ?? \"local-admin@localhost\",\n    action: `invoice.provider.${parsed.data.action}`,\n    resource: \"admin_invoice\",\n    resourceId: id,\n    details: {\n      organization_id: organization.id,\n      provider: parsed.data.provider,\n      external_id: externalId,\n      status: updated.data.provider_status,\n    },\n  });\n  return finish(200, { data: updated.data, provider: body.data ?? null });\n}\n"
      x-permission: "receipts:send"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_invoices_id_providerRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_invoices_id_providerResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/loyalty:
    get:
      operationId: get_admin_loyalty
      summary: "Read Admin / Loyalty"
      description: "Admin / Loyalty. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listLoyaltyAccounts,\n  getOrCreateLoyaltyAccount,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const tier = req.nextUrl.searchParams.get(\"tier\") as \"standard\" | \"silver\" | \"gold\" | \"platinum\" | null;\n  const data = await listLoyaltyAccounts(sb, { tier: tier ?? undefined });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { email, medusa_customer_id } = await req.json();\n  if (!email) {\n    return correlatedJson(cid, { error: \"email is required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const account = await getOrCreateLoyaltyAccount(sb, email, medusa_customer_id);\n  return correlatedJson(cid, { data: account }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/loyalty:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_loyaltyResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_loyalty
      summary: "Create or execute Admin / Loyalty"
      description: "Admin / Loyalty. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listLoyaltyAccounts,\n  getOrCreateLoyaltyAccount,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const tier = req.nextUrl.searchParams.get(\"tier\") as \"standard\" | \"silver\" | \"gold\" | \"platinum\" | null;\n  const data = await listLoyaltyAccounts(sb, { tier: tier ?? undefined });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { email, medusa_customer_id } = await req.json();\n  if (!email) {\n    return correlatedJson(cid, { error: \"email is required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const account = await getOrCreateLoyaltyAccount(sb, email, medusa_customer_id);\n  return correlatedJson(cid, { data: account }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/loyalty:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_loyaltyRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_loyaltyResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/loyalty/lookup:
    get:
      operationId: get_admin_loyalty_lookup
      summary: "Read Admin / Loyalty / Lookup"
      description: "Admin / Loyalty / Lookup. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  lookupByQr,\n  lookupByPhone,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const qr = req.nextUrl.searchParams.get(\"qr\");\n  const phone = req.nextUrl.searchParams.get(\"phone\");\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n\n  if (qr) {\n    const account = await lookupByQr(sb, qr);\n    if (!account) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n    return correlatedJson(cid, { data: account });\n  }\n  if (phone) {\n    const account = await lookupByPhone(sb, phone);\n    if (!account) return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n    return correlatedJson(cid, { data: account });\n  }\n  return correlatedJson(cid, { error: \"qr or phone query param required\" }, { status: 400 });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_loyalty_lookupResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/loyalty/points:
    post:
      operationId: post_admin_loyalty_points
      summary: "Create or execute Admin / Loyalty / Points"
      description: "Admin / Loyalty / Points. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  addPoints,\n  redeemPoints,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { account_id, points, reason, order_id, action } = await req.json();\n  if (!account_id || points == null || !reason) {\n    return correlatedJson(cid, { error: \"account_id, points, and reason are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  if (action === \"redeem\") {\n    const account = await redeemPoints(sb, account_id, Math.abs(points), reason);\n    return correlatedJson(cid, { data: account });\n  }\n  const account = await addPoints(sb, account_id, points, reason, order_id);\n  return correlatedJson(cid, { data: account });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/loyalty/points:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_loyalty_pointsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_loyalty_pointsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/loyalty/rewards:
    get:
      operationId: get_admin_loyalty_rewards
      summary: "Read Admin / Loyalty / Rewards"
      description: "Admin / Loyalty / Rewards. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listRewards,\n  createReward,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const data = await listRewards(sb, { activeOnly: true });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = await req.json();\n  if (!body.name || body.points_cost == null) {\n    return correlatedJson(cid, { error: \"name and points_cost are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const reward = await createReward(sb, body);\n  return correlatedJson(cid, { data: reward }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/loyalty/rewards:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_loyalty_rewardsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_loyalty_rewards
      summary: "Create or execute Admin / Loyalty / Rewards"
      description: "Admin / Loyalty / Rewards. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Loyalty"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listRewards,\n  createReward,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const data = await listRewards(sb, { activeOnly: true });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"loyalty:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = await req.json();\n  if (!body.name || body.points_cost == null) {\n    return correlatedJson(cid, { error: \"name and points_cost are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const reward = await createReward(sb, body);\n  return correlatedJson(cid, { data: reward }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/loyalty/rewards:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_loyalty_rewardsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_loyalty_rewardsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/medusa/payment-providers:
    get:
      operationId: get_admin_medusa_payment_providers
      summary: "Read Admin / Medusa / Payment Providers"
      description: "Admin / Medusa / Payment Providers. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Medusa"]
      x-source: "import { fetchMedusaPaymentProvidersFromRegions } from \"@/lib/payment-providers-bridge\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  let providers;\n  try {\n    providers = await fetchMedusaPaymentProvidersFromRegions();\n  } catch {\n    return correlatedJson(correlationId, { error: \"Unable to load payment providers\" }, { status: 502 });\n  }\n\n  logAdminApiEvent({\n    route: \"GET /api/admin/medusa/payment-providers\",\n    correlationId,\n    phase: \"ok\",\n    detail: { count: providers.length },\n  });\n\n  return correlatedJson(correlationId, {\n    providers,\n    installmentNote:\n      \"Installment and BNPL depend on your payment provider module (for example Stripe, PayMongo) and region configuration in Admin.\",\n  });\n}\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_medusa_payment_providersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/offline-queue:
    get:
      operationId: get_admin_offline_queue
      summary: "Read Admin / Offline Queue"
      description: "Admin / Offline Queue. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Offline Queue"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listPendingQueue,\n  enqueueOfflineSale,\n  markSynced,\n  markFailed,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const deviceName = req.nextUrl.searchParams.get(\"device\") ?? undefined;\n  const data = await listPendingQueue(sb, { deviceName });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = await req.json();\n  if (!body.device_name || !body.payload) {\n    return correlatedJson(cid, { error: \"device_name and payload are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const item = await enqueueOfflineSale(sb, body);\n  return correlatedJson(cid, { data: item }, { status: 201 });\n}\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id, action, error_message } = await req.json();\n  if (!id || !action) {\n    return correlatedJson(cid, { error: \"id and action required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  if (action === \"synced\") {\n    await markSynced(sb, id);\n  } else if (action === \"failed\") {\n    await markFailed(sb, id, error_message ?? \"Unknown error\");\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/offline-queue:POST\", post);\nexport const PATCH = withAdminMutationIdempotency(\"/admin/offline-queue:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_offline_queueResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    patch:
      operationId: patch_admin_offline_queue
      summary: "Update Admin / Offline Queue"
      description: "Admin / Offline Queue. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Offline Queue"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listPendingQueue,\n  enqueueOfflineSale,\n  markSynced,\n  markFailed,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const deviceName = req.nextUrl.searchParams.get(\"device\") ?? undefined;\n  const data = await listPendingQueue(sb, { deviceName });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = await req.json();\n  if (!body.device_name || !body.payload) {\n    return correlatedJson(cid, { error: \"device_name and payload are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const item = await enqueueOfflineSale(sb, body);\n  return correlatedJson(cid, { data: item }, { status: 201 });\n}\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id, action, error_message } = await req.json();\n  if (!id || !action) {\n    return correlatedJson(cid, { error: \"id and action required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  if (action === \"synced\") {\n    await markSynced(sb, id);\n  } else if (action === \"failed\") {\n    await markFailed(sb, id, error_message ?? \"Unknown error\");\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/offline-queue:POST\", post);\nexport const PATCH = withAdminMutationIdempotency(\"/admin/offline-queue:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_offline_queueRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_offline_queueResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_offline_queue
      summary: "Create or execute Admin / Offline Queue"
      description: "Admin / Offline Queue. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Offline Queue"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  listPendingQueue,\n  enqueueOfflineSale,\n  markSynced,\n  markFailed,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const deviceName = req.nextUrl.searchParams.get(\"device\") ?? undefined;\n  const data = await listPendingQueue(sb, { deviceName });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const body = await req.json();\n  if (!body.device_name || !body.payload) {\n    return correlatedJson(cid, { error: \"device_name and payload are required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const item = await enqueueOfflineSale(sb, body);\n  return correlatedJson(cid, { data: item }, { status: 201 });\n}\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id, action, error_message } = await req.json();\n  if (!id || !action) {\n    return correlatedJson(cid, { error: \"id and action required\" }, { status: 400 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  if (action === \"synced\") {\n    await markSynced(sb, id);\n  } else if (action === \"failed\") {\n    await markFailed(sb, id, error_message ?? \"Unknown error\");\n  }\n  return correlatedJson(cid, { success: true });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/offline-queue:POST\", post);\nexport const PATCH = withAdminMutationIdempotency(\"/admin/offline-queue:PATCH\", patch);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_offline_queueRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_offline_queueResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/operator-notes:
    get:
      operationId: get_admin_operator_notes
      summary: "Read Admin / Operator Notes"
      description: "Admin / Operator Notes. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Operator Notes"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  createOperatorNote,\n  listOperatorNotes,\n} from \"@/lib/admin-operator-notes\";\nimport type { EntityWorkflowType } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst ENTITY_NOTE_PERMS: Record<\n  EntityWorkflowType,\n  { read: string; write: string }\n> = {\n  catalog_product: { read: \"catalog:read\", write: \"catalog:write\" },\n  sales_order: { read: \"orders:read\", write: \"orders:write\" },\n  inventory_adjustment: { read: \"inventory:read\", write: \"inventory:write\" },\n  campaign: { read: \"campaigns:read\", write: \"campaigns:write\" },\n  cms_page: { read: \"content:read\", write: \"content:write\" },\n  chat_order: { read: \"chat_orders:manage\", write: \"chat_orders:manage\" },\n};\n\nfunction isEntityWorkflowType(v: string): v is EntityWorkflowType {\n  return v in ENTITY_NOTE_PERMS;\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const url = new URL(req.url);\n  const entityType = url.searchParams.get(\"entity_type\")?.trim() ?? \"\";\n  const entityId = url.searchParams.get(\"entity_id\")?.trim() ?? \"\";\n  if (!entityType || !entityId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"entity_type and entity_id query params are required\" },\n      { status: 400 },\n    );\n  }\n  if (!isEntityWorkflowType(entityType)) {\n    return correlatedJson(correlationId, { error: \"Invalid entity_type\" }, { status: 400 });\n  }\n  const readPerm = ENTITY_NOTE_PERMS[entityType].read;\n  if (!staffSessionAllows(session, readPerm)) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  const notes = await listOperatorNotes(sup.client, entityType, entityId);\n  return correlatedJson(correlationId, { notes });\n}\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const body = (await req.json().catch(() => ({}))) as Record<string, unknown>;\n  const entityType = typeof body.entity_type === \"string\" ? body.entity_type : \"\";\n  const entityId = typeof body.entity_id === \"string\" ? body.entity_id : \"\";\n  const text = typeof body.body === \"string\" ? body.body : \"\";\n  if (!entityType || !entityId || !text.trim()) {\n    return correlatedJson(\n      correlationId,\n      { error: \"entity_type, entity_id, and body are required\" },\n      { status: 400 },\n    );\n  }\n  if (!isEntityWorkflowType(entityType)) {\n    return correlatedJson(correlationId, { error: \"Invalid entity_type\" }, { status: 400 });\n  }\n  const writePerm = ENTITY_NOTE_PERMS[entityType].write;\n  if (!staffSessionAllows(session, writePerm)) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  const row = await createOperatorNote(sup.client, {\n    entityType,\n    entityId,\n    body: text,\n    authorEmail: session.user.email,\n  });\n  if (!row) {\n    return correlatedJson(correlationId, { error: \"Unable to save note\" }, { status: 502 });\n  }\n  return correlatedJson(correlationId, { id: row.id }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/operator-notes:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_operator_notesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_operator_notes
      summary: "Create or execute Admin / Operator Notes"
      description: "Admin / Operator Notes. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Operator Notes"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  createOperatorNote,\n  listOperatorNotes,\n} from \"@/lib/admin-operator-notes\";\nimport type { EntityWorkflowType } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst ENTITY_NOTE_PERMS: Record<\n  EntityWorkflowType,\n  { read: string; write: string }\n> = {\n  catalog_product: { read: \"catalog:read\", write: \"catalog:write\" },\n  sales_order: { read: \"orders:read\", write: \"orders:write\" },\n  inventory_adjustment: { read: \"inventory:read\", write: \"inventory:write\" },\n  campaign: { read: \"campaigns:read\", write: \"campaigns:write\" },\n  cms_page: { read: \"content:read\", write: \"content:write\" },\n  chat_order: { read: \"chat_orders:manage\", write: \"chat_orders:manage\" },\n};\n\nfunction isEntityWorkflowType(v: string): v is EntityWorkflowType {\n  return v in ENTITY_NOTE_PERMS;\n}\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const url = new URL(req.url);\n  const entityType = url.searchParams.get(\"entity_type\")?.trim() ?? \"\";\n  const entityId = url.searchParams.get(\"entity_id\")?.trim() ?? \"\";\n  if (!entityType || !entityId) {\n    return correlatedJson(\n      correlationId,\n      { error: \"entity_type and entity_id query params are required\" },\n      { status: 400 },\n    );\n  }\n  if (!isEntityWorkflowType(entityType)) {\n    return correlatedJson(correlationId, { error: \"Invalid entity_type\" }, { status: 400 });\n  }\n  const readPerm = ENTITY_NOTE_PERMS[entityType].read;\n  if (!staffSessionAllows(session, readPerm)) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  const notes = await listOperatorNotes(sup.client, entityType, entityId);\n  return correlatedJson(correlationId, { notes });\n}\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  const body = (await req.json().catch(() => ({}))) as Record<string, unknown>;\n  const entityType = typeof body.entity_type === \"string\" ? body.entity_type : \"\";\n  const entityId = typeof body.entity_id === \"string\" ? body.entity_id : \"\";\n  const text = typeof body.body === \"string\" ? body.body : \"\";\n  if (!entityType || !entityId || !text.trim()) {\n    return correlatedJson(\n      correlationId,\n      { error: \"entity_type, entity_id, and body are required\" },\n      { status: 400 },\n    );\n  }\n  if (!isEntityWorkflowType(entityType)) {\n    return correlatedJson(correlationId, { error: \"Invalid entity_type\" }, { status: 400 });\n  }\n  const writePerm = ENTITY_NOTE_PERMS[entityType].write;\n  if (!staffSessionAllows(session, writePerm)) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  const row = await createOperatorNote(sup.client, {\n    entityType,\n    entityId,\n    body: text,\n    authorEmail: session.user.email,\n  });\n  if (!row) {\n    return correlatedJson(correlationId, { error: \"Unable to save note\" }, { status: 502 });\n  }\n  return correlatedJson(correlationId, { id: row.id }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/operator-notes:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_operator_notesRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_operator_notesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/orders/{orderId}/refund:
    post:
      operationId: post_admin_orders_orderId_refund
      summary: "Create or execute Admin / Orders / OrderId / Refund"
      description: "Admin / Orders / OrderId / Refund. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Orders"]
      x-source: "import { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  completePaymentRefundAudit,\n  enqueueOutboxEvent,\n  insertPaymentRefundAudit,\n  PAYMENT_OUTBOX_EVENT_TYPES,\n  tryCreateSupabaseClient,\n} from \"@universal-music-store/platform-data\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport {\n  fetchMedusaOrderPaymentsForAdmin,\n  refundMedusaPayment,\n} from \"@/lib/medusa-order-bridge\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getRequestHash,\n  parseAdminJson,\n  requireIdempotencyKey,\n  stepUpRequired,\n} from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst refundSchema = z.object({\n  payment_id: z.string().trim().min(1).max(128).optional(),\n  amount_minor: z.number().int().positive().max(100_000_000).optional(),\n  note: z.string().trim().max(500).optional(),\n}).strict();\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function POST(\n  req: Request,\n  ctx: { params: Promise<{ orderId: string }> },\n) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"orders:write\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const idempotencyKey = requireIdempotencyKey(req);\n  if (!idempotencyKey) {\n    return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  }\n  if (!stepUpRequired(\"orders.refund\", req)) {\n    return correlatedJson(correlationId, { error: \"Step-up authentication required\" }, { status: 403 });\n  }\n\n  const { orderId } = await ctx.params;\n  if (!orderId?.startsWith(\"order_\")) {\n    return correlatedJson(correlationId, { error: \"Invalid order id\" }, { status: 400 });\n  }\n\n  const parsed = await parseAdminJson(req, refundSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const body = parsed.data;\n\n  const payments = await fetchMedusaOrderPaymentsForAdmin(orderId);\n  if (payments.length === 0) {\n    return correlatedJson(\n      correlationId,\n      { error: \"No payments found for this order\" },\n      { status: 404 },\n    );\n  }\n\n  const paymentId =\n    typeof body.payment_id === \"string\" && body.payment_id.trim().length > 0\n      ? body.payment_id.trim()\n      : payments[0]?.id;\n  if (!paymentId) {\n    return correlatedJson(correlationId, { error: \"No payment id\" }, { status: 400 });\n  }\n\n  const selected = payments.find((p) => p.id === paymentId);\n  if (!selected) {\n    return correlatedJson(correlationId, { error: \"Payment not on this order\" }, { status: 400 });\n  }\n\n  const captured =\n    selected.captured_amount != null && Number.isFinite(selected.captured_amount)\n      ? selected.captured_amount\n      : selected.amount;\n  const alreadyRefunded =\n    selected.refunded_amount != null && Number.isFinite(selected.refunded_amount)\n      ? selected.refunded_amount\n      : 0;\n  const refundable = Math.max(0, captured - alreadyRefunded);\n\n  let amountMinor =\n    body.amount_minor != null && Number.isFinite(Number(body.amount_minor))\n      ? Math.floor(Number(body.amount_minor))\n      : refundable;\n  if (amountMinor <= 0) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Nothing to refund for this payment\" },\n      { status: 400 },\n    );\n  }\n  if (amountMinor > refundable) {\n    return correlatedJson(\n      correlationId,\n      {\n        error: `Amount exceeds refundable balance (${refundable} minor units).`,\n      },\n      { status: 400 },\n    );\n  }\n\n  const sb = tryCreateSupabaseClient();\n  const claim = await claimAdminIdempotency(sb, {\n    actorKey: typeof session.user.email === \"string\" ? session.user.email : \"unknown\",\n    actionKey: `orders.refund:${orderId}:${paymentId}`,\n    idempotencyKey,\n    requestHash: getRequestHash({ orderId, paymentId, amountMinor, note: body.note ?? null }),\n  });\n  if (claim.kind === \"unavailable\") {\n    return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  }\n  if (claim.kind === \"conflict\") {\n    return correlatedJson(correlationId, { error: \"Request is already being processed or key was reused\" }, { status: 409 });\n  }\n  if (claim.kind === \"replay\") {\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/admin/orders/[orderId]/refund\",\n    correlationId,\n    phase: \"start\",\n    detail: { orderId, paymentId, amountMinor },\n  });\n\n  let refundAuditId: string | null = null;\n  if (sb) {\n    refundAuditId = await insertPaymentRefundAudit(sb, {\n      medusaOrderId: orderId,\n      medusaPaymentId: paymentId,\n      amountMinor,\n      actorEmail: typeof session.user.email === \"string\" ? session.user.email : null,\n      note: typeof body.note === \"string\" ? body.note : null,\n      requestCorrelationId: correlationId,\n    }).catch(() => null);\n    await enqueueOutboxEvent(sb, {\n      aggregate_type: \"payment_refund\",\n      aggregate_id: orderId,\n      event_type: PAYMENT_OUTBOX_EVENT_TYPES.PAYMENT_ATTEMPT_REFUND_REQUESTED,\n      payload: {\n        order_id: orderId,\n        payment_id: paymentId,\n        amount_minor: amountMinor,\n        actor_email:\n          typeof session.user.email === \"string\" ? session.user.email : null,\n        note: typeof body.note === \"string\" ? body.note : null,\n        request_correlation_id: correlationId,\n        refund_audit_id: refundAuditId,\n      },\n    }).catch(() => {});\n  }\n\n  const result = await refundMedusaPayment(\n    paymentId,\n    amountMinor,\n    typeof body.note === \"string\" ? body.note : undefined,\n  );\n\n  if (!result.ok) {\n    const status = result.status >= 400 ? result.status : 502;\n    const responseBody = { error: result.error ?? \"Refund did not complete\" };\n    if (sb && refundAuditId) {\n      await completePaymentRefundAudit(sb, refundAuditId, false, result.error ?? \"refund_failed\").catch(\n        () => {},\n      );\n    }\n    await completeAdminIdempotency(sb, claim.id, status, responseBody);\n    logAdminApiEvent({\n      route: \"POST /api/admin/orders/[orderId]/refund\",\n      correlationId,\n      phase: \"error\",\n      detail: { orderId, error: result.error },\n    });\n    return correlatedJson(\n      correlationId,\n      responseBody,\n      { status },\n    );\n  }\n\n  if (sb && refundAuditId) {\n    await completePaymentRefundAudit(sb, refundAuditId, true, null).catch(() => {});\n  }\n\n  await completeAdminIdempotency(sb, claim.id, 200, {\n    ok: true,\n    payment_id: paymentId,\n    amount_minor: amountMinor,\n  });\n\n  logAdminApiEvent({\n    route: \"POST /api/admin/orders/[orderId]/refund\",\n    correlationId,\n    phase: \"ok\",\n    detail: { orderId, paymentId, amountMinor },\n  });\n\n  return correlatedJson(correlationId, {\n    ok: true as const,\n    payment_id: paymentId,\n    amount_minor: amountMinor,\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_orders_orderId_refundRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_orders_orderId_refundResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/orders/bulk-fulfill:
    post:
      operationId: post_admin_orders_bulk_fulfill
      summary: "Create or execute Admin / Orders / Bulk Fulfill"
      description: "Admin / Orders / Bulk Fulfill. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Orders"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\r\n\r\nconst MAX_CONCURRENCY = 3;\r\n\r\ntype BulkFulfillBody = {\r\n  orderIds: string[];\r\n  trackingNumber?: string;\r\n  carrierId?: string;\r\n  notifyCustomer?: boolean;\r\n};\r\n\r\ntype FulfillResult = {\r\n  orderId: string;\r\n  ok: boolean;\r\n  error?: string;\r\n};\r\n\r\nasync function fulfillOrder(\r\n  orderId: string,\r\n  opts: { trackingNumber?: string; carrierId?: string; notifyCustomer?: boolean },\r\n): Promise<FulfillResult> {\r\n  try {\r\n    const orderRes = await medusaAdminFetch(`/admin/orders/${encodeURIComponent(orderId)}`, {\r\n      method: \"GET\",\r\n    });\r\n    if (!orderRes.ok) {\r\n      return { orderId, ok: false, error: `Order fetch failed: ${orderRes.status}` };\r\n    }\r\n    const orderJson = (await orderRes.json()) as { order?: Record<string, unknown> };\r\n    const order = orderJson.order ?? {};\r\n\r\n    if (order.fulfillment_status === \"fulfilled\" || order.fulfillment_status === \"shipped\") {\r\n      return { orderId, ok: true, error: \"Already fulfilled\" };\r\n    }\r\n\r\n    const items = Array.isArray(order.items) ? order.items as Array<Record<string, unknown>> : [];\r\n    const fulfillItems = items\r\n      .filter((i) => Number(i.quantity ?? 0) > Number(i.fulfilled_quantity ?? 0))\r\n      .map((i) => ({\r\n        id: String(i.id),\r\n        quantity: Number(i.quantity ?? 1) - Number(i.fulfilled_quantity ?? 0),\r\n      }));\r\n\r\n    if (fulfillItems.length === 0) {\r\n      return { orderId, ok: true, error: \"No items to fulfill\" };\r\n    }\r\n\r\n    const fulfillBody: Record<string, unknown> = {\r\n      items: fulfillItems,\r\n      no_notification: !opts.notifyCustomer,\r\n    };\r\n\r\n    const fulfillRes = await medusaAdminFetch(\r\n      `/admin/orders/${encodeURIComponent(orderId)}/fulfillments`,\r\n      {\r\n        method: \"POST\",\r\n        headers: { \"Content-Type\": \"application/json\" },\r\n        body: JSON.stringify(fulfillBody),\r\n      },\r\n    );\r\n\r\n    if (!fulfillRes.ok) {\r\n      const errText = await fulfillRes.text().catch(() => fulfillRes.status.toString());\r\n      return { orderId, ok: false, error: `Fulfill failed: ${errText}` };\r\n    }\r\n\r\n    const fulfillJson = (await fulfillRes.json()) as {\r\n      order?: { fulfillments?: Array<{ id: string }> };\r\n    };\r\n\r\n    if (opts.trackingNumber) {\r\n      const fulfillmentId = fulfillJson.order?.fulfillments?.[0]?.id;\r\n      if (fulfillmentId) {\r\n        await medusaAdminFetch(\r\n          `/admin/orders/${encodeURIComponent(orderId)}/fulfillments/${encodeURIComponent(fulfillmentId)}/shipments`,\r\n          {\r\n            method: \"POST\",\r\n            headers: { \"Content-Type\": \"application/json\" },\r\n            body: JSON.stringify({\r\n              tracking_numbers: [opts.trackingNumber],\r\n            }),\r\n          },\r\n        ).catch(() => null);\r\n      }\r\n    }\r\n\r\n    return { orderId, ok: true };\r\n  } catch (err) {\r\n    return {\r\n      orderId,\r\n      ok: false,\r\n      error: err instanceof Error ? err.message : String(err),\r\n    };\r\n  }\r\n}\r\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"orders:fulfill\")) {\r\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\r\n  }\r\n\r\n  let body: BulkFulfillBody;\r\n  try {\r\n    body = (await req.json()) as BulkFulfillBody;\r\n  } catch {\r\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\r\n  }\r\n\r\n  const { orderIds, trackingNumber, carrierId, notifyCustomer = true } = body;\r\n\r\n  if (!Array.isArray(orderIds) || orderIds.length === 0) {\r\n    return correlatedJson(cid, { error: \"orderIds must be a non-empty array\" }, { status: 400 });\r\n  }\r\n  if (orderIds.length > 100) {\r\n    return correlatedJson(cid, { error: \"Maximum 100 orders per bulk request\" }, { status: 400 });\r\n  }\r\n\r\n  const results: FulfillResult[] = [];\r\n\r\n  for (let i = 0; i < orderIds.length; i += MAX_CONCURRENCY) {\r\n    const chunk = orderIds.slice(i, i + MAX_CONCURRENCY);\r\n    const chunkResults = await Promise.all(\r\n      chunk.map((id) =>\r\n        fulfillOrder(id, { trackingNumber, carrierId, notifyCustomer }),\r\n      ),\r\n    );\r\n    results.push(...chunkResults);\r\n  }\r\n\r\n  const succeeded = results.filter((r) => r.ok && !r.error?.includes(\"Already\")).map((r) => r.orderId);\r\n  const failed = results.filter((r) => !r.ok);\r\n  const skipped = results.filter((r) => r.ok && r.error);\r\n\r\n  const sup = adminSupabaseOr503(cid);\r\n  if (!(\"response\" in sup)) {\r\n    const staffEmail = session.user.email ?? \"unknown\";\r\n    await sup.client\r\n      .from(\"audit_log\")\r\n      .insert({\r\n        actor_email: staffEmail,\r\n        action: \"bulk_fulfill\",\r\n        resource_type: \"order\",\r\n        resource_ids: orderIds,\r\n        metadata: {\r\n          succeeded: succeeded.length,\r\n          failed: failed.length,\r\n          skipped: skipped.length,\r\n          trackingNumber: trackingNumber ?? null,\r\n        },\r\n      })\r\n      .then(\r\n        () => null,\r\n        () => null,\r\n      );\r\n  }\r\n\r\n  return correlatedJson(cid, {\r\n    total: orderIds.length,\r\n    succeeded: succeeded.length,\r\n    failed: failed.length,\r\n    skipped: skipped.length,\r\n    results,\r\n  });\r\n}\r\n\nexport const POST = withAdminMutationIdempotency(\"/admin/orders/bulk-fulfill:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_orders_bulk_fulfillRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_orders_bulk_fulfillResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/orders/export-jnt-csv:
    post:
      operationId: post_admin_orders_export_jnt_csv
      summary: "Create or execute Admin / Orders / Export Jnt Csv"
      description: "Admin / Orders / Export Jnt Csv. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Orders"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest, NextResponse } from \"next/server\";\nimport { z } from \"zod\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\ntype JntCsvRow = {\n  orderNumber: string;\n  consigneeName: string;\n  phone: string;\n  address: string;\n  city: string;\n  province: string;\n  postalCode: string;\n  weightKg: string;\n  codAmount: string;\n  remarks: string;\n};\n\nconst exportRequestSchema = z\n  .object({\n    orderIds: z\n      .array(z.string().trim().min(1).max(128))\n      .min(1)\n      .max(200)\n      .superRefine((ids, ctx) => {\n        if (new Set(ids).size !== ids.length) {\n          ctx.addIssue({ code: z.ZodIssueCode.custom, message: \"Duplicate order IDs\" });\n        }\n      }),\n  })\n  .strict();\n\nconst MAX_CONCURRENCY = 4;\n\nfunction escapeCsv(val: string): string {\n  if (val.includes(\",\") || val.includes('\"') || val.includes(\"\\n\")) {\n    return `\"${val.replace(/\"/g, '\"\"')}\"`;\n  }\n  return val;\n}\n\nfunction buildCsvRow(row: JntCsvRow): string {\n  return [\n    row.orderNumber,\n    row.consigneeName,\n    row.phone,\n    row.address,\n    row.city,\n    row.province,\n    row.postalCode,\n    row.weightKg,\n    row.codAmount,\n    row.remarks,\n  ]\n    .map(escapeCsv)\n    .join(\",\");\n}\n\n/**\n * POST /api/admin/orders/export-jnt-csv\n * Body: { orderIds: string[] }\n *\n * Fetches each order's shipping address from Medusa and returns a J&T Express\n * bulk upload CSV. The CSV format follows J&T Philippines import template:\n * Order No. | Consignee | Phone | Address | City | Province | Postal Code |\n * Weight (kg) | COD Amount | Remarks\n */\nasync function post(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"analytics:export\");\n  if (!staff.ok) {\n    staff.response.headers.set(\"x-request-id\", correlationId);\n    return staff.response;\n  }\n\n  const parsed = await parseAdminJson(req, exportRequestSchema);\n  if (!parsed.ok) {\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error, code: \"VALIDATION_ERROR\", requestId: correlationId },\n      { status: parsed.status },\n    );\n  }\n\n  const { orderIds } = parsed.data;\n\n  const rows: JntCsvRow[] = [];\n  const errors: string[] = [];\n\n  for (let index = 0; index < orderIds.length; index += MAX_CONCURRENCY) {\n    const chunk = orderIds.slice(index, index + MAX_CONCURRENCY);\n    await Promise.all(chunk.map(async (orderId) => {\n      try {\n        const res = await medusaAdminFetch(\n          `/admin/orders/${encodeURIComponent(orderId)}?fields=id,display_id,email,total,payment_status,metadata,*shipping_address,*items`,\n        );\n        if (!res.ok) {\n          errors.push(orderId);\n          return;\n        }\n        const json = (await res.json()) as {\n          order?: {\n            id?: string;\n            display_id?: number;\n            total?: number;\n            payment_status?: string;\n            shipping_address?: {\n              first_name?: string;\n              last_name?: string;\n              phone?: string;\n              address_1?: string;\n              address_2?: string;\n              city?: string;\n              province?: string;\n              postal_code?: string;\n            } | null;\n            items?: Array<{\n              quantity?: number;\n              variant?: { weight?: number } | null;\n            }> | null;\n          };\n        };\n\n        const o = json.order;\n        if (!o) {\n          errors.push(orderId);\n          return;\n        }\n\n        const addr = o.shipping_address;\n        const firstName = addr?.first_name?.trim() ?? \"\";\n        const lastName = addr?.last_name?.trim() ?? \"\";\n        const consigneeName =\n          [firstName, lastName].filter(Boolean).join(\" \") || \"Unknown\";\n        const phone = addr?.phone?.trim() ?? \"\";\n        const addressLine = [addr?.address_1, addr?.address_2]\n          .map((a) => a?.trim())\n          .filter(Boolean)\n          .join(\", \");\n        const city = addr?.city?.trim() ?? \"\";\n        const province = addr?.province?.trim() ?? \"\";\n        const postalCode = addr?.postal_code?.trim() ?? \"\";\n\n        const totalWeightG = (o.items ?? []).reduce((sum, item) => {\n          const itemWeight = item.variant?.weight ?? 500;\n          return sum + itemWeight * (item.quantity ?? 1);\n        }, 0);\n        const weightKg = (totalWeightG / 1000).toFixed(2);\n\n        const isCod =\n          o.payment_status === \"not_paid\" || o.payment_status === \"awaiting\";\n        const codAmount = isCod ? ((o.total ?? 0) / 100).toFixed(2) : \"0.00\";\n\n        rows.push({\n          orderNumber: o.display_id != null ? `#${o.display_id}` : orderId,\n          consigneeName,\n          phone,\n          address: addressLine,\n          city,\n          province,\n          postalCode,\n          weightKg,\n          codAmount,\n          remarks: `Universal Music Store Order ${o.display_id ?? orderId}`,\n        });\n      } catch {\n        errors.push(orderId);\n      }\n    }));\n  }\n\n  const header =\n    \"Order No.,Consignee Name,Phone,Address,City,Province,Postal Code,Weight (kg),COD Amount,Remarks\";\n  const csvLines = [header, ...rows.map(buildCsvRow)];\n  const csv = csvLines.join(\"\\r\\n\");\n\n  return new NextResponse(csv, {\n    headers: {\n      \"Content-Type\": \"text/csv; charset=utf-8\",\n      \"Content-Disposition\": `attachment; filename=\"jnt-export-${new Date().toISOString().slice(0, 10)}.csv\"`,\n      \"X-Export-Rows\": String(rows.length),\n      ...(errors.length > 0\n        ? { \"X-Export-Failures\": String(errors.length) }\n        : {}),\n      \"x-request-id\": correlationId,\n    },\n  });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/orders/export-jnt-csv:POST\", post);\n"
      x-permission: "analytics:export"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_orders_export_jnt_csvRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_orders_export_jnt_csvResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payment-attempts/export:
    get:
      operationId: get_admin_payment_attempts_export
      summary: "Read Admin / Payment Attempts / Export"
      description: "Admin / Payment Attempts / Export. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payment Attempts"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\n\nexport const dynamic = \"force-dynamic\";\nfunction csv(value: unknown) {\n  const text = String(value ?? \"\");\n  return /[\",\\n]/.test(text) ? `\"${text.replace(/\"/g, '\"\"')}\"` : text;\n}\nexport async function GET(_request: Request) {\n  const staff = await requireStaffApiSession(\"analytics:export\");\n  if (!staff.ok) return staff.response;\n  const cid = getCorrelationId(_request);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return new Response(\"Tenant scope unavailable\", { status: 403 });\n  const { data, error } = await sup.client\n    .from(\"payment_attempts\")\n    .select(\"id,provider,status,amount_minor,currency,updated_at\")\n    .eq(\"organization_id\", organization.id)\n    .order(\"updated_at\", { ascending: false })\n    .limit(500);\n  if (error)\n    return new Response(\"Unable to export payment attempts\", { status: 502 });\n  const rows = (data ?? []) as Array<Record<string, unknown>>;\n  const lines = [\n    [\"id\", \"provider\", \"status\", \"amount_minor\", \"currency\", \"updated_at\"],\n    ...rows.map((row) => [\n      row.id,\n      row.provider,\n      row.status,\n      row.amount_minor,\n      row.currency,\n      row.updated_at,\n    ]),\n  ].map((row) => row.map(csv).join(\",\"));\n  const body = `${lines.join(\"\\n\")}\\n`;\n  if (new TextEncoder().encode(body).byteLength > 5 * 1024 * 1024)\n    return new Response(\"Export too large\", { status: 413 });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staff.session.user?.email ?? \"unknown\",\n    action: \"payment_attempts.export\",\n    resource: \"payment_attempts\",\n    details: {\n      organization_id: organization.id,\n      count: rows.length,\n      correlation_id: cid,\n    },\n  });\n  return new Response(body, {\n    headers: {\n      \"Content-Type\": \"text/csv; charset=utf-8\",\n      \"Content-Disposition\": \"attachment; filename=payment-attempts.csv\",\n      \"Cache-Control\": \"no-store\",\n    },\n  });\n}\n"
      x-permission: "analytics:export"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_payment_attempts_exportResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payment-health:
    get:
      operationId: get_admin_payment_health
      summary: "Read Admin / Payment Health"
      description: "Admin / Payment Health. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payment Health"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype ProviderStatus = {\r\n  enabled: boolean;\r\n  webhookConfigured: boolean;\r\n  sandboxMode?: boolean;\r\n  notes?: string;\r\n};\r\n\r\n/**\r\n * PH-19: Payment provider health check.\r\n * Reports whether each PSP is configured, its webhook is set, and whether\r\n * sandbox/production mode is correct. Operators and CI can query this\r\n * endpoint to verify payment readiness before deploying.\r\n *\r\n * GET /api/admin/payment-health\r\n */\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"payments:read\")) {\r\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\r\n  }\r\n\r\n  const env = process.env;\n  const supabase = adminSupabaseOr503(cid);\n  if (\"response\" in supabase) return supabase.response;\n  const organization = await resolveStaffOrganization(supabase.client, session.user.email);\n  const { data: connections } = organization\n    ? await supabase.client\n        .from(\"payment_nango_connections\")\n        .select(\"provider\")\n        .eq(\"organization_id\", organization.id)\n        .eq(\"active\", true)\n    : { data: [] as Array<{ provider: string | null }> };\n  const connectedProviders = new Set((connections ?? []).map((item) => item.provider));\n\r\n  const stripe: ProviderStatus = {\n    enabled: connectedProviders.has(\"stripe\"),\n    webhookConfigured: Boolean(env.STRIPE_WEBHOOK_SECRET?.trim()),\r\n  };\r\n\r\n  const paypal: ProviderStatus = {\n    enabled: connectedProviders.has(\"paypal\"),\n    webhookConfigured: Boolean(env.PAYPAL_WEBHOOK_ID?.trim()),\r\n    sandboxMode: env.PAYPAL_ENVIRONMENT?.trim().toLowerCase() !== \"live\",\r\n    notes: !env.PAYPAL_WEBHOOK_ID?.trim()\r\n      ? \"PAYPAL_WEBHOOK_ID is required in production. Register the webhook in PayPal Developer Dashboard -> My Apps -> Webhooks.\"\r\n      : undefined,\r\n  };\r\n\r\n  const paymongo: ProviderStatus = {\r\n    enabled: Boolean(env.PAYMONGO_SECRET_KEY?.trim()),\r\n    webhookConfigured: Boolean(env.PAYMONGO_WEBHOOK_SECRET?.trim()),\r\n  };\r\n\r\n  const maya: ProviderStatus = {\r\n    enabled: Boolean(env.MAYA_SECRET_KEY?.trim()),\r\n    webhookConfigured: Boolean(env.MAYA_WEBHOOK_SECRET?.trim()),\r\n    sandboxMode: env.MAYA_SANDBOX?.trim().toLowerCase() !== \"false\",\r\n    notes: !env.MAYA_WEBHOOK_SECRET?.trim()\r\n      ? \"MAYA_WEBHOOK_SECRET required for production signature verification.\"\r\n      : undefined,\r\n  };\r\n\r\n  const cod: ProviderStatus = {\r\n    enabled: true,\r\n    webhookConfigured: true,\r\n    notes: \"COD does not use webhooks. Fulfilled manually by staff.\",\r\n  };\r\n\r\n  const vercelEnv = env.VERCEL_ENV?.trim();\r\n  const nodeEnv = env.NODE_ENV?.trim();\r\n  const isProduction = vercelEnv === \"production\" || nodeEnv === \"production\";\r\n\r\n  const warnings: string[] = [];\r\n\r\n  if (isProduction) {\r\n    if (paypal.enabled && !paypal.webhookConfigured) {\r\n      warnings.push(\"PayPal is enabled but PAYPAL_WEBHOOK_ID is not set — PayPal webhook verification will fail.\");\r\n    }\r\n    if (paypal.enabled && paypal.sandboxMode) {\n      warnings.push(\"PayPal appears to be in sandbox mode in production.\");\r\n    }\r\n    if (maya.enabled && !maya.webhookConfigured) {\r\n      warnings.push(\"Maya is enabled but MAYA_WEBHOOK_SECRET is not set — webhook signatures cannot be verified.\");\r\n    }\r\n    if (maya.enabled && maya.sandboxMode) {\r\n      warnings.push(\"Maya appears to be in sandbox mode in production.\");\r\n    }\r\n    if (stripe.enabled && !stripe.webhookConfigured) {\r\n      warnings.push(\"Stripe is enabled but STRIPE_WEBHOOK_SECRET is not set.\");\r\n    }\r\n  }\r\n\r\n  return correlatedJson(cid, {\r\n    environment: vercelEnv ?? nodeEnv ?? \"unknown\",\r\n    isProduction,\r\n    providers: { stripe, paypal, paymongo, maya, cod },\r\n    warnings,\r\n    ok: warnings.length === 0,\r\n  });\r\n}\r\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_payment_healthResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments:
    get:
      operationId: get_admin_payments
      summary: "Read Admin / Payments"
      description: "Admin / Payments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { listRecentPaymentAttempts } from \"@universal-music-store/platform-data\";\n\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { randomUUID } from \"crypto\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function GET(request: Request) {\n  const correlationId = randomUUID();\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n\n  const { searchParams } = new URL(request.url);\n  const limit = Math.min(200, Math.max(1, Number(searchParams.get(\"limit\")) || 80));\n\n  try {\n    const rows = await listRecentPaymentAttempts(sup.client, limit);\n    return correlatedJson(correlationId, {\n      attempts: rows.map((r) => ({\n        id: r.id,\n        correlationId: r.correlation_id,\n        cartId: r.cart_id,\n        provider: r.provider,\n        status: r.status,\n        checkoutState: r.checkout_state,\n        amountMinor: r.amount_minor,\n        currency: r.currency,\n        medusaOrderId: r.medusa_order_id,\n        quoteFingerprint: r.quote_fingerprint,\n        staleReason: r.stale_reason,\n        invalidatedAt: r.invalidated_at,\n        invalidatedBy: r.invalidated_by,\n        providerSessionId: r.provider_session_id,\n        lastError: r.last_error,\n        finalizeAttempts: r.finalize_attempts,\n        webhookLastStatus: r.webhook_last_status,\n        updatedAt: r.updated_at,\n        finalizedAt: r.finalized_at,\n      })),\n    });\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"List failed\";\n    return correlatedJson(correlationId, { error: msg }, { status: 500 });\n  }\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_paymentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/{id}/mark-review:
    post:
      operationId: post_admin_payments_id_mark_review
      summary: "Create or execute Admin / Payments / Id / Mark Review"
      description: "Admin / Payments / Id / Mark Review. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { getPaymentAttemptByCorrelationId, updatePaymentAttemptByCorrelationId } from \"@universal-music-store/platform-data\";\n\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { randomUUID } from \"crypto\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\n\nasync function post(\n  _req: Request,\n  ctx: { params: Promise<{ id: string }> },\n) {\n  const correlationId = randomUUID();\n  const staff = await requireStaffApiSession(\"orders:write\");\n  if (!staff.ok) return staff.response;\n\n  const { id } = await ctx.params;\n  if (!id?.trim()) {\n    return correlatedJson(correlationId, { error: \"Missing id\" }, { status: 400 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n\n  const row = await getPaymentAttemptByCorrelationId(sup.client, id.trim());\n  if (!row) {\n    return correlatedJson(correlationId, { error: \"Not found\" }, { status: 404 });\n  }\n\n  await updatePaymentAttemptByCorrelationId(sup.client, id.trim(), {\n    status: \"needs_review\",\n    checkout_state: \"needs_review\",\n    last_error: (row.last_error ? `${row.last_error}; ` : \"\") + \"escalated_by_operator\",\n  });\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: typeof staff.session.user?.email === \"string\" ? staff.session.user.email : \"unknown\",\n    action: \"payment.mark_review\",\n    resource: \"payment_attempt\",\n    resourceId: id.trim(),\n    details: { status: \"needs_review\" },\n  });\n\n  return correlatedJson(correlationId, { ok: true });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/[id]/mark-review:POST\", post);\n"
      x-permission: "orders:write"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_id_mark_reviewRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_id_mark_reviewResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/{id}/retry:
    post:
      operationId: post_admin_payments_id_retry
      summary: "Create or execute Admin / Payments / Id / Retry"
      description: "Admin / Payments / Id / Retry. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { randomUUID } from \"crypto\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getRequestHash,\n  requireIdempotencyKey,\n  stepUpRequired,\n} from \"@/lib/admin-api-security\";\nimport { tryCreateSupabaseClient } from \"@universal-music-store/platform-data\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function POST(\n  _req: Request,\n  ctx: { params: Promise<{ id: string }> },\n) {\n  const correlationId = _req.headers.get(\"x-request-id\")?.trim() || randomUUID();\n  const staff = await requireStaffApiSession(\"orders:write\");\n  if (!staff.ok) return staff.response;\n\n  const { id } = await ctx.params;\n  if (!id?.trim()) {\n    return correlatedJson(correlationId, { error: \"Missing id\" }, { status: 400 });\n  }\n  const idempotencyKey = requireIdempotencyKey(_req);\n  if (!idempotencyKey) {\n    return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  }\n  if (!stepUpRequired(\"payments.retry\", _req)) {\n    return correlatedJson(correlationId, { error: \"Step-up authentication required\" }, { status: 403 });\n  }\n\n  const sb = tryCreateSupabaseClient();\n  const claim = await claimAdminIdempotency(sb, {\n    actorKey: typeof staff.session.user?.email === \"string\" ? staff.session.user.email : \"unknown\",\n    actionKey: `payments.retry:${id.trim()}`,\n    idempotencyKey,\n    requestHash: getRequestHash({ id: id.trim() }),\n  });\n  if (claim.kind === \"unavailable\") {\n    return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  }\n  if (claim.kind === \"conflict\") {\n    return correlatedJson(correlationId, { error: \"Request is already being processed or key was reused\" }, { status: 409 });\n  }\n  if (claim.kind === \"replay\") {\n    return correlatedJson(correlationId, claim.body, { status: claim.status });\n  }\n\n  const origin = process.env.STOREFRONT_ORIGIN?.replace(/\\/$/, \"\").trim();\n  const secret = process.env.STOREFRONT_INTERNAL_RECONCILE_SECRET?.trim();\n  if (!origin || !secret) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"STOREFRONT_ORIGIN and STOREFRONT_INTERNAL_RECONCILE_SECRET must be set for retry finalization.\",\n      },\n      { status: 503 },\n    );\n  }\n\n  try {\n    const res = await fetch(`${origin}/api/internal/reconcile-payment-attempt`, {\n      method: \"POST\",\n      headers: {\n        \"Content-Type\": \"application/json\",\n        \"x-internal-secret\": secret,\n      },\n      body: JSON.stringify({ correlationId: id.trim() }),\n    });\n    const json = (await res.json().catch(() => ({}))) as Record<string, unknown>;\n    if (!res.ok) {\n      const status = res.status >= 400 && res.status < 600 ? res.status : 502;\n      const body = {\n        error: typeof json.error === \"string\" ? json.error : \"Retry failed\",\n      };\n      await completeAdminIdempotency(sb, claim.id, status, body);\n      return correlatedJson(\n        correlationId,\n        body,\n        { status },\n      );\n    }\n    await completeAdminIdempotency(sb, claim.id, 200, json);\n    if (sb) await insertStaffAuditLog(sb, {\n      actorEmail: typeof staff.session.user?.email === \"string\" ? staff.session.user.email : \"unknown\",\n      action: \"payment.retry\",\n      resource: \"payment_attempt\",\n      resourceId: id.trim(),\n      details: { result: json },\n    });\n    return correlatedJson(correlationId, json);\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"Request failed\";\n    const body = { error: msg };\n    await completeAdminIdempotency(sb, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n}\n"
      x-permission: "orders:write"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_id_retryRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_id_retryResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/capabilities:
    get:
      operationId: get_admin_payments_capabilities
      summary: "Read Admin / Payments / Capabilities"
      description: "Admin / Payments / Capabilities. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import {\n  getPaymentProviderCapabilities,\n  type PaymentProvider,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\n\nexport const dynamic = \"force-dynamic\";\nconst providers = [\n  \"stripe\",\n  \"paypal\",\n  \"xendit\",\n] as const satisfies readonly PaymentProvider[];\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) return staff.response;\n  const supabase = adminSupabaseOr503(correlationId);\n  if (\"response\" in supabase) return supabase.response;\n  const organization = await resolveStaffOrganization(\n    supabase.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const { data: connections } = await supabase.client\n    .from(\"payment_nango_connections\")\n    .select(\"provider_config_key,active,merchant_identity\")\n    .eq(\"organization_id\", organization.id)\n    .eq(\"active\", true);\n  const connected = new Set(\n    (connections ?? []).map((row) => String(row.provider_config_key)),\n  );\n  return correlatedJson(correlationId, {\n    data: providers.map((provider) => ({\n      ...getPaymentProviderCapabilities(provider),\n      configured:\n        (provider !== \"stripe\" || stripeAvailableForMerchant()) &&\n        (connected.has(provider) ||\n          connected.has(`${provider}-sandbox`) ||\n          (provider === \"xendit\" &&\n            Boolean(\n              process.env.XENDIT_SECRET_KEY?.trim() &&\n                process.env.XENDIT_WEBHOOK_TOKEN?.trim(),\n            ))),\n      policy:\n        provider === \"stripe\" && !stripeAvailableForMerchant()\n          ? { locked: true, code: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY }\n          : { locked: false },\n      unavailableInUvs: getPaymentProviderCapabilities(\n        provider,\n      ).capabilities.filter(\n        (capability) =>\n          !getPaymentProviderCapabilities(\n            provider,\n          ).implementedCapabilities.includes(capability),\n      ),\n    })),\n  });\n}\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_payments_capabilitiesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/connect-session:
    post:
      operationId: post_admin_payments_connect_session
      summary: "Create or execute Admin / Payments / Connect Session"
      description: "Admin / Payments / Connect Session. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport { configuredPaymentIntegrations } from \"@/lib/payment-nango\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport {\n  resolveStaffOrganization,\n  organizationCanManagePayments,\n} from \"@/lib/staff-organization\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\n\nconst connectSessionSchema = z\n  .object({\n    integration_id: z.string().trim().min(1).max(100),\n  })\n  .strict();\n\n/** Creates a Nango Connect session; provider credentials never pass through this API. */\nasync function post(request: NextRequest) {\n  const cid = getCorrelationId(request);\n  const access = await requireStaffApiSession(\"settings:write\");\n  if (!access.ok) return access.response;\n  const session = access.session;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can connect payment providers\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\", code: \"NANGO_NOT_CONFIGURED\" },\n      { status: 503 },\n    );\n\n  const parsed = await parseAdminJson(request, connectSessionSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const integrationId = parsed.data.integration_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!integrationId || !allowed.includes(integrationId)) {\n    return correlatedJson(\n      cid,\n      { error: \"integration_id must be one of NANGO_PAYMENT_INTEGRATIONS\" },\n      { status: 400 },\n    );\n  }\n  if (integrationId === \"stripe\" && !stripeAvailableForMerchant()) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Stripe is not available for merchants in this country\",\n        code: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n      },\n      { status: 409 },\n    );\n  }\n\n  const endUserId = session.user.email?.trim().toLowerCase();\n  if (!endUserId)\n    return correlatedJson(\n      cid,\n      { error: \"Authenticated merchant identity is required\" },\n      { status: 400 },\n    );\n  const organizationId = organization.id;\n  const response = await fetch(\"https://api.nango.dev/connect/sessions\", {\n    method: \"POST\",\n    headers: {\n      Authorization: `Bearer ${apiKey}`,\n      \"Content-Type\": \"application/json\",\n    },\n    body: JSON.stringify({\n      allowed_integrations: [integrationId],\n      tags: {\n        end_user_id: endUserId,\n        end_user_email: session.user.email ?? endUserId,\n        organization_id: organizationId,\n      },\n    }),\n  });\n  const payload = await response.json().catch(() => ({}));\n  if (!response.ok)\n    return correlatedJson(\n      cid,\n      { error: \"Nango Connect session creation failed\", detail: payload },\n      { status: 502 },\n    );\n  return correlatedJson(cid, {\n    data: {\n      session_token: payload.data?.token ?? payload.token ?? null,\n      integration_id: integrationId,\n    },\n  });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/connect-session:POST\", post);\n"
      x-permission: "settings:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_connect_sessionRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_connect_sessionResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/connections:
    delete:
      operationId: delete_admin_payments_connections
      summary: "Delete Admin / Payments / Connections"
      description: "Admin / Payments / Connections. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport {\n  configuredPaymentIntegrations,\n  nangoErrorMessage,\n  paymentIntegrationLabel,\n  type PaymentNangoConnection,\n} from \"@/lib/payment-nango\";\nimport {\n  resolveStaffOrganization,\n  organizationCanManagePayments,\n} from \"@/lib/staff-organization\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst connectionSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nconst disconnectSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    nango_connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nasync function nangoConnections(\n  apiKey: string,\n  identity: string,\n  allowed: string[],\n) {\n  const query = new URLSearchParams({\n    \"tags[end_user_id]\": identity,\n    limit: \"100\",\n  });\n  const response = await fetch(\n    `https://api.nango.dev/connections?${query.toString()}`,\n    {\n      headers: { Authorization: `Bearer ${apiKey}` },\n      cache: \"no-store\",\n    },\n  );\n  const payload = (await response.json().catch(() => ({}))) as {\n    connections?: Array<Record<string, unknown>>;\n  };\n  if (!response.ok) throw new Error(\"Nango connection status unavailable\");\n  return (payload.connections ?? []).filter((connection) => {\n    const key =\n      typeof connection.provider_config_key === \"string\"\n        ? connection.provider_config_key\n        : \"\";\n    const tags = connection.tags as Record<string, unknown> | undefined;\n    return allowed.includes(key) && tags?.end_user_id === identity;\n  });\n}\n\nasync function storedConnections(\n  identity: string,\n  organizationId: string,\n  cid: string,\n) {\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const { data, error } = await sup.client\n    .from(\"payment_nango_connections\")\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .eq(\"organization_id\", organizationId)\n    .order(\"updated_at\", { ascending: false });\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load payment connection status\" },\n      { status: 502 },\n    );\n  const configured = configuredPaymentIntegrations();\n  const known = new Set(configured.map((item) => item.id));\n  const integrations = [\n    ...configured,\n    ...(data ?? [])\n      .map((row) => row.provider_config_key)\n      .filter(\n        (id): id is string =>\n          typeof id === \"string\" && Boolean(id) && !known.has(id),\n      )\n      .map((id) => ({ id, label: paymentIntegrationLabel(id) })),\n  ];\n  return correlatedJson(cid, {\n    data: (data ?? []).map((row) => ({\n      ...row,\n      status: row.active ? \"connected\" : \"needs_attention\",\n      error: row.active ? null : \"Payment provider is disconnected\",\n    })),\n    integrations,\n    source: \"ledger-fallback\",\n  });\n}\n\nexport async function GET(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) return staff.response;\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return storedConnections(identity, organization.id, cid);\n  try {\n    const remote = await nangoConnections(apiKey, identity, allowed);\n    const rows: PaymentNangoConnection[] = remote.map((connection) => {\n      const errors = nangoErrorMessage(connection.errors);\n      return {\n        provider_config_key: String(connection.provider_config_key),\n        nango_connection_id: String(connection.connection_id),\n        provider:\n          typeof connection.provider === \"string\" ? connection.provider : null,\n        active: !errors,\n        status: errors ? \"needs_attention\" : \"connected\",\n        error: errors,\n        updated_at: String(\n          connection.updated_at ??\n            connection.created ??\n            new Date().toISOString(),\n        ),\n      };\n    });\n    return correlatedJson(cid, {\n      data: rows,\n      integrations: configuredPaymentIntegrations(),\n      source: \"nango\",\n    });\n  } catch {\n    return storedConnections(identity, organization.id, cid);\n  }\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n\n  const parsed = await parseAdminJson(request, connectionSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey)) {\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  }\n  if (providerConfigKey === \"stripe\" && !stripeAvailableForMerchant()) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Stripe is not available for merchants in this country\",\n        code: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n      },\n      { status: 409 },\n    );\n  }\n\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const organizationId = organization.id;\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" },\n  );\n  const payload = (await response.json().catch(() => ({}))) as Record<\n    string,\n    unknown\n  > & { data?: Record<string, unknown> };\n  if (!response.ok) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to verify payment provider connection\" },\n      { status: 502 },\n    );\n  }\n  const remote = payload.data ?? payload;\n  if (String(remote.connection_id ?? connectionId) !== connectionId)\n    return correlatedJson(\n      cid,\n      { error: \"Payment provider connection could not be verified\" },\n      { status: 409 },\n    );\n\n  const { data, error } = await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .upsert(\n      {\n        provider_config_key: providerConfigKey,\n        nango_connection_id: connectionId,\n        merchant_identity: identity,\n        organization_id: organizationId,\n        provider: typeof remote.provider === \"string\" ? remote.provider : null,\n        metadata: { source: \"nango-connect-ui\" },\n        active: true,\n        updated_at: new Date().toISOString(),\n      },\n      { onConflict: \"provider_config_key,merchant_identity\" },\n    )\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .single();\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to persist payment provider connection\" },\n      { status: 502 },\n    );\n  return correlatedJson(\n    cid,\n    { data: { ...data, status: \"connected\", error: null } },\n    { status: 201 },\n  );\n}\n\nasync function deleteHandler(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.nango_connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey))\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    {\n      method: \"DELETE\",\n      headers: { Authorization: `Bearer ${apiKey}` },\n    },\n  );\n  if (!response.ok && response.status !== 404)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to disconnect payment provider\" },\n      { status: 502 },\n    );\n  await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .update({ active: false, updated_at: new Date().toISOString() })\n    .eq(\"organization_id\", organization.id)\n    .eq(\"provider_config_key\", providerConfigKey)\n    .eq(\"nango_connection_id\", connectionId);\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/connections:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/payments/connections:DELETE\", deleteHandler);\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/delete_admin_payments_connectionsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete_admin_payments_connectionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    get:
      operationId: get_admin_payments_connections
      summary: "Read Admin / Payments / Connections"
      description: "Admin / Payments / Connections. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport {\n  configuredPaymentIntegrations,\n  nangoErrorMessage,\n  paymentIntegrationLabel,\n  type PaymentNangoConnection,\n} from \"@/lib/payment-nango\";\nimport {\n  resolveStaffOrganization,\n  organizationCanManagePayments,\n} from \"@/lib/staff-organization\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst connectionSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nconst disconnectSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    nango_connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nasync function nangoConnections(\n  apiKey: string,\n  identity: string,\n  allowed: string[],\n) {\n  const query = new URLSearchParams({\n    \"tags[end_user_id]\": identity,\n    limit: \"100\",\n  });\n  const response = await fetch(\n    `https://api.nango.dev/connections?${query.toString()}`,\n    {\n      headers: { Authorization: `Bearer ${apiKey}` },\n      cache: \"no-store\",\n    },\n  );\n  const payload = (await response.json().catch(() => ({}))) as {\n    connections?: Array<Record<string, unknown>>;\n  };\n  if (!response.ok) throw new Error(\"Nango connection status unavailable\");\n  return (payload.connections ?? []).filter((connection) => {\n    const key =\n      typeof connection.provider_config_key === \"string\"\n        ? connection.provider_config_key\n        : \"\";\n    const tags = connection.tags as Record<string, unknown> | undefined;\n    return allowed.includes(key) && tags?.end_user_id === identity;\n  });\n}\n\nasync function storedConnections(\n  identity: string,\n  organizationId: string,\n  cid: string,\n) {\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const { data, error } = await sup.client\n    .from(\"payment_nango_connections\")\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .eq(\"organization_id\", organizationId)\n    .order(\"updated_at\", { ascending: false });\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load payment connection status\" },\n      { status: 502 },\n    );\n  const configured = configuredPaymentIntegrations();\n  const known = new Set(configured.map((item) => item.id));\n  const integrations = [\n    ...configured,\n    ...(data ?? [])\n      .map((row) => row.provider_config_key)\n      .filter(\n        (id): id is string =>\n          typeof id === \"string\" && Boolean(id) && !known.has(id),\n      )\n      .map((id) => ({ id, label: paymentIntegrationLabel(id) })),\n  ];\n  return correlatedJson(cid, {\n    data: (data ?? []).map((row) => ({\n      ...row,\n      status: row.active ? \"connected\" : \"needs_attention\",\n      error: row.active ? null : \"Payment provider is disconnected\",\n    })),\n    integrations,\n    source: \"ledger-fallback\",\n  });\n}\n\nexport async function GET(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) return staff.response;\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return storedConnections(identity, organization.id, cid);\n  try {\n    const remote = await nangoConnections(apiKey, identity, allowed);\n    const rows: PaymentNangoConnection[] = remote.map((connection) => {\n      const errors = nangoErrorMessage(connection.errors);\n      return {\n        provider_config_key: String(connection.provider_config_key),\n        nango_connection_id: String(connection.connection_id),\n        provider:\n          typeof connection.provider === \"string\" ? connection.provider : null,\n        active: !errors,\n        status: errors ? \"needs_attention\" : \"connected\",\n        error: errors,\n        updated_at: String(\n          connection.updated_at ??\n            connection.created ??\n            new Date().toISOString(),\n        ),\n      };\n    });\n    return correlatedJson(cid, {\n      data: rows,\n      integrations: configuredPaymentIntegrations(),\n      source: \"nango\",\n    });\n  } catch {\n    return storedConnections(identity, organization.id, cid);\n  }\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n\n  const parsed = await parseAdminJson(request, connectionSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey)) {\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  }\n  if (providerConfigKey === \"stripe\" && !stripeAvailableForMerchant()) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Stripe is not available for merchants in this country\",\n        code: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n      },\n      { status: 409 },\n    );\n  }\n\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const organizationId = organization.id;\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" },\n  );\n  const payload = (await response.json().catch(() => ({}))) as Record<\n    string,\n    unknown\n  > & { data?: Record<string, unknown> };\n  if (!response.ok) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to verify payment provider connection\" },\n      { status: 502 },\n    );\n  }\n  const remote = payload.data ?? payload;\n  if (String(remote.connection_id ?? connectionId) !== connectionId)\n    return correlatedJson(\n      cid,\n      { error: \"Payment provider connection could not be verified\" },\n      { status: 409 },\n    );\n\n  const { data, error } = await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .upsert(\n      {\n        provider_config_key: providerConfigKey,\n        nango_connection_id: connectionId,\n        merchant_identity: identity,\n        organization_id: organizationId,\n        provider: typeof remote.provider === \"string\" ? remote.provider : null,\n        metadata: { source: \"nango-connect-ui\" },\n        active: true,\n        updated_at: new Date().toISOString(),\n      },\n      { onConflict: \"provider_config_key,merchant_identity\" },\n    )\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .single();\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to persist payment provider connection\" },\n      { status: 502 },\n    );\n  return correlatedJson(\n    cid,\n    { data: { ...data, status: \"connected\", error: null } },\n    { status: 201 },\n  );\n}\n\nasync function deleteHandler(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.nango_connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey))\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    {\n      method: \"DELETE\",\n      headers: { Authorization: `Bearer ${apiKey}` },\n    },\n  );\n  if (!response.ok && response.status !== 404)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to disconnect payment provider\" },\n      { status: 502 },\n    );\n  await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .update({ active: false, updated_at: new Date().toISOString() })\n    .eq(\"organization_id\", organization.id)\n    .eq(\"provider_config_key\", providerConfigKey)\n    .eq(\"nango_connection_id\", connectionId);\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/connections:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/payments/connections:DELETE\", deleteHandler);\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_payments_connectionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_payments_connections
      summary: "Create or execute Admin / Payments / Connections"
      description: "Admin / Payments / Connections. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport {\n  configuredPaymentIntegrations,\n  nangoErrorMessage,\n  paymentIntegrationLabel,\n  type PaymentNangoConnection,\n} from \"@/lib/payment-nango\";\nimport {\n  resolveStaffOrganization,\n  organizationCanManagePayments,\n} from \"@/lib/staff-organization\";\nimport {\n  stripeAvailableForMerchant,\n  STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n} from \"@/lib/payment-country-policy\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst connectionSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nconst disconnectSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    nango_connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nasync function nangoConnections(\n  apiKey: string,\n  identity: string,\n  allowed: string[],\n) {\n  const query = new URLSearchParams({\n    \"tags[end_user_id]\": identity,\n    limit: \"100\",\n  });\n  const response = await fetch(\n    `https://api.nango.dev/connections?${query.toString()}`,\n    {\n      headers: { Authorization: `Bearer ${apiKey}` },\n      cache: \"no-store\",\n    },\n  );\n  const payload = (await response.json().catch(() => ({}))) as {\n    connections?: Array<Record<string, unknown>>;\n  };\n  if (!response.ok) throw new Error(\"Nango connection status unavailable\");\n  return (payload.connections ?? []).filter((connection) => {\n    const key =\n      typeof connection.provider_config_key === \"string\"\n        ? connection.provider_config_key\n        : \"\";\n    const tags = connection.tags as Record<string, unknown> | undefined;\n    return allowed.includes(key) && tags?.end_user_id === identity;\n  });\n}\n\nasync function storedConnections(\n  identity: string,\n  organizationId: string,\n  cid: string,\n) {\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const { data, error } = await sup.client\n    .from(\"payment_nango_connections\")\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .eq(\"organization_id\", organizationId)\n    .order(\"updated_at\", { ascending: false });\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to load payment connection status\" },\n      { status: 502 },\n    );\n  const configured = configuredPaymentIntegrations();\n  const known = new Set(configured.map((item) => item.id));\n  const integrations = [\n    ...configured,\n    ...(data ?? [])\n      .map((row) => row.provider_config_key)\n      .filter(\n        (id): id is string =>\n          typeof id === \"string\" && Boolean(id) && !known.has(id),\n      )\n      .map((id) => ({ id, label: paymentIntegrationLabel(id) })),\n  ];\n  return correlatedJson(cid, {\n    data: (data ?? []).map((row) => ({\n      ...row,\n      status: row.active ? \"connected\" : \"needs_attention\",\n      error: row.active ? null : \"Payment provider is disconnected\",\n    })),\n    integrations,\n    source: \"ledger-fallback\",\n  });\n}\n\nexport async function GET(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:read\");\n  if (!staff.ok) return staff.response;\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey) return storedConnections(identity, organization.id, cid);\n  try {\n    const remote = await nangoConnections(apiKey, identity, allowed);\n    const rows: PaymentNangoConnection[] = remote.map((connection) => {\n      const errors = nangoErrorMessage(connection.errors);\n      return {\n        provider_config_key: String(connection.provider_config_key),\n        nango_connection_id: String(connection.connection_id),\n        provider:\n          typeof connection.provider === \"string\" ? connection.provider : null,\n        active: !errors,\n        status: errors ? \"needs_attention\" : \"connected\",\n        error: errors,\n        updated_at: String(\n          connection.updated_at ??\n            connection.created ??\n            new Date().toISOString(),\n        ),\n      };\n    });\n    return correlatedJson(cid, {\n      data: rows,\n      integrations: configuredPaymentIntegrations(),\n      source: \"nango\",\n    });\n  } catch {\n    return storedConnections(identity, organization.id, cid);\n  }\n}\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n\n  const parsed = await parseAdminJson(request, connectionSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey)) {\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  }\n  if (providerConfigKey === \"stripe\" && !stripeAvailableForMerchant()) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Stripe is not available for merchants in this country\",\n        code: STRIPE_UNAVAILABLE_IN_MERCHANT_COUNTRY,\n      },\n      { status: 409 },\n    );\n  }\n\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const organizationId = organization.id;\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    { headers: { Authorization: `Bearer ${apiKey}` }, cache: \"no-store\" },\n  );\n  const payload = (await response.json().catch(() => ({}))) as Record<\n    string,\n    unknown\n  > & { data?: Record<string, unknown> };\n  if (!response.ok) {\n    return correlatedJson(\n      cid,\n      { error: \"Unable to verify payment provider connection\" },\n      { status: 502 },\n    );\n  }\n  const remote = payload.data ?? payload;\n  if (String(remote.connection_id ?? connectionId) !== connectionId)\n    return correlatedJson(\n      cid,\n      { error: \"Payment provider connection could not be verified\" },\n      { status: 409 },\n    );\n\n  const { data, error } = await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .upsert(\n      {\n        provider_config_key: providerConfigKey,\n        nango_connection_id: connectionId,\n        merchant_identity: identity,\n        organization_id: organizationId,\n        provider: typeof remote.provider === \"string\" ? remote.provider : null,\n        metadata: { source: \"nango-connect-ui\" },\n        active: true,\n        updated_at: new Date().toISOString(),\n      },\n      { onConflict: \"provider_config_key,merchant_identity\" },\n    )\n    .select(\n      \"provider_config_key,nango_connection_id,provider,active,updated_at\",\n    )\n    .single();\n  if (error)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to persist payment provider connection\" },\n      { status: 502 },\n    );\n  return correlatedJson(\n    cid,\n    { data: { ...data, status: \"connected\", error: null } },\n    { status: 201 },\n  );\n}\n\nasync function deleteHandler(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const membershipSup = adminSupabaseOr503(cid);\n  if (\"response\" in membershipSup) return membershipSup.response;\n  const organization = await resolveStaffOrganization(\n    membershipSup.client,\n    staff.session.user?.email,\n  );\n  if (!organization || !organizationCanManagePayments(organization.role))\n    return correlatedJson(\n      cid,\n      { error: \"Only the organization owner can manage payment connections\" },\n      { status: 403 },\n    );\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n  const parsed = await parseAdminJson(request, disconnectSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const providerConfigKey = parsed.data.provider_config_key;\n  const connectionId = parsed.data.nango_connection_id;\n  const allowed = configuredPaymentIntegrations().map((item) => item.id);\n  if (!connectionId || !allowed.includes(providerConfigKey))\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  const response = await fetch(\n    `https://api.nango.dev/connections/${encodeURIComponent(connectionId)}?provider_config_key=${encodeURIComponent(providerConfigKey)}`,\n    {\n      method: \"DELETE\",\n      headers: { Authorization: `Bearer ${apiKey}` },\n    },\n  );\n  if (!response.ok && response.status !== 404)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to disconnect payment provider\" },\n      { status: 502 },\n    );\n  await membershipSup.client\n    .from(\"payment_nango_connections\")\n    .update({ active: false, updated_at: new Date().toISOString() })\n    .eq(\"organization_id\", organization.id)\n    .eq(\"provider_config_key\", providerConfigKey)\n    .eq(\"nango_connection_id\", connectionId);\n  return correlatedJson(cid, { data: { disconnected: true } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/connections:POST\", post);\nexport const DELETE = withAdminMutationIdempotency(\"/admin/payments/connections:DELETE\", deleteHandler);\n"
      x-permission: "settings:read"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_connectionsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_connectionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/connections/reconnect:
    post:
      operationId: post_admin_payments_connections_reconnect
      summary: "Create or execute Admin / Payments / Connections / Reconnect"
      description: "Admin / Payments / Connections / Reconnect. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { configuredPaymentIntegrations } from \"@/lib/payment-nango\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst reconnectSchema = z\n  .object({\n    provider_config_key: z.string().trim().min(1).max(100),\n    nango_connection_id: z.string().trim().min(1).max(200),\n  })\n  .strict();\n\nasync function post(request: Request) {\n  const cid = getCorrelationId(request);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const apiKey = process.env.NANGO_API_KEY?.trim();\n  if (!apiKey)\n    return correlatedJson(\n      cid,\n      { error: \"Nango is not configured\" },\n      { status: 503 },\n    );\n  const parsed = await parseAdminJson(request, reconnectSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const integrationId = parsed.data.provider_config_key;\n  const connectionId = parsed.data.nango_connection_id;\n  if (\n    !connectionId ||\n    !configuredPaymentIntegrations().some((item) => item.id === integrationId)\n  )\n    return correlatedJson(\n      cid,\n      { error: \"Invalid payment connection\" },\n      { status: 400 },\n    );\n  const identity = (staff.session.user?.email ?? \"local-admin@localhost\")\n    .trim()\n    .toLowerCase();\n  const sup = adminSupabaseOr503(cid);\n  if (!(\"response\" in sup)) {\n    const organization = await resolveStaffOrganization(\n      sup.client,\n      staff.session.user?.email,\n    );\n    if (!organization)\n      return correlatedJson(\n        cid,\n        { error: \"Organization membership is not configured\" },\n        { status: 403 },\n      );\n    const { data } = await sup.client\n      .from(\"payment_nango_connections\")\n      .select(\"nango_connection_id\")\n      .eq(\"organization_id\", organization.id)\n      .eq(\"merchant_identity\", identity)\n      .eq(\"provider_config_key\", integrationId)\n      .eq(\"nango_connection_id\", connectionId)\n      .maybeSingle();\n    if (!data)\n      return correlatedJson(\n        cid,\n        { error: \"Payment connection not found\" },\n        { status: 404 },\n      );\n  }\n  const response = await fetch(\n    \"https://api.nango.dev/connect/sessions/reconnect\",\n    {\n      method: \"POST\",\n      headers: {\n        Authorization: `Bearer ${apiKey}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        connection_id: connectionId,\n        integration_id: integrationId,\n        tags: { end_user_id: identity, end_user_email: identity },\n      }),\n    },\n  );\n  const payload = (await response.json().catch(() => ({}))) as {\n    data?: { token?: string };\n  };\n  if (!response.ok || !payload.data?.token)\n    return correlatedJson(\n      cid,\n      { error: \"Unable to start payment provider reconnect\" },\n      { status: 502 },\n    );\n  return correlatedJson(cid, {\n    data: { session_token: payload.data.token, integration_id: integrationId },\n  });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/connections/reconnect:POST\", post);\n"
      x-permission: "settings:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_connections_reconnectRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_connections_reconnectResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/payments/provider-operation:
    post:
      operationId: post_admin_payments_provider_operation
      summary: "Create or execute Admin / Payments / Provider Operation"
      description: "Admin / Payments / Provider Operation. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Payments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport {\n  requestProviderReconciliationJob,\n  upsertPaymentProviderArtifact,\n} from \"@universal-music-store/platform-data\";\n\nconst xenditSchema = z.discriminatedUnion(\"operation\", [\n  z\n    .object({\n      provider: z.literal(\"xendit\"),\n      operation: z.literal(\"reconcile\"),\n      period_start: z.string().datetime(),\n      period_end: z.string().datetime(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      provider: z.literal(\"xendit\"),\n      operation: z.literal(\"future_charge\"),\n      payment_token_id: z.string().trim().min(8).max(160),\n      reference_id: z.string().trim().min(1).max(127),\n      amount_minor: z.number().int().positive().max(10_000_000_000),\n      currency: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{3}$/),\n      country: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{2}$/),\n      channel_code: z.string().trim().min(2).max(80),\n      channel_properties: z.record(z.string(), z.unknown()).default({}),\n      metadata: z.record(z.string(), z.string().max(500)).optional(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      provider: z.literal(\"xendit\"),\n      operation: z.literal(\"payout\"),\n      reference_id: z.string().trim().min(1).max(127),\n      channel_code: z.string().trim().min(2).max(80),\n      amount_minor: z.number().int().positive().max(10_000_000_000),\n      currency: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{3}$/),\n      channel_properties: z.record(z.string(), z.unknown()),\n      description: z.string().trim().max(255).optional(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n]);\n\nconst stripeSchema = z.discriminatedUnion(\"operation\", [\n  z\n    .object({\n      operation: z.literal(\"reconcile\"),\n      period_start: z.string().datetime(),\n      period_end: z.string().datetime(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"setup_intent\"),\n      customer: z.string().trim().max(255).optional(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"subscription\"),\n      customer: z.string().trim().min(1).max(255),\n      items: z\n        .array(\n          z\n            .object({\n              price: z.string().trim().min(1).max(255),\n              quantity: z.number().int().positive().max(1000).default(1),\n            })\n            .strict(),\n        )\n        .min(1)\n        .max(50),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"invoice\"),\n      customer: z.string().trim().min(1).max(255).optional(),\n      customer_email: z.string().email().max(320).optional(),\n      invoice_id: z.string().trim().min(1).max(255).optional(),\n      currency: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{3}$/),\n      items: z\n        .array(\n          z\n            .object({\n              amount_minor: z.number().int().positive().max(10_000_000_000),\n              description: z.string().trim().min(1).max(500),\n            })\n            .strict(),\n        )\n        .min(1)\n        .max(100),\n      action: z.enum([\"draft\", \"finalize\", \"pay\", \"send\"]).default(\"draft\"),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"payment\"),\n      action: z.enum([\"retrieve\", \"authorize\", \"capture\", \"void\"]),\n      order_id: z.string().trim().min(1).max(255).optional(),\n      authorization_id: z.string().trim().min(1).max(255).optional(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"payment_link\"),\n      action: z.enum([\"create\", \"retrieve\", \"deactivate\"]),\n      payment_resource_id: z.string().trim().min(1).max(255).optional(),\n      payload: z.record(z.string(), z.unknown()).default({}),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"dispute\"),\n      dispute_id: z.string().trim().min(1).max(255),\n      action: z.enum([\"retrieve\", \"update\", \"close\"]),\n      evidence: z.record(z.string(), z.string().max(10000)).optional(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"payout\"),\n      amount_minor: z.number().int().positive().max(10_000_000_000),\n      currency: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{3}$/),\n      method: z.enum([\"standard\", \"instant\"]).default(\"standard\"),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"connect_account\"),\n      country: z\n        .string()\n        .trim()\n        .regex(/^[A-Za-z]{2}$/),\n      email: z.string().email().max(320).optional(),\n      return_url: z\n        .string()\n        .url()\n        .refine((value) => value.startsWith(\"https://\"), \"HTTPS required\"),\n      refresh_url: z\n        .string()\n        .url()\n        .refine((value) => value.startsWith(\"https://\"), \"HTTPS required\"),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n]);\n\nconst paypalSchema = z.discriminatedUnion(\"operation\", [\n  z\n    .object({\n      operation: z.literal(\"reconcile\"),\n      period_start: z.string().datetime(),\n      period_end: z.string().datetime(),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"invoice\"),\n      action: z.enum([\"create\", \"send\", \"remind\", \"cancel\"]),\n      invoice_id: z.string().trim().min(1).max(255).optional(),\n      payload: z.record(z.string(), z.unknown()).default({}),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"subscription\"),\n      action: z.enum([\"create\", \"suspend\", \"cancel\"]),\n      subscription_id: z.string().trim().min(1).max(255).optional(),\n      payload: z.record(z.string(), z.unknown()).default({}),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"dispute\"),\n      action: z.enum([\n        \"get\",\n        \"evidence\",\n        \"accept\",\n        \"escalate\",\n        \"deny\",\n        \"adjudicate\",\n      ]),\n      dispute_id: z.string().trim().min(1).max(255),\n      payload: z.record(z.string(), z.unknown()).default({}),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n  z\n    .object({\n      operation: z.literal(\"payout\"),\n      payload: z.record(z.string(), z.unknown()),\n      idempotency_key: z.string().trim().min(8).max(255),\n    })\n    .strict(),\n]);\n\nconst paymentOperationSchema = z.union([\n  stripeSchema.and(z.object({ provider: z.literal(\"stripe\") })),\n  paypalSchema.and(z.object({ provider: z.literal(\"paypal\") })),\n  xenditSchema.and(z.object({ provider: z.literal(\"xendit\") })),\n]);\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) return staff.response;\n  const raw = (await req.json().catch(() => null)) as Record<\n    string,\n    unknown\n  > | null;\n  const parsed = paymentOperationSchema.safeParse(raw);\n  if (!parsed.success)\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid payment operation payload\" },\n      { status: 400 },\n    );\n  if (\n    parsed.data.provider === \"stripe\" &&\n    parsed.data.operation === \"invoice\" &&\n    !parsed.data.customer &&\n    !parsed.data.customer_email &&\n    !parsed.data.invoice_id\n  ) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invoice customer or invoice id is required\" },\n      { status: 400 },\n    );\n  }\n\n  const supabase = adminSupabaseOr503(correlationId);\n  if (\"response\" in supabase) return supabase.response;\n  const organization = await resolveStaffOrganization(\n    supabase.client,\n    staff.session.user?.email,\n  );\n  if (!organization)\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const { data: connection } = await supabase.client\n    .from(\"payment_nango_connections\")\n    .select(\"nango_connection_id,provider_config_key\")\n    .eq(\"organization_id\", organization.id)\n    .eq(\"provider\", parsed.data.provider)\n    .eq(\"active\", true)\n    .order(\"updated_at\", { ascending: false })\n    .limit(1)\n    .maybeSingle();\n  if (\n    (parsed.data.provider === \"stripe\" || parsed.data.provider === \"paypal\") &&\n    parsed.data.operation !== \"reconcile\" &&\n    !connection?.nango_connection_id\n  ) {\n    return correlatedJson(\n      correlationId,\n      {\n        error: \"Payment provider connection is not configured\",\n        code: \"PAYMENT_CONNECTION_REQUIRED\",\n      },\n      { status: 503 },\n    );\n  }\n  if (parsed.data.operation === \"reconcile\") {\n    const start = new Date(parsed.data.period_start);\n    const end = new Date(parsed.data.period_end);\n    if (!(start < end)) {\n      return correlatedJson(\n        correlationId,\n        { error: \"Reconciliation period_start must be before period_end\" },\n        { status: 400 },\n      );\n    }\n    const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n    const providerRequest = { ...((({ provider: _provider, ...operation }) => operation)(parsed.data)), ...(parsed.data.provider === \"xendit\" ? {\n      payment_request_ids: (await supabase.client\n        .from(\"payment_provider_artifacts\")\n        .select(\"external_id\")\n        .eq(\"organization_id\", organization.id)\n        .eq(\"provider\", \"xendit\")\n        .in(\"artifact_type\", [\"payment_request\", \"payment_intent\"])\n        .gte(\"created_at\", start.toISOString())\n        .lt(\"created_at\", end.toISOString())\n        .limit(100)).data?.map((row) => row.external_id).filter((id): id is string => typeof id === \"string\" && id.length > 0),\n    } : {}) };\n    if (internalToken && (parsed.data.provider !== \"xendit\" || (providerRequest.payment_request_ids?.length ?? 0) > 0)) {\n      const providerResponse = await medusaAdminFetch(`/admin/payment-provider/${parsed.data.provider}`, {\n        method: \"POST\",\n        headers: {\n          \"x-uvs-internal-token\": internalToken,\n          ...(connection?.nango_connection_id ? { \"x-nango-connection-id\": connection.nango_connection_id } : {}),\n          ...(connection?.provider_config_key ? { \"x-nango-provider-config-key\": connection.provider_config_key } : {}),\n        },\n        body: JSON.stringify(providerRequest),\n      });\n      const providerBody = await providerResponse.json().catch(() => ({ error: \"Payment provider reconciliation failed\" }));\n      if (providerResponse.ok) {\n        const report = providerBody && typeof providerBody === \"object\" && \"data\" in providerBody ? (providerBody as { data: unknown }).data : providerBody;\n        const externalId = `reconcile:${parsed.data.provider}:${start.toISOString()}:${end.toISOString()}`;\n        await upsertPaymentProviderArtifact(supabase.client, {\n          organization_id: organization.id,\n          merchant_identity: connection?.nango_connection_id ?? organization.id,\n          provider: parsed.data.provider,\n          artifact_type: \"reconciliation\",\n          external_id: externalId,\n          status: \"synced\",\n          idempotency_key: parsed.data.idempotency_key,\n          metadata: { period_start: start.toISOString(), period_end: end.toISOString(), provider_report: report },\n        });\n        return correlatedJson(correlationId, { data: { provider_api_pull: true, provider_report: report, artifact_external_id: externalId } });\n      }\n    }\n    const result = await requestProviderReconciliationJob(supabase.client, {\n      organizationId: organization.id,\n      merchantIdentity: connection?.nango_connection_id ?? organization.id,\n      provider: parsed.data.provider,\n      periodStart: start.toISOString(),\n      periodEnd: end.toISOString(),\n      idempotencyKey: parsed.data.idempotency_key,\n      createdBy: staff.session.user?.email ?? \"admin\",\n    });\n    if (!result.jobId) {\n      return correlatedJson(\n        correlationId,\n        { error: \"Unable to enqueue reconciliation job\" },\n        { status: 503 },\n      );\n    }\n    return correlatedJson(correlationId, {\n      data: {\n        job_id: result.jobId,\n        artifact_external_id: result.artifactExternalId,\n        reused: result.reused,\n        provider_api_pull: false,\n      },\n    });\n  }\n  const internalToken = process.env.MEDUSA_INTERNAL_ADMIN_TOKEN?.trim();\n  if (!internalToken)\n    return correlatedJson(\n      correlationId,\n      { error: \"Payment operations are not configured\" },\n      { status: 503 },\n    );\n  const response = await medusaAdminFetch(\n    `/admin/payment-provider/${parsed.data.provider}`,\n    {\n      method: \"POST\",\n      headers: {\n        \"x-uvs-internal-token\": internalToken,\n        ...(connection?.nango_connection_id\n          ? { \"x-nango-connection-id\": connection.nango_connection_id }\n          : {}),\n        ...(connection?.provider_config_key\n          ? { \"x-nango-provider-config-key\": connection.provider_config_key }\n          : {}),\n      },\n      body: JSON.stringify(\n        (({ provider: _provider, ...operation }) => operation)(parsed.data),\n      ),\n    },\n  );\n  const body = await response\n    .json()\n    .catch(() => ({ error: \"Payment operation failed\" }));\n  if (response.ok && body && typeof body === \"object\") {\n    const data = (body as { data?: unknown }).data;\n    const externalId =\n      data &&\n      typeof data === \"object\" &&\n      typeof (data as { id?: unknown }).id === \"string\"\n        ? (data as { id: string }).id\n        : null;\n    const artifactType =\n      parsed.data.operation === \"invoice\"\n        ? \"invoice\"\n        : parsed.data.operation === \"payment_link\"\n          ? \"payment_link\"\n          : parsed.data.operation === \"payment\"\n            ? parsed.data.action === \"authorize\" || parsed.data.action === \"void\"\n              ? \"authorization\"\n              : parsed.data.action === \"capture\"\n                ? \"capture\"\n                : \"checkout_session\"\n            : parsed.data.operation === \"subscription\"\n              ? \"payment_request\"\n              : parsed.data.operation === \"dispute\"\n                ? \"dispute\"\n                : parsed.data.operation === \"payout\"\n                  ? \"payout\"\n                  : parsed.data.operation === \"setup_intent\" ||\n                      parsed.data.operation === \"future_charge\"\n                    ? \"payment_token\"\n                    : null;\n    if (externalId && artifactType) {\n      await upsertPaymentProviderArtifact(supabase.client, {\n        organization_id: organization.id,\n        merchant_identity: connection?.nango_connection_id ?? organization.id,\n        provider: parsed.data.provider,\n        artifact_type: artifactType,\n        external_id: externalId,\n        parent_external_id:\n          \"invoice_id\" in parsed.data\n            ? parsed.data.invoice_id\n            : \"subscription_id\" in parsed.data\n              ? parsed.data.subscription_id\n              : \"dispute_id\" in parsed.data\n                ? parsed.data.dispute_id\n                : \"order_id\" in parsed.data\n                  ? parsed.data.order_id\n                  : \"authorization_id\" in parsed.data\n                    ? parsed.data.authorization_id\n                    : null,\n        status:\n          data &&\n          typeof data === \"object\" &&\n          typeof (data as { status?: unknown }).status === \"string\"\n            ? (data as { status: string }).status\n            : \"created\",\n        currency:\n          \"currency\" in parsed.data && typeof parsed.data.currency === \"string\"\n            ? parsed.data.currency.toUpperCase()\n            : null,\n        idempotency_key: parsed.data.idempotency_key,\n        metadata: { operation: parsed.data.operation },\n      });\n    }\n  }\n  return correlatedJson(correlationId, body, { status: response.status });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/payments/provider-operation:POST\", post);\n"
      x-permission: "settings:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_payments_provider_operationRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_payments_provider_operationResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/pin-approval:
    post:
      operationId: post_admin_pin_approval
      summary: "Create or execute Admin / Pin Approval"
      description: "Admin / Pin Approval. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Pin Approval"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { requirePinApproval } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst pinApprovalSchema = z.object({\n  approver_employee_id: z.string().uuid(),\n  pin: z.string().regex(/^\\d{4,8}$/),\n  required_role: z.enum([\"admin\", \"manager\"]).default(\"manager\"),\n}).strict();\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, cid);\n  }\n\n  const parsed = await parseAdminJson(req, pinApprovalSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const sessionOrganization = await resolveStaffOrganization(sb, staff.session.user?.email);\n  if (!sessionOrganization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  try {\n    const result = await requirePinApproval(\n      sb,\n      parsed.data.approver_employee_id,\n      parsed.data.pin,\n      parsed.data.required_role,\n    );\n    return correlatedJson(cid, result);\n  } catch {\n    return correlatedJson(cid, { error: \"Unable to verify PIN approval\" }, { status: 503 });\n  }\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/pin-approval:POST\", post);\n"
      x-permission: "pos:use"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_pin_approvalRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_pin_approvalResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/pos/enterprise:
    get:
      operationId: get_admin_pos_enterprise
      summary: "Read Admin / Pos / Enterprise"
      description: "Admin / Pos / Enterprise. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Pos"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { upsertPaymentTerminalArtifactBinding, validateFiscalProfile, validateTerminalCertification, type PaymentProvider } from \"@universal-music-store/platform-data\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst bodySchema = z.discriminatedUnion(\"kind\", [\n  z.object({ kind: z.literal(\"fiscal\"), jurisdiction: z.string().trim().min(2).max(16), registrationNumber: z.string().trim().min(1).max(64), invoicePrefix: z.string().trim().min(1).max(16), enabled: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"certification\"), provider: z.string().trim().min(1).max(120), model: z.string().trim().min(1).max(120), firmware: z.string().trim().min(1).max(80), certificationId: z.string().trim().min(1).max(120), expiresAt: z.string().datetime().nullable().optional() }).strict(),\n  z.object({ kind: z.literal(\"payment_terminal\"), provider: z.string().trim().min(1).max(120), model: z.string().trim().min(1).max(120), serialNumber: z.string().trim().min(1).max(120), deviceId: z.string().uuid().optional(), providerTerminalExternalId: z.string().trim().min(1).max(255).optional(), certificationId: z.string().trim().max(120).optional(), status: z.enum([\"pending\", \"certified\", \"degraded\", \"disabled\"]).default(\"pending\"), metadata: z.record(z.string().max(80), z.unknown()).default({}) }).strict(),\n]);\n\nconst paymentArtifactProviders = new Set<string>([\"stripe\", \"paypal\", \"xendit\"]);\n\nasync function sessionFor(req: NextRequest, permission: \"pos:use\" | \"pos:shift_manage\") {\n  const session = await getStaffSession();\n  if (!session?.user) return { response: correlatedJson(getCorrelationId(req), { error: \"Unauthorized\" }, { status: 401 }) };\n  if (!staffSessionAllows(session, permission)) return { response: correlatedJson(getCorrelationId(req), { error: \"Forbidden\" }, { status: 403 }) };\n  return { session };\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req); const auth = await sessionFor(req, \"pos:use\"); if (\"response\" in auth) return auth.response;\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const [fiscal, certifications, terminals] = await Promise.all([\n    sup.client.from(\"pos_fiscal_profiles\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n    sup.client.from(\"pos_terminal_certifications\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n    sup.client.from(\"pos_payment_terminals\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n  ]);\n  if ([fiscal, certifications, terminals].some((result) => result.error && !/relation .* does not exist/i.test(result.error.message))) return correlatedJson(cid, { error: \"Unable to load POS controls\" }, { status: 503 });\n  return correlatedJson(cid, { data: { fiscal: fiscal.data ?? [], certifications: certifications.data ?? [], paymentTerminals: terminals.data ?? [] } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req); const auth = await sessionFor(req, \"pos:shift_manage\"); if (\"response\" in auth) return auth.response;\n  let parsed; try { parsed = bodySchema.safeParse(await req.json()); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid POS control payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const value = parsed.data;\n  if (value.kind === \"fiscal\") {\n    const fiscal = validateFiscalProfile({ jurisdiction: value.jurisdiction, registrationNumber: value.registrationNumber, invoicePrefix: value.invoicePrefix, enabled: value.enabled });\n    const result = await sup.client.from(\"pos_fiscal_profiles\").upsert({ organization_id: organization.id, jurisdiction: fiscal.jurisdiction, registration_number: fiscal.registrationNumber, invoice_prefix: fiscal.invoicePrefix, enabled: fiscal.enabled }, { onConflict: \"organization_id,jurisdiction,registration_number\" }).select(\"*\").single();\n    if (result.error) return correlatedJson(cid, { error: \"Unable to save fiscal profile\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (value.kind === \"certification\") {\n    const certification = validateTerminalCertification({ provider: value.provider, model: value.model, firmware: value.firmware, certificationId: value.certificationId, expiresAt: value.expiresAt });\n    const result = await sup.client.from(\"pos_terminal_certifications\").upsert({ organization_id: organization.id, provider: certification.provider, model: certification.model, firmware: certification.firmware, certification_id: certification.certificationId, expires_at: certification.expiresAt }, { onConflict: \"organization_id,certification_id\" }).select(\"*\").single();\n    if (result.error) return correlatedJson(cid, { error: \"Unable to save terminal certification\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (value.deviceId && value.providerTerminalExternalId) {\n    if (!paymentArtifactProviders.has(value.provider)) return correlatedJson(cid, { error: \"Unsupported payment artifact provider\" }, { status: 400 });\n    const ok = await upsertPaymentTerminalArtifactBinding(sup.client, { organization_id: organization.id, merchant_identity: organization.id, provider: value.provider as PaymentProvider, external_id: value.providerTerminalExternalId, device_id: value.deviceId, model: value.model, serial_number: value.serialNumber, status: value.status, metadata: { ...value.metadata, certification_id: value.certificationId ?? null } });\n    if (!ok) return correlatedJson(cid, { error: \"Unable to save payment terminal artifact mapping\" }, { status: 503 });\n    const saved = await sup.client.from(\"pos_payment_terminals\").select(\"*\").eq(\"organization_id\", organization.id).eq(\"provider_terminal_external_id\", value.providerTerminalExternalId).single();\n    if (saved.error) return correlatedJson(cid, { error: \"Unable to load payment terminal\" }, { status: 503 });\n    return correlatedJson(cid, { data: saved.data }, { status: 201 });\n  }\n  const result = await sup.client.from(\"pos_payment_terminals\").upsert({ organization_id: organization.id, device_id: value.deviceId ?? null, provider: value.provider, model: value.model, serial_number: value.serialNumber, provider_terminal_external_id: value.providerTerminalExternalId ?? null, certification_id: value.certificationId ?? null, status: value.status, metadata: value.metadata }, { onConflict: \"organization_id,serial_number\" }).select(\"*\").single();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to save payment terminal\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/pos/enterprise:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_pos_enterpriseResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_pos_enterprise
      summary: "Create or execute Admin / Pos / Enterprise"
      description: "Admin / Pos / Enterprise. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Pos"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { upsertPaymentTerminalArtifactBinding, validateFiscalProfile, validateTerminalCertification, type PaymentProvider } from \"@universal-music-store/platform-data\";\nimport { z } from \"zod\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nconst bodySchema = z.discriminatedUnion(\"kind\", [\n  z.object({ kind: z.literal(\"fiscal\"), jurisdiction: z.string().trim().min(2).max(16), registrationNumber: z.string().trim().min(1).max(64), invoicePrefix: z.string().trim().min(1).max(16), enabled: z.boolean().default(false) }).strict(),\n  z.object({ kind: z.literal(\"certification\"), provider: z.string().trim().min(1).max(120), model: z.string().trim().min(1).max(120), firmware: z.string().trim().min(1).max(80), certificationId: z.string().trim().min(1).max(120), expiresAt: z.string().datetime().nullable().optional() }).strict(),\n  z.object({ kind: z.literal(\"payment_terminal\"), provider: z.string().trim().min(1).max(120), model: z.string().trim().min(1).max(120), serialNumber: z.string().trim().min(1).max(120), deviceId: z.string().uuid().optional(), providerTerminalExternalId: z.string().trim().min(1).max(255).optional(), certificationId: z.string().trim().max(120).optional(), status: z.enum([\"pending\", \"certified\", \"degraded\", \"disabled\"]).default(\"pending\"), metadata: z.record(z.string().max(80), z.unknown()).default({}) }).strict(),\n]);\n\nconst paymentArtifactProviders = new Set<string>([\"stripe\", \"paypal\", \"xendit\"]);\n\nasync function sessionFor(req: NextRequest, permission: \"pos:use\" | \"pos:shift_manage\") {\n  const session = await getStaffSession();\n  if (!session?.user) return { response: correlatedJson(getCorrelationId(req), { error: \"Unauthorized\" }, { status: 401 }) };\n  if (!staffSessionAllows(session, permission)) return { response: correlatedJson(getCorrelationId(req), { error: \"Forbidden\" }, { status: 403 }) };\n  return { session };\n}\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req); const auth = await sessionFor(req, \"pos:use\"); if (\"response\" in auth) return auth.response;\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const [fiscal, certifications, terminals] = await Promise.all([\n    sup.client.from(\"pos_fiscal_profiles\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n    sup.client.from(\"pos_terminal_certifications\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n    sup.client.from(\"pos_payment_terminals\").select(\"*\").eq(\"organization_id\", organization.id).order(\"created_at\", { ascending: false }),\n  ]);\n  if ([fiscal, certifications, terminals].some((result) => result.error && !/relation .* does not exist/i.test(result.error.message))) return correlatedJson(cid, { error: \"Unable to load POS controls\" }, { status: 503 });\n  return correlatedJson(cid, { data: { fiscal: fiscal.data ?? [], certifications: certifications.data ?? [], paymentTerminals: terminals.data ?? [] } });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req); const auth = await sessionFor(req, \"pos:shift_manage\"); if (\"response\" in auth) return auth.response;\n  let parsed; try { parsed = bodySchema.safeParse(await req.json()); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid POS control payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, auth.session.user?.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const value = parsed.data;\n  if (value.kind === \"fiscal\") {\n    const fiscal = validateFiscalProfile({ jurisdiction: value.jurisdiction, registrationNumber: value.registrationNumber, invoicePrefix: value.invoicePrefix, enabled: value.enabled });\n    const result = await sup.client.from(\"pos_fiscal_profiles\").upsert({ organization_id: organization.id, jurisdiction: fiscal.jurisdiction, registration_number: fiscal.registrationNumber, invoice_prefix: fiscal.invoicePrefix, enabled: fiscal.enabled }, { onConflict: \"organization_id,jurisdiction,registration_number\" }).select(\"*\").single();\n    if (result.error) return correlatedJson(cid, { error: \"Unable to save fiscal profile\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (value.kind === \"certification\") {\n    const certification = validateTerminalCertification({ provider: value.provider, model: value.model, firmware: value.firmware, certificationId: value.certificationId, expiresAt: value.expiresAt });\n    const result = await sup.client.from(\"pos_terminal_certifications\").upsert({ organization_id: organization.id, provider: certification.provider, model: certification.model, firmware: certification.firmware, certification_id: certification.certificationId, expires_at: certification.expiresAt }, { onConflict: \"organization_id,certification_id\" }).select(\"*\").single();\n    if (result.error) return correlatedJson(cid, { error: \"Unable to save terminal certification\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n  }\n  if (value.deviceId && value.providerTerminalExternalId) {\n    if (!paymentArtifactProviders.has(value.provider)) return correlatedJson(cid, { error: \"Unsupported payment artifact provider\" }, { status: 400 });\n    const ok = await upsertPaymentTerminalArtifactBinding(sup.client, { organization_id: organization.id, merchant_identity: organization.id, provider: value.provider as PaymentProvider, external_id: value.providerTerminalExternalId, device_id: value.deviceId, model: value.model, serial_number: value.serialNumber, status: value.status, metadata: { ...value.metadata, certification_id: value.certificationId ?? null } });\n    if (!ok) return correlatedJson(cid, { error: \"Unable to save payment terminal artifact mapping\" }, { status: 503 });\n    const saved = await sup.client.from(\"pos_payment_terminals\").select(\"*\").eq(\"organization_id\", organization.id).eq(\"provider_terminal_external_id\", value.providerTerminalExternalId).single();\n    if (saved.error) return correlatedJson(cid, { error: \"Unable to load payment terminal\" }, { status: 503 });\n    return correlatedJson(cid, { data: saved.data }, { status: 201 });\n  }\n  const result = await sup.client.from(\"pos_payment_terminals\").upsert({ organization_id: organization.id, device_id: value.deviceId ?? null, provider: value.provider, model: value.model, serial_number: value.serialNumber, provider_terminal_external_id: value.providerTerminalExternalId ?? null, certification_id: value.certificationId ?? null, status: value.status, metadata: value.metadata }, { onConflict: \"organization_id,serial_number\" }).select(\"*\").single();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to save payment terminal\" }, { status: 503 }); return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/pos/enterprise:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_pos_enterpriseRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_pos_enterpriseResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/pos/feature-mappings:
    get:
      operationId: get_admin_pos_feature_mappings
      summary: "Read Admin / Pos / Feature Mappings"
      description: "Admin / Pos / Feature Mappings. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Pos"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { POS_FEATURE_MAPPINGS } from \"@universal-music-store/platform-data\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\n\nexport async function GET(req: NextRequest) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  return correlatedJson(correlationId, { data: POS_FEATURE_MAPPINGS });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_pos_feature_mappingsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/profile:
    patch:
      operationId: patch_admin_profile
      summary: "Update Admin / Profile"
      description: "Admin / Profile. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Profile"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nasync function patch(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"settings:write\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, cid);\n  }\n  const session = staff.session;\n  if (!session?.user?.email) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const raw = body as { name?: unknown };\n  const name =\n    typeof raw.name === \"string\" ? raw.name.trim().slice(0, 200) : \"\";\n\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n\n  const email = session.user.email.trim().toLowerCase();\n  const { error } = await sup.client\n    .from(\"users\")\n    .update({\n      name: name.length > 0 ? name : null,\n      updated_at: new Date().toISOString(),\n    })\n    .eq(\"email\", email);\n\n  if (error) {\n    return correlatedJson(cid, { error: error.message }, { status: 500 });\n  }\n\n  return correlatedJson(cid, { ok: true, name: name.length > 0 ? name : null });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/profile:PATCH\", patch);\n"
      x-permission: "settings:write"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_profileRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_profileResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/profile/avatar:
    get:
      operationId: get_admin_profile_avatar
      summary: "Read Admin / Profile / Avatar"
      description: "Admin / Profile / Avatar. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Profile"]
      x-source: "import { readdir, readFile } from \"node:fs/promises\";\nimport path from \"node:path\";\nimport { NextResponse } from \"next/server\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\n\n// The source asset is intentionally served through the admin origin so the\n// admin app does not depend on the storefront's public directory.\nexport const runtime = \"nodejs\";\n\nfunction hashSeed(seed: string) {\n  let hash = 0;\n  for (const char of seed) hash = (hash * 31 + char.charCodeAt(0)) >>> 0;\n  return hash;\n}\n\nexport async function GET(request: Request) {\n  const auth = await requireStaffApiSession(\"content:read\");\n  if (!auth.ok) return auth.response;\n  try {\n    const directory = path.join(\n      process.cwd(),\n      \"..\",\n      \"..\",\n      \"public\",\n      \"avatar\",\n      \"Flat Assets\",\n      \"Flat Assets\",\n      \"Templates\",\n      \"Bust\",\n    );\n    const files = (await readdir(directory))\n      .filter((file) => /\\.(png|svg)$/i.test(file))\n      .sort();\n    if (files.length === 0) return NextResponse.json({ error: \"Avatar unavailable\" }, { status: 404 });\n    const seed = new URL(request.url).searchParams.get(\"seed\")?.trim() || \"staff\";\n    const fileName = files[hashSeed(seed) % files.length];\n    const file = await readFile(path.join(directory, fileName));\n    const contentType = fileName.toLowerCase().endsWith(\".svg\") ? \"image/svg+xml\" : \"image/png\";\n    return new NextResponse(file, {\n      headers: {\n        \"Content-Type\": contentType,\n        \"Cache-Control\": \"public, max-age=86400, immutable\",\n      },\n    });\n  } catch {\n    return NextResponse.json({ error: \"Avatar unavailable\" }, { status: 404 });\n  }\n}\n"
      x-permission: "content:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_profile_avatarResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/receipts:
    get:
      operationId: get_admin_receipts
      summary: "Read Admin / Receipts"
      description: "Admin / Receipts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Receipts"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getReceiptByOrder,\n  saveReceipt,\n  markReceiptSent,\n  buildReceiptHtml,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { sendResendTransactionalEmail } from \"@universal-music-store/resend-mail\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n  parseAdminJson,\n} from \"@/lib/admin-api-security\";\nimport { fetchMedusaOrderJson } from \"@/lib/medusa-order-bridge\";\nimport { canonicalReceiptOrderFromMedusa } from \"@/lib/receipt-order\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst receiptRequestSchema = z\n  .object({\n    order_id: z.string().trim().min(1).max(200),\n    send: z.boolean().default(false),\n  })\n  .strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user)\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"receipts:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const orderId = req.nextUrl.searchParams.get(\"order_id\");\n  if (!orderId) {\n    return correlatedJson(\n      cid,\n      { error: \"order_id is required\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const receipt = await getReceiptByOrder(sb, orderId, organization.id);\n  if (!receipt)\n    return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: receipt });\n}\n\nexport async function POST(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user)\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"receipts:send\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = await parseAdminJson(req, receiptRequestSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const body = parsed.data;\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey) {\n    return correlatedJson(\n      cid,\n      { error: \"Idempotency-Key is required for receipt mutations\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const orderPayload = await fetchMedusaOrderJson(body.order_id);\n  if (!orderPayload?.id)\n    return correlatedJson(cid, { error: \"Order not found\" }, { status: 404 });\n  const order = canonicalReceiptOrderFromMedusa(orderPayload);\n  if (!order.id)\n    return correlatedJson(cid, { error: \"Order not found\" }, { status: 404 });\n  if (body.send && !order.customer_email) {\n    return correlatedJson(\n      cid,\n      { error: \"The order has no customer email\" },\n      { status: 400 },\n    );\n  }\n  if (body.send && !process.env.RESEND_API_KEY?.trim()) {\n    return correlatedJson(\n      cid,\n      { error: \"Receipt email delivery is not configured\" },\n      { status: 503 },\n    );\n  }\n  const claim = await claimAdminIdempotency(sb, {\n    actorKey: `${organization.id}:${session.user.email!.toLowerCase()}`,\n    actionKey: `receipt:${order.id}`,\n    idempotencyKey,\n    requestHash: getRequestHash(body),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(cid, claim.body, { status: claim.status });\n  if (claim.kind !== \"claimed\") {\n    return correlatedJson(\n      cid,\n      { error: \"Receipt mutation is already in progress or conflicts with a previous request\" },\n      { status: 409 },\n    );\n  }\n  const idempotencyId = claim.id;\n  let receipt = await getReceiptByOrder(sb, order.id, organization.id);\n  const html = buildReceiptHtml({\n    ...order,\n    storeName: process.env.STORE_NAME?.trim() || \"Universal Music Store\",\n  });\n  if (!receipt) {\n    try {\n      receipt = await saveReceipt(sb, {\n        order_id: order.id,\n        organization_id: organization.id,\n        customer_email: order.customer_email ?? undefined,\n        receipt_html: html,\n      });\n    } catch {\n      const response = correlatedJson(\n        cid,\n        { error: \"Unable to create receipt\" },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(sb, idempotencyId, response.status, {\n        error: \"Unable to create receipt\",\n      });\n      return response;\n    }\n    await insertStaffAuditLog(sb, {\n      actorEmail: session.user.email ?? \"unknown\",\n      action: \"receipt.create\",\n      resource: \"digital_receipts\",\n      resourceId: receipt.id,\n      details: { order_id: order.id, idempotency_key: idempotencyKey ?? null },\n    });\n  }\n\n  if (order.customer_email && body.send && !receipt.sent_at) {\n    const resendKey = process.env.RESEND_API_KEY?.trim();\n    const fromAddr =\n      process.env.RESEND_FROM_EMAIL?.trim() ||\n      process.env.RESEND_FROM?.trim() ||\n      \"noreply@universal-music-store.com\";\n    if (resendKey) {\n      const sent = await sendResendTransactionalEmail({\n        apiKey: resendKey,\n        from: fromAddr,\n        to: order.customer_email,\n        subject: `Your receipt for Order #${order.display_id}`,\n        html,\n        tags: [{ name: \"type\", value: \"staff_receipt\" }],\n      });\n      if (!sent.ok) {\n        const response = correlatedJson(\n          cid,\n          { error: \"Failed to send receipt email\" },\n          { status: 502 },\n        );\n        await completeAdminIdempotency(sb, idempotencyId, response.status, {\n          error: \"Failed to send receipt email\",\n        });\n        return response;\n      }\n      await markReceiptSent(sb, receipt.id);\n      receipt = { ...receipt, sent_at: new Date().toISOString() };\n      await insertStaffAuditLog(sb, {\n        actorEmail: session.user.email ?? \"unknown\",\n        action: \"receipt.send\",\n        resource: \"digital_receipts\",\n        resourceId: receipt.id,\n        details: { order_id: order.id, idempotency_key: idempotencyKey },\n      });\n    }\n  }\n\n  const response = correlatedJson(cid, { data: receipt }, { status: 200 });\n  await completeAdminIdempotency(sb, idempotencyId, response.status, {\n    data: receipt,\n  });\n  return response;\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_receiptsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_receipts
      summary: "Create or execute Admin / Receipts"
      description: "Admin / Receipts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Receipts"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getReceiptByOrder,\n  saveReceipt,\n  markReceiptSent,\n  buildReceiptHtml,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { sendResendTransactionalEmail } from \"@universal-music-store/resend-mail\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getIdempotencyKey,\n  getRequestHash,\n  parseAdminJson,\n} from \"@/lib/admin-api-security\";\nimport { fetchMedusaOrderJson } from \"@/lib/medusa-order-bridge\";\nimport { canonicalReceiptOrderFromMedusa } from \"@/lib/receipt-order\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst receiptRequestSchema = z\n  .object({\n    order_id: z.string().trim().min(1).max(200),\n    send: z.boolean().default(false),\n  })\n  .strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user)\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"receipts:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const orderId = req.nextUrl.searchParams.get(\"order_id\");\n  if (!orderId) {\n    return correlatedJson(\n      cid,\n      { error: \"order_id is required\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const receipt = await getReceiptByOrder(sb, orderId, organization.id);\n  if (!receipt)\n    return correlatedJson(cid, { error: \"Not found\" }, { status: 404 });\n  return correlatedJson(cid, { data: receipt });\n}\n\nexport async function POST(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user)\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"receipts:send\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const parsed = await parseAdminJson(req, receiptRequestSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      cid,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const body = parsed.data;\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey) {\n    return correlatedJson(\n      cid,\n      { error: \"Idempotency-Key is required for receipt mutations\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization)\n    return correlatedJson(\n      cid,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  const orderPayload = await fetchMedusaOrderJson(body.order_id);\n  if (!orderPayload?.id)\n    return correlatedJson(cid, { error: \"Order not found\" }, { status: 404 });\n  const order = canonicalReceiptOrderFromMedusa(orderPayload);\n  if (!order.id)\n    return correlatedJson(cid, { error: \"Order not found\" }, { status: 404 });\n  if (body.send && !order.customer_email) {\n    return correlatedJson(\n      cid,\n      { error: \"The order has no customer email\" },\n      { status: 400 },\n    );\n  }\n  if (body.send && !process.env.RESEND_API_KEY?.trim()) {\n    return correlatedJson(\n      cid,\n      { error: \"Receipt email delivery is not configured\" },\n      { status: 503 },\n    );\n  }\n  const claim = await claimAdminIdempotency(sb, {\n    actorKey: `${organization.id}:${session.user.email!.toLowerCase()}`,\n    actionKey: `receipt:${order.id}`,\n    idempotencyKey,\n    requestHash: getRequestHash(body),\n  });\n  if (claim.kind === \"replay\")\n    return correlatedJson(cid, claim.body, { status: claim.status });\n  if (claim.kind !== \"claimed\") {\n    return correlatedJson(\n      cid,\n      { error: \"Receipt mutation is already in progress or conflicts with a previous request\" },\n      { status: 409 },\n    );\n  }\n  const idempotencyId = claim.id;\n  let receipt = await getReceiptByOrder(sb, order.id, organization.id);\n  const html = buildReceiptHtml({\n    ...order,\n    storeName: process.env.STORE_NAME?.trim() || \"Universal Music Store\",\n  });\n  if (!receipt) {\n    try {\n      receipt = await saveReceipt(sb, {\n        order_id: order.id,\n        organization_id: organization.id,\n        customer_email: order.customer_email ?? undefined,\n        receipt_html: html,\n      });\n    } catch {\n      const response = correlatedJson(\n        cid,\n        { error: \"Unable to create receipt\" },\n        { status: 502 },\n      );\n      await completeAdminIdempotency(sb, idempotencyId, response.status, {\n        error: \"Unable to create receipt\",\n      });\n      return response;\n    }\n    await insertStaffAuditLog(sb, {\n      actorEmail: session.user.email ?? \"unknown\",\n      action: \"receipt.create\",\n      resource: \"digital_receipts\",\n      resourceId: receipt.id,\n      details: { order_id: order.id, idempotency_key: idempotencyKey ?? null },\n    });\n  }\n\n  if (order.customer_email && body.send && !receipt.sent_at) {\n    const resendKey = process.env.RESEND_API_KEY?.trim();\n    const fromAddr =\n      process.env.RESEND_FROM_EMAIL?.trim() ||\n      process.env.RESEND_FROM?.trim() ||\n      \"noreply@universal-music-store.com\";\n    if (resendKey) {\n      const sent = await sendResendTransactionalEmail({\n        apiKey: resendKey,\n        from: fromAddr,\n        to: order.customer_email,\n        subject: `Your receipt for Order #${order.display_id}`,\n        html,\n        tags: [{ name: \"type\", value: \"staff_receipt\" }],\n      });\n      if (!sent.ok) {\n        const response = correlatedJson(\n          cid,\n          { error: \"Failed to send receipt email\" },\n          { status: 502 },\n        );\n        await completeAdminIdempotency(sb, idempotencyId, response.status, {\n          error: \"Failed to send receipt email\",\n        });\n        return response;\n      }\n      await markReceiptSent(sb, receipt.id);\n      receipt = { ...receipt, sent_at: new Date().toISOString() };\n      await insertStaffAuditLog(sb, {\n        actorEmail: session.user.email ?? \"unknown\",\n        action: \"receipt.send\",\n        resource: \"digital_receipts\",\n        resourceId: receipt.id,\n        details: { order_id: order.id, idempotency_key: idempotencyKey },\n      });\n    }\n  }\n\n  const response = correlatedJson(cid, { data: receipt }, { status: 200 });\n  await completeAdminIdempotency(sb, idempotencyId, response.status, {\n    data: receipt,\n  });\n  return response;\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_receiptsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_receiptsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/reconciliation:
    get:
      operationId: get_admin_reconciliation
      summary: "Read Admin / Reconciliation"
      description: "Admin / Reconciliation. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Reconciliation"]
      x-source: "import { NextResponse } from \"next/server\";\nimport {\n  getPaymentPlatformMetrics,\n  listPaymentProviderArtifacts,\n  listRecentPaymentAttempts,\n  type PaymentAttemptRow,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport type ReconciliationRow = {\n  date: string;\n  provider: string;\n  medusaOrderCount: number;\n  medusaTotalMinor: number;\n  providerConfirmedCount: number;\n  providerConfirmedMinor: number;\n  openAttemptCount: number;\n  problemAttemptCount: number;\n  discrepancyMinor: number;\n  status: \"matched\" | \"discrepancy\" | \"pending\";\n};\n\nexport type ReconciliationSummary = {\n  period: string;\n  rows: ReconciliationRow[];\n  totalMedusaMinor: number;\n  totalProviderConfirmedMinor: number;\n  totalDiscrepancyMinor: number;\n  paymentAttemptsStaleFinalize: number;\n  paymentAttemptsNeedsReview: number;\n  recentProblemAttempts: Array<{\n    correlationId: string;\n    provider: string;\n    status: string;\n    checkoutState: string;\n    staleReason: string | null;\n    updatedAt: string;\n  }>;\n  providerReconciliationArtifacts: Array<{\n    provider: string;\n    status: string;\n    externalId: string;\n    jobId: string | null;\n    periodStart: string | null;\n    periodEnd: string | null;\n    idempotencyKey: string | null;\n    updatedAt: string;\n  }>;\n};\n\nconst PROVIDER_CONFIRMED_STATUSES = new Set([\n  \"paid\",\n  \"authorized\",\n  \"completed\",\n  \"pending_capture\",\n  \"paid_awaiting_order\",\n  \"finalizing_order\",\n]);\nconst OPEN_STATUSES = new Set([\n  \"initiated\",\n  \"pending_provider_redirect\",\n  \"awaiting_completion\",\n  \"paid_awaiting_order\",\n  \"finalizing_order\",\n]);\nconst PROBLEM_STATUSES = new Set([\"expired\", \"needs_review\", \"failed\"]);\n\nfunction bucketDate(row: PaymentAttemptRow): string {\n  const stamp = row.finalized_at ?? row.updated_at ?? row.created_at;\n  return stamp.slice(0, 10);\n}\n\nfunction reconciliationProviders(input: string): string[] {\n  return input === \"all\"\n    ? [\"stripe\", \"paypal\", \"xendit\", \"cod\"]\n    : [input];\n}\n\nexport async function GET(request: Request) {\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n  const sup = adminSupabaseOr503(\"reconciliation\");\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    staff.session.user?.email,\n  );\n  if (!organization) {\n    return NextResponse.json(\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  }\n\n  const { searchParams } = new URL(request.url);\n  const days = Math.min(Number(searchParams.get(\"days\")) || 7, 90);\n  const provider = searchParams.get(\"provider\") || \"all\";\n\n  const rows: ReconciliationRow[] = [];\n  const now = new Date();\n\n  for (let i = 0; i < days; i++) {\n    const date = new Date(now);\n    date.setDate(date.getDate() - i);\n    const dateStr = date.toISOString().slice(0, 10);\n\n    const providers = reconciliationProviders(provider);\n\n    for (const p of providers) {\n      rows.push({\n        date: dateStr,\n        provider: p,\n        medusaOrderCount: 0,\n        medusaTotalMinor: 0,\n        providerConfirmedCount: 0,\n        providerConfirmedMinor: 0,\n        openAttemptCount: 0,\n        problemAttemptCount: 0,\n        discrepancyMinor: 0,\n        status: \"pending\",\n      });\n    }\n  }\n\n  const metrics = await getPaymentPlatformMetrics(sup.client, organization.id);\n  const recentAttempts = await listRecentPaymentAttempts(sup.client, 500, organization.id);\n  const reconciliationArtifacts = await listPaymentProviderArtifacts(sup.client, {\n    organization_id: organization.id,\n    provider:\n      provider === \"stripe\" || provider === \"paypal\" || provider === \"xendit\"\n        ? provider\n        : \"all\",\n    artifactTypes: [\"reconciliation\"],\n    limit: 25,\n  });\n  const rowMap = new Map(rows.map((row) => [`${row.date}:${row.provider}`, row]));\n\n  const dayFloor = new Date(now);\n  dayFloor.setUTCDate(dayFloor.getUTCDate() - (days - 1));\n  dayFloor.setUTCHours(0, 0, 0, 0);\n\n  for (const attempt of recentAttempts) {\n    const stamp = attempt.finalized_at ?? attempt.updated_at ?? attempt.created_at;\n    const parsed = new Date(stamp);\n    if (Number.isNaN(parsed.getTime()) || parsed < dayFloor) continue;\n    const day = bucketDate(attempt);\n    const key = `${day}:${attempt.provider}`;\n    const row = rowMap.get(key);\n    if (!row) continue;\n    const amountMinor =\n      typeof attempt.amount_minor === \"number\" && Number.isFinite(attempt.amount_minor)\n        ? attempt.amount_minor\n        : 0;\n\n    if (attempt.medusa_order_id) {\n      row.medusaOrderCount += 1;\n      row.medusaTotalMinor += amountMinor;\n    }\n    if (\n      attempt.provider_payment_id ||\n      attempt.webhook_last_status ||\n      PROVIDER_CONFIRMED_STATUSES.has(attempt.status)\n    ) {\n      row.providerConfirmedCount += 1;\n      row.providerConfirmedMinor += amountMinor;\n    }\n    if (OPEN_STATUSES.has(attempt.status)) {\n      row.openAttemptCount += 1;\n    }\n    if (PROBLEM_STATUSES.has(attempt.status)) {\n      row.problemAttemptCount += 1;\n    }\n  }\n\n  for (const row of rows) {\n    row.discrepancyMinor = row.medusaTotalMinor - row.providerConfirmedMinor;\n    row.status =\n      row.problemAttemptCount > 0 || row.openAttemptCount > 0\n        ? \"pending\"\n        : row.discrepancyMinor !== 0 || row.medusaOrderCount !== row.providerConfirmedCount\n          ? \"discrepancy\"\n          : row.medusaOrderCount > 0 || row.providerConfirmedCount > 0\n            ? \"matched\"\n            : \"pending\";\n  }\n\n  const totalMedusa = rows.reduce((s, r) => s + r.medusaTotalMinor, 0);\n  const totalProviderConfirmed = rows.reduce(\n    (sum, row) => sum + row.providerConfirmedMinor,\n    0,\n  );\n  const recentProblemAttempts = recentAttempts\n    .filter(\n      (row) =>\n        row.status === \"expired\" ||\n        row.status === \"needs_review\" ||\n        row.status === \"paid_awaiting_order\" ||\n        row.status === \"finalizing_order\",\n    )\n    .slice(0, 8)\n    .map((row) => ({\n      correlationId: row.correlation_id,\n      provider: row.provider,\n      status: row.status,\n      checkoutState: row.checkout_state,\n      staleReason: row.stale_reason ?? row.last_error,\n      updatedAt: row.updated_at,\n    }));\n\n  const summary: ReconciliationSummary = {\n    period: `Last ${days} days`,\n    rows,\n    totalMedusaMinor: totalMedusa,\n    totalProviderConfirmedMinor: totalProviderConfirmed,\n    totalDiscrepancyMinor: totalMedusa - totalProviderConfirmed,\n    paymentAttemptsStaleFinalize: metrics?.paymentAttemptsStaleFinalize ?? 0,\n    paymentAttemptsNeedsReview: metrics?.paymentAttemptsNeedsReview ?? 0,\n    recentProblemAttempts,\n    providerReconciliationArtifacts: reconciliationArtifacts.map((row) => ({\n      provider: row.provider,\n      status: row.status,\n      externalId: row.external_id,\n      jobId:\n        typeof row.metadata?.job_id === \"string\" ? row.metadata.job_id : null,\n      periodStart:\n        typeof row.metadata?.period_start === \"string\"\n          ? row.metadata.period_start\n          : null,\n      periodEnd:\n        typeof row.metadata?.period_end === \"string\"\n          ? row.metadata.period_end\n          : null,\n      idempotencyKey: row.idempotency_key,\n      updatedAt: row.updated_at,\n    })),\n  };\n\n  return NextResponse.json(summary);\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_reconciliationResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/reviews:
    get:
      operationId: get_admin_reviews
      summary: "Read Admin / Reviews"
      description: "Admin / Reviews. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Reviews"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst READ_PERM = \"content:read\";\n\n/**\n * List product reviews for moderation (all statuses). Staff: `content:read`.\n */\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(READ_PERM);\n  if (!staff.ok) {\n    return staff.response;\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n\n  const url = new URL(req.url);\n  const status = url.searchParams.get(\"status\")?.trim().toLowerCase() ?? \"\";\n  const productId = url.searchParams.get(\"medusaProductId\")?.trim() ?? \"\";\n  const q = url.searchParams.get(\"q\")?.trim().toLowerCase() ?? \"\";\n  const limit = Math.min(\n    200,\n    Math.max(1, Number(url.searchParams.get(\"limit\")) || 80),\n  );\n\n  let query = sup.client\n    .from(\"product_reviews\")\n    .select(\n      \"id,product_slug,medusa_product_id,rating,author_name,body,status,created_at,is_verified_buyer,moderated_at,moderation_note\",\n    )\n    .order(\"created_at\", { ascending: false })\n    .limit(limit);\n\n  if (\n    status &&\n    [\"pending\", \"approved\", \"rejected\", \"hidden\"].includes(status)\n  ) {\n    query = query.eq(\"status\", status);\n  }\n  if (productId) {\n    query = query.eq(\"medusa_product_id\", productId);\n  }\n  if (q.length >= 2) {\n    const safe = q.replace(/[%_]/g, \" \").trim();\n    if (safe.length >= 2) {\n      query = query.ilike(\"body\", `%${safe}%`);\n    }\n  }\n\n  const { data, error } = await query;\n  if (error) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to load reviews\", code: \"REVIEWS_QUERY_FAILED\" },\n      { status: 502 },\n    );\n  }\n\n  return correlatedJson(correlationId, { reviews: data ?? [] });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_reviewsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/reviews/{id}:
    patch:
      operationId: patch_admin_reviews_id
      summary: "Update Admin / Reviews / Id"
      description: "Admin / Reviews / Id. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Reviews"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { z } from \"zod\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport const dynamic = \"force-dynamic\";\nconst moderationSchema = z\n  .object({\n    status: z.enum([\"approved\", \"rejected\", \"hidden\", \"pending\"]),\n    moderation_note: z.string().trim().max(2000).optional().default(\"\"),\n    expected_updated_at: z.string().datetime().optional(),\n  })\n  .strict();\n\nasync function patch(\n  req: Request,\n  ctx: { params: Promise<{ id: string }> },\n) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"content:write\");\n  if (!staff.ok) return staff.response;\n  const reviewId = (await ctx.params).id?.trim();\n  if (!reviewId)\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing id\" },\n      { status: 400 },\n    );\n  const parsed = await parseAdminJson(req, moderationSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const staffEmail =\n    staff.session.user?.email?.trim().toLowerCase() ?? \"unknown\";\n  let update = sup.client\n    .from(\"product_reviews\")\n    .update({\n      status: parsed.data.status,\n      moderated_by_staff_email: staffEmail,\n      moderated_at: new Date().toISOString(),\n      moderation_note: parsed.data.moderation_note || null,\n    })\n    .eq(\"id\", reviewId);\n  if (parsed.data.expected_updated_at)\n    update = update.eq(\"updated_at\", parsed.data.expected_updated_at);\n  const { data, error } = await update.select(\"id,status\").maybeSingle();\n  if (error)\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to update review\", code: \"REVIEW_UPDATE_FAILED\" },\n      { status: 502 },\n    );\n  if (!data)\n    return correlatedJson(\n      correlationId,\n      {\n        error: parsed.data.expected_updated_at\n          ? \"Review changed; reload before moderating\"\n          : \"Review not found\",\n      },\n      { status: parsed.data.expected_updated_at ? 409 : 404 },\n    );\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: staffEmail,\n    action: \"review.moderate\",\n    resource: \"product_reviews\",\n    resourceId: reviewId,\n    details: { status: parsed.data.status },\n  });\n  return correlatedJson(correlationId, { ok: true, review: data });\n}\n\nexport const PATCH = withAdminMutationIdempotency(\"/admin/reviews/[id]:PATCH\", patch);\n"
      x-permission: "content:write"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_admin_reviews_idRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_admin_reviews_idResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/roles:
    get:
      operationId: get_admin_roles
      summary: "Read Admin / Roles"
      description: "Admin / Roles. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Roles"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"employees:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n\n  const [{ data: users, error: usersError }, { data: roleRows, error: rolesError }, { data: grants }] = await Promise.all([\n    sup.client.from(\"users\").select(\"id,created_at\"),\n    sup.client.from(\"user_roles\").select(\"user_id,role\"),\n    sup.client.from(\"staff_permission_grants\").select(\"user_id,permission_key\"),\n  ]);\n  if (usersError || rolesError) return correlatedJson(cid, { error: \"Unable to load roles\" }, { status: 502 });\n\n  const createdAt = new Map((users ?? []).map((user) => [String(user.id), user.created_at as string | null]));\n  const grantsByUser = new Map<string, string[]>();\n  for (const grant of grants ?? []) {\n    const key = String(grant.user_id);\n    grantsByUser.set(key, [...(grantsByUser.get(key) ?? []), String(grant.permission_key)]);\n  }\n  const grouped = new Map<string, { users: number; permissionSets: Set<string>; lastReview: string }>();\n  for (const row of roleRows ?? []) {\n    const role = String(row.role ?? \"unknown\");\n    const current = grouped.get(role) ?? { users: 0, permissionSets: new Set<string>(), lastReview: \"Not reviewed\" };\n    current.users += 1;\n    for (const permission of grantsByUser.get(String(row.user_id)) ?? []) current.permissionSets.add(permission);\n    const date = createdAt.get(String(row.user_id));\n    if (date && (current.lastReview === \"Not reviewed\" || date > current.lastReview)) current.lastReview = date;\n    grouped.set(role, current);\n  }\n  const data = [...grouped.entries()].map(([role, value]) => ({\n    role: role[0]?.toUpperCase() + role.slice(1),\n    group: role === \"admin\" ? \"System roles\" : \"Custom roles\",\n    accessLevel: role === \"admin\" ? \"Full\" : \"Scoped\",\n    users: value.users,\n    permissionSets: role === \"admin\" ? [\"All permissions\"] : [...value.permissionSets],\n    lastReview: value.lastReview === \"Not reviewed\" ? value.lastReview : new Date(value.lastReview).toLocaleDateString(\"en-US\", { year: \"numeric\", month: \"short\", day: \"numeric\" }),\n    owner: role === \"admin\" ? \"System\" : \"Staff policy\",\n    status: \"Active\" as const,\n  }));\n  return correlatedJson(cid, { data });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_rolesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/segments:
    get:
      operationId: get_admin_segments
      summary: "Read Admin / Segments"
      description: "Admin / Segments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Segments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listSegments,\n  createSegment,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst segmentSchema = z.object({\n  name: z.string().trim().min(1).max(160),\n  description: z.string().trim().max(1000).optional(),\n  rule_type: z.enum([\"spend_above\", \"spend_below\", \"order_count_above\", \"inactive_days\", \"product_category\", \"tier\", \"manual\"]),\n  rule_config: z.record(z.string(), z.unknown()).default({}),\n  auto_refresh: z.boolean().default(true),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"crm:segments\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listSegments(sb, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"crm:segments\");\n  if (!auth.ok) return auth.response;\n  const parsed = await parseAdminJson(req, segmentSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const segment = await createSegment(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: segment }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/segments:POST\", post);\n"
      x-permission: "crm:segments"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_segmentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_segments
      summary: "Create or execute Admin / Segments"
      description: "Admin / Segments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Segments"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport {\n  listSegments,\n  createSegment,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst segmentSchema = z.object({\n  name: z.string().trim().min(1).max(160),\n  description: z.string().trim().max(1000).optional(),\n  rule_type: z.enum([\"spend_above\", \"spend_below\", \"order_count_above\", \"inactive_days\", \"product_category\", \"tier\", \"manual\"]),\n  rule_config: z.record(z.string(), z.unknown()).default({}),\n  auto_refresh: z.boolean().default(true),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"crm:segments\");\n  if (!auth.ok) return auth.response;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await listSegments(sb, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"crm:segments\");\n  if (!auth.ok) return auth.response;\n  const parsed = await parseAdminJson(req, segmentSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, auth.session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const segment = await createSegment(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: segment }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/segments:POST\", post);\n"
      x-permission: "crm:segments"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_segmentsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_segmentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/segments/{id}/members:
    get:
      operationId: get_admin_segments_id_members
      summary: "Read Admin / Segments / Id / Members"
      description: "Admin / Segments / Id / Members. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Segments"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getSegmentMembers,\n  addSegmentMembers,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst membersSchema = z.object({\n  members: z.array(z.object({\n    customer_email: z.string().trim().toLowerCase().email().max(320),\n    medusa_customer_id: z.string().trim().max(200).optional(),\n  }).strict()).min(1).max(500),\n}).strict();\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:segments\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await getSegmentMembers(sb, id, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nexport async function POST(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:segments\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, membersSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { data: segment } = await sb.from(\"customer_segments\").select(\"id\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (!segment) return correlatedJson(cid, { error: \"Segment not found\" }, { status: 404 });\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const claim = await claimAdminIdempotency(sb, { actorKey: `${organization.id}:${session.user.email!.toLowerCase()}`, actionKey: `segment.members:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(cid, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(cid, { error: \"Idempotency key is already in use\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(cid, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  try {\n    const count = await addSegmentMembers(sb, id, organization.id, parsed.data.members);\n    const body = { count };\n    await completeAdminIdempotency(sb, claim.id, 200, body);\n    return correlatedJson(cid, body);\n  } catch {\n    const body = { error: \"Unable to update segment members\" };\n    await completeAdminIdempotency(sb, claim.id, 502, body);\n    return correlatedJson(cid, body, { status: 502 });\n  }\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_segments_id_membersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_segments_id_members
      summary: "Create or execute Admin / Segments / Id / Members"
      description: "Admin / Segments / Id / Members. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Segments"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getSegmentMembers,\n  addSegmentMembers,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { parseAdminJson, claimAdminIdempotency, completeAdminIdempotency, getIdempotencyKey, getRequestHash } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\n\nconst membersSchema = z.object({\n  members: z.array(z.object({\n    customer_email: z.string().trim().toLowerCase().email().max(320),\n    medusa_customer_id: z.string().trim().max(200).optional(),\n  }).strict()).min(1).max(500),\n}).strict();\n\ntype Ctx = { params: Promise<{ id: string }> };\n\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:segments\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const data = await getSegmentMembers(sb, id, organization.id);\n  return correlatedJson(cid, { data });\n}\n\nexport async function POST(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"crm:segments\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const parsed = await parseAdminJson(req, membersSchema);\n  if (!parsed.ok) return correlatedJson(cid, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const { data: segment } = await sb.from(\"customer_segments\").select(\"id\").eq(\"id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (!segment) return correlatedJson(cid, { error: \"Segment not found\" }, { status: 404 });\n  const idempotencyKey = getIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const claim = await claimAdminIdempotency(sb, { actorKey: `${organization.id}:${session.user.email!.toLowerCase()}`, actionKey: `segment.members:${id}`, idempotencyKey, requestHash: getRequestHash(parsed.data) });\n  if (claim.kind === \"replay\") return correlatedJson(cid, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(cid, { error: \"Idempotency key is already in use\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(cid, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  try {\n    const count = await addSegmentMembers(sb, id, organization.id, parsed.data.members);\n    const body = { count };\n    await completeAdminIdempotency(sb, claim.id, 200, body);\n    return correlatedJson(cid, body);\n  } catch {\n    const body = { error: \"Unable to update segment members\" };\n    await completeAdminIdempotency(sb, claim.id, 502, body);\n    return correlatedJson(cid, body, { status: 502 });\n  }\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_segments_id_membersRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_segments_id_membersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/shifts:
    get:
      operationId: get_admin_shifts
      summary: "Read Admin / Shifts"
      description: "Admin / Shifts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Shifts"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  openShift,\n  listShifts,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst openShiftSchema = z.object({\n  employee_id: z.string().uuid(),\n  device_name: z.string().trim().min(1).max(120).optional(),\n  opening_cash: z.number().finite().nonnegative().max(1_000_000).optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const status = req.nextUrl.searchParams.get(\"status\") as \"open\" | \"closed\" | null;\n  const data = await listShifts(sb, { status: status ?? undefined, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:shift_manage\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = openShiftSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid shift payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const shift = await openShift(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: shift }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/shifts:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_shiftsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_shifts
      summary: "Create or execute Admin / Shifts"
      description: "Admin / Shifts. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Shifts"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  openShift,\n  listShifts,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst openShiftSchema = z.object({\n  employee_id: z.string().uuid(),\n  device_name: z.string().trim().min(1).max(120).optional(),\n  opening_cash: z.number().finite().nonnegative().max(1_000_000).optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const status = req.nextUrl.searchParams.get(\"status\") as \"open\" | \"closed\" | null;\n  const data = await listShifts(sb, { status: status ?? undefined, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:shift_manage\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = openShiftSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid shift payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const shift = await openShift(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: shift }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/shifts:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_shiftsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_shiftsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/shifts/{id}/close:
    post:
      operationId: post_admin_shifts_id_close
      summary: "Create or execute Admin / Shifts / Id / Close"
      description: "Admin / Shifts / Id / Close. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Shifts"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { calculatePosReconciliation, closeShift, getShiftById, listVoids } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { z } from \"zod\";\n\ntype Ctx = { params: Promise<{ id: string }> };\nconst closeShiftSchema = z.object({\n  closing_cash: z.number().finite().nonnegative(),\n  notes: z.string().trim().max(1000).optional(),\n}).strict();\n\nasync function post(req: NextRequest, ctx: Ctx) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:shift_manage\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const { id } = await ctx.params;\n  const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n  if (!idempotencyKey) return correlatedJson(cid, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  let rawBody: unknown;\n  try { rawBody = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = closeShiftSchema.safeParse(rawBody);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid shift close payload\" }, { status: 400 });\n  const body = parsed.data;\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const existing = await sb.from(\"pos_shift_reconciliations\").select(\"*\").eq(\"shift_id\", id).eq(\"organization_id\", organization.id).maybeSingle();\n  if (existing.error && !/relation .* does not exist/i.test(existing.error.message)) return correlatedJson(cid, { error: \"Unable to load reconciliation\" }, { status: 503 });\n  if (existing.data) {\n    if (existing.data.idempotency_key !== idempotencyKey) return correlatedJson(cid, { error: \"Shift already reconciled\" }, { status: 409 });\n    return correlatedJson(cid, { data: { reconciliation: existing.data } });\n  }\n  const currentShift = await getShiftById(sb, id, organization.id);\n  if (!currentShift) return correlatedJson(cid, { error: \"Shift not found\" }, { status: 404 });\n  let cashSales = 0;\n  for (let offset = 0; offset < 5000; offset += 50) {\n    const query = new URLSearchParams({ limit: \"50\", offset: String(offset), fields: \"id,total,metadata\", order: \"-created_at\" });\n    const response = await medusaAdminFetch(`/admin/orders?${query.toString()}`, { method: \"GET\" });\n    if (!response.ok) return correlatedJson(cid, { error: \"Unable to load shift sales\" }, { status: 503 });\n    const orders = ((await response.json()) as { orders?: Array<{ total?: unknown; metadata?: Record<string, unknown> | null }> }).orders ?? [];\n    for (const order of orders) {\n      if (String(order.metadata?.pos_shift_id ?? \"\") !== id) continue;\n      const total = Number(order.total ?? 0);\n      if (Number.isFinite(total) && total >= 0) cashSales += Math.round(total);\n    }\n    if (orders.length < 50) break;\n  }\n  const voids = await listVoids(sb, { shiftId: id, limit: 200, organizationId: organization.id });\n  const cashRefunds = voids.reduce((sum, value) => sum + Math.max(0, Math.round(value.amount ?? 0)), 0);\n  const reconciliation = calculatePosReconciliation({ openingCash: currentShift.opening_cash, cashSales, cashRefunds, payouts: 0, countedCash: body.closing_cash });\n  const shift = await closeShift(sb, id, { organization_id: organization.id, closing_cash: body.closing_cash, expected_cash: reconciliation.expectedCash, notes: body.notes });\n  const result = await sb.from(\"pos_shift_reconciliations\").upsert({ organization_id: organization.id, shift_id: id, idempotency_key: idempotencyKey, opening_cash: reconciliation.openingCash, cash_sales: reconciliation.cashSales, cash_refunds: reconciliation.cashRefunds, payouts: reconciliation.payouts, expected_cash: reconciliation.expectedCash, counted_cash: reconciliation.countedCash, variance: reconciliation.variance, created_by_email: session.user.email?.trim().toLowerCase() ?? \"system\" }, { onConflict: \"shift_id\" }).select(\"*\").single();\n  if (result.error) return correlatedJson(cid, { error: \"Unable to persist reconciliation\" }, { status: 503 });\n  return correlatedJson(cid, { data: { shift, reconciliation: result.data } });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/shifts/[id]/close:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_shifts_id_closeRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_shifts_id_closeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/shifts/{id}/reconciliation:
    get:
      operationId: get_admin_shifts_id_reconciliation
      summary: "Read Admin / Shifts / Id / Reconciliation"
      description: "Admin / Shifts / Id / Reconciliation. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Shifts"]
      x-source: "import { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { listVoids, getShiftById } from \"@universal-music-store/platform-data\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\ntype Ctx = { params: Promise<{ id: string }> };\n\n/**\n * Register-grade shift reconciliation: sums Medusa orders tagged with metadata.pos_shift_id\n * and lists void rows for the shift.\n */\nexport async function GET(req: NextRequest, ctx: Ctx) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(correlationId, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(correlationId, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const { id: shiftId } = await ctx.params;\n  if (!shiftId?.trim()) {\n    return correlatedJson(correlationId, { error: \"Missing shift id\" }, { status: 400 });\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, session.user.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const shift = await getShiftById(sup.client, shiftId.trim(), organization.id);\n  if (!shift) {\n    return correlatedJson(correlationId, { error: \"Shift not found\" }, { status: 404 });\n  }\n\n  const voids = await listVoids(sup.client, { shiftId: shiftId.trim(), limit: 200, organizationId: organization.id });\n  const voidAmountMinor = voids.reduce((s, v) => s + (v.amount != null ? Math.round(v.amount) : 0), 0);\n\n  let ordersMatched = 0;\n  let salesTotalMinor = 0;\n  const pageSize = 50;\n  for (let offset = 0; offset < 5000; offset += pageSize) {\n    const qs = new URLSearchParams();\n    qs.set(\"limit\", String(pageSize));\n    qs.set(\"offset\", String(offset));\n    qs.set(\"fields\", \"id,total,metadata\");\n    qs.set(\"order\", \"-created_at\");\n    const res = await medusaAdminFetch(`/admin/orders?${qs.toString()}`, {\n      method: \"GET\",\n    });\n    if (!res.ok) break;\n    const json = (await res.json()) as {\n      orders?: Array<{ total?: unknown; metadata?: Record<string, unknown> | null }>;\n    };\n    const orders = json.orders ?? [];\n    if (orders.length === 0) break;\n    for (const o of orders) {\n      const sid = o.metadata?.pos_shift_id;\n      if (sid != null && String(sid) === shiftId.trim()) {\n        ordersMatched += 1;\n        const t = o.total;\n        const n =\n          typeof t === \"bigint\"\n            ? Number(t)\n            : typeof t === \"number\"\n              ? t\n              : Number(t ?? 0);\n        if (Number.isFinite(n)) salesTotalMinor += Math.round(n);\n      }\n    }\n    if (orders.length < pageSize) break;\n  }\n\n  const openingCash = shift.opening_cash;\n  const closingCash = shift.closing_cash;\n  const expectedCash = shift.expected_cash;\n\n  return correlatedJson(correlationId, {\n    shift,\n    voids,\n    voidCount: voids.length,\n    voidAmountMinor,\n    medusaOrdersForShift: ordersMatched,\n    medusaSalesTotalMinor: salesTotalMinor,\n    openingCash,\n    closingCash,\n    expectedCash,\n    cashVariance:\n      closingCash != null && expectedCash != null\n        ? closingCash - expectedCash\n        : null,\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_shifts_id_reconciliationResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/sse:
    get:
      operationId: get_admin_sse
      summary: "Read Admin / Sse"
      description: "Admin / Sse. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Sse"]
      x-source: "import { NextRequest } from \"next/server\";\nimport {\n  registerSseClient,\n  unregisterSseClient,\n} from \"@/lib/admin-sse-hub\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, cid);\n  }\n  const userId = staff.session.user?.email ?? \"anon\";\n\n  const stream = new ReadableStream({\n    start(controller) {\n      const client = registerSseClient(controller, userId);\n      controller.enqueue(\n        new TextEncoder().encode(`event: connected\\ndata: ${JSON.stringify({ userId })}\\n\\n`),\n      );\n      req.signal.addEventListener(\"abort\", () => {\n        unregisterSseClient(client);\n      });\n    },\n  });\n\n  return new Response(stream, {\n    headers: {\n      \"Content-Type\": \"text/event-stream\",\n      \"Cache-Control\": \"no-cache\",\n      Connection: \"keep-alive\",\n    },\n  });\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_sseResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/storefront-home:
    get:
      operationId: get_admin_storefront_home
      summary: "Read Admin / Storefront Home"
      description: "Admin / Storefront Home. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Storefront Home"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getStorefrontHomeContent,\n  mergeStorefrontHomePayload,\n  upsertStorefrontHomeContent,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\n\nexport async function GET(req: Request) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const data = await getStorefrontHomeContent(sb);\n  return correlatedJson(cid, { data, devMode: process.env.AUTH_DISABLED === \"true\" });\n}\n\nasync function put(req: Request) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const merged = mergeStorefrontHomePayload(body);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  await upsertStorefrontHomeContent(sb, merged);\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/storefront-home:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_storefront_homeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_storefront_home
      summary: "Update Admin / Storefront Home"
      description: "Admin / Storefront Home. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Storefront Home"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getStorefrontHomeContent,\n  mergeStorefrontHomePayload,\n  upsertStorefrontHomeContent,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\n\nexport async function GET(req: Request) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const data = await getStorefrontHomeContent(sb);\n  return correlatedJson(cid, { data, devMode: process.env.AUTH_DISABLED === \"true\" });\n}\n\nasync function put(req: Request) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const merged = mergeStorefrontHomePayload(body);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  await upsertStorefrontHomeContent(sb, merged);\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/storefront-home:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_storefront_homeRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_storefront_homeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/storefront-public-metadata:
    get:
      operationId: get_admin_storefront_public_metadata
      summary: "Read Admin / Storefront Public Metadata"
      description: "Admin / Storefront Public Metadata. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Storefront Public Metadata"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getStorefrontPublicMetadata,\n  mergeStorefrontPublicMetadataPayload,\n  upsertStorefrontPublicMetadata,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const data = await getStorefrontPublicMetadata(sup.client);\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const merged = mergeStorefrontPublicMetadataPayload(body);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  await upsertStorefrontPublicMetadata(sup.client, merged);\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/storefront-public-metadata:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_storefront_public_metadataResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    put:
      operationId: put_admin_storefront_public_metadata
      summary: "Update Admin / Storefront Public Metadata"
      description: "Admin / Storefront Public Metadata. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Storefront Public Metadata"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  getStorefrontPublicMetadata,\n  mergeStorefrontPublicMetadataPayload,\n  upsertStorefrontPublicMetadata,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:read\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const data = await getStorefrontPublicMetadata(sup.client);\n  return correlatedJson(cid, { data });\n}\n\nasync function put(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"settings:write\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try {\n    body = await req.json();\n  } catch {\n    return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 });\n  }\n  const merged = mergeStorefrontPublicMetadataPayload(body);\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  await upsertStorefrontPublicMetadata(sup.client, merged);\n  return correlatedJson(cid, { data: merged });\n}\n\nexport const PUT = withAdminMutationIdempotency(\"/admin/storefront-public-metadata:PUT\", put);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/put_admin_storefront_public_metadataRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/put_admin_storefront_public_metadataResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/tasks/today:
    get:
      operationId: get_admin_tasks_today
      summary: "Read Admin / Tasks / Today"
      description: "Admin / Tasks / Today. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Tasks"]
      x-source: "import { NextResponse } from \"next/server\";\nimport { getPaymentPlatformMetrics } from \"@universal-music-store/platform-data\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\n\ntype TaskItem = {\n  id: string;\n  type: string;\n  title: string;\n  description: string;\n  urgency: \"high\" | \"medium\" | \"low\";\n  link: string;\n  count?: number;\n};\n\nexport async function GET() {\n  const staff = await requireStaffApiSession(\"dashboard:read\");\n  if (!staff.ok) return staff.response;\n\n  const tasks: TaskItem[] = [];\n  const sup = adminSupabaseOr503(\"tasks-today\");\n\n  try {\n    const medusaUrl = process.env.MEDUSA_BACKEND_URL || \"http://localhost:9000\";\n    const medusaKey = process.env.MEDUSA_SECRET_API_KEY || \"\";\n\n    const ordersRes = await fetch(\n      `${medusaUrl}/admin/orders?status=pending&limit=100`,\n      { headers: { \"x-medusa-access-token\": medusaKey } },\n    );\n    if (ordersRes.ok) {\n      const ordersJson = (await ordersRes.json()) as { orders?: unknown[]; count?: number };\n      const pendingCount = ordersJson.count ?? ordersJson.orders?.length ?? 0;\n      if (pendingCount > 0) {\n        tasks.push({\n          id: \"pending-orders\",\n          type: \"overdue_order\",\n          title: \"Pending orders\",\n          description: `${pendingCount} orders awaiting processing`,\n          urgency: pendingCount > 10 ? \"high\" : \"medium\",\n          link: \"/admin/orders?status=pending\",\n          count: pendingCount,\n        });\n      }\n    }\n\n    const inventoryRes = await fetch(\n      `${medusaUrl}/admin/inventory-items?limit=500`,\n      { headers: { \"x-medusa-access-token\": medusaKey } },\n    );\n    if (inventoryRes.ok) {\n      const inventoryJson = (await inventoryRes.json()) as {\n        inventory_items?: Array<{\n          stocked_quantity?: number;\n          reserved_quantity?: number;\n        }>;\n      };\n      const lowStock = (inventoryJson.inventory_items ?? []).filter((item) => {\n        const available = (item.stocked_quantity ?? 0) - (item.reserved_quantity ?? 0);\n        return available <= 5 && available >= 0;\n      });\n      if (lowStock.length > 0) {\n        tasks.push({\n          id: \"low-stock\",\n          type: \"low_stock\",\n          title: \"Low stock alerts\",\n          description: `${lowStock.length} items running low (5 or fewer units)`,\n          urgency: lowStock.length > 5 ? \"high\" : \"medium\",\n          link: \"/admin/inventory?filter=low-stock\",\n          count: lowStock.length,\n        });\n      }\n    }\n\n    const fulfillmentRes = await fetch(\n      `${medusaUrl}/admin/orders?fulfillment_status=not_fulfilled&limit=100`,\n      { headers: { \"x-medusa-access-token\": medusaKey } },\n    );\n    if (fulfillmentRes.ok) {\n      const fulfillmentJson = (await fulfillmentRes.json()) as { count?: number; orders?: unknown[] };\n      const shipmentsDue = fulfillmentJson.count ?? fulfillmentJson.orders?.length ?? 0;\n      if (shipmentsDue > 0) {\n        tasks.push({\n          id: \"shipments-due\",\n          type: \"shipment_due\",\n          title: \"Shipments due\",\n          description: `${shipmentsDue} orders need to be shipped`,\n          urgency: shipmentsDue > 5 ? \"high\" : \"medium\",\n          link: \"/admin/orders?fulfillment=not_fulfilled\",\n          count: shipmentsDue,\n        });\n      }\n    }\n  } catch (err) {\n    console.error(\"[tasks/today] Failed to fetch tasks:\", err);\n  }\n\n  try {\n    if (\"client\" in sup) {\n      const metrics = await getPaymentPlatformMetrics(sup.client);\n      if (metrics) {\n        if (metrics.paymentAttemptsStaleFinalize > 0) {\n          tasks.push({\n            id: \"stale-payment-attempts\",\n            type: \"pending_review\",\n            title: \"Stale payment sessions\",\n            description: `${metrics.paymentAttemptsStaleFinalize} checkout attempts need recovery or finalization review`,\n            urgency: metrics.paymentAttemptsStaleFinalize > 5 ? \"high\" : \"medium\",\n            link: \"/admin/payments\",\n            count: metrics.paymentAttemptsStaleFinalize,\n          });\n        }\n        if (metrics.paymentAttemptsNeedsReview > 0) {\n          tasks.push({\n            id: \"payment-needs-review\",\n            type: \"pending_review\",\n            title: \"Payment attempts need review\",\n            description: `${metrics.paymentAttemptsNeedsReview} payment rows are already marked for operator review`,\n            urgency: metrics.paymentAttemptsNeedsReview > 5 ? \"high\" : \"medium\",\n            link: \"/admin/payments\",\n            count: metrics.paymentAttemptsNeedsReview,\n          });\n        }\n      }\n    }\n  } catch (err) {\n    console.error(\"[tasks/today] Failed to fetch payment metrics:\", err);\n  }\n\n  tasks.sort((a, b) => {\n    const urgencyOrder = { high: 0, medium: 1, low: 2 };\n    return urgencyOrder[a.urgency] - urgencyOrder[b.urgency];\n  });\n\n  return NextResponse.json({ tasks });\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_tasks_todayResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/terminal-open-drawer:
    post:
      operationId: post_admin_terminal_open_drawer
      summary: "Create or execute Admin / Terminal Open Drawer"
      description: "Admin / Terminal Open Drawer. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Terminal Open Drawer"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson, stepUpRequired } from \"@/lib/admin-api-security\";\nimport { verifyDeviceBinding } from \"@/lib/admin-api-security\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { z } from \"zod\";\n\nconst drawerRequestSchema = z.object({\n  device_id: z.string().min(1).max(128).optional(),\n  reason: z.string().trim().min(1).max(200).optional(),\n}).strict();\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  if (!stepUpRequired(\"terminal.open_drawer\", req)) {\n    return correlatedJson(cid, { error: \"Step-up authentication required\" }, { status: 403 });\n  }\n  const bodyResult = await parseAdminJson(req, drawerRequestSchema);\n  if (!bodyResult.ok) return correlatedJson(cid, { error: bodyResult.error }, { status: bodyResult.status });\n  const deviceId = req.headers.get(\"x-device-id\")?.trim() || bodyResult.data.device_id;\n  if (process.env.TERMINAL_DEVICE_BINDING_REQUIRED === \"true\" && !deviceId) {\n    return correlatedJson(cid, { error: \"Device authentication required\" }, { status: 403 });\n  }\n  if (process.env.TERMINAL_DEVICE_BINDING_REQUIRED === \"true\" && deviceId) {\n    const sup = adminSupabaseOr503(cid);\n    if (\"response\" in sup) return sup.response;\n    if (!(await verifyDeviceBinding(sup.client, req, deviceId))) {\n      return correlatedJson(cid, { error: \"Device authentication failed\" }, { status: 403 });\n    }\n  }\n  const base =\n    process.env.TERMINAL_AGENT_URL?.trim() ||\n    process.env.NEXT_PUBLIC_TERMINAL_AGENT_URL?.trim() ||\n    \"http://127.0.0.1:17711\";\n  const bodyText = JSON.stringify({ ...bodyResult.data, device_id: deviceId });\n  const secret = process.env.TERMINAL_AGENT_SECRET?.trim();\n  const headers: Record<string, string> = {\n    \"Content-Type\": \"application/json\",\n  };\n  if (secret) {\n    headers[\"X-Terminal-Agent-Secret\"] = secret;\n  }\n  const res = await fetch(`${base.replace(/\\/$/, \"\")}/open-drawer`, {\n    method: \"POST\",\n    headers,\n    body: bodyText.length > 0 ? bodyText : \"{}\",\n  });\n  const text = await res.text();\n  let parsed: unknown = text;\n  try {\n    parsed = JSON.parse(text) as unknown;\n  } catch {\n    parsed = { raw: text };\n  }\n  return correlatedJson(cid, parsed, { status: res.status });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/terminal-open-drawer:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_terminal_open_drawerRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_terminal_open_drawerResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/terminal-print:
    post:
      operationId: post_admin_terminal_print
      summary: "Create or execute Admin / Terminal Print"
      description: "Admin / Terminal Print. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Terminal Print"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { terminalPrintBodySchema } from \"@/lib/terminal-print-schemas\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const raw = await req.json().catch(() => null);\n  const parsed = terminalPrintBodySchema.safeParse(raw);\n  if (!parsed.success) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Invalid receipt print payload\",\n        details: parsed.error.flatten(),\n      },\n      { status: 400 },\n    );\n  }\n\n  const base =\n    process.env.TERMINAL_AGENT_URL?.trim() ||\n    process.env.NEXT_PUBLIC_TERMINAL_AGENT_URL?.trim() ||\n    \"http://127.0.0.1:17711\";\n  const bodyText = JSON.stringify(parsed.data);\n  const secret = process.env.TERMINAL_AGENT_SECRET?.trim();\n  const headers: Record<string, string> = {\n    \"Content-Type\": \"application/json\",\n  };\n  if (secret) {\n    headers[\"X-Terminal-Agent-Secret\"] = secret;\n  }\n  const res = await fetch(`${base.replace(/\\/$/, \"\")}/print-receipt`, {\n    method: \"POST\",\n    headers,\n    body: bodyText,\n  });\n  const text = await res.text();\n  let agentPayload: unknown = text;\n  try {\n    agentPayload = JSON.parse(text) as unknown;\n  } catch {\n    agentPayload = { raw: text };\n  }\n  return correlatedJson(cid, agentPayload, { status: res.status });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/terminal-print:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_terminal_printRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_terminal_printResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/terminal-print-label:
    post:
      operationId: post_admin_terminal_print_label
      summary: "Create or execute Admin / Terminal Print Label"
      description: "Admin / Terminal Print Label. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Terminal Print Label"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { terminalPrintLabelBodySchema } from \"@/lib/terminal-print-schemas\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) {\n    return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  }\n  if (!staffSessionAllows(session, \"pos:use\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n\n  const raw = await req.json().catch(() => null);\n  const parsed = terminalPrintLabelBodySchema.safeParse(raw);\n  if (!parsed.success) {\n    return correlatedJson(\n      cid,\n      {\n        error: \"Invalid print label payload\",\n        details: parsed.error.flatten(),\n      },\n      { status: 400 },\n    );\n  }\n\n  const base =\n    process.env.TERMINAL_AGENT_URL?.trim() ||\n    process.env.NEXT_PUBLIC_TERMINAL_AGENT_URL?.trim() ||\n    \"http://127.0.0.1:17711\";\n  const secret = process.env.TERMINAL_AGENT_SECRET?.trim();\n  const headers: Record<string, string> = {\n    \"Content-Type\": \"application/json\",\n  };\n  if (secret) {\n    headers[\"X-Terminal-Agent-Secret\"] = secret;\n  }\n  const res = await fetch(`${base.replace(/\\/$/, \"\")}/print-label`, {\n    method: \"POST\",\n    headers,\n    body: JSON.stringify(parsed.data),\n  });\n  const text = await res.text();\n  let parsedOut: unknown = text;\n  try {\n    parsedOut = JSON.parse(text) as unknown;\n  } catch {\n    parsedOut = { raw: text };\n  }\n  return correlatedJson(cid, parsedOut, { status: res.status });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/terminal-print-label:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_terminal_print_labelRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_terminal_print_labelResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/voids:
    get:
      operationId: get_admin_voids
      summary: "Read Admin / Voids"
      description: "Admin / Voids. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Voids"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  recordVoid,\n  listVoids,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst voidSchema = z.object({\n  shift_id: z.string().uuid().optional(),\n  employee_id: z.string().uuid(),\n  approved_by: z.string().uuid().optional(),\n  order_id: z.string().trim().max(120).optional(),\n  line_item_id: z.string().trim().max(120).optional(),\n  action: z.enum([\"void_item\", \"void_order\", \"refund\", \"discount_override\"]),\n  amount: z.number().finite().nonnegative().max(1_000_000).optional(),\n  reason: z.string().trim().min(1).max(500).optional(),\n  pin_verified: z.boolean().optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:void\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const shiftId = req.nextUrl.searchParams.get(\"shift_id\") ?? undefined;\n  const data = await listVoids(sb, { shiftId, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:void\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = voidSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid void payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const v = await recordVoid(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: v }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/voids:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_voidsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_admin_voids
      summary: "Create or execute Admin / Voids"
      description: "Admin / Voids. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Voids"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { NextRequest } from \"next/server\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport {\n  recordVoid,\n  listVoids,\n} from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nconst voidSchema = z.object({\n  shift_id: z.string().uuid().optional(),\n  employee_id: z.string().uuid(),\n  approved_by: z.string().uuid().optional(),\n  order_id: z.string().trim().max(120).optional(),\n  line_item_id: z.string().trim().max(120).optional(),\n  action: z.enum([\"void_item\", \"void_order\", \"refund\", \"discount_override\"]),\n  amount: z.number().finite().nonnegative().max(1_000_000).optional(),\n  reason: z.string().trim().min(1).max(500).optional(),\n  pin_verified: z.boolean().optional(),\n}).strict();\n\nexport async function GET(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:void\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const shiftId = req.nextUrl.searchParams.get(\"shift_id\") ?? undefined;\n  const data = await listVoids(sb, { shiftId, organizationId: organization.id });\n  return correlatedJson(cid, { data });\n}\n\nasync function post(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user) return correlatedJson(cid, { error: \"Unauthorized\" }, { status: 401 });\n  if (!staffSessionAllows(session, \"pos:void\")) {\n    return correlatedJson(cid, { error: \"Forbidden\" }, { status: 403 });\n  }\n  let body: unknown;\n  try { body = await req.json(); } catch { return correlatedJson(cid, { error: \"Invalid JSON\" }, { status: 400 }); }\n  const parsed = voidSchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid void payload\" }, { status: 400 });\n  const sup = adminSupabaseOr503(cid);\n  if (\"response\" in sup) return sup.response;\n  const sb = sup.client;\n  const organization = await resolveStaffOrganization(sb, session.user.email);\n  if (!organization) return correlatedJson(cid, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const v = await recordVoid(sb, { ...parsed.data, organization_id: organization.id });\n  return correlatedJson(cid, { data: v }, { status: 201 });\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/voids:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_voidsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_voidsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/workflow/entities:
    get:
      operationId: get_admin_workflow_entities
      summary: "Read Admin / Workflow / Entities"
      description: "Admin / Workflow / Entities. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Workflow"]
      x-source: "import { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { listEntityWorkflows } from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport const dynamic = \"force-dynamic\";\n\n/**\n * Lists recent `admin_entity_workflow` rows (catalog, CMS, campaigns, etc.).\n * Permission: `dashboard:read`.\n */\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const auth = await requireStaffApiSession(\"dashboard:read\");\n  if (!auth.ok) return auth.response;\n\n  const url = new URL(req.url);\n  const limit = Math.min(\n    200,\n    Math.max(1, Number(url.searchParams.get(\"limit\") ?? \"50\") || 50),\n  );\n  const offset = Math.max(0, Number(url.searchParams.get(\"offset\") ?? \"0\") || 0);\n  const entityType = url.searchParams.get(\"entity_type\")?.trim() || undefined;\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    auth.session.user?.email,\n  );\n  if (!organization) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  }\n\n  try {\n    const rows = await listEntityWorkflows(sup.client, {\n      organizationId: organization.id,\n      limit,\n      offset,\n      entityType,\n    });\n    return correlatedJson(correlationId, { rows });\n  } catch (e) {\n    const message = e instanceof Error ? e.message : \"Workflow list unavailable\";\n    return correlatedJson(correlationId, { error: message }, { status: 502 });\n  }\n}\n"
      x-permission: "dashboard:read"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_admin_workflow_entitiesResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/workflow/transition:
    post:
      operationId: post_admin_workflow_transition
      summary: "Create or execute Admin / Workflow / Transition"
      description: "Admin / Workflow / Transition. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Admin / Workflow"]
      x-source: "import { withAdminMutationIdempotency } from \"@/lib/admin-mutation-idempotency\";\nimport { staffSessionAllows } from \"@universal-music-store/database\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  transitionEntityWorkflow,\n  type EntityWorkflowType,\n} from \"@/lib/admin-workflow\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { jsonFromAdminOperationResult } from \"@/lib/staff-api-operation\";\nimport { insertStaffAuditLog } from \"@/lib/staff-audit\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { parseAdminJson } from \"@/lib/admin-api-security\";\nimport { z } from \"zod\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\n\nexport const dynamic = \"force-dynamic\";\n\nconst ENTITY_PERMS: Record<EntityWorkflowType, string> = {\n  catalog_product: \"catalog:write\",\n  sales_order: \"orders:write\",\n  inventory_adjustment: \"inventory:write\",\n  campaign: \"campaigns:write\",\n  cms_page: \"content:write\",\n  chat_order: \"chat_orders:manage\",\n};\nconst transitionSchema = z\n  .object({\n    entity_type: z.enum([\n      \"catalog_product\",\n      \"sales_order\",\n      \"inventory_adjustment\",\n      \"campaign\",\n      \"cms_page\",\n      \"chat_order\",\n    ]),\n    entity_id: z.string().trim().min(1).max(200),\n    to_state: z.string().trim().min(1).max(40),\n    notes: z.string().trim().max(2000).nullable().optional(),\n    expected_updated_at: z.string().datetime().optional(),\n  })\n  .strict();\n\nasync function post(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const session = await getStaffSession();\n  if (!session?.user?.email) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unauthorized\" },\n      { status: 401 },\n    );\n  }\n\n  const parsed = await parseAdminJson(req, transitionSchema);\n  if (!parsed.ok)\n    return correlatedJson(\n      correlationId,\n      { error: parsed.error },\n      { status: parsed.status },\n    );\n  const {\n    entity_type: entityType,\n    entity_id: entityId,\n    to_state: toState,\n    notes,\n    expected_updated_at: expectedUpdatedAt,\n  } = parsed.data;\n\n  const perm = ENTITY_PERMS[entityType];\n  if (!perm) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid entity_type\" },\n      { status: 400 },\n    );\n  }\n  if (!staffSessionAllows(session, perm)) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Forbidden\" },\n      { status: 403 },\n    );\n  }\n\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) {\n    return sup.response;\n  }\n  const organization = await resolveStaffOrganization(\n    sup.client,\n    session.user.email,\n  );\n  if (!organization) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Organization membership is not configured\" },\n      { status: 403 },\n    );\n  }\n\n  const scopedTableByEntity: Partial<Record<EntityWorkflowType, string>> = {\n    campaign: \"campaigns\",\n    chat_order: \"chat_order_intake\",\n    cms_page: \"cms_pages\",\n    inventory_adjustment: \"staff_catalog_inventory_audit\",\n  };\n  const scopedTable = scopedTableByEntity[entityType];\n  if (scopedTable) {\n    const { data: entity, error } = await sup.client\n      .from(scopedTable)\n      .select(\"id\")\n      .eq(\"id\", entityId)\n      .eq(\"organization_id\", organization.id)\n      .maybeSingle();\n    if (error) {\n      console.error(JSON.stringify({\n        scope: \"workflow.entity_lookup\",\n        entity_type: entityType,\n        correlation_id: correlationId,\n        error: error.message,\n      }));\n      return correlatedJson(correlationId, { error: \"Resource lookup unavailable\" }, { status: 502 });\n    }\n    if (!entity) {\n      return correlatedJson(correlationId, { error: \"Resource not found\" }, { status: 404 });\n    }\n  }\n\n  const result = await transitionEntityWorkflow(sup.client, {\n    organizationId: organization.id,\n    entityType,\n    entityId,\n    toState,\n    actorEmail: session.user.email,\n    notes,\n    expectedUpdatedAt,\n  });\n\n  if (!result.ok) {\n    return jsonFromAdminOperationResult(correlationId, result, result.httpStatus);\n  }\n\n  await insertStaffAuditLog(sup.client, {\n    actorEmail: session.user.email,\n    action: \"workflow.transition\",\n    resource: `${entityType}:${entityId}`,\n    details: { to_state: toState },\n  });\n\n  return jsonFromAdminOperationResult(correlationId, result, 200);\n}\n\nexport const POST = withAdminMutationIdempotency(\"/admin/workflow/transition:POST\", post);\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_admin_workflow_transitionRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_admin_workflow_transitionResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /auth/{...nextauth}:
    get:
      operationId: get_auth_nextauth
      summary: "Read Auth / ...Nextauth"
      description: "Auth / ...Nextauth. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Auth / ...Nextauth"]
      x-source: "import type { NextRequest } from \"next/server\";\nimport NextAuth from \"next-auth\";\n\nimport { buildAuthOptions } from \"@/lib/auth\";\n\nexport const dynamic = \"force-dynamic\";\n\ntype NextAuthRouteContext = { params: Promise<{ nextauth: string[] }> };\n\nexport async function GET(req: NextRequest, context: NextAuthRouteContext) {\n  return NextAuth(req, context, buildAuthOptions());\n}\n\nexport async function POST(req: NextRequest, context: NextAuthRouteContext) {\n  return NextAuth(req, context, buildAuthOptions());\n}\n"
      x-permission: "internal-signature"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: ...nextauth
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_auth_nextauthResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
    post:
      operationId: post_auth_nextauth
      summary: "Create or execute Auth / ...Nextauth"
      description: "Auth / ...Nextauth. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Auth / ...Nextauth"]
      x-source: "import type { NextRequest } from \"next/server\";\nimport NextAuth from \"next-auth\";\n\nimport { buildAuthOptions } from \"@/lib/auth\";\n\nexport const dynamic = \"force-dynamic\";\n\ntype NextAuthRouteContext = { params: Promise<{ nextauth: string[] }> };\n\nexport async function GET(req: NextRequest, context: NextAuthRouteContext) {\n  return NextAuth(req, context, buildAuthOptions());\n}\n\nexport async function POST(req: NextRequest, context: NextAuthRouteContext) {\n  return NextAuth(req, context, buildAuthOptions());\n}\n"
      x-permission: "internal-signature"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: ...nextauth
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_auth_nextauthRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_auth_nextauthResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /integrations/channels/webhook:
    post:
      operationId: post_integrations_channels_webhook
      summary: "Create or execute Integrations / Channels / Webhook"
      description: "Integrations / Channels / Webhook. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Integrations / Channels"]
      x-source: "import { tryCreateSupabaseClient } from \"@universal-music-store/database\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { gateChannelWebhookSecretConfigured } from \"@/lib/channel-webhook-policy\";\nimport { verifyChannelWebhookSignature } from \"@/lib/channel-webhook-signature\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport {\n  containsDangerousKey,\n  verifySignedRequest,\n} from \"@/lib/admin-api-security\";\nimport {\n  hashChannelPayload,\n  validateChannelScope,\n} from \"@/lib/channel-security\";\n\nexport const dynamic = \"force-dynamic\";\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const contentLength = Number(req.headers.get(\"content-length\") ?? \"0\");\n  if (Number.isFinite(contentLength) && contentLength > 512_000)\n    return correlatedJson(\n      correlationId,\n      { error: \"Payload too large\" },\n      { status: 413 },\n    );\n  const raw = await req.text();\n  if (new TextEncoder().encode(raw).byteLength > 512_000)\n    return correlatedJson(\n      correlationId,\n      { error: \"Payload too large\" },\n      { status: 413 },\n    );\n  const secret = process.env.CHANNEL_WEBHOOK_SECRET?.trim();\n  const gate = gateChannelWebhookSecretConfigured(\n    secret,\n    process.env.VERCEL_ENV,\n    process.env.NODE_ENV,\n  );\n  if (!gate.ok) {\n    logAdminApiEvent({\n      route: \"POST /api/integrations/channels/webhook\",\n      correlationId,\n      phase: \"error\",\n      detail: { reason: \"missing_channel_webhook_secret\" },\n    });\n    return correlatedJson(\n      correlationId,\n      { error: gate.error },\n      { status: gate.status },\n    );\n  }\n  const tenantHeader = req.headers.get(\"x-tenant-key\")?.trim();\n  if (!tenantHeader || !/^[A-Za-z0-9._:-]{1,128}$/.test(tenantHeader)) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing tenant scope\" },\n      { status: 400 },\n    );\n  }\n  if (secret) {\n    const sig = req.headers.get(\"x-channel-signature\") ?? \"\";\n    const timestamp = req.headers.get(\"x-channel-timestamp\") ?? \"\";\n    const signed = timestamp\n      ? verifySignedRequest(\n          `${tenantHeader || \"default\"}:${raw}`,\n          secret,\n          sig,\n          timestamp,\n        )\n      : process.env.NODE_ENV !== \"production\" &&\n        verifyChannelWebhookSignature(\n          `${tenantHeader || \"default\"}:${raw}`,\n          secret,\n          sig,\n        );\n    if (!signed) {\n      logAdminApiEvent({\n        route: \"POST /api/integrations/channels/webhook\",\n        correlationId,\n        phase: \"error\",\n        detail: { reason: \"bad_signature\" },\n      });\n      return correlatedJson(\n        correlationId,\n        { error: \"Invalid signature\" },\n        { status: 401 },\n      );\n    }\n  }\n\n  let payload: Record<string, unknown> = {};\n  try {\n    const parsed: unknown = raw ? JSON.parse(raw) : {};\n    if (!parsed || typeof parsed !== \"object\" || Array.isArray(parsed))\n      throw new Error(\"object_required\");\n    payload = parsed as Record<string, unknown>;\n    if (containsDangerousKey(payload)) throw new Error(\"unsafe_payload\");\n    for (const key of [\"channel\", \"source\", \"event_type\", \"type\"]) {\n      if (\n        key in payload &&\n        (typeof payload[key] !== \"string\" || String(payload[key]).length > 256)\n      )\n        throw new Error(\"invalid_field\");\n    }\n  } catch {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid JSON\" },\n      { status: 400 },\n    );\n  }\n\n  let channel = \"unknown\";\n  if (typeof payload.channel === \"string\") channel = payload.channel;\n  else if (typeof payload.source === \"string\") channel = payload.source;\n  const requestedTenant = tenantHeader;\n  const allowedChannels = (process.env.MEDUSA_SALES_CHANNEL_IDS ?? \"\")\n    .split(\",\")\n    .map((value) => value.trim())\n    .filter(Boolean);\n  if (\n    allowedChannels.length &&\n    !validateChannelScope({ requested: channel, allowed: allowedChannels })\n  ) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Channel is not authorized\" },\n      { status: 403 },\n    );\n  }\n\n  let eventType = \"ingest\";\n  if (typeof payload.event_type === \"string\") eventType = payload.event_type;\n  else if (typeof payload.type === \"string\") eventType = payload.type;\n\n  try {\n    const supabase = tryCreateSupabaseClient();\n    if (!supabase) {\n      return correlatedJson(\n        correlationId,\n        {\n          error: \"Supabase is not configured\",\n          code: \"SUPABASE_NOT_CONFIGURED\",\n        },\n        { status: 503 },\n      );\n    }\n    const nonce = req.headers.get(\"x-channel-nonce\")?.trim();\n    if (\n      process.env.NODE_ENV === \"production\" &&\n      (!nonce || nonce.length < 16)\n    ) {\n      return correlatedJson(\n        correlationId,\n        { error: \"Missing replay protection\" },\n        { status: 400 },\n      );\n    }\n    if (nonce && !/^[A-Za-z0-9._:-]{16,200}$/.test(nonce)) {\n      return correlatedJson(\n        correlationId,\n        { error: \"Invalid replay token\" },\n        { status: 400 },\n      );\n    }\n    if (nonce) {\n      const { error: replayError } = await supabase\n        .from(\"admin_webhook_replays\")\n        .insert({\n          channel: `${requestedTenant}:${channel}`,\n          nonce,\n          correlation_id: correlationId,\n        });\n      if (replayError) {\n        return correlatedJson(\n          correlationId,\n          { error: \"Duplicate or invalid webhook\" },\n          { status: 409 },\n        );\n      }\n    }\n    const { error } = await supabase.from(\"channel_sync_events\").insert({\n      tenant_key: requestedTenant,\n      channel,\n      event_type: eventType,\n      payload,\n      payload_hash: hashChannelPayload(raw),\n      metadata: { correlation_id: correlationId, nonce: nonce ?? null },\n    });\n    if (error?.code === \"23505\") {\n      logAdminApiEvent({\n        route: \"POST /api/integrations/channels/webhook\",\n        correlationId,\n        phase: \"ok\",\n        detail: { channel, deduplicated: true },\n      });\n      return correlatedJson(correlationId, { ok: true, deduplicated: true });\n    }\n    if (error) {\n      logAdminApiEvent({\n        route: \"POST /api/integrations/channels/webhook\",\n        correlationId,\n        phase: \"error\",\n        detail: { db: error.message },\n      });\n      return correlatedJson(\n        correlationId,\n        { error: \"Unable to persist event\" },\n        { status: 502 },\n      );\n    }\n  } catch {\n    return correlatedJson(\n      correlationId,\n      { error: \"Unable to process webhook\" },\n      { status: 502 },\n    );\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/integrations/channels/webhook\",\n    correlationId,\n    phase: \"ok\",\n    detail: { channel },\n  });\n\n  return correlatedJson(correlationId, { ok: true });\n}\n"
      x-permission: "internal-signature"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_integrations_channels_webhookResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /integrations/chat-orders/intake:
    post:
      operationId: post_integrations_chat_orders_intake
      summary: "Create or execute Integrations / Chat Orders / Intake"
      description: "Integrations / Chat Orders / Intake. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Integrations / Chat Orders"]
      x-source: "import {\n  tryCreateSupabaseClient,\n  staffHasPermission,\n} from \"@universal-music-store/database\";\nimport {\n  getMedusaAdminSdk,\n  getMedusaRegionId,\n  getMedusaSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffSession } from \"@/lib/requireStaffSession\";\nimport {\n  correlatedJson,\n  correlatedError,\n  tagResponse,\n} from \"@/lib/staff-api-response\";\nimport { getServerSession } from \"next-auth/next\";\nimport { authOptions } from \"@/lib/auth\";\nimport { parseAdminJson, verifySignedRequest } from \"@/lib/admin-api-security\";\nimport { getStaffSession } from \"@/lib/requireStaffSession\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { z } from \"zod\";\n\nexport const dynamic = \"force-dynamic\";\n\ntype IntakeItem = { variantId: string; quantity: number };\nconst intakeSchema = z\n  .object({\n    source: z.string().trim().min(1).max(40).default(\"chat\"),\n    raw_text: z.string().max(10_000).optional(),\n    phone: z.string().trim().max(40).optional(),\n    address: z.string().trim().max(500).optional(),\n    items: z\n      .array(\n        z\n          .object({\n            variantId: z.string().trim().min(1).max(128),\n            quantity: z.number().int().positive().max(100),\n          })\n          .strict(),\n      )\n      .min(1, \"At least one item is required\")\n      .max(100),\n  })\n  .strict();\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const internalKey = process.env.INTERNAL_CHAT_INTAKE_KEY?.trim();\n  const headerKey = req.headers.get(\"x-internal-key\")?.trim();\n  const useInternal = internalKey && headerKey === internalKey;\n\n  if (!useInternal) {\n    const staff = await requireStaffSession();\n    if (!staff.ok) {\n      return tagResponse(staff.response, correlationId);\n    }\n    const session = await getServerSession(authOptions);\n    const perms = session?.user?.permissions;\n    if (!staffHasPermission(perms ?? [], \"chat_orders:manage\")) {\n      return correlatedError(\n        correlationId,\n        403,\n        \"Forbidden\",\n        \"MISSING_PERMISSION\",\n      );\n    }\n  }\n\n  if (useInternal && process.env.NODE_ENV === \"production\") {\n    const secret = process.env.INTERNAL_CHAT_INTAKE_KEY?.trim();\n    const timestamp = req.headers.get(\"x-internal-timestamp\") ?? \"\";\n    const signature = req.headers.get(\"x-internal-signature\") ?? \"\";\n    const raw = await req.clone().text();\n    if (!secret || !verifySignedRequest(raw, secret, signature, timestamp)) {\n      return correlatedError(\n        correlationId,\n        401,\n        \"Unauthorized\",\n        \"UNAUTHORIZED\",\n      );\n    }\n  }\n\n  const parsed = await parseAdminJson(req, intakeSchema);\n  if (!parsed.ok)\n    return correlatedError(\n      correlationId,\n      parsed.status,\n      parsed.error,\n      \"VALIDATION_ERROR\",\n    );\n  const body = parsed.data;\n  const actorSession = useInternal ? null : await getStaffSession();\n  const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n  if (!idempotencyKey || !/^[A-Za-z0-9._:-]{8,200}$/.test(idempotencyKey)) {\n    return correlatedError(\n      correlationId,\n      400,\n      \"Idempotency-Key is required\",\n      \"BAD_REQUEST\",\n    );\n  }\n\n  const source = body.source;\n  const items: IntakeItem[] = [];\n  for (const row of body.items ?? [])\n    items.push({ variantId: row.variantId, quantity: row.quantity });\n\n  const supabase = tryCreateSupabaseClient();\n  if (!supabase) {\n    return correlatedError(\n      correlationId,\n      503,\n      \"Supabase is not configured\",\n      \"SUPABASE_NOT_CONFIGURED\",\n    );\n  }\n  const organizationId = useInternal\n    ? process.env.INTERNAL_CHAT_ORGANIZATION_ID?.trim()\n    : (await resolveStaffOrganization(supabase, actorSession?.user?.email))?.id;\n  if (!organizationId) {\n    return correlatedError(\n      correlationId,\n      403,\n      \"Organization membership is not configured\",\n      \"FORBIDDEN\",\n    );\n  }\n  const { data: existing } = await supabase\n    .from(\"chat_order_intake\")\n    .select(\"id,medusa_draft_order_id,status\")\n    .eq(\"idempotency_key\", idempotencyKey)\n    .eq(\"organization_id\", organizationId)\n    .maybeSingle();\n  if (existing) {\n    return correlatedJson(correlationId, {\n      intakeId: existing.id,\n      draftOrderId: existing.medusa_draft_order_id ?? null,\n      status: existing.status,\n      replayed: true,\n    });\n  }\n  const { data: inserted, error: insErr } = await supabase\n    .from(\"chat_order_intake\")\n    .insert({\n      idempotency_key: idempotencyKey,\n      organization_id: organizationId,\n      source,\n      raw_text: body.raw_text ?? null,\n      phone: body.phone ?? null,\n      address: body.address ?? null,\n      items,\n      status: \"pending\",\n      metadata: { correlation_id: correlationId },\n    })\n    .select(\"id\")\n    .maybeSingle();\n\n  if (insErr?.code === \"23505\") {\n    const { data: raced } = await supabase\n      .from(\"chat_order_intake\")\n      .select(\"id,medusa_draft_order_id,status\")\n      .eq(\"idempotency_key\", idempotencyKey)\n      .eq(\"organization_id\", organizationId)\n      .maybeSingle();\n    if (raced)\n      return correlatedJson(correlationId, {\n        intakeId: raced.id,\n        draftOrderId: raced.medusa_draft_order_id ?? null,\n        status: raced.status,\n        replayed: true,\n      });\n  }\n  if (insErr || !inserted?.id) {\n    logAdminApiEvent({\n      route: \"POST /api/integrations/chat-orders/intake\",\n      correlationId,\n      phase: \"error\",\n      detail: { db: insErr?.message },\n    });\n    return correlatedError(\n      correlationId,\n      502,\n      \"Unable to record chat order\",\n      \"INTERNAL_ERROR\",\n    );\n  }\n\n  let draftOrderId: string | undefined;\n  if (items.length > 0) {\n    const adminSdk = getMedusaAdminSdk();\n    const regionId = getMedusaRegionId();\n    const salesChannelId = getMedusaSalesChannelId();\n    if (adminSdk && regionId && salesChannelId) {\n      try {\n        const { draft_order } = await adminSdk.admin.draftOrder.create({\n          email: \"chat-intake@instore.local\",\n          region_id: regionId,\n          sales_channel_id: salesChannelId,\n          items: items.map((i) => ({\n            variant_id: i.variantId,\n            quantity: Math.max(1, Math.floor(Number(i.quantity) || 1)),\n          })),\n          metadata: { chat_intake_id: inserted.id },\n        });\n        draftOrderId = draft_order?.id;\n        if (draftOrderId) {\n          await supabase\n            .from(\"chat_order_intake\")\n            .update({\n              medusa_draft_order_id: draftOrderId,\n              status: \"draft_created\",\n            })\n            .eq(\"id\", inserted.id);\n        }\n      } catch (e) {\n        logAdminApiEvent({\n          route: \"POST /api/integrations/chat-orders/intake\",\n          correlationId,\n          phase: \"error\",\n          detail: { medusa: e instanceof Error ? e.message : String(e) },\n        });\n      }\n    }\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/integrations/chat-orders/intake\",\n    correlationId,\n    phase: \"ok\",\n    detail: { intakeId: inserted.id },\n  });\n\n  return correlatedJson(correlationId, {\n    intakeId: inserted.id,\n    draftOrderId: draftOrderId ?? null,\n  });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_integrations_chat_orders_intakeRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_integrations_chat_orders_intakeResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /integrations/couriers:
    get:
      operationId: get_integrations_couriers
      summary: "Read Integrations / Couriers"
      description: "Integrations / Couriers. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Integrations / Couriers"]
      x-source: "import { COURIER_REGISTRY } from \"@/lib/courier-registry\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffSession();\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n  logAdminApiEvent({\n    route: \"GET /api/integrations/couriers\",\n    correlationId,\n    phase: \"ok\",\n    detail: { count: COURIER_REGISTRY.length },\n  });\n  return correlatedJson(correlationId, { couriers: COURIER_REGISTRY });\n}\n"
      x-permission: "staff-session"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_integrations_couriersResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /integrations/couriers/telemetry:
    post:
      operationId: post_integrations_couriers_telemetry
      summary: "Create or execute Integrations / Couriers / Telemetry"
      description: "Integrations / Couriers / Telemetry. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Integrations / Couriers"]
      x-source: "import { createHmac, timingSafeEqual } from \"node:crypto\";\nimport { NextRequest } from \"next/server\";\nimport { z } from \"zod\";\nimport { validateGeoPoint } from \"@universal-music-store/platform-data\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\n\nconst telemetrySchema = z.object({\n  shipment_id: z.string().trim().min(1).max(160),\n  courier_id: z.string().trim().min(1).max(160),\n  latitude: z.number().finite().min(-90).max(90),\n  longitude: z.number().finite().min(-180).max(180),\n  speed_kph: z.number().finite().nonnegative().max(500).optional(),\n  heading: z.number().finite().min(0).max(360).optional(),\n  captured_at: z.string().datetime().optional(),\n}).strict();\n\nfunction signatureMatches(raw: string, timestamp: string, signature: string, secret: string): boolean {\n  if (!/^\\d{10}$/.test(timestamp) || !/^[a-f0-9]{64}$/i.test(signature)) return false;\n  if (Math.abs(Math.floor(Date.now() / 1000) - Number(timestamp)) > 300) return false;\n  const expected = createHmac(\"sha256\", secret).update(`${timestamp}.${raw}`).digest(\"hex\");\n  return timingSafeEqual(Buffer.from(expected, \"hex\"), Buffer.from(signature, \"hex\"));\n}\n\nexport async function POST(req: NextRequest) {\n  const cid = getCorrelationId(req);\n  const secret = process.env.COURIER_TELEMETRY_SECRET?.trim() || process.env.CHANNEL_WEBHOOK_SECRET?.trim();\n  const timestamp = req.headers.get(\"x-telemetry-timestamp\")?.trim() ?? \"\";\n  const signature = req.headers.get(\"x-telemetry-signature\")?.trim() ?? \"\";\n  const idempotencyKey = req.headers.get(\"idempotency-key\")?.trim();\n  const tenantKey = req.headers.get(\"x-tenant-key\")?.trim();\n  if (!secret || !tenantKey || !idempotencyKey) return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 400 });\n  if (idempotencyKey.length > 160) return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 400 });\n  const raw = await req.text();\n  if (!signatureMatches(raw, timestamp, signature, secret)) return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 401 });\n  let body: unknown;\n  try { body = JSON.parse(raw); } catch { return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 400 }); }\n  const parsed = telemetrySchema.safeParse(body);\n  if (!parsed.success) return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 400 });\n  const point = { latitude: parsed.data.latitude, longitude: parsed.data.longitude };\n  validateGeoPoint(point);\n  const sup = adminSupabaseOr503(cid); if (\"response\" in sup) return sup.response;\n  const courierQuery = /^[0-9a-f-]{36}$/i.test(parsed.data.courier_id)\n    ? sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"id\", parsed.data.courier_id).eq(\"tenant_key\", tenantKey)\n    : sup.client.from(\"delivery_logistics_couriers\").select(\"id\").eq(\"slug\", parsed.data.courier_id).eq(\"tenant_key\", tenantKey);\n  const courier = await courierQuery.maybeSingle();\n  if (!courier.data?.id) return correlatedJson(cid, { error: \"Invalid courier request\" }, { status: 404 });\n  const result = await sup.client.from(\"delivery_logistics_telemetry\").insert({\n    tenant_key: tenantKey,\n    shipment_id: parsed.data.shipment_id,\n    courier_id: courier.data.id,\n    latitude: point.latitude,\n    longitude: point.longitude,\n    speed_kph: parsed.data.speed_kph ?? null,\n    heading: parsed.data.heading ?? null,\n    captured_at: parsed.data.captured_at ?? new Date().toISOString(),\n    idempotency_key: idempotencyKey,\n  }).select(\"*\").single();\n  if (result.error) {\n    if (/duplicate key/i.test(result.error.message)) return correlatedJson(cid, { error: \"Duplicate telemetry event\" }, { status: 409 });\n    return correlatedJson(cid, { error: \"Unable to record telemetry\" }, { status: 503 });\n  }\n  return correlatedJson(cid, { data: result.data }, { status: 201 });\n}\n"
      x-permission: "internal-signature"
      x-tenant-scoped: false
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_integrations_couriers_telemetryRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_integrations_couriers_telemetryResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /medusa/orders/{orderId}:
    patch:
      operationId: patch_medusa_orders_orderId
      summary: "Update Medusa / Orders / OrderId"
      description: "Medusa / Orders / OrderId. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Medusa / Orders"]
      x-source: "import { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport {\n  claimAdminIdempotency,\n  completeAdminIdempotency,\n  getRequestHash,\n  parseAdminJson,\n  requireIdempotencyKey,\n} from \"@/lib/admin-api-security\";\nimport {\n  fetchMedusaOrderJson,\n  patchMedusaOrderMetadata,\n} from \"@/lib/medusa-order-bridge\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { z } from \"zod\";\n\nconst orderStatusSchema = z.object({\n  status: z.string().trim().min(1).max(64),\n}).strict();\n\nexport async function PATCH(\n  req: Request,\n  ctx: { params: Promise<{ orderId: string }> },\n) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"orders:write\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  logAdminApiEvent({\n    route: \"PATCH /api/medusa/orders/[orderId]\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  const { orderId } = await ctx.params;\n  if (!orderId?.startsWith(\"order_\")) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Invalid order id\" },\n      { status: 400 },\n    );\n  }\n\n  const idempotencyKey = requireIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(req, orderStatusSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const status = parsed.data.status;\n\n  const order = await fetchMedusaOrderJson(orderId);\n  if (!order) {\n    return correlatedJson(correlationId, { error: \"Order not found\" }, { status: 404 });\n  }\n  const currentMetadata = (order.metadata as Record<string, unknown> | undefined) ?? {};\n  if (currentMetadata.organization_id !== organization.id) {\n    return correlatedJson(correlationId, { error: \"Order not found\" }, { status: 404 });\n  }\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email?.trim().toLowerCase() ?? \"unknown\"}`,\n    actionKey: `medusa.order.status:${orderId}`,\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Request is already being processed or key was reused\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n\n  const meta = { ...currentMetadata, oms_status: status };\n  const result = await patchMedusaOrderMetadata(orderId, meta);\n  if (!result.ok) {\n    logAdminApiEvent({\n      route: \"PATCH /api/medusa/orders/[orderId]\",\n      correlationId,\n      phase: \"error\",\n      detail: { orderId, error: result.error },\n    });\n    const body = { error: \"Unable to update order\" };\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n\n  logAdminApiEvent({\n    route: \"PATCH /api/medusa/orders/[orderId]\",\n    correlationId,\n    phase: \"ok\",\n    detail: { orderId, status },\n  });\n\n  const body = { status };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "orders:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/patch_medusa_orders_orderIdRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/patch_medusa_orders_orderIdResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /medusa/shipments:
    post:
      operationId: post_medusa_shipments
      summary: "Create or execute Medusa / Shipments"
      description: "Medusa / Shipments. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Medusa / Shipments"]
      x-source: "import { randomUUID } from \"node:crypto\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { claimAdminIdempotency, completeAdminIdempotency, getRequestHash, parseAdminJson, requireIdempotencyKey } from \"@/lib/admin-api-security\";\nimport {\n  fetchMedusaOrderJson,\n  patchMedusaOrderMetadata,\n} from \"@/lib/medusa-order-bridge\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { z } from \"zod\";\n\nconst shipmentSchema = z.object({\n  orderId: z.string().trim().regex(/^order_/),\n  trackingNumber: z.string().trim().min(1).max(200),\n  carrierSlug: z.string().trim().min(1).max(80).default(\"jtexpress-ph\"),\n  labelUrl: z.string().trim().url().max(2_000).optional(),\n}).strict();\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"orders:write\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/medusa/shipments\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  const idempotencyKey = requireIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const parsed = await parseAdminJson(req, shipmentSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const { orderId, trackingNumber, carrierSlug, labelUrl } = parsed.data;\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n\n  const order = await fetchMedusaOrderJson(orderId);\n  if (!order) {\n    return correlatedJson(correlationId, { error: \"Order not found\" }, { status: 404 });\n  }\n\n  const prev = (order.metadata as Record<string, unknown> | undefined) ?? {};\n  if (prev.organization_id !== organization.id) {\n    return correlatedJson(correlationId, { error: \"Order not found\" }, { status: 404 });\n  }\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email?.trim().toLowerCase() ?? \"unknown\"}`,\n    actionKey: `medusa.shipment.create:${orderId}`,\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Request is already being processed or key was reused\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const list = Array.isArray(prev.fulfillment_shipments)\n    ? [...(prev.fulfillment_shipments as unknown[])]\n    : [];\n\n  list.push({\n    id: randomUUID(),\n    tracking_number: trackingNumber,\n    carrier_slug: carrierSlug,\n    label_url: labelUrl ?? null,\n    shipped_at: new Date().toISOString(),\n    status: \"pending\",\n  });\n\n  const meta = {\n    ...prev,\n    fulfillment_shipments: list,\n  };\n\n  const result = await patchMedusaOrderMetadata(orderId, meta);\n  if (!result.ok) {\n    logAdminApiEvent({\n      route: \"POST /api/medusa/shipments\",\n      correlationId,\n      phase: \"error\",\n      detail: { orderId, error: result.error },\n    });\n    const body = { error: \"Unable to update order\" };\n    await completeAdminIdempotency(sup.client, claim.id, 502, body);\n    return correlatedJson(correlationId, body, { status: 502 });\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/medusa/shipments\",\n    correlationId,\n    phase: \"ok\",\n    detail: { orderId },\n  });\n\n  const body = { ok: true };\n  await completeAdminIdempotency(sup.client, claim.id, 200, body);\n  return correlatedJson(correlationId, body);\n}\n"
      x-permission: "orders:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_medusa_shipmentsRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_medusa_shipmentsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/commit-sale:
    post:
      operationId: post_pos_medusa_commit_sale
      summary: "Create or execute Pos / Medusa / Commit Sale"
      description: "Pos / Medusa / Commit Sale. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { evaluatePosSalePolicy } from \"@universal-music-store/omnichannel-policy\";\nimport { getShiftById } from \"@universal-music-store/platform-data\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { medusaAdminFetch } from \"@/lib/medusa-admin-http\";\nimport { patchMedusaOrderMetadata } from \"@/lib/medusa-order-bridge\";\nimport {\n  getCompletedPosCommitOrderNumber,\n  rememberCompletedPosCommit,\n} from \"@/lib/pos-commit-idempotency\";\nimport { handlePosCommitSaleRequest } from \"@/lib/pos-commit-sale-route-handler\";\nimport { assertPosCartStock } from \"@/lib/pos-inventory-guard\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport {\n  getMedusaAdminSdk,\n  getMedusaRegionId,\n  getMedusaSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { posCommitSaleRouteLogic } from \"@/lib/pos-commit-sale-route-logic\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\n\nconst inflight = new Map<string, number>();\nconst INFLIGHT_TTL_MS = 30_000;\nconst INFLIGHT_MAX = 5_000;\n\nfunction pruneInflight(): void {\n  if (inflight.size < INFLIGHT_MAX) return;\n  const now = Date.now();\n  for (const [k, ts] of inflight) {\n    if (now - ts > INFLIGHT_TTL_MS) inflight.delete(k);\n  }\n}\n\nasync function findOrderByPosOfflineId(\n  offlineSaleId: string,\n  organizationId: string,\n): Promise<{ display_id: unknown; id: string } | null> {\n  const pageSize = 50;\n  for (let offset = 0; offset < 200; offset += pageSize) {\n    const qs = new URLSearchParams();\n    qs.set(\"limit\", String(pageSize));\n    qs.set(\"offset\", String(offset));\n    qs.set(\"fields\", \"id,display_id,metadata\");\n    qs.set(\"order\", \"-created_at\");\n    const res = await medusaAdminFetch(`/admin/orders?${qs.toString()}`);\n    if (!res.ok) return null;\n    const json = (await res.json()) as {\n      orders?: Array<{\n        id?: string;\n        display_id?: unknown;\n        metadata?: Record<string, unknown> | null;\n      }>;\n    };\n    const orders = json.orders ?? [];\n    for (const o of orders) {\n      if (\n        o.metadata?.pos_offline_id === offlineSaleId &&\n        o.metadata?.organization_id === organizationId &&\n        o.id\n      ) {\n        return { id: o.id, display_id: o.display_id };\n      }\n    }\n    if (orders.length < pageSize) break;\n  }\n  return null;\n}\n\nexport async function POST(req: Request) {\n  const adminSdk = getMedusaAdminSdk();\n  const regionId = getMedusaRegionId();\n  const salesChannelId = getMedusaSalesChannelId();\n  return handlePosCommitSaleRequest(req, {\n    getCorrelationId,\n    requireStaffApiSession,\n    resolveOrganizationId: async (correlationId) => {\n      const sup = adminSupabaseOr503(correlationId);\n      if (\"response\" in sup) return null;\n      const staff = await requireStaffApiSession(\"pos:use\");\n      if (!staff.ok) return null;\n      return (\n        (await resolveStaffOrganization(sup.client, staff.session.user?.email))\n          ?.id ?? null\n      );\n    },\n    logAdminApiEvent,\n    getIdempotencyKey: (request) =>\n      request.headers.get(\"idempotency-key\") ?? undefined,\n    getCompletedReplayOrderNumber: (key) =>\n      getCompletedPosCommitOrderNumber(key) ?? undefined,\n    isInflight: (key) => {\n      pruneInflight();\n      return inflight.has(key);\n    },\n    startInflight: (key) => {\n      inflight.set(key, Date.now());\n    },\n    clearInflight: (key) => {\n      inflight.delete(key);\n    },\n    executeCommitSale: async ({\n      body,\n      correlationId,\n      idempotencyKey,\n      organizationId,\n    }) => {\n      if (!organizationId) {\n        return {\n          status: 403,\n          body: { error: \"Organization scope required\", code: \"FORBIDDEN\" },\n          logPhase: \"error\" as const,\n          logDetail: { message: \"Organization scope required\" },\n        };\n      }\n      try {\n        return await posCommitSaleRouteLogic({\n          body: {\n            ...body,\n            paymentMethod: body.paymentMethod ?? \"cash\",\n            receiptReference:\n              body.receiptReference?.trim() ||\n              `pos:${idempotencyKey ?? correlationId}`,\n          },\n          correlationId,\n          idempotencyKey,\n          envReady: Boolean(adminSdk && regionId && salesChannelId),\n          completedReplayOrderNumber: null,\n          findExistingOrderByOfflineSaleId: async (offlineSaleId) => {\n            const existing = await findOrderByPosOfflineId(\n              offlineSaleId,\n              organizationId,\n            );\n            if (!existing) {\n              return null;\n            }\n            return {\n              id: existing.id,\n              displayId:\n                existing.display_id != null\n                  ? String(existing.display_id)\n                  : existing.id,\n            };\n          },\n          assertStock: async (items) => assertPosCartStock(items),\n          loadShiftStatus: async (shiftId) => {\n            const sup = adminSupabaseOr503(correlationId);\n            if (\"response\" in sup) {\n              throw new Error(\"SUPABASE_UNAVAILABLE\");\n            }\n            const row = await getShiftById(sup.client, shiftId, organizationId);\n            return row?.status === \"open\" ? \"open\" : row ? \"closed\" : \"missing\";\n          },\n          assertTerminalReady: async (terminalId) => {\n            const sup = adminSupabaseOr503(correlationId);\n            if (\"response\" in sup) throw new Error(\"SUPABASE_UNAVAILABLE\");\n            const { data, error } = await sup.client\n              .from(\"pos_payment_terminals\")\n              .select(\"status,certification_id\")\n              .eq(\"organization_id\", organizationId)\n              .eq(\"id\", terminalId)\n              .maybeSingle();\n            if (error) throw new Error(\"POS_TERMINAL_LOOKUP_FAILED\");\n            return (\n              data?.status === \"certified\" && Boolean(data.certification_id)\n            );\n          },\n          evaluatePolicy: (policyInput) => evaluatePosSalePolicy(policyInput),\n          createDraftOrder: async (draftInput) => {\n            if (!adminSdk || !regionId || !salesChannelId) {\n              return {};\n            }\n            const { draft_order } = await adminSdk.admin.draftOrder.create({\n              email: draftInput.email,\n              region_id: regionId,\n              sales_channel_id: salesChannelId,\n              items: draftInput.items,\n              metadata: {\n                ...(draftInput.metadata ?? {}),\n                organization_id: organizationId,\n              },\n            } as never);\n            return { id: draft_order?.id };\n          },\n          convertDraftToOrder: async (draftOrderId) => {\n            if (!adminSdk) {\n              return {};\n            }\n            const { order } =\n              await adminSdk.admin.draftOrder.convertToOrder(draftOrderId);\n            const total = (order as { total?: unknown } | undefined)?.total;\n            return {\n              id: order?.id != null ? String(order.id) : undefined,\n              display_id: order?.display_id,\n              total: typeof total === \"number\" ? total : undefined,\n            };\n          },\n          patchOrderMetadata: async (orderId, metadata) => {\n            await patchMedusaOrderMetadata(orderId, metadata);\n          },\n          recordSaleLedger: async ({\n            orderId,\n            orderNumber,\n            shiftId,\n            terminalId,\n            totalMinor,\n            idempotencyKey,\n          }) => {\n            const sup = adminSupabaseOr503(correlationId);\n            if (\"response\" in sup) return false;\n            const { error } = await sup.client.from(\"pos_sale_ledger\").upsert(\n              {\n                organization_id: organizationId,\n                order_id: orderId,\n                order_number: orderNumber,\n                shift_id: shiftId ?? null,\n                terminal_id: terminalId ?? null,\n                total_minor: totalMinor,\n                currency: \"PHP\",\n                idempotency_key: idempotencyKey ?? null,\n              },\n              { onConflict: \"organization_id,order_id\" },\n            );\n            return !error;\n          },\n          rememberCompletedReplay: (key, orderNumber) => {\n            rememberCompletedPosCommit(key, orderNumber);\n          },\n        });\n      } catch (e) {\n        const msg =\n          e instanceof Error && e.message === \"SUPABASE_UNAVAILABLE\"\n            ? \"Supabase admin connection is not configured\"\n            : e instanceof Error\n              ? e.message\n              : \"Unable to complete POS sale\";\n        return {\n          status:\n            msg === \"Supabase admin connection is not configured\" ? 503 : 502,\n          body: {\n            error: msg,\n            code:\n              msg === \"Supabase admin connection is not configured\"\n                ? \"SUPABASE_NOT_CONFIGURED\"\n                : \"INTERNAL_ERROR\",\n          },\n          logPhase: \"error\" as const,\n          logDetail: { message: msg },\n        };\n      }\n    },\n  });\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_pos_medusa_commit_saleRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_pos_medusa_commit_saleResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/draft-order:
    post:
      operationId: post_pos_medusa_draft_order
      summary: "Create or execute Pos / Medusa / Draft Order"
      description: "Pos / Medusa / Draft Order. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  getMedusaAdminSdk,\n  getMedusaRegionId,\n  getMedusaSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { resolveStaffOrganization } from \"@/lib/staff-organization\";\nimport { claimAdminIdempotency, completeAdminIdempotency, getRequestHash, parseAdminJson, requireIdempotencyKey } from \"@/lib/admin-api-security\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\nimport { buildPosSaleFeatureMetadata } from \"@universal-music-store/platform-data\";\nimport { z } from \"zod\";\n\nconst draftOrderSchema = z.object({\n  items: z.array(z.object({ variantId: z.string().trim().min(1).max(128), quantity: z.number().int().positive().max(100) }).strict()).min(1).max(100),\n  email: z.string().trim().email().max(320).optional(),\n  posFeatures: z.object({\n    orderTag: z.string().trim().max(80).optional(),\n    eInvoiceRequested: z.boolean().optional(),\n    eInvoiceCustomerEmail: z.string().trim().email().max(320).optional(),\n    eInvoiceCustomerTin: z.string().trim().max(40).optional(),\n  }).strict().optional(),\n}).strict();\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  logAdminApiEvent({\n    route: \"POST /api/pos/medusa/draft-order\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  const adminSdk = getMedusaAdminSdk();\n  const regionId = getMedusaRegionId();\n  const salesChannelId = getMedusaSalesChannelId();\n  if (!adminSdk || !regionId || !salesChannelId) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"POS environment incomplete (MEDUSA_SECRET_API_KEY, MEDUSA_REGION_ID, MEDUSA_SALES_CHANNEL_ID)\",\n      },\n      { status: 503 },\n    );\n  }\n\n  const parsed = await parseAdminJson(req, draftOrderSchema);\n  if (!parsed.ok) return correlatedJson(correlationId, { error: parsed.error }, { status: parsed.status });\n  const idempotencyKey = requireIdempotencyKey(req);\n  if (!idempotencyKey) return correlatedJson(correlationId, { error: \"Idempotency-Key is required\" }, { status: 400 });\n  const sup = adminSupabaseOr503(correlationId);\n  if (\"response\" in sup) return sup.response;\n  const organization = await resolveStaffOrganization(sup.client, staff.session.user?.email);\n  if (!organization) return correlatedJson(correlationId, { error: \"Organization membership is not configured\" }, { status: 403 });\n  const claim = await claimAdminIdempotency(sup.client, {\n    actorKey: `${organization.id}:${staff.session.user?.email?.trim().toLowerCase() ?? \"unknown\"}`,\n    actionKey: \"pos.medusa.draft-order.create\",\n    idempotencyKey,\n    requestHash: getRequestHash(parsed.data),\n  });\n  if (claim.kind === \"replay\") return correlatedJson(correlationId, claim.body, { status: claim.status });\n  if (claim.kind === \"conflict\") return correlatedJson(correlationId, { error: \"Request is already being processed or key was reused\" }, { status: 409 });\n  if (claim.kind !== \"claimed\") return correlatedJson(correlationId, { error: \"Idempotency service unavailable\" }, { status: 503 });\n  const body = parsed.data;\n  const items = body.items;\n\n  try {\n    const { draft_order } = await adminSdk.admin.draftOrder.create({\n      email: (body.email?.trim() || \"pos@instore.local\").slice(0, 320),\n      region_id: regionId,\n      sales_channel_id: salesChannelId,\n      items: items.map((i) => ({\n        variant_id: i.variantId,\n        quantity: Math.max(1, Math.floor(i.quantity)),\n      })),\n      metadata: { ...buildPosSaleFeatureMetadata(body.posFeatures ?? {}), organization_id: organization.id },\n    });\n\n    logAdminApiEvent({\n      route: \"POST /api/pos/medusa/draft-order\",\n      correlationId,\n      phase: \"ok\",\n      detail: { draftOrderId: draft_order?.id },\n    });\n\n    const responseBody = {\n      draftOrderId: draft_order?.id,\n      displayId: draft_order?.display_id,\n    };\n    if (!draft_order?.id) {\n      await completeAdminIdempotency(sup.client, claim.id, 502, { error: \"Unable to create draft order\" });\n      return correlatedJson(correlationId, { error: \"Unable to create draft order\" }, { status: 502 });\n    }\n    await completeAdminIdempotency(sup.client, claim.id, 200, responseBody);\n    return correlatedJson(correlationId, responseBody);\n  } catch {\n    const msg = \"Unable to create draft order\";\n    logAdminApiEvent({\n      route: \"POST /api/pos/medusa/draft-order\",\n      correlationId,\n      phase: \"error\",\n      detail: { message: msg },\n    });\n    const responseBody = { error: msg };\n    await completeAdminIdempotency(sup.client, claim.id, 502, responseBody);\n    return correlatedJson(correlationId, responseBody, { status: 502 });\n  }\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_pos_medusa_draft_orderRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_pos_medusa_draft_orderResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/lookup:
    post:
      operationId: post_pos_medusa_lookup
      summary: "Create or execute Pos / Medusa / Lookup"
      description: "Pos / Medusa / Lookup. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { NextResponse } from \"next/server\";\nimport { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  type ProductThumbSource,\n  getMedusaAdminSdk,\n  getMedusaRegionId,\n  getMedusaStoreSdk,\n  optionRowsToSizeColor,\n  resolvePosProductImageUrl,\n  variantPricePhpFromCalculated,\n  withSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function POST(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n  logAdminApiEvent({\n    route: \"POST /api/pos/medusa/lookup\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  const body = (await req.json().catch(() => ({}))) as {\n    barcode?: string;\n    sku?: string;\n  };\n  const barcode = typeof body.barcode === \"string\" ? body.barcode.trim() : \"\";\n  const sku = typeof body.sku === \"string\" ? body.sku.trim() : \"\";\n  const trimmed = barcode || sku;\n  if (!trimmed) {\n    return correlatedJson(\n      correlationId,\n      { error: \"Missing barcode or sku\" },\n      { status: 400 },\n    );\n  }\n\n  const regionId = getMedusaRegionId();\n  const storeSdk = getMedusaStoreSdk();\n  const adminSdk = getMedusaAdminSdk();\n  if (!regionId || !storeSdk || !adminSdk) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"POS environment incomplete (NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY, MEDUSA_REGION_ID, MEDUSA_SECRET_API_KEY)\",\n      },\n      { status: 503 },\n    );\n  }\n\n  type VariantLike = {\n    id?: string;\n    sku?: string | null;\n    calculated_price?: { calculated_amount?: number | null } | null;\n    options?: Array<{ option?: { title?: string | null } | null; value?: string | null }>;\n    product?: { title?: string | null; id?: string | null };\n    product_id?: string | null;\n  };\n\n  let variant: VariantLike | null = null;\n  let productTitle = \"\";\n  let thumbSource: ProductThumbSource | undefined;\n\n  try {\n    const storeList = await storeSdk.store.product.list(\n      withSalesChannelId({\n        region_id: regionId,\n        q: trimmed,\n        limit: 50,\n        fields:\n          \"*variants,*variants.calculated_price,*variants.options,*variants.sku,*variants.barcode,+title,+thumbnail,*images\",\n      }) as Parameters<typeof storeSdk.store.product.list>[0],\n    );\n    outerStore: for (const p of storeList.products ?? []) {\n      for (const v of p.variants ?? []) {\n        const vSku = String(v.sku ?? \"\").trim();\n        const ext = v as VariantLike & { ean?: string; barcode?: string };\n        const barcodeMatch =\n          String(ext.ean ?? \"\").trim() === trimmed ||\n          String(ext.barcode ?? \"\").trim() === trimmed;\n        if (vSku === trimmed || barcodeMatch) {\n          variant = v as VariantLike;\n          productTitle = p.title ?? \"\";\n          thumbSource = p;\n          break outerStore;\n        }\n      }\n    }\n  } catch {\n    /* try admin */\n  }\n\n  if (!variant) {\n    try {\n      const adminProducts = await adminSdk.admin.product.list({\n        q: trimmed,\n        limit: 25,\n        fields:\n          \"+variants.id,+variants.sku,+variants.barcode,+variants.options.*,+title\",\n      });\n      outerAdmin: for (const p of adminProducts.products ?? []) {\n        const variants = p.variants ?? [];\n        const exact = variants.find(\n          (x) => String(x.sku ?? \"\").trim() === trimmed,\n        );\n        const pick = exact ?? variants[0];\n        const pid = p.id;\n        if (pick?.id && pid) {\n          const prod = await storeSdk.store.product.retrieve(\n            pid,\n            withSalesChannelId({\n              region_id: regionId,\n              fields:\n                \"*variants,*variants.calculated_price,*variants.options,*variants.sku,*variants.barcode,+title,+thumbnail,*images\",\n            }) as Parameters<typeof storeSdk.store.product.retrieve>[1],\n          );\n          const vmatch = prod.product?.variants?.find((x) => x.id === pick.id);\n          variant = (vmatch ?? pick) as VariantLike;\n          productTitle = prod.product?.title ?? p.title ?? \"\";\n          thumbSource = prod.product ?? thumbSource;\n          break outerAdmin;\n        }\n      }\n    } catch {\n      /* not found */\n    }\n  }\n\n  if (!variant?.id) {\n    logAdminApiEvent({\n      route: \"POST /api/pos/medusa/lookup\",\n      correlationId,\n      phase: \"error\",\n      detail: { reason: \"not_found\" },\n    });\n    return tagResponse(new NextResponse(null, { status: 404 }), correlationId);\n  }\n\n  const { size, color } = optionRowsToSizeColor(variant.options);\n  const price = variantPricePhpFromCalculated(variant.calculated_price);\n  const ext = variant as { barcode?: string | null; ean?: string | null };\n  const barcodeRaw =\n    String(ext.ean ?? \"\").trim() || String(ext.barcode ?? \"\").trim();\n\n  logAdminApiEvent({\n    route: \"POST /api/pos/medusa/lookup\",\n    correlationId,\n    phase: \"ok\",\n    detail: { variantId: variant.id },\n  });\n\n  return correlatedJson(correlationId, {\n    id: variant.id,\n    sku: String(variant.sku ?? \"\"),\n    barcode: barcodeRaw || undefined,\n    size,\n    color,\n    price,\n    products: { name: productTitle },\n    imageUrl: resolvePosProductImageUrl(thumbSource),\n  });\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post_pos_medusa_lookupRequest'
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_pos_medusa_lookupResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/quick-products:
    get:
      operationId: get_pos_medusa_quick_products
      summary: "Read Pos / Medusa / Quick Products"
      description: "Pos / Medusa / Quick Products. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  getMedusaRegionId,\n  getMedusaStoreSdk,\n  optionRowsToSizeColor,\n  resolvePosProductImageUrl,\n  variantPricePhpFromCalculated,\n  withSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  logAdminApiEvent({\n    route: \"GET /api/pos/medusa/quick-products\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  const regionId = getMedusaRegionId();\n  const storeSdk = getMedusaStoreSdk();\n  if (!regionId || !storeSdk) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"POS environment incomplete (NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY, MEDUSA_REGION_ID)\",\n      },\n      { status: 503 },\n    );\n  }\n\n  try {\n    const { products } = await storeSdk.store.product.list(\n      withSalesChannelId({\n        region_id: regionId,\n        limit: 4,\n        fields:\n          \"*variants,*variants.calculated_price,*variants.options,*variants.sku,*variants.barcode,+title,+thumbnail,*images\",\n      }) as Parameters<typeof storeSdk.store.product.list>[0],\n    );\n\n    const list: Array<{\n      variantId: string;\n      name: string;\n      sku: string;\n      barcode?: string;\n      size: string;\n      color: string;\n      price: number;\n      imageUrl?: string;\n    }> = [];\n\n    for (const p of products ?? []) {\n      const v = p.variants?.[0];\n      if (!v?.id) continue;\n      const { size, color } = optionRowsToSizeColor(\n        v.options as Parameters<typeof optionRowsToSizeColor>[0],\n      );\n      const vb = v as { barcode?: string | null; ean?: string | null };\n      const barcode =\n        String(vb.ean ?? \"\").trim() || String(vb.barcode ?? \"\").trim();\n      list.push({\n        variantId: v.id,\n        name: p.title ?? \"\",\n        sku: String(v.sku ?? \"\"),\n        barcode: barcode || undefined,\n        size,\n        color,\n        price: variantPricePhpFromCalculated(v.calculated_price),\n        imageUrl: resolvePosProductImageUrl(p),\n      });\n    }\n\n    logAdminApiEvent({\n      route: \"GET /api/pos/medusa/quick-products\",\n      correlationId,\n      phase: \"ok\",\n      detail: { count: list.length },\n    });\n\n    return correlatedJson(correlationId, { products: list });\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"Quick products unavailable\";\n    logAdminApiEvent({\n      route: \"GET /api/pos/medusa/quick-products\",\n      correlationId,\n      phase: \"error\",\n      detail: { message: msg },\n    });\n    return correlatedJson(correlationId, { error: msg }, { status: 502 });\n  }\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_pos_medusa_quick_productsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/search:
    get:
      operationId: get_pos_medusa_search
      summary: "Read Pos / Medusa / Search"
      description: "Pos / Medusa / Search. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  getMedusaRegionId,\n  getMedusaStoreSdk,\n  optionRowsToSizeColor,\n  resolvePosProductImageUrl,\n  variantPricePhpFromCalculated,\n  withSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nconst DEFAULT_LIMIT = 24;\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  const url = new URL(req.url);\n  const q = (url.searchParams.get(\"q\") ?? \"\").trim();\n  if (!q) {\n    return correlatedJson(correlationId, { products: [] });\n  }\n\n  const regionId = getMedusaRegionId();\n  const storeSdk = getMedusaStoreSdk();\n  if (!regionId || !storeSdk) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"POS environment incomplete (NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY, MEDUSA_REGION_ID)\",\n      },\n      { status: 503 },\n    );\n  }\n\n  logAdminApiEvent({\n    route: \"GET /api/pos/medusa/search\",\n    correlationId,\n    phase: \"start\",\n  });\n\n  try {\n    const { products } = await storeSdk.store.product.list(\n      withSalesChannelId({\n        region_id: regionId,\n        q,\n        limit: DEFAULT_LIMIT,\n        fields:\n          \"*variants,*variants.calculated_price,*variants.options,*variants.sku,*variants.barcode,+title,+thumbnail,*images\",\n      }) as Parameters<typeof storeSdk.store.product.list>[0],\n    );\n\n    const list: Array<{\n      variantId: string;\n      name: string;\n      sku: string;\n      barcode?: string;\n      size: string;\n      color: string;\n      price: number;\n      imageUrl?: string;\n    }> = [];\n\n    for (const p of products ?? []) {\n      for (const v of p.variants ?? []) {\n        if (!v?.id) continue;\n        const { size, color } = optionRowsToSizeColor(\n          v.options as Parameters<typeof optionRowsToSizeColor>[0],\n        );\n        const vb = v as { barcode?: string | null; ean?: string | null };\n        const barcode =\n          String(vb.ean ?? \"\").trim() || String(vb.barcode ?? \"\").trim();\n        list.push({\n          variantId: v.id,\n          name: p.title ?? \"\",\n          sku: String(v.sku ?? \"\"),\n          barcode: barcode || undefined,\n          size,\n          color,\n          price: variantPricePhpFromCalculated(v.calculated_price),\n          imageUrl: resolvePosProductImageUrl(p),\n        });\n      }\n    }\n\n    logAdminApiEvent({\n      route: \"GET /api/pos/medusa/search\",\n      correlationId,\n      phase: \"ok\",\n      detail: { count: list.length },\n    });\n\n    return correlatedJson(correlationId, { products: list });\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"Product search unavailable\";\n    logAdminApiEvent({\n      route: \"GET /api/pos/medusa/search\",\n      correlationId,\n      phase: \"error\",\n      detail: { message: msg },\n    });\n    return correlatedJson(correlationId, { error: msg }, { status: 502 });\n  }\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_pos_medusa_searchResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /pos/medusa/suggestions:
    get:
      operationId: get_pos_medusa_suggestions
      summary: "Read Pos / Medusa / Suggestions"
      description: "Pos / Medusa / Suggestions. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Pos / Medusa"]
      x-source: "import { logAdminApiEvent } from \"@/lib/admin-api-log\";\nimport { getCorrelationId } from \"@/lib/request-correlation\";\nimport {\n  getMedusaRegionId,\n  getMedusaStoreSdk,\n  optionRowsToSizeColor,\n  resolvePosProductImageUrl,\n  variantPricePhpFromCalculated,\n  withSalesChannelId,\n} from \"@/lib/medusa-pos\";\nimport { requireStaffApiSession } from \"@/lib/requireStaffSession\";\nimport { correlatedJson, tagResponse } from \"@/lib/staff-api-response\";\n\nexport async function GET(req: Request) {\n  const correlationId = getCorrelationId(req);\n  const staff = await requireStaffApiSession(\"pos:use\");\n  if (!staff.ok) {\n    return tagResponse(staff.response, correlationId);\n  }\n\n  const regionId = getMedusaRegionId();\n  const storeSdk = getMedusaStoreSdk();\n  if (!regionId || !storeSdk) {\n    return correlatedJson(\n      correlationId,\n      {\n        error:\n          \"POS environment incomplete (NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY, MEDUSA_REGION_ID)\",\n      },\n      { status: 503 },\n    );\n  }\n\n  try {\n    const { products } = await storeSdk.store.product.list(\n      withSalesChannelId({\n        region_id: regionId,\n        limit: 12,\n        fields:\n          \"*variants,*variants.calculated_price,*variants.options,*variants.sku,*variants.barcode,+title,+thumbnail,*images\",\n      }) as Parameters<typeof storeSdk.store.product.list>[0],\n    );\n\n    const suggestions: Array<{\n      variantId: string;\n      name: string;\n      sku: string;\n      barcode?: string;\n      size: string;\n      color: string;\n      price: number;\n      imageUrl?: string;\n    }> = [];\n\n    for (const p of products ?? []) {\n      for (const v of p.variants ?? []) {\n        if (!v?.id) continue;\n        const { size, color } = optionRowsToSizeColor(\n          v.options as Parameters<typeof optionRowsToSizeColor>[0],\n        );\n        const vb = v as { barcode?: string | null; ean?: string | null };\n        const barcode =\n          String(vb.ean ?? \"\").trim() || String(vb.barcode ?? \"\").trim();\n        suggestions.push({\n          variantId: v.id,\n          name: p.title ?? \"\",\n          sku: String(v.sku ?? \"\"),\n          barcode: barcode || undefined,\n          size,\n          color,\n          price: variantPricePhpFromCalculated(v.calculated_price),\n          imageUrl: resolvePosProductImageUrl(p),\n        });\n        if (suggestions.length >= 8) break;\n      }\n      if (suggestions.length >= 8) break;\n    }\n\n    logAdminApiEvent({\n      route: \"GET /api/pos/medusa/suggestions\",\n      correlationId,\n      phase: \"ok\",\n      detail: { count: suggestions.length },\n    });\n\n    return correlatedJson(correlationId, { suggestions });\n  } catch (e) {\n    const msg = e instanceof Error ? e.message : \"Suggestions unavailable\";\n    return correlatedJson(correlationId, { error: msg }, { status: 502 });\n  }\n}\n"
      x-permission: "pos:use"
      x-tenant-scoped: false
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get_pos_medusa_suggestionsResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
  /admin/tracking-capabilities/revoke:
    post:
      operationId: post_admin_tracking_capabilities_revoke
      summary: "Revoke Admin / Tracking Capabilities"
      description: "Revokes an opaque tracking capability for its resolved order resource."
      tags: ["Admin / Orders"]
      x-source: "apps/admin/src/app/api/admin/tracking-capabilities/revoke/route.ts"
      x-permission: "orders:write"
      x-tenant-scoped: true
      x-idempotency-required: true
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [token, resourceId]
              properties:
                token:
                  type: string
                  minLength: 8
                  maxLength: 4096
                resourceId:
                  type: string
                  minLength: 1
                  maxLength: 200
                reason:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Tracking capability revoked.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [ok, revoked]
                properties:
                  ok:
                    type: boolean
                  revoked:
                    type: boolean
        "400":
          description: Invalid capability request.
        "401":
          description: Authentication or permission required.
        "503":
          description: Revocation service unavailable.
  /webhooks/nango:
    post:
      operationId: post_webhooks_nango
      summary: "Create or execute Webhooks / Nango"
      description: "Webhooks / Nango. Contract metadata is derived from the route handler and checked against its runtime controls."
      tags: ["Webhooks / Nango"]
      x-source: "import crypto from \"node:crypto\";\nimport { adminSupabaseOr503 } from \"@/lib/require-admin-supabase\";\nimport { correlatedJson } from \"@/lib/staff-api-response\";\nimport { containsDangerousKey } from \"@/lib/admin-api-security\";\n\nfunction validSignature(\n  raw: string,\n  provided: string | null,\n  secret: string,\n): boolean {\n  if (!provided) return false;\n  const expected = crypto\n    .createHmac(\"sha256\", secret)\n    .update(raw)\n    .digest(\"hex\");\n  const a = Buffer.from(expected, \"utf8\");\n  const b = Buffer.from(provided, \"utf8\");\n  return a.length === b.length && crypto.timingSafeEqual(a, b);\n}\n\nexport async function POST(request: Request) {\n  const contentLength = Number(request.headers.get(\"content-length\") ?? \"0\");\n  if (Number.isFinite(contentLength) && contentLength > 256_000) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Payload too large\" },\n      { status: 413 },\n    );\n  }\n  const raw = await request.text();\n  if (new TextEncoder().encode(raw).byteLength > 256_000) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Payload too large\" },\n      { status: 413 },\n    );\n  }\n  const secret = process.env.NANGO_WEBHOOK_SIGNING_KEY?.trim();\n  if (\n    !secret ||\n    !validSignature(raw, request.headers.get(\"x-nango-hmac-sha256\"), secret)\n  ) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Invalid webhook signature\" },\n      { status: 401 },\n    );\n  }\n  let payload: Record<string, unknown>;\n  try {\n    const parsed: unknown = JSON.parse(raw);\n    if (\n      !parsed ||\n      typeof parsed !== \"object\" ||\n      Array.isArray(parsed) ||\n      containsDangerousKey(parsed)\n    )\n      throw new Error(\"invalid\");\n    payload = parsed as Record<string, unknown>;\n  } catch {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Invalid JSON\" },\n      { status: 400 },\n    );\n  }\n  const eventIdValue =\n    request.headers.get(\"x-nango-event-id\") ??\n    request.headers.get(\"x-nango-webhook-id\") ??\n    payload.id ??\n    payload.eventId;\n  const eventId = typeof eventIdValue === \"string\" ? eventIdValue.trim() : \"\";\n  if (!/^[A-Za-z0-9._:-]{8,200}$/.test(eventId)) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Missing replay protection\" },\n      { status: 400 },\n    );\n  }\n  if (\n    payload.type !== \"auth\" ||\n    payload.operation !== \"creation\" ||\n    payload.success !== true\n  ) {\n    return correlatedJson(\"nango-webhook\", { accepted: true });\n  }\n  const tags = (payload.tags ?? {}) as Record<string, unknown>;\n  if (!tags || typeof tags !== \"object\" || Array.isArray(tags)) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Invalid connection ownership tags\" },\n      { status: 400 },\n    );\n  }\n  const merchantIdentity =\n    typeof tags.end_user_id === \"string\"\n      ? tags.end_user_id.trim().toLowerCase()\n      : \"\";\n  const organizationId =\n    typeof tags.organization_id === \"string\" ? tags.organization_id.trim() : \"\";\n  const providerConfigKey =\n    typeof payload.providerConfigKey === \"string\"\n      ? payload.providerConfigKey.trim()\n      : \"\";\n  const connectionId =\n    typeof payload.connectionId === \"string\" ? payload.connectionId.trim() : \"\";\n  if (\n    !merchantIdentity ||\n    !organizationId ||\n    !providerConfigKey ||\n    !connectionId\n  ) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Missing connection ownership tags\" },\n      { status: 400 },\n    );\n  }\n  if (\n    [merchantIdentity, organizationId, providerConfigKey, connectionId].some(\n      (value) => value.length > 256,\n    )\n  ) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Invalid connection ownership tags\" },\n      { status: 400 },\n    );\n  }\n  const sup = adminSupabaseOr503(\"nango-webhook\");\n  if (\"response\" in sup) return sup.response;\n  const replay = await sup.client.from(\"admin_webhook_replays\").insert({\n    channel: \"nango\",\n    nonce: eventId,\n    correlation_id: \"nango-webhook\",\n  });\n  if (replay.error?.code === \"23505\")\n    return correlatedJson(\"nango-webhook\", {\n      accepted: true,\n      deduplicated: true,\n    });\n  if (replay.error)\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Unable to validate webhook replay\" },\n      { status: 502 },\n    );\n  const existing = await sup.client\n    .from(\"payment_nango_connections\")\n    .select(\"organization_id,nango_connection_id\")\n    .eq(\"provider_config_key\", providerConfigKey)\n    .eq(\"merchant_identity\", merchantIdentity)\n    .maybeSingle();\n  if (existing.error) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Unable to validate connection ownership\" },\n      { status: 502 },\n    );\n  }\n  if (existing.data && existing.data.organization_id !== organizationId) {\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Connection ownership conflict\" },\n      { status: 409 },\n    );\n  }\n  const { error } = await sup.client.from(\"payment_nango_connections\").upsert(\n    {\n      provider_config_key: providerConfigKey,\n      nango_connection_id: connectionId,\n      merchant_identity: merchantIdentity,\n      organization_id: organizationId,\n      provider: typeof payload.provider === \"string\" ? payload.provider : null,\n      metadata: {\n        auth_mode: payload.authMode ?? null,\n        environment: payload.environment ?? null,\n      },\n      active: true,\n    },\n    { onConflict: \"provider_config_key,merchant_identity\" },\n  );\n  if (error)\n    return correlatedJson(\n      \"nango-webhook\",\n      { error: \"Failed to persist Nango connection reference\" },\n      { status: 502 },\n    );\n  return correlatedJson(\"nango-webhook\", { accepted: true });\n}\n"
      x-permission: "internal-signature"
      x-tenant-scoped: true
      x-idempotency-required: false
      x-idempotency-persisted: true
      security:
        - AdminSession: []
        - StaffApiKey: []
      responses:
        "200":
          description: Operation completed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post_webhooks_nangoResponse'
        "400":
          description: Invalid request.
        "401":
          description: Authentication or permission required.
        "500":
          description: Internal operation failure.
components:
  securitySchemes:
    AdminSession:
      type: apiKey
      in: cookie
      name: next-auth.session-token
      description: Staff session issued by the admin sign-in flow.
    StaffApiKey:
      type: http
      scheme: bearer
      bearerFormat: Admin API key
      description: Optional internal bearer credential for approved integrations.
  schemas:
    delete_admin_catalog_products_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/catalog/products/{id}. Runtime source: apps/admin/src/app/api/admin/catalog/products/[id]/route.ts."
    delete_admin_catalog_products_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/catalog/products/{id}. Runtime source: apps/admin/src/app/api/admin/catalog/products/[id]/route.ts."
    delete_admin_cms_announcementRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/announcement. Runtime source: apps/admin/src/app/api/admin/cms/announcement/route.ts."
    delete_admin_cms_announcementResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/announcement. Runtime source: apps/admin/src/app/api/admin/cms/announcement/route.ts."
    delete_admin_cms_block_presets_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/block-presets/{id}. Runtime source: apps/admin/src/app/api/admin/cms/block-presets/[id]/route.ts."
    delete_admin_cms_block_presets_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/block-presets/{id}. Runtime source: apps/admin/src/app/api/admin/cms/block-presets/[id]/route.ts."
    delete_admin_cms_blog_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/blog/{id}. Runtime source: apps/admin/src/app/api/admin/cms/blog/[id]/route.ts."
    delete_admin_cms_blog_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/blog/{id}. Runtime source: apps/admin/src/app/api/admin/cms/blog/[id]/route.ts."
    delete_admin_cms_components_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    delete_admin_cms_components_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    delete_admin_cms_media_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/media/{id}. Runtime source: apps/admin/src/app/api/admin/cms/media/[id]/route.ts."
    delete_admin_cms_media_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/media/{id}. Runtime source: apps/admin/src/app/api/admin/cms/media/[id]/route.ts."
    delete_admin_cms_pages_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/pages/{id}. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/route.ts."
    delete_admin_cms_pages_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/pages/{id}. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/route.ts."
    delete_admin_cms_redirects_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/cms/redirects/{id}. Runtime source: apps/admin/src/app/api/admin/cms/redirects/[id]/route.ts."
    delete_admin_cms_redirects_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/cms/redirects/{id}. Runtime source: apps/admin/src/app/api/admin/cms/redirects/[id]/route.ts."
    delete_admin_crm_nangoRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/crm/nango. Runtime source: apps/admin/src/app/api/admin/crm/nango/route.ts."
      properties:
        provider_config_key:
          type: string
        connection_id:
          type: string
        nango_connection_id:
          type: string
    delete_admin_crm_nangoResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/crm/nango. Runtime source: apps/admin/src/app/api/admin/crm/nango/route.ts."
    delete_admin_crm_notes_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/crm/notes/{id}. Runtime source: apps/admin/src/app/api/admin/crm/notes/[id]/route.ts."
    delete_admin_crm_notes_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/crm/notes/{id}. Runtime source: apps/admin/src/app/api/admin/crm/notes/[id]/route.ts."
    delete_admin_crm_operationsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
      properties:
        kind:
          type: string
          enum: ["deal", "activity"]
        subject:
          type: string
        activity_type:
          type: string
          enum: ["email", "call", "meeting", "note", "task"]
        body:
          type: string
        completed:
          type: boolean
        title:
          type: string
        stage:
          type: string
        value:
          type: number
        probability:
          type: number
        source:
          type: string
        period_start:
          type: string
        period_end:
          type: string
        target_value:
          type: number
        target_deals:
          type: number
    delete_admin_crm_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
    delete_admin_employees_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/employees/{id}. Runtime source: apps/admin/src/app/api/admin/employees/[id]/route.ts."
    delete_admin_employees_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/employees/{id}. Runtime source: apps/admin/src/app/api/admin/employees/[id]/route.ts."
    delete_admin_payments_connectionsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for DELETE /admin/payments/connections. Runtime source: apps/admin/src/app/api/admin/payments/connections/route.ts."
      properties:
        provider_config_key:
          type: string
        connection_id:
          type: string
        nango_connection_id:
          type: string
    delete_admin_payments_connectionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for DELETE /admin/payments/connections. Runtime source: apps/admin/src/app/api/admin/payments/connections/route.ts."
    get_admin_analytics_clvResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/analytics/clv. Runtime source: apps/admin/src/app/api/admin/analytics/clv/route.ts."
    get_admin_analytics_retentionResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/analytics/retention. Runtime source: apps/admin/src/app/api/admin/analytics/retention/route.ts."
    get_admin_analytics_sales_trendsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/analytics/sales-trends. Runtime source: apps/admin/src/app/api/admin/analytics/sales-trends/route.ts."
    get_admin_audit_logsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/audit-logs. Runtime source: apps/admin/src/app/api/admin/audit-logs/route.ts."
    get_admin_campaignsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/campaigns. Runtime source: apps/admin/src/app/api/admin/campaigns/route.ts."
    get_admin_catalog_categoriesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/catalog/categories. Runtime source: apps/admin/src/app/api/admin/catalog/categories/route.ts."
    get_admin_catalog_mediaResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/catalog/media. Runtime source: apps/admin/src/app/api/admin/catalog/media/route.ts."
    get_admin_catalog_products_suggestionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/catalog/products/suggestions. Runtime source: apps/admin/src/app/api/admin/catalog/products/suggestions/route.ts."
    get_admin_channels_eventsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/channels/events. Runtime source: apps/admin/src/app/api/admin/channels/events/route.ts."
    get_admin_chat_orders_variant_suggestionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/chat-orders/variant-suggestions. Runtime source: apps/admin/src/app/api/admin/chat-orders/variant-suggestions/route.ts."
    get_admin_cms_announcementResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/announcement. Runtime source: apps/admin/src/app/api/admin/cms/announcement/route.ts."
    get_admin_cms_block_presetsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/block-presets. Runtime source: apps/admin/src/app/api/admin/cms/block-presets/route.ts."
    get_admin_cms_blog_exportResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/blog/export. Runtime source: apps/admin/src/app/api/admin/cms/blog/export/route.ts."
    get_admin_cms_blog_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/blog/{id}. Runtime source: apps/admin/src/app/api/admin/cms/blog/[id]/route.ts."
    get_admin_cms_blogResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/blog. Runtime source: apps/admin/src/app/api/admin/cms/blog/route.ts."
    get_admin_cms_category_content_catalog_gapsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/category-content/catalog-gaps. Runtime source: apps/admin/src/app/api/admin/cms/category-content/catalog-gaps/route.ts."
    get_admin_cms_category_content_medusa_categoriesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/category-content/medusa-categories. Runtime source: apps/admin/src/app/api/admin/cms/category-content/medusa-categories/route.ts."
    get_admin_cms_category_contentResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/category-content. Runtime source: apps/admin/src/app/api/admin/cms/category-content/route.ts."
    get_admin_cms_components_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    get_admin_cms_componentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/components. Runtime source: apps/admin/src/app/api/admin/cms/components/route.ts."
    get_admin_cms_experimentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/experiments. Runtime source: apps/admin/src/app/api/admin/cms/experiments/route.ts."
    get_admin_cms_forms_settingsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/forms/settings. Runtime source: apps/admin/src/app/api/admin/cms/forms/settings/route.ts."
    get_admin_cms_forms_submissions_exportResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/forms/submissions/export. Runtime source: apps/admin/src/app/api/admin/cms/forms/submissions/export/route.ts."
    get_admin_cms_forms_submissionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/forms/submissions. Runtime source: apps/admin/src/app/api/admin/cms/forms/submissions/route.ts."
    get_admin_cms_media_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/media/{id}. Runtime source: apps/admin/src/app/api/admin/cms/media/[id]/route.ts."
    get_admin_cms_mediaResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/media. Runtime source: apps/admin/src/app/api/admin/cms/media/route.ts."
    get_admin_cms_navigationResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/navigation. Runtime source: apps/admin/src/app/api/admin/cms/navigation/route.ts."
    get_admin_cms_pages_id_mutationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/pages/{id}/mutations. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/mutations/route.ts."
    get_admin_cms_pages_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/pages/{id}. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/route.ts."
    get_admin_cms_pagesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/pages. Runtime source: apps/admin/src/app/api/admin/cms/pages/route.ts."
    get_admin_cms_redirects_exportResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/redirects/export. Runtime source: apps/admin/src/app/api/admin/cms/redirects/export/route.ts."
    get_admin_cms_redirects_resolveResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/redirects/resolve. Runtime source: apps/admin/src/app/api/admin/cms/redirects/resolve/route.ts."
    get_admin_cms_redirectsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cms/redirects. Runtime source: apps/admin/src/app/api/admin/cms/redirects/route.ts."
    get_admin_commerce_products_lookupResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/commerce/products/lookup. Runtime source: apps/admin/src/app/api/admin/commerce/products/lookup/route.ts."
    get_admin_commerce_products_searchResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/commerce/products/search. Runtime source: apps/admin/src/app/api/admin/commerce/products/search/route.ts."
    get_admin_commerce_recovery_metricsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/commerce-recovery-metrics. Runtime source: apps/admin/src/app/api/admin/commerce-recovery-metrics/route.ts."
    get_admin_cost_visibilityResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/cost-visibility. Runtime source: apps/admin/src/app/api/admin/cost-visibility/route.ts."
    get_admin_crm_bridgeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/crm/bridge. Runtime source: apps/admin/src/app/api/admin/crm/bridge/route.ts."
    get_admin_crm_nangoResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/crm/nango. Runtime source: apps/admin/src/app/api/admin/crm/nango/route.ts."
    get_admin_crm_notesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/crm/notes. Runtime source: apps/admin/src/app/api/admin/crm/notes/route.ts."
    get_admin_crm_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
    get_admin_delivery_logistics_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/delivery-logistics/operations. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/operations/route.ts."
    get_admin_delivery_logistics_shipmentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/delivery-logistics/shipments. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/shipments/route.ts."
    get_admin_delivery_logisticsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/delivery-logistics. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/route.ts."
      properties:
        ok:
          type: string
    get_admin_devicesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/devices. Runtime source: apps/admin/src/app/api/admin/devices/route.ts."
    get_admin_employees_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/employees/{id}. Runtime source: apps/admin/src/app/api/admin/employees/[id]/route.ts."
    get_admin_employeesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/employees. Runtime source: apps/admin/src/app/api/admin/employees/route.ts."
    get_admin_feature_mappingsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/feature-mappings. Runtime source: apps/admin/src/app/api/admin/feature-mappings/route.ts."
    get_admin_integration_healthResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/integration-health. Runtime source: apps/admin/src/app/api/admin/integration-health/route.ts."
    get_admin_integrations_pancakeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/integrations/pancake. Runtime source: apps/admin/src/app/api/admin/integrations/pancake/route.ts."
    get_admin_inventory_cycle_counts_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/cycle-counts/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/[id]/route.ts."
    get_admin_inventory_cycle_countsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/cycle-counts. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/route.ts."
    get_admin_inventory_ledgerResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/ledger. Runtime source: apps/admin/src/app/api/admin/inventory/ledger/route.ts."
    get_admin_inventory_purchase_orders_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/purchase-orders/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/[id]/route.ts."
    get_admin_inventory_purchase_ordersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/purchase-orders. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/route.ts."
    get_admin_inventory_reservationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/reservations. Runtime source: apps/admin/src/app/api/admin/inventory/reservations/route.ts."
    get_admin_inventory_streamResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/stream. Runtime source: apps/admin/src/app/api/admin/inventory/stream/route.ts."
    get_admin_inventory_transfers_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/transfers/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/[id]/route.ts."
    get_admin_inventory_transfersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory/transfers. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/route.ts."
    get_admin_inventoryResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/inventory. Runtime source: apps/admin/src/app/api/admin/inventory/route.ts."
    get_admin_invoicesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/invoices. Runtime source: apps/admin/src/app/api/admin/invoices/route.ts."
    get_admin_loyalty_lookupResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/loyalty/lookup. Runtime source: apps/admin/src/app/api/admin/loyalty/lookup/route.ts."
    get_admin_loyalty_rewardsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/loyalty/rewards. Runtime source: apps/admin/src/app/api/admin/loyalty/rewards/route.ts."
    get_admin_loyaltyResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/loyalty. Runtime source: apps/admin/src/app/api/admin/loyalty/route.ts."
    get_admin_medusa_payment_providersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/medusa/payment-providers. Runtime source: apps/admin/src/app/api/admin/medusa/payment-providers/route.ts."
    get_admin_offline_queueResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/offline-queue. Runtime source: apps/admin/src/app/api/admin/offline-queue/route.ts."
    get_admin_operator_notesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/operator-notes. Runtime source: apps/admin/src/app/api/admin/operator-notes/route.ts."
    get_admin_payment_attempts_exportResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/payment-attempts/export. Runtime source: apps/admin/src/app/api/admin/payment-attempts/export/route.ts."
    get_admin_payment_healthResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/payment-health. Runtime source: apps/admin/src/app/api/admin/payment-health/route.ts."
    get_admin_payments_capabilitiesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/payments/capabilities. Runtime source: apps/admin/src/app/api/admin/payments/capabilities/route.ts."
    get_admin_payments_connectionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/payments/connections. Runtime source: apps/admin/src/app/api/admin/payments/connections/route.ts."
    get_admin_paymentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/payments. Runtime source: apps/admin/src/app/api/admin/payments/route.ts."
    get_admin_pos_enterpriseResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/pos/enterprise. Runtime source: apps/admin/src/app/api/admin/pos/enterprise/route.ts."
    get_admin_pos_feature_mappingsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/pos/feature-mappings. Runtime source: apps/admin/src/app/api/admin/pos/feature-mappings/route.ts."
    get_admin_profile_avatarResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/profile/avatar. Runtime source: apps/admin/src/app/api/admin/profile/avatar/route.ts."
      properties:
        error:
          type: string
    get_admin_receiptsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/receipts. Runtime source: apps/admin/src/app/api/admin/receipts/route.ts."
    get_admin_reconciliationResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/reconciliation. Runtime source: apps/admin/src/app/api/admin/reconciliation/route.ts."
      properties:
        error:
          type: string
    get_admin_reviewsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/reviews. Runtime source: apps/admin/src/app/api/admin/reviews/route.ts."
    get_admin_rolesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/roles. Runtime source: apps/admin/src/app/api/admin/roles/route.ts."
    get_admin_segments_id_membersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/segments/{id}/members. Runtime source: apps/admin/src/app/api/admin/segments/[id]/members/route.ts."
    get_admin_segmentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/segments. Runtime source: apps/admin/src/app/api/admin/segments/route.ts."
    get_admin_shifts_id_reconciliationResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/shifts/{id}/reconciliation. Runtime source: apps/admin/src/app/api/admin/shifts/[id]/reconciliation/route.ts."
    get_admin_shiftsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/shifts. Runtime source: apps/admin/src/app/api/admin/shifts/route.ts."
    get_admin_sseResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/sse. Runtime source: apps/admin/src/app/api/admin/sse/route.ts."
    get_admin_storefront_homeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/storefront-home. Runtime source: apps/admin/src/app/api/admin/storefront-home/route.ts."
    get_admin_storefront_public_metadataResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/storefront-public-metadata. Runtime source: apps/admin/src/app/api/admin/storefront-public-metadata/route.ts."
    get_admin_tasks_todayResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/tasks/today. Runtime source: apps/admin/src/app/api/admin/tasks/today/route.ts."
    get_admin_voidsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/voids. Runtime source: apps/admin/src/app/api/admin/voids/route.ts."
    get_admin_workflow_entitiesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /admin/workflow/entities. Runtime source: apps/admin/src/app/api/admin/workflow/entities/route.ts."
    get_auth_nextauthResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /auth/{...nextauth}. Runtime source: apps/admin/src/app/api/auth/[...nextauth]/route.ts."
    get_integrations_couriersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /integrations/couriers. Runtime source: apps/admin/src/app/api/integrations/couriers/route.ts."
    get_pos_medusa_quick_productsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /pos/medusa/quick-products. Runtime source: apps/admin/src/app/api/pos/medusa/quick-products/route.ts."
    get_pos_medusa_searchResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /pos/medusa/search. Runtime source: apps/admin/src/app/api/pos/medusa/search/route.ts."
    get_pos_medusa_suggestionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for GET /pos/medusa/suggestions. Runtime source: apps/admin/src/app/api/pos/medusa/suggestions/route.ts."
    patch_admin_campaigns_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/campaigns/{id}. Runtime source: apps/admin/src/app/api/admin/campaigns/[id]/route.ts."
      properties:
        name:
          type: string
        subject:
          type: string
        body_template:
          type: string
        is_active:
          type: boolean
        schedule_cron:
          type: string
        segment_id:
          type: string
    patch_admin_campaigns_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/campaigns/{id}. Runtime source: apps/admin/src/app/api/admin/campaigns/[id]/route.ts."
    patch_admin_catalog_products_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/catalog/products/{id}. Runtime source: apps/admin/src/app/api/admin/catalog/products/[id]/route.ts."
    patch_admin_catalog_products_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/catalog/products/{id}. Runtime source: apps/admin/src/app/api/admin/catalog/products/[id]/route.ts."
    patch_admin_cms_components_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    patch_admin_cms_components_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    patch_admin_cms_forms_submissions_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/cms/forms/submissions/{id}. Runtime source: apps/admin/src/app/api/admin/cms/forms/submissions/[id]/route.ts."
    patch_admin_cms_forms_submissions_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/cms/forms/submissions/{id}. Runtime source: apps/admin/src/app/api/admin/cms/forms/submissions/[id]/route.ts."
    patch_admin_cms_media_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/cms/media/{id}. Runtime source: apps/admin/src/app/api/admin/cms/media/[id]/route.ts."
    patch_admin_cms_media_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/cms/media/{id}. Runtime source: apps/admin/src/app/api/admin/cms/media/[id]/route.ts."
    patch_admin_cms_redirects_bulkRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/cms/redirects/bulk. Runtime source: apps/admin/src/app/api/admin/cms/redirects/bulk/route.ts."
    patch_admin_cms_redirects_bulkResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/cms/redirects/bulk. Runtime source: apps/admin/src/app/api/admin/cms/redirects/bulk/route.ts."
    patch_admin_crm_operationsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
      properties:
        kind:
          type: string
          enum: ["deal", "activity"]
        subject:
          type: string
        activity_type:
          type: string
          enum: ["email", "call", "meeting", "note", "task"]
        body:
          type: string
        completed:
          type: boolean
        title:
          type: string
        stage:
          type: string
        value:
          type: number
        probability:
          type: number
        source:
          type: string
        period_start:
          type: string
        period_end:
          type: string
        target_value:
          type: number
        target_deals:
          type: number
    patch_admin_crm_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
    patch_admin_devices_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/devices/{id}. Runtime source: apps/admin/src/app/api/admin/devices/[id]/route.ts."
      properties:
        ip_address:
          type: string
        config:
          type: object
        is_active:
          type: boolean
    patch_admin_devices_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/devices/{id}. Runtime source: apps/admin/src/app/api/admin/devices/[id]/route.ts."
    patch_admin_employees_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/employees/{id}. Runtime source: apps/admin/src/app/api/admin/employees/[id]/route.ts."
    patch_admin_employees_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/employees/{id}. Runtime source: apps/admin/src/app/api/admin/employees/[id]/route.ts."
    patch_admin_offline_queueRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/offline-queue. Runtime source: apps/admin/src/app/api/admin/offline-queue/route.ts."
    patch_admin_offline_queueResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/offline-queue. Runtime source: apps/admin/src/app/api/admin/offline-queue/route.ts."
    patch_admin_profileRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/profile. Runtime source: apps/admin/src/app/api/admin/profile/route.ts."
    patch_admin_profileResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/profile. Runtime source: apps/admin/src/app/api/admin/profile/route.ts."
    patch_admin_reviews_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /admin/reviews/{id}. Runtime source: apps/admin/src/app/api/admin/reviews/[id]/route.ts."
      properties:
        status:
          type: string
          enum: ["approved", "rejected", "hidden", "pending"]
        moderation_note:
          type: string
        expected_updated_at:
          type: string
    patch_admin_reviews_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /admin/reviews/{id}. Runtime source: apps/admin/src/app/api/admin/reviews/[id]/route.ts."
    patch_medusa_orders_orderIdRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PATCH /medusa/orders/{orderId}. Runtime source: apps/admin/src/app/api/medusa/orders/[orderId]/route.ts."
      properties:
        status:
          type: string
    patch_medusa_orders_orderIdResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PATCH /medusa/orders/{orderId}. Runtime source: apps/admin/src/app/api/medusa/orders/[orderId]/route.ts."
    post_admin_campaigns_id_executeRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/campaigns/{id}/execute. Runtime source: apps/admin/src/app/api/admin/campaigns/[id]/execute/route.ts."
    post_admin_campaigns_id_executeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/campaigns/{id}/execute. Runtime source: apps/admin/src/app/api/admin/campaigns/[id]/execute/route.ts."
    post_admin_campaignsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/campaigns. Runtime source: apps/admin/src/app/api/admin/campaigns/route.ts."
      properties:
        name:
          type: string
        type:
          type: string
          enum: ["winback", "birthday", "first_purchase", "upsell", "custom"]
        segment_id:
          type: string
        subject:
          type: string
        body_template:
          type: string
        schedule_cron:
          type: string
    post_admin_campaignsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/campaigns. Runtime source: apps/admin/src/app/api/admin/campaigns/route.ts."
    post_admin_catalog_categoriesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/catalog/categories. Runtime source: apps/admin/src/app/api/admin/catalog/categories/route.ts."
    post_admin_catalog_categoriesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/catalog/categories. Runtime source: apps/admin/src/app/api/admin/catalog/categories/route.ts."
    post_admin_catalog_mediaRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/catalog/media. Runtime source: apps/admin/src/app/api/admin/catalog/media/route.ts."
    post_admin_catalog_mediaResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/catalog/media. Runtime source: apps/admin/src/app/api/admin/catalog/media/route.ts."
    post_admin_catalog_productsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/catalog/products. Runtime source: apps/admin/src/app/api/admin/catalog/products/route.ts."
    post_admin_catalog_productsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/catalog/products. Runtime source: apps/admin/src/app/api/admin/catalog/products/route.ts."
    post_admin_channels_events_id_processRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/channels/events/{id}/process. Runtime source: apps/admin/src/app/api/admin/channels/events/[id]/process/route.ts."
    post_admin_channels_events_id_processResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/channels/events/{id}/process. Runtime source: apps/admin/src/app/api/admin/channels/events/[id]/process/route.ts."
    post_admin_chat_orders_id_settleRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/chat-orders/{id}/settle. Runtime source: apps/admin/src/app/api/admin/chat-orders/[id]/settle/route.ts."
      properties:
        provider:
          type: string
          enum: ["stripe", "paypal", "xendit"]
        payment_external_id:
          type: string
        payment_request_id:
          type: string
        action:
          type: string
          enum: ["retrieve", "capture"]
    post_admin_chat_orders_id_settleResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/chat-orders/{id}/settle. Runtime source: apps/admin/src/app/api/admin/chat-orders/[id]/settle/route.ts."
    post_admin_chat_orders_id_statusRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/chat-orders/{id}/status. Runtime source: apps/admin/src/app/api/admin/chat-orders/[id]/status/route.ts."
      properties:
        status:
          type: string
          enum: ["pending", "draft_created", "processing", "pending_payment", "completed", "failed", "cancelled"]
        expected_updated_at:
          type: string
    post_admin_chat_orders_id_statusResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/chat-orders/{id}/status. Runtime source: apps/admin/src/app/api/admin/chat-orders/[id]/status/route.ts."
    post_admin_cms_block_presetsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/block-presets. Runtime source: apps/admin/src/app/api/admin/cms/block-presets/route.ts."
    post_admin_cms_block_presetsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/block-presets. Runtime source: apps/admin/src/app/api/admin/cms/block-presets/route.ts."
    post_admin_cms_blog_bulkRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/blog/bulk. Runtime source: apps/admin/src/app/api/admin/cms/blog/bulk/route.ts."
    post_admin_cms_blog_bulkResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/blog/bulk. Runtime source: apps/admin/src/app/api/admin/cms/blog/bulk/route.ts."
    post_admin_cms_blogRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/blog. Runtime source: apps/admin/src/app/api/admin/cms/blog/route.ts."
    post_admin_cms_blogResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/blog. Runtime source: apps/admin/src/app/api/admin/cms/blog/route.ts."
    post_admin_cms_category_content_sync_from_medusaRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/category-content/sync-from-medusa. Runtime source: apps/admin/src/app/api/admin/cms/category-content/sync-from-medusa/route.ts."
    post_admin_cms_category_content_sync_from_medusaResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/category-content/sync-from-medusa. Runtime source: apps/admin/src/app/api/admin/cms/category-content/sync-from-medusa/route.ts."
    post_admin_cms_category_contentRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/category-content. Runtime source: apps/admin/src/app/api/admin/cms/category-content/route.ts."
    post_admin_cms_category_contentResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/category-content. Runtime source: apps/admin/src/app/api/admin/cms/category-content/route.ts."
    post_admin_cms_components_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    post_admin_cms_components_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/components/{id}. Runtime source: apps/admin/src/app/api/admin/cms/components/[id]/route.ts."
    post_admin_cms_componentsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/components. Runtime source: apps/admin/src/app/api/admin/cms/components/route.ts."
    post_admin_cms_componentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/components. Runtime source: apps/admin/src/app/api/admin/cms/components/route.ts."
    post_admin_cms_experimentsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/experiments. Runtime source: apps/admin/src/app/api/admin/cms/experiments/route.ts."
    post_admin_cms_experimentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/experiments. Runtime source: apps/admin/src/app/api/admin/cms/experiments/route.ts."
    post_admin_cms_mediaRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/media. Runtime source: apps/admin/src/app/api/admin/cms/media/route.ts."
    post_admin_cms_mediaResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/media. Runtime source: apps/admin/src/app/api/admin/cms/media/route.ts."
    post_admin_cms_navigation_publishRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/navigation/publish. Runtime source: apps/admin/src/app/api/admin/cms/navigation/publish/route.ts."
    post_admin_cms_navigation_publishResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/navigation/publish. Runtime source: apps/admin/src/app/api/admin/cms/navigation/publish/route.ts."
    post_admin_cms_pagesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/pages. Runtime source: apps/admin/src/app/api/admin/cms/pages/route.ts."
    post_admin_cms_pagesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/pages. Runtime source: apps/admin/src/app/api/admin/cms/pages/route.ts."
    post_admin_cms_redirects_importRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/redirects/import. Runtime source: apps/admin/src/app/api/admin/cms/redirects/import/route.ts."
    post_admin_cms_redirects_importResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/redirects/import. Runtime source: apps/admin/src/app/api/admin/cms/redirects/import/route.ts."
    post_admin_cms_redirectsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/cms/redirects. Runtime source: apps/admin/src/app/api/admin/cms/redirects/route.ts."
    post_admin_cms_redirectsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/cms/redirects. Runtime source: apps/admin/src/app/api/admin/cms/redirects/route.ts."
    post_admin_crm_bridgeRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/crm/bridge. Runtime source: apps/admin/src/app/api/admin/crm/bridge/route.ts."
    post_admin_crm_bridgeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/crm/bridge. Runtime source: apps/admin/src/app/api/admin/crm/bridge/route.ts."
    post_admin_crm_nango_connect_sessionRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/crm/nango/connect-session. Runtime source: apps/admin/src/app/api/admin/crm/nango/connect-session/route.ts."
      properties:
        integration_id:
          type: string
    post_admin_crm_nango_connect_sessionResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/crm/nango/connect-session. Runtime source: apps/admin/src/app/api/admin/crm/nango/connect-session/route.ts."
    post_admin_crm_nangoRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/crm/nango. Runtime source: apps/admin/src/app/api/admin/crm/nango/route.ts."
      properties:
        provider_config_key:
          type: string
        connection_id:
          type: string
        nango_connection_id:
          type: string
    post_admin_crm_nangoResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/crm/nango. Runtime source: apps/admin/src/app/api/admin/crm/nango/route.ts."
    post_admin_crm_notesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/crm/notes. Runtime source: apps/admin/src/app/api/admin/crm/notes/route.ts."
      properties:
        customer_email:
          type: string
        note_body:
          type: string
    post_admin_crm_notesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/crm/notes. Runtime source: apps/admin/src/app/api/admin/crm/notes/route.ts."
    post_admin_crm_operationsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
      properties:
        kind:
          type: string
          enum: ["deal", "activity"]
        subject:
          type: string
        activity_type:
          type: string
          enum: ["email", "call", "meeting", "note", "task"]
        body:
          type: string
        completed:
          type: boolean
        title:
          type: string
        stage:
          type: string
        value:
          type: number
        probability:
          type: number
        source:
          type: string
        period_start:
          type: string
        period_end:
          type: string
        target_value:
          type: number
        target_deals:
          type: number
    post_admin_crm_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/crm/operations. Runtime source: apps/admin/src/app/api/admin/crm/operations/route.ts."
    post_admin_delivery_logistics_operationsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/delivery-logistics/operations. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/operations/route.ts."
      properties:
        latitude:
          type: number
        longitude:
          type: number
        shipment_id:
          type: string
        slug:
          type: string
        label:
          type: string
        phone:
          type: string
        status:
          type: string
          enum: ["available", "assigned", "offline", "suspended"]
        rating:
          type: number
        max_weight_kg:
          type: number
        max_volume_cm3:
          type: number
        cash_limit:
          type: number
        metadata:
          type: object
        courier_id:
          type: string
        speed_kph:
          type: number
        heading:
          type: number
        captured_at:
          type: string
        method:
          type: string
          enum: ["signature", "photo", "otp", "contactless"]
        recipient_name:
          type: string
        photo_url:
          type: string
        signature:
          type: string
        contact_log:
          type: string
        otp_digest:
          type: string
        otp:
          type: string
        expected_otp:
          type: string
        verified:
          type: boolean
        exception_type:
          type: string
        severity:
          type: string
          enum: ["low", "medium", "high", "critical"]
        details:
          type: string
        delivery_fee:
          type: number
        driver_earnings:
          type: number
        tolls:
          type: number
        tip:
          type: number
        cod_collected:
          type: number
        remitted:
          type: boolean
        address:
          type: string
        points:
          type: array
          items: {}
    post_admin_delivery_logistics_operationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/delivery-logistics/operations. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/operations/route.ts."
    post_admin_delivery_logistics_shipmentsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/delivery-logistics/shipments. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/shipments/route.ts."
    post_admin_delivery_logistics_shipmentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/delivery-logistics/shipments. Runtime source: apps/admin/src/app/api/admin/delivery-logistics/shipments/route.ts."
    post_admin_devicesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/devices. Runtime source: apps/admin/src/app/api/admin/devices/route.ts."
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
          enum: ["terminal", "printer", "kds", "scanner"]
        ip_address:
          type: string
        is_active:
          type: boolean
        config:
          type: object
    post_admin_devicesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/devices. Runtime source: apps/admin/src/app/api/admin/devices/route.ts."
    post_admin_employees_id_pinRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/employees/{id}/pin. Runtime source: apps/admin/src/app/api/admin/employees/[id]/pin/route.ts."
      properties:
        pin:
          type: string
    post_admin_employees_id_pinResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/employees/{id}/pin. Runtime source: apps/admin/src/app/api/admin/employees/[id]/pin/route.ts."
    post_admin_employeesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/employees. Runtime source: apps/admin/src/app/api/admin/employees/route.ts."
    post_admin_employeesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/employees. Runtime source: apps/admin/src/app/api/admin/employees/route.ts."
    post_admin_inventory_adjustRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/adjust. Runtime source: apps/admin/src/app/api/admin/inventory/adjust/route.ts."
      properties:
        productId:
          type: string
        variantId:
          type: string
        stockedQuantity:
          type: number
        delta:
          type: number
        expectedStockedQuantity:
          type: number
        locationId:
          type: string
    post_admin_inventory_adjustResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/adjust. Runtime source: apps/admin/src/app/api/admin/inventory/adjust/route.ts."
    post_admin_inventory_cycle_counts_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/cycle-counts/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/[id]/route.ts."
      properties:
        action:
          type: string
          enum: ["record", "complete", "cancel"]
        expectedRevision:
          type: number
        lines:
          type: array
          items: {}
        countedQuantity:
          type: number
    post_admin_inventory_cycle_counts_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/cycle-counts/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/[id]/route.ts."
    post_admin_inventory_cycle_countsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/cycle-counts. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/route.ts."
      properties:
        productId:
          type: string
        variantId:
          type: string
        locationId:
          type: string
        lines:
          type: array
          items: {}
    post_admin_inventory_cycle_countsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/cycle-counts. Runtime source: apps/admin/src/app/api/admin/inventory/cycle-counts/route.ts."
    post_admin_inventory_purchase_orders_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/purchase-orders/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/[id]/route.ts."
      properties:
        action:
          type: string
          enum: ["submit", "receive", "cancel"]
        expectedRevision:
          type: number
        lines:
          type: array
          items: {}
        quantity:
          type: number
    post_admin_inventory_purchase_orders_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/purchase-orders/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/[id]/route.ts."
    post_admin_inventory_purchase_ordersRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/purchase-orders. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/route.ts."
      properties:
        productId:
          type: string
        variantId:
          type: string
        orderedQuantity:
          type: number
        unitCostMinor:
          type: number
        supplierName:
          type: string
        destinationLocationId:
          type: string
        lines:
          type: array
          items: {}
    post_admin_inventory_purchase_ordersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/purchase-orders. Runtime source: apps/admin/src/app/api/admin/inventory/purchase-orders/route.ts."
    post_admin_inventory_reservations_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/reservations/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/reservations/[id]/route.ts."
      properties:
        operation:
          type: string
          enum: ["release", "commit", "attach_medusa", "close_medusa"]
        medusaReservationId:
          type: string
    post_admin_inventory_reservations_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/reservations/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/reservations/[id]/route.ts."
    post_admin_inventory_reservationsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/reservations. Runtime source: apps/admin/src/app/api/admin/inventory/reservations/route.ts."
      properties:
        locationId:
          type: string
        inventoryItemId:
          type: string
        quantity:
          type: number
        referenceType:
          type: string
        referenceId:
          type: string
        metadata:
          type: object
    post_admin_inventory_reservationsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/reservations. Runtime source: apps/admin/src/app/api/admin/inventory/reservations/route.ts."
    post_admin_inventory_transfers_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/transfers/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/[id]/route.ts."
      properties:
        action:
          type: string
          enum: ["approve", "ship", "complete", "cancel"]
        expectedRevision:
          type: number
    post_admin_inventory_transfers_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/transfers/{id}. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/[id]/route.ts."
    post_admin_inventory_transfersRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/inventory/transfers. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/route.ts."
      properties:
        productId:
          type: string
        variantId:
          type: string
        quantity:
          type: number
        sourceLocationId:
          type: string
        destinationLocationId:
          type: string
        lines:
          type: array
          items: {}
    post_admin_inventory_transfersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/inventory/transfers. Runtime source: apps/admin/src/app/api/admin/inventory/transfers/route.ts."
    post_admin_invoices_id_providerRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/invoices/{id}/provider. Runtime source: apps/admin/src/app/api/admin/invoices/[id]/provider/route.ts."
      properties:
        provider:
          type: string
          enum: ["stripe", "paypal", "xendit"]
        action:
          type: string
          enum: ["create", "finalize", "send", "pay", "remind", "cancel"]
    post_admin_invoices_id_providerResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/invoices/{id}/provider. Runtime source: apps/admin/src/app/api/admin/invoices/[id]/provider/route.ts."
    post_admin_invoicesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/invoices. Runtime source: apps/admin/src/app/api/admin/invoices/route.ts."
      properties:
        id:
          type: string
        description:
          type: string
        quantity:
          type: number
        unitPrice:
          type: number
        name:
          type: string
        email:
          type: string
        phone:
          type: string
        website:
          type: string
        addressLines:
          type: array
          items: {}
        taxId:
          type: string
          enum: ["gst", "vat", "service-tax", "none"]
        issuerName:
          type: string
        issuedDate:
          type: string
        paymentDueDate:
          type: string
        discountType:
          type: string
          enum: ["fixed", "percent"]
        discountValue:
          type: number
        items:
          type: array
          items: {}
        mode:
          type: string
          enum: ["draft", "send"]
    post_admin_invoicesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/invoices. Runtime source: apps/admin/src/app/api/admin/invoices/route.ts."
    post_admin_loyalty_pointsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/loyalty/points. Runtime source: apps/admin/src/app/api/admin/loyalty/points/route.ts."
    post_admin_loyalty_pointsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/loyalty/points. Runtime source: apps/admin/src/app/api/admin/loyalty/points/route.ts."
    post_admin_loyalty_rewardsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/loyalty/rewards. Runtime source: apps/admin/src/app/api/admin/loyalty/rewards/route.ts."
    post_admin_loyalty_rewardsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/loyalty/rewards. Runtime source: apps/admin/src/app/api/admin/loyalty/rewards/route.ts."
    post_admin_loyaltyRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/loyalty. Runtime source: apps/admin/src/app/api/admin/loyalty/route.ts."
    post_admin_loyaltyResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/loyalty. Runtime source: apps/admin/src/app/api/admin/loyalty/route.ts."
    post_admin_offline_queueRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/offline-queue. Runtime source: apps/admin/src/app/api/admin/offline-queue/route.ts."
    post_admin_offline_queueResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/offline-queue. Runtime source: apps/admin/src/app/api/admin/offline-queue/route.ts."
    post_admin_operator_notesRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/operator-notes. Runtime source: apps/admin/src/app/api/admin/operator-notes/route.ts."
    post_admin_operator_notesResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/operator-notes. Runtime source: apps/admin/src/app/api/admin/operator-notes/route.ts."
    post_admin_orders_bulk_fulfillRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/orders/bulk-fulfill. Runtime source: apps/admin/src/app/api/admin/orders/bulk-fulfill/route.ts."
    post_admin_orders_bulk_fulfillResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/orders/bulk-fulfill. Runtime source: apps/admin/src/app/api/admin/orders/bulk-fulfill/route.ts."
    post_admin_orders_export_jnt_csvRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/orders/export-jnt-csv. Runtime source: apps/admin/src/app/api/admin/orders/export-jnt-csv/route.ts."
    post_admin_orders_export_jnt_csvResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/orders/export-jnt-csv. Runtime source: apps/admin/src/app/api/admin/orders/export-jnt-csv/route.ts."
    post_admin_orders_orderId_refundRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/orders/{orderId}/refund. Runtime source: apps/admin/src/app/api/admin/orders/[orderId]/refund/route.ts."
      properties:
        payment_id:
          type: string
        amount_minor:
          type: number
        note:
          type: string
    post_admin_orders_orderId_refundResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/orders/{orderId}/refund. Runtime source: apps/admin/src/app/api/admin/orders/[orderId]/refund/route.ts."
    post_admin_payments_connect_sessionRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/connect-session. Runtime source: apps/admin/src/app/api/admin/payments/connect-session/route.ts."
      properties:
        integration_id:
          type: string
    post_admin_payments_connect_sessionResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/connect-session. Runtime source: apps/admin/src/app/api/admin/payments/connect-session/route.ts."
    post_admin_payments_connections_reconnectRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/connections/reconnect. Runtime source: apps/admin/src/app/api/admin/payments/connections/reconnect/route.ts."
      properties:
        provider_config_key:
          type: string
        nango_connection_id:
          type: string
    post_admin_payments_connections_reconnectResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/connections/reconnect. Runtime source: apps/admin/src/app/api/admin/payments/connections/reconnect/route.ts."
    post_admin_payments_connectionsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/connections. Runtime source: apps/admin/src/app/api/admin/payments/connections/route.ts."
      properties:
        provider_config_key:
          type: string
        connection_id:
          type: string
        nango_connection_id:
          type: string
    post_admin_payments_connectionsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/connections. Runtime source: apps/admin/src/app/api/admin/payments/connections/route.ts."
    post_admin_payments_id_mark_reviewRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/{id}/mark-review. Runtime source: apps/admin/src/app/api/admin/payments/[id]/mark-review/route.ts."
    post_admin_payments_id_mark_reviewResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/{id}/mark-review. Runtime source: apps/admin/src/app/api/admin/payments/[id]/mark-review/route.ts."
    post_admin_payments_id_retryRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/{id}/retry. Runtime source: apps/admin/src/app/api/admin/payments/[id]/retry/route.ts."
    post_admin_payments_id_retryResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/{id}/retry. Runtime source: apps/admin/src/app/api/admin/payments/[id]/retry/route.ts."
    post_admin_payments_provider_operationRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/payments/provider-operation. Runtime source: apps/admin/src/app/api/admin/payments/provider-operation/route.ts."
      properties:
        period_start:
          type: string
        period_end:
          type: string
        idempotency_key:
          type: string
        payment_token_id:
          type: string
        reference_id:
          type: string
        amount_minor:
          type: number
        channel_code:
          type: string
        channel_properties:
          type: object
        metadata:
          type: object
        description:
          type: string
        customer:
          type: string
        price:
          type: string
        quantity:
          type: number
        customer_email:
          type: string
        invoice_id:
          type: string
        action:
          type: string
          enum: ["get", "evidence", "accept", "escalate", "deny", "adjudicate"]
        order_id:
          type: string
        authorization_id:
          type: string
        payment_resource_id:
          type: string
        payload:
          type: object
        dispute_id:
          type: string
        evidence:
          type: object
        method:
          type: string
          enum: ["standard", "instant"]
        email:
          type: string
        subscription_id:
          type: string
    post_admin_payments_provider_operationResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/payments/provider-operation. Runtime source: apps/admin/src/app/api/admin/payments/provider-operation/route.ts."
    post_admin_pin_approvalRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/pin-approval. Runtime source: apps/admin/src/app/api/admin/pin-approval/route.ts."
      properties:
        approver_employee_id:
          type: string
        pin:
          type: string
        required_role:
          type: string
          enum: ["admin", "manager"]
    post_admin_pin_approvalResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/pin-approval. Runtime source: apps/admin/src/app/api/admin/pin-approval/route.ts."
    post_admin_pos_enterpriseRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/pos/enterprise. Runtime source: apps/admin/src/app/api/admin/pos/enterprise/route.ts."
      properties:
        jurisdiction:
          type: string
        registrationNumber:
          type: string
        invoicePrefix:
          type: string
        enabled:
          type: boolean
        provider:
          type: string
        model:
          type: string
        firmware:
          type: string
        certificationId:
          type: string
        expiresAt:
          type: string
        serialNumber:
          type: string
        deviceId:
          type: string
        providerTerminalExternalId:
          type: string
        status:
          type: string
          enum: ["pending", "certified", "degraded", "disabled"]
        metadata:
          type: object
    post_admin_pos_enterpriseResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/pos/enterprise. Runtime source: apps/admin/src/app/api/admin/pos/enterprise/route.ts."
    post_admin_receiptsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/receipts. Runtime source: apps/admin/src/app/api/admin/receipts/route.ts."
      properties:
        order_id:
          type: string
        send:
          type: boolean
    post_admin_receiptsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/receipts. Runtime source: apps/admin/src/app/api/admin/receipts/route.ts."
    post_admin_segments_id_membersRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/segments/{id}/members. Runtime source: apps/admin/src/app/api/admin/segments/[id]/members/route.ts."
      properties:
        members:
          type: array
          items: {}
        medusa_customer_id:
          type: string
    post_admin_segments_id_membersResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/segments/{id}/members. Runtime source: apps/admin/src/app/api/admin/segments/[id]/members/route.ts."
    post_admin_segmentsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/segments. Runtime source: apps/admin/src/app/api/admin/segments/route.ts."
      properties:
        name:
          type: string
        description:
          type: string
        rule_type:
          type: string
          enum: ["spend_above", "spend_below", "order_count_above", "inactive_days", "product_category", "tier", "manual"]
        rule_config:
          type: object
        auto_refresh:
          type: boolean
    post_admin_segmentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/segments. Runtime source: apps/admin/src/app/api/admin/segments/route.ts."
    post_admin_shifts_id_closeRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/shifts/{id}/close. Runtime source: apps/admin/src/app/api/admin/shifts/[id]/close/route.ts."
      properties:
        closing_cash:
          type: number
        notes:
          type: string
    post_admin_shifts_id_closeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/shifts/{id}/close. Runtime source: apps/admin/src/app/api/admin/shifts/[id]/close/route.ts."
    post_admin_shiftsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/shifts. Runtime source: apps/admin/src/app/api/admin/shifts/route.ts."
      properties:
        employee_id:
          type: string
        device_name:
          type: string
        opening_cash:
          type: number
    post_admin_shiftsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/shifts. Runtime source: apps/admin/src/app/api/admin/shifts/route.ts."
    post_admin_terminal_open_drawerRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/terminal-open-drawer. Runtime source: apps/admin/src/app/api/admin/terminal-open-drawer/route.ts."
      properties:
        device_id:
          type: string
        reason:
          type: string
    post_admin_terminal_open_drawerResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/terminal-open-drawer. Runtime source: apps/admin/src/app/api/admin/terminal-open-drawer/route.ts."
    post_admin_terminal_print_labelRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/terminal-print-label. Runtime source: apps/admin/src/app/api/admin/terminal-print-label/route.ts."
    post_admin_terminal_print_labelResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/terminal-print-label. Runtime source: apps/admin/src/app/api/admin/terminal-print-label/route.ts."
    post_admin_terminal_printRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/terminal-print. Runtime source: apps/admin/src/app/api/admin/terminal-print/route.ts."
    post_admin_terminal_printResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/terminal-print. Runtime source: apps/admin/src/app/api/admin/terminal-print/route.ts."
    post_admin_voidsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/voids. Runtime source: apps/admin/src/app/api/admin/voids/route.ts."
      properties:
        shift_id:
          type: string
        employee_id:
          type: string
        approved_by:
          type: string
        order_id:
          type: string
        line_item_id:
          type: string
        action:
          type: string
          enum: ["void_item", "void_order", "refund", "discount_override"]
        amount:
          type: number
        reason:
          type: string
        pin_verified:
          type: boolean
    post_admin_voidsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/voids. Runtime source: apps/admin/src/app/api/admin/voids/route.ts."
    post_admin_workflow_transitionRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /admin/workflow/transition. Runtime source: apps/admin/src/app/api/admin/workflow/transition/route.ts."
      properties:
        entity_type:
          type: string
          enum: ["catalog_product", "sales_order", "inventory_adjustment", "campaign", "cms_page", "chat_order"]
        entity_id:
          type: string
        to_state:
          type: string
        notes:
          type: string
        expected_updated_at:
          type: string
    post_admin_workflow_transitionResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /admin/workflow/transition. Runtime source: apps/admin/src/app/api/admin/workflow/transition/route.ts."
    post_auth_nextauthRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /auth/{...nextauth}. Runtime source: apps/admin/src/app/api/auth/[...nextauth]/route.ts."
    post_auth_nextauthResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /auth/{...nextauth}. Runtime source: apps/admin/src/app/api/auth/[...nextauth]/route.ts."
    post_integrations_channels_webhookResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /integrations/channels/webhook. Runtime source: apps/admin/src/app/api/integrations/channels/webhook/route.ts."
    post_integrations_chat_orders_intakeRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /integrations/chat-orders/intake. Runtime source: apps/admin/src/app/api/integrations/chat-orders/intake/route.ts."
      properties:
        source:
          type: string
        raw_text:
          type: string
        phone:
          type: string
        address:
          type: string
        variantId:
          type: string
        quantity:
          type: number
    post_integrations_chat_orders_intakeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /integrations/chat-orders/intake. Runtime source: apps/admin/src/app/api/integrations/chat-orders/intake/route.ts."
    post_integrations_couriers_telemetryRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /integrations/couriers/telemetry. Runtime source: apps/admin/src/app/api/integrations/couriers/telemetry/route.ts."
      properties:
        shipment_id:
          type: string
        courier_id:
          type: string
        latitude:
          type: number
        longitude:
          type: number
        speed_kph:
          type: number
        heading:
          type: number
        captured_at:
          type: string
    post_integrations_couriers_telemetryResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /integrations/couriers/telemetry. Runtime source: apps/admin/src/app/api/integrations/couriers/telemetry/route.ts."
    post_medusa_shipmentsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /medusa/shipments. Runtime source: apps/admin/src/app/api/medusa/shipments/route.ts."
      properties:
        orderId:
          type: string
        trackingNumber:
          type: string
        carrierSlug:
          type: string
        labelUrl:
          type: string
    post_medusa_shipmentsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /medusa/shipments. Runtime source: apps/admin/src/app/api/medusa/shipments/route.ts."
    post_pos_medusa_commit_saleRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /pos/medusa/commit-sale. Runtime source: apps/admin/src/app/api/pos/medusa/commit-sale/route.ts."
    post_pos_medusa_commit_saleResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /pos/medusa/commit-sale. Runtime source: apps/admin/src/app/api/pos/medusa/commit-sale/route.ts."
    post_pos_medusa_draft_orderRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /pos/medusa/draft-order. Runtime source: apps/admin/src/app/api/pos/medusa/draft-order/route.ts."
      properties:
        items:
          type: array
          items: {}
        quantity:
          type: number
        email:
          type: string
        posFeatures:
          type: object
        eInvoiceRequested:
          type: boolean
        eInvoiceCustomerEmail:
          type: string
        eInvoiceCustomerTin:
          type: string
    post_pos_medusa_draft_orderResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /pos/medusa/draft-order. Runtime source: apps/admin/src/app/api/pos/medusa/draft-order/route.ts."
    post_pos_medusa_lookupRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for POST /pos/medusa/lookup. Runtime source: apps/admin/src/app/api/pos/medusa/lookup/route.ts."
    post_pos_medusa_lookupResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /pos/medusa/lookup. Runtime source: apps/admin/src/app/api/pos/medusa/lookup/route.ts."
    post_webhooks_nangoResponse:
      type: object
      additionalProperties: false
      description: "Response contract for POST /webhooks/nango. Runtime source: apps/admin/src/app/api/webhooks/nango/route.ts."
    put_admin_cms_announcementRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/announcement. Runtime source: apps/admin/src/app/api/admin/cms/announcement/route.ts."
    put_admin_cms_announcementResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/announcement. Runtime source: apps/admin/src/app/api/admin/cms/announcement/route.ts."
    put_admin_cms_blog_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/blog/{id}. Runtime source: apps/admin/src/app/api/admin/cms/blog/[id]/route.ts."
    put_admin_cms_blog_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/blog/{id}. Runtime source: apps/admin/src/app/api/admin/cms/blog/[id]/route.ts."
    put_admin_cms_experiments_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/experiments/{id}. Runtime source: apps/admin/src/app/api/admin/cms/experiments/[id]/route.ts."
    put_admin_cms_experiments_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/experiments/{id}. Runtime source: apps/admin/src/app/api/admin/cms/experiments/[id]/route.ts."
    put_admin_cms_forms_settingsRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/forms/settings. Runtime source: apps/admin/src/app/api/admin/cms/forms/settings/route.ts."
    put_admin_cms_forms_settingsResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/forms/settings. Runtime source: apps/admin/src/app/api/admin/cms/forms/settings/route.ts."
    put_admin_cms_navigationRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/navigation. Runtime source: apps/admin/src/app/api/admin/cms/navigation/route.ts."
    put_admin_cms_navigationResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/navigation. Runtime source: apps/admin/src/app/api/admin/cms/navigation/route.ts."
    put_admin_cms_pages_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/pages/{id}. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/route.ts."
    put_admin_cms_pages_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/pages/{id}. Runtime source: apps/admin/src/app/api/admin/cms/pages/[id]/route.ts."
    put_admin_cms_redirects_idRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/cms/redirects/{id}. Runtime source: apps/admin/src/app/api/admin/cms/redirects/[id]/route.ts."
    put_admin_cms_redirects_idResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/cms/redirects/{id}. Runtime source: apps/admin/src/app/api/admin/cms/redirects/[id]/route.ts."
    put_admin_employees_id_pinRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/employees/{id}/pin. Runtime source: apps/admin/src/app/api/admin/employees/[id]/pin/route.ts."
      properties:
        pin:
          type: string
    put_admin_employees_id_pinResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/employees/{id}/pin. Runtime source: apps/admin/src/app/api/admin/employees/[id]/pin/route.ts."
    put_admin_storefront_homeRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/storefront-home. Runtime source: apps/admin/src/app/api/admin/storefront-home/route.ts."
    put_admin_storefront_homeResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/storefront-home. Runtime source: apps/admin/src/app/api/admin/storefront-home/route.ts."
    put_admin_storefront_public_metadataRequest:
      type: object
      additionalProperties: false
      description: "Validated request payload for PUT /admin/storefront-public-metadata. Runtime source: apps/admin/src/app/api/admin/storefront-public-metadata/route.ts."
    put_admin_storefront_public_metadataResponse:
      type: object
      additionalProperties: false
      description: "Response contract for PUT /admin/storefront-public-metadata. Runtime source: apps/admin/src/app/api/admin/storefront-public-metadata/route.ts."
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
        requestId:
          type: string
          description: Correlation identifier for support and audit review.
