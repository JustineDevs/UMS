import { fetchWorkerCrmOperationsForAdmin, mutateWorkerCrmOperationsForAdmin } from "@/lib/worker-admin-bridge";
import { getCorrelationId } from "@/lib/request-correlation";
export const dynamic = "force-dynamic";
async function mutation(request: Request, method: "POST" | "PATCH" | "DELETE") { const requestId = getCorrelationId(request); const key = request.headers.get("Idempotency-Key")?.trim(); if (!key) return new Response(JSON.stringify({ error: "Idempotency-Key is required", requestId }), { status: 400 }); let body: Record<string, unknown>; try { const raw = await request.text(); if (raw.length > 256 * 1024) return new Response(JSON.stringify({ error: "Payload too large", requestId }), { status: 413 }); const parsed = JSON.parse(raw) as unknown; if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) throw new Error("invalid"); body = parsed as Record<string, unknown>; } catch { return new Response(JSON.stringify({ error: "Invalid JSON body", requestId }), { status: 400 }); } const response = await mutateWorkerCrmOperationsForAdmin(method, body, key); return response ?? new Response(JSON.stringify({ error: "Worker backend is unavailable", requestId }), { status: 503 }); }
export async function GET(request: Request) { const requestId = getCorrelationId(request); const response = await fetchWorkerCrmOperationsForAdmin(); return response ?? new Response(JSON.stringify({ error: "Worker backend is unavailable", requestId }), { status: 503 }); }
export const POST = (request: Request) => mutation(request, "POST");
export const PATCH = (request: Request) => mutation(request, "PATCH");
export const DELETE = (request: Request) => mutation(request, "DELETE");
