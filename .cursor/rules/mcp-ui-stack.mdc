---
description: MCP servers and UI stack (shadcn, ui-layouts, GSAP, Tailwind).
globs: apps/**/src/**/*.tsx,packages/ui/**/*.tsx
alwaysApply: false
---

# MCP UI stack

Production UI uses Tailwind and `@universal-music-store/ui` in `packages/ui`. Extend that package instead of adding MUI, Mantine, Chakra, HeroUI, DaisyUI, or Kibo as app dependencies unless an ADR says otherwise. Those MCPs are for pattern reference only.

Read `AGENT.md` and `AGENT.md` MCP section. Enabled server keys live in `.cursor/mcp.json`. Use GSAP MCP patterns for motion; align with `HomeScrollExperience` and reduced-motion checks.
