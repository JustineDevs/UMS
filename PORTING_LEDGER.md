# Vvveb TypeScript Porting Ledger

Source specification: `.omx/context/CMS(1).md`
Original source: `internal/admin/Vvveb/public/js/vvvebjs/`

This ledger is the completion authority for the Vvveb port. Existing CMS code is
not treated as verified until it is matched to the original implementation and
covered by a reproducible test.

## Status

- `TODO`: not started
- `IN_PROGRESS`: implementation is underway; not complete
- `BLOCKED`: a named external or source-evidence blocker prevents verification
- `NEEDS-VERIFICATION`: implementation exists but required proof is missing
- `VERIFIED`: implementation, type safety, source comparison, and automated/manual proof exist

## Work Loop

For each cycle: select the highest-priority non-verified row, record its plan,
implement it, run the smallest proving test, then record actual evidence here.
Rows are never deleted. New discoveries go in the append-only section below.

## Findings Ledger

| ID | Finding / matrix item | Status | Original source anchor | Plan | Evidence |
|---|---|---|---|---|---|
| G-01 | `StyleManager.getSelectorForElement()` stable selector generation | VERIFIED | `builder.js:3218-3260`; ignored defaults `builder.js:894` | Port parent-chain, ignored-class, ID-stop, body-stop algorithm; test selectors | `apps/admin/src/lib/vvveb/style-selector.ts`; `apps/admin/src/lib/vvveb/style-selector.test.ts`; `pnpm exec tsx --test apps/admin/src/lib/vvveb/style-selector.test.ts` passed 2/2. Structural DOM type preserves browser `Element` compatibility without adding a DOM test dependency. |
| G-02 | `StyleManager.addSelectorState()` pseudo-state suffix logic | VERIFIED | `builder.js:3262-3268` | Port state suffix behavior; test empty and populated states | `apps/admin/src/lib/vvveb/style-selector.ts`; `apps/admin/src/lib/vvveb/style-selector.test.ts`; focused tests passed 3/3; admin TypeScript compilation passed. |
| G-03 | `ColorPaletteManager` CSS variable introspection | VERIFIED | `builder.js:4854-4906`; `inputs.js:438+` | Port variable discovery and palette integration; test CSS custom properties | `apps/admin/src/lib/vvveb/color-palette.ts`; `apps/admin/src/lib/vvveb/color-palette.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| G-04 | `GoogleFontsManager` provider integration contract | VERIFIED | `plugin-google-fonts.js:22-55` | Port provider registration, font-list loading, and lifecycle contract | `apps/admin/src/lib/vvveb/google-fonts.ts`; `apps/admin/src/lib/vvveb/google-fonts.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| G-05 | `Breadcrumb.loadBreadcrumb()` trail construction | VERIFIED | `builder.js:4482-4497` | Port selected-node ancestor trail and actions; test nested nodes | `apps/admin/src/lib/vvveb/breadcrumb.ts`; `apps/admin/src/lib/vvveb/breadcrumb.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. DOM click/scroll wiring is intentionally covered by matrix rows M-13/M-14, not claimed here. |
| G-06 | `drawComponentsTree()` recursive layers renderer | VERIFIED | `builder.js:3506-3554` | Port recursive tree rendering using node IDs in TypeScript | `apps/admin/src/lib/vvveb/components-tree.ts`; `apps/admin/src/lib/vvveb/components-tree.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. The factory is injected for browser/test portability; node identity is retained on the rendered item as in the source. |
| M-01 | Component registry and lookup indexes | VERIFIED | `builder.js:192-206` | Implement typed registry and indexes; parity tests | `apps/admin/src/lib/vvveb/component-registry.ts`; `apps/admin/src/lib/vvveb/component-registry.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| M-02 | Component registration | VERIFIED | `builder.js:231-273` | Implement register/add index population; parity tests | Same implementation/test as M-01; registration indexes verified for nodes, attributes, classes, and regex classes. |
| M-03 | DOM-to-component resolution | VERIFIED | `builder.js:308-362` | Implement attribute, class, regex, and tag priority; parity tests | Same implementation/test as M-01; priority and fallback behavior verified. |
| M-04 | Palette groups | VERIFIED | `builder.js:187-190`, `930+` | Implement typed group configuration and lookup | `apps/admin/src/lib/vvveb/registry.ts`; `apps/admin/src/lib/vvveb/registry.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| M-05 | Sibling registries | VERIFIED | `builder.js:500+` | Reuse generic registry for blocks, sections, and styles | Same implementation/test as M-04; generic registry behavior verified. |
| M-06 | Concrete component definitions | VERIFIED | `components-common.js`; `components-html.js`; `components-elements.js`; `components-bootstrap5.js`; `components-widgets.js`; `components-embeds.js`; `components-server.js`; `components/ecommerce/index.js`; `components/ecommerce/*.js` | Port every family’s actual properties, markup, lifecycle hooks, and inheritance | `vvveb-source-capture.ts` retains original markup, node/attribute matching, property keys, input controls, parent inheritance, and hook presence for 124 literal/dynamic registrations. `vvveb-ecommerce-capture.ts` preserves the eight imported ecommerce component objects, exact templates, attributes, property order, inheritance flags, and empty-property definitions. `component-definitions.ts` executes ported lifecycle bodies for heading/image, gallery/carousel, maps/openstreetmap/embed-video/Twitter/Facebook/Chart.js/Lottie, server products, and inherited ecommerce components. `component-definitions.test.ts` proves source metadata parity, ecommerce object parity, and lifecycle execution. Admin typecheck passes; the complete Vvveb suite passes 45/45; the CMS browser matrix previously passed 5/5. |
| M-07 | Palette rendering | VERIFIED | `builder.js:930+` | Render registry-backed draggable palette groups; test DOM output | Production Components palette emits both legacy CMS and `application/x-vvveb-component` payloads; `cms-editor-flow.spec.ts` CMS-05..08 proves browser drag/drop; `palette-renderer.test.ts` proves grouped DOM rendering. |
| M-08 | Property panel rendering | VERIFIED | `builder.js:366-545` | Render typed property metadata into sections; test controls | Same implementation/test as M-07; metadata-driven sections and defaults verified. |
| M-09 | Property-to-DOM binding dispatcher | VERIFIED | `builder.js:367-453` | Implement attribute, style, HTML, text, and undo dispatch | Same implementation/test as M-07; content/style/attribute mutations verified. |
| M-10 | Base Input class and typed subclasses | VERIFIED | `inputs.js:22-161` | Port lifecycle and typed property-change events | Same implementation/test as M-07; typed inputs, value updates, and property-change events verified. |
| M-11 | Builder core/state controller | VERIFIED | `builder.js:1000+` | Integrate typed state controller with the production builder route and iframe state | Production `CmsPageBuilder` owns selection, viewport, page tree, component canvas, history, and iframe lifecycle; full CMS browser matrix passed 5/5 and controller unit tests passed. |
| M-12 | Canvas bootstrap and iframe lifecycle | VERIFIED | `builder.js:1126+` | Integrate lifecycle with a real sandboxed iframe and browser test | `apps/admin/src/lib/vvveb/live-canvas-editor.ts` attaches to the live storefront iframe on load and observes dynamic DOM; `cms-editor-flow.spec.ts` CMS-01..04 passed after asserting stable `data-vvveb-id`; `canvas-editor-bridge.test.ts` passed. |
| M-13 | Node selection and overlay | VERIFIED | `builder.js:1366+`, `1630+` | Integrate selection geometry and overlay rendering with real browser coordinates | Storefront bridge paints the real iframe overlay from `getBoundingClientRect`; CMS-01 browser proof asserts visible `data-vvveb-overlay`, stable ID, and selection; full CMS matrix passed 5/5. |
| M-14 | Selected-node to property-panel glue | VERIFIED | `builder.js:1400+` | Connect selected DOM nodes to the production property panel | Live adapter maps canonical `data-cms-id` to builder selection; CMS-01..04 and CMS-07..09 browser tests passed with Content/Style/Layout/Responsive/Advanced/Code panels. |
| M-15 | Drag and resize state machine | VERIFIED | `builder.js:1500+` | Port typed pointer state and resize delta behavior | Same implementation/test as M-11; pointer state and handle delta math verified. |
| M-16 | Drag handle wiring | VERIFIED | `builder.js:1800+` | Wire real pointer handlers to production canvas controls | Live iframe selection overlay mounts a pointer-driven bottom-right resize handle; release emits validated width/height mutations; CMS-01 asserts the handle and storefront/admin typechecks pass. |
| M-17 | Drag completion to mutation commit | VERIFIED | `builder.js:1900+` | Commit real DOM insert/move mutations through UndoStack | Live iframe drop emits schema-validated `cms-builder-dom-drop`; admin commits to the canonical tree and durable mutation list; CMS-05..08 plus page mutation reload proof passed. |
| M-18 | Node clone and reorder actions | VERIFIED | `builder.js:1292-1365` | Port real DOM clone/up/down operations and mutation records | `DomEditorSession.clone()` and `move()` produce replayable DOM commands; `dom-command-engine.test.ts` proves clone, reorder, undo, and redo; CMS-05..08 proves nested reorder/remove behavior. |
| M-19 | HTML import/export | VERIFIED | `builder.js:2251-2335` | Strip/restore editor helpers on serialize/deserialize | `apps/admin/src/lib/vvveb/builder-actions.ts`; `apps/admin/src/lib/vvveb/builder-actions.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| M-20 | Undo/redo engine with discriminated mutations | VERIFIED | `undo.js:22-118` | Wire the discriminated mutation engine to real DOM operations and all editor commands | Canonical production history stores discriminated CMS mutations, replays nested structural/property changes, and persists them with page revisions; DOM command history independently proves real DOM replay; CMS-05..08 and CMS-13 passed. |
| M-21 | Rich text editing | VERIFIED | `builder.js:667-890` | Integrate contenteditable selection, blur commits, and custom history in the browser canvas | Storefront leaf selection enables contenteditable editing and posts bounded text mutations; isolated component canvas input is browser-proven by CMS-09..12; rich-text history unit test passes. |
| M-22 | Global action dispatch and keyboard shortcuts | VERIFIED | `builder.js:2500+` | Implement declarative action registry and shortcuts | Same implementation/test as M-19; action registration, dispatch, and modifier shortcut mapping verified. |
| M-23 | Save, export, and viewport actions | VERIFIED | `builder.js:2387+`; viewport state shared with StyleManager | Connect typed save/download actions to production UI and API | `VvvebCmsClient` is used by production page and component saves with envelope validation, version headers, idempotency, and publish; CMS-05, CMS-13, and `cms-rest-client.test.ts` prove the paths. |
| M-24 | Layers/tree panel | VERIFIED | `builder.js:3446-3554` | Mount node-id tree state into the production layers panel and selection actions | `components-tree.ts` provides node-id recursive rendering; CMS-01..04, CMS-05..08, and CMS-13 browser tests prove Navigator selection and persisted nested tree behavior. |
| M-25 | CSS-rule-based style editor | VERIFIED | `builder.js:3130-3402` | Port responsive CSS rule storage, read/write, and generation | `apps/admin/src/lib/vvveb/style-manager.ts`; `apps/admin/src/lib/vvveb/style-manager.test.ts`; focused tests passed 2/2; admin TypeScript compilation passed. |
| M-26 | Content accessors | VERIFIED | `builder.js:3405-3444` | Port typed attribute, HTML, and text accessors | Same implementation/test as M-20; typed accessor behavior verified. |
| M-27 | Font asset tracking | VERIFIED | `builder.js:4758-4829` | Port active-font tracking and unused-font cleanup | Same implementation/test as M-20; provider lifecycle and unused cleanup verified. |
| M-28 | Color palette store | VERIFIED | `builder.js:4833-4847` | Port typed palette store and theme initialization | Same implementation/test as M-20; add/remove/copy-safe reads verified. |
| M-29 | Dynamic/runtime-generated property metadata | VERIFIED | `components-common.js:1084-1151` | Port runtime property generation from CSS variables | `apps/admin/src/lib/vvveb/dynamic-properties.ts`; `apps/admin/src/lib/vvveb/dynamic-properties.test.ts`; focused tests passed 1/1; admin TypeScript compilation passed. |
| M-30 | REST/CMS backend contract | VERIFIED | `public/rest/default/openapi.json`; `rest/controller/index.php`; `config/rest-routes.php` | Generate/implement the complete typed client against the actual CMS routes and schemas | Typed page/component client validates response envelopes and enforces session credentials, optimistic versions, idempotency, and publish; production saves use it and contract tests pass. |
| M-31 | Server component to editor bridge | VERIFIED | `system/component/component.php`; `app/component/*.php`; `server-component.js` | Implement server-side sanitize, persistence, optimistic versioning, and rerender integration | Production component canvas uses the typed save/publish bridge; server-side request sanitizer and optimistic response validation are covered by `server-component-bridge.test.ts`, with CMS-09..12 browser proof. |

## New Findings

Append newly discovered gaps here; do not silently fold them into another row.

| ID | Discovery | Status | Evidence |
|---|---|---|---|
| NF-001 | No new findings discovered during M-06 through M-31 implementation | VERIFIED | Cycle review and full dedicated Vvveb suite |
| NF-002 | Audit triage checker truncated large `pnpm audit --json` output with the default child-process buffer | VERIFIED | `stress-test/scripts/check-audit-triage.js` now uses a 32 MiB buffer and explicit spawn-error handling; `node stress-test/scripts/check-audit-triage.js` passed with 12 high, 11 moderate, 3 low advisories documented by the existing triage policy. |
| NF-003 | Wrangler is not installed as a workspace dependency | VERIFIED | `package.json` pins Wrangler `4.131.0`, Worker scripts use `pnpm exec wrangler`, `pnpm install --frozen-lockfile` succeeds, and `corepack pnpm exec wrangler --version` returns `4.131.0`. Cloudflare account checks now run through the reproducible workspace toolchain. |

## Worker-only Commerce Migration Ledger

This section is a separate completion gate for `.omx/plans/prd-workers-only-commerce-migration.md`.
It must not be inferred from the Vvveb rows above.

| ID | Scope | Status | Evidence / blocker |
|---|---|---|---|
| W-01 | Hyperdrive PostgreSQL client and transaction boundary | IN_PROGRESS | `workers/backend/src/database.ts` now has explicit `app` and `medusa` roles, named `APP_HYPERDRIVE`/`MEDUSA_HYPERDRIVE` bindings, explicit `APP_DB_URL`/`MEDUSA_DB_URL` fallbacks, and rejects the generic Medusa `HYPERDRIVE` alias for APP queries. `wrangler.jsonc` now binds separate APP and Medusa Hyperdrive configs for dev and production. Live `wrangler hyperdrive list` confirms APP configs `uvs-app-dev` (`670f984853514e0ea8f61aa4956d2155`) and `uvs-app-production` (`801b922b54e74e0295c0e7d785b80966`) point to the APP Supabase origin with caching disabled, while Medusa configs remain separate. `database.test.ts` proves each role selects its own binding and that APP cannot silently use the generic binding. `router.ts` now fails closed with `database_not_configured` instead of dispatching APP-owned or cross-database native routes to the legacy container when a partial database configuration is present. `index.test.ts` proves both misconfiguration cases; full Worker suite (140/140), strict typecheck, config preflight, and `git diff --check` pass. APP migration `001_worker_payment_ledger.sql` has now been applied and schema-verified by `pnpm db:migrate:worker`/`pnpm db:check:worker` against the configured APP database. Live Worker transaction and recovery evidence remain pending. |
| W-02 | Durable mutation idempotency | NEEDS-VERIFICATION | `workers/backend/src/idempotency.ts`, migration `112_worker_idempotency_records.sql`, and focused tests pass. `pnpm db:migrate:worker` applied and verified `001_worker_payment_ledger.sql`; current `pnpm db:check:worker` reports `applied: true`, `schema.valid: true`, and no missing tables or columns for the configured APP database. Production/preview migration, live replay, and cross-runtime evidence remain pending. |
| W-03 | Worker queue job contract | NEEDS-VERIFICATION | `workers/backend/src/queue.ts` now validates job names, bounded IDs, payload objects, attempt range, and timestamps before dispatch; invalid jobs are acknowledged without invoking handlers. The fresh Worker suite passes 130/130 and strict Worker TypeScript compilation passes; queue binding, redelivery, and dead-letter infrastructure evidence remains pending. |
| W-04 | Catalog and inventory reads | NEEDS-VERIFICATION | `workers/backend/src/catalog.ts`, `inventory.ts`, focused tests pass; live schema and browser contract evidence pending. |
| W-05 | Cart create, read, update, add, quantity, and removal | IN_PROGRESS | `workers/backend/src/cart.ts` now provides idempotent native cart creation/update plus read/add/quantity/removal paths, preserving `region_id`, `sales_channel_id`, currency, checkout email, metadata, and transactional addresses; `cart.test.ts` proves server-generated IDs, default currency, channel preservation, update/address transaction, idempotency, stock checks, quantity updates, and zero-quantity removal. `workers/backend/src/cart-reconcile.ts` and `cart-reconcile.test.ts` now reconcile storefront lines against published Worker catalog prices and inventory, fail closed on missing variants, and preserve requested quantities. Storefront cart line, attach, merge, resume, and reconciliation paths use the Worker when `API_URL` is configured; resume no longer requires legacy Medusa publishable-key/region settings. Full Worker suite is 151/151 and strict Worker compilation passes; live Hyperdrive transaction and browser evidence remain pending. |
| W-06 | Hosted checkout session | IN_PROGRESS | `workers/backend/src/checkout.ts`, `providers.ts`, `payment-attempts.ts`; HTTPS callback validation, persisted `payment_attempts`, stable provider correlation metadata, cookie-scoped status polling, server totals, provider configuration errors, and idempotency are covered by the fresh Worker suite. Non-COD checkout preview now reads authoritative variant prices through `POST /store/checkout/preview`; COD remains on the profile-aware legacy validation path until native COD placement exists. `finalizeNativeOrderAcrossDatabases` fences active claims, permits reclaim only after a 10-minute stale-finalization lease, and persists `worker_payment_correlation_id`. Full Worker suite is 151/151 and strict typecheck passes. Hosted return/client cutover, production recovery, and sandbox evidence remain unproven. |
| W-07 | Provider webhook ingress and deduplication | IN_PROGRESS | `workers/backend/src/webhooks.ts`, migration `113_worker_webhook_events.sql`; verified events enter canonical `payment_webhook_events` with SHA-256 payload hashes, deduplicate before mutation, update only provider-matched attempts, and mark matched events processed while leaving unmatched events available for reconciliation. Provider signature/replay, correlation matching, duplicate delivery, cancellation mapping, and monotonic terminal-state protection are covered by the fresh Worker suite. Attempt updates now use `RETURNING correlation_id`, so provider-ID-only callbacks enqueue the authoritative UUID; webhook persistence is explicitly classified to APP. Production provider delivery and reconciliation evidence remain unexecuted. |
| W-08 | Account, order, delivery, invoice, refund, CMS, admin, compliance contracts | IN_PROGRESS | Native CMS page create/update, navigation draft/live/publish, announcement upsert, blog admin CRUD, category-content admin upsert, media metadata listing, profile, wishlist, compliance, delivery, invoice, and refund Worker modules are wired with explicit JWT claims, tenant scoping, bounded validation, idempotency, optimistic page versions, snapshots, mutation records, and publish-tree validation where applicable. Storefront payment-method discovery now uses `/store/payment-methods` and reports capability without exposing secrets; DSAR storefront calls now use the Worker’s required `X-Internal-API-Key` contract. Remaining account order reads/mutations, cart reconciliation, receipt upload, review/newsletter/back-in-stock workflows, staff-session/client cutover, live schemas, and browser/provider evidence remain open. |
| W-09 | Worker-only deployment configuration | IN_PROGRESS | `workers/backend/src/router.ts` has no container dispatch, `workers/backend/src/index.ts` has no container runtime or compatibility exports, `wrangler.jsonc` has no Container bindings or Durable Object migrations, and the Worker package no longer depends on `@cloudflare/containers`. Docker/Compose artifacts remain intentionally retained until W-08 through W-11 route parity, data, provider, and recovery gates are verified. |
| W-10 | Database migration/data validation | IN_PROGRESS | `scripts/run-worker-migrations.mjs` now requires explicit `APP_DB_URL`, refuses a database exposing the Medusa commerce table set, records a SHA-256 migration checksum, upgrades legacy unchecksummed rows only after schema validation, and verifies required Worker tables/columns after apply or status. `pnpm db:migrate:worker` applied and verified `001_worker_payment_ledger.sql` in the configured APP database; current `pnpm db:check:worker` reports `applied: true`, `schema.valid: true`, and no missing tables or columns. `scripts/worker-migration-contract.test.mjs` proves checksum drift detection and missing-schema reporting; fresh Worker suite is 149/149, strict Worker TypeScript compilation passes, and the prior `pnpm ci:local` run exited 0. Production/preview migration, complete APP/MEDUSA migration inventory, backfill checks, row reconciliation, and rollback/restore evidence remain pending. |
| W-11 | API, browser, payment, security, load, recovery gates | IN_PROGRESS | Fresh evidence: Worker suite passes 151/151, strict Worker TypeScript compilation passes, storefront typecheck passes, Node 20 storefront tests pass 292/292, and the latest `pnpm ci:local` run exits 0 through the Act release workflow including checkout, install, lint, build, security, audit triage, migration boundary, admin guard, OpenAPI parity, and unit/integration stages. `git diff --check` passes. Browser, provider sandbox/live delivery, production migration, Hyperdrive split-binding, queue redelivery/DLQ, restore, load, Web Vitals, and production infrastructure evidence remains pending. |
| W-12 | Delete Docker/Compose/container dependencies | BLOCKED | Must remain until W-08 through W-11 are VERIFIED per the migration plan. Runtime container dispatch and bindings are removed, but Docker/Compose artifacts and dependencies cannot be deleted until route parity, database migration/reconciliation, provider, browser, queue, load, and recovery gates have real evidence. |
| W-13 | Worker JWT authentication and customer order/profile reads | IN_PROGRESS | `workers/backend/src/auth.ts` verifies Supabase Auth RS256 JWTs through the project issuer JWKS with strict issuer, audience, expiry, not-before, key-id, and signature checks; `orders.ts` and `profile.ts` accept the Supabase issuer configuration. Profile GET/PUT now uses the Worker APP route when `API_URL` is configured, including avatar persistence and recent-auth enforcement; order reads, address semantics, cancellation/returns, hosted session/browser compatibility, and live proof remain open. Worker suite is 151/151 and strict Worker compilation passes. |
| W-14 | Native CMS public page contract | IN_PROGRESS | `workers/backend/src/cms.ts` and `cms.test.ts`; canonical `cms_pages.tree` is returned without block conversion, visibility is enforced for published/scheduled pages, every query is tenant-scoped, cache policy is explicit, and unconfigured tenants fail closed. Verified by `corepack pnpm exec tsx --test workers/backend/src/*.test.ts` (58/58) and strict Worker `tsc`; storefront cutover and staff authoring remain open. |
| W-15 | Native CMS public navigation contract | IN_PROGRESS | `workers/backend/src/navigation.ts` and `navigation.test.ts`; `cms_navigation` default row is tenant-scoped, nested links and scheduled visibility are filtered, empty rows fail safe, and cache policy is explicit. Verified by the Worker suite (60/60) and strict Worker `tsc`; storefront client cutover and staff draft/publish writes remain open. |
| W-16 | Native CMS public announcement contract | IN_PROGRESS | `workers/backend/src/announcement.ts` and `announcement.test.ts`; announcements are tenant/locale scoped, region and time windows are enforced, stack groups select the highest priority row, and cache policy is explicit. Verified by the Worker suite (61/61), strict Worker `tsc`, and `git diff --check`; analytics mutations and staff writes remain open. |
| W-17 | Durable webhook-to-finalization handoff | IN_PROGRESS | `workers/backend/src/webhooks.ts` enqueues a bounded `webhook-finalization` CommerceJob after a verified, non-duplicate payment attempt transition when `COMMERCE_QUEUE` is bound; the fresh Worker suite (151/151) proves the enqueue path and monotonic state handling. `workers/backend/src/index.ts` dispatches through `finalizeNativeOrderAcrossDatabases`; validated `payment-reconciliation` jobs use the same UUID validator and finalizer path, and provider-ID callbacks resolve their UUID through SQL `RETURNING`. The real queue path still requires the payment-ledger schema, live queue execution, and provider evidence. |
| W-18 | Queue batch consumer isolation | IN_PROGRESS | `workers/backend/src/queue.ts` adds independent acknowledgement/retry handling, bounded exponential retry, malformed-message acknowledgement, and an explicit dead-letter sink. `workers/backend/src/jobs.ts` now dispatches tenant-scoped `inventory-reservation-expiry` jobs to the APP `inventory_reservation_expire_due` RPC and `notification-delivery` jobs to an atomic Resend-backed delivery-attempt handler with idempotency and retry-state recording. The Worker suite passes 151/151 and strict Worker TypeScript compilation passes. Live queue consumer, redelivery, dead-letter delivery, provider delivery, and production RPC evidence remain pending. |
| W-19 | Native CMS blog public contract | IN_PROGRESS | `workers/backend/src/blog.ts` exposes tenant-scoped list/detail reads, filters drafts and future scheduled posts, and applies bounded cache headers; `blog.test.ts` proves published visibility and tenant query parameters. Worker suite is 63/63 and strict compilation passes. Storefront client cutover and staff authoring remain open. |
| W-20 | Native CMS category public contract | IN_PROGRESS | `workers/backend/src/category.ts` exposes tenant/locale-scoped category content at `/store/categories/:handle`, preserves blocks/media fields, validates references, and applies cache-safe responses; `category.test.ts` proves tenant and locale parameters. Worker suite is 64/64 and strict compilation passes. Native catalog collection integration and staff writes remain open. |
| W-21 | Native CMS sitemap source | IN_PROGRESS | `workers/backend/src/sitemap.ts` unions tenant-scoped CMS page/blog records and filters unpublished or future-scheduled entries; `sitemap.test.ts` proves draft exclusion and tenant parameterization. A parameter-count defect was caught and fixed before acceptance. Worker suite is 76/76, strict compilation passes, and `git diff --check` passes. Catalog route discovery and production sitemap cutover remain open. |
| W-22 | Native paid order creation transaction | IN_PROGRESS | `workers/backend/src/order-finalization.ts` creates a native Medusa order transaction by copying authoritative cart addresses and lines, writing `order_item` quantities, completing the cart, and linking the payment attempt; the route is wired at `POST /store/checkout-intents/:correlationId/finalize`. `finalizeNativeOrderAcrossDatabases()` now routes APP payment-attempt queries to APP and commerce writes to Medusa, with a split-topology regression test; Worker suite is 122/122. This is a compatibility saga, not a distributed transaction: payment-attempt updates are outside the Medusa transaction, so claim fencing, recovery reconciliation, live rollback, and provider/browser evidence remain required before this row can advance. |
| W-23 | Authenticated native order and receipt retrieval | IN_PROGRESS | `workers/backend/src/orders.ts` provides customer order list/detail/receipt reads with JWT ownership checks. The order list/detail quantity aggregation is aligned with the checked-in schema through `order_item.quantity` joined by `order_line_item.item_id`; `orders.test.ts` proves the SQL shape and unauthenticated receipt rejection. Strict Worker compilation, 76/76 Worker tests, and diff checks pass; live receipt schema and browser proof remain open. |
| W-24 | Native catalog collection contract | IN_PROGRESS | `workers/backend/src/catalog.ts` now exposes native collection listing and `GET /store/collections/:handle`, using the checked-in `product_collection`/`product`/`product_variant` schema, filtering deleted/unpublished records, preserving variants, and returning safe not-found responses. `catalog.test.ts` proves list/detail response shapes; focused Worker suite is 76/76 and strict compilation passes. Live schema, storefront browser parity, and production cutover evidence remain pending. |
| W-25 | Live Hyperdrive binding and connectivity | IN_PROGRESS | `workers/backend/src/database.ts` uses `pg` with `nodejs_compat`. Wrangler now has separate APP configs `uvs-app-dev` (`670f984853514e0ea8f61aa4956d2155`) and `uvs-app-production` (`801b922b54e74e0295c0e7d785b80966`), both confirmed by `wrangler hyperdrive list` against the APP Supabase origin with caching disabled; Medusa configs remain `uvs-commerce-dev` (`0dca18c059c348afac213abe2f7103af`) and `uvs-commerce-production` (`e911d409602b4d029dea7bf323801c0f`). A custom DNS hostname is not required; the Worker must be verified through the deployed URL configured in `API_URL`. APP migration `001_worker_payment_ledger.sql` is now applied and schema-verified against the configured APP database; Worker transaction, rollback, reconciliation, and current deployment proof remain pending. |
| W-26 | Dedicated Worker commerce queue binding | IN_PROGRESS | Wrangler lists isolated `uvs-commerce-dev`, `uvs-commerce-dev-dlq`, and `uvs-commerce-production` queues. Current live inventory is `1/0`, `1/0`, and `0/0` producers/consumers respectively; no queue consumer is active. `webhook-finalization` and validated `payment-reconciliation` dispatch to the native cross-database finalizer; unsupported reservation-expiry and notification jobs throw and retry rather than being acknowledged. Live redelivery, completion, DLQ delivery, and remaining handler evidence remain pending. |
| W-27 | Native storefront regions and collections reads | IN_PROGRESS | `workers/backend/src/catalog.ts` now exposes native `GET /store/regions` and paginated `GET /store/collections`, matching the existing storefront contract shape and excluding deleted rows. `catalog.test.ts` proves currency fields, pagination, and metadata stripping; focused Worker suite is 76/76. Live schema and browser contract evidence remain pending. |
| W-28 | Native storefront cart creation | IN_PROGRESS | `workers/backend/src/cart.ts` now exposes native `POST /store/carts` when Hyperdrive is bound, validates currency, preserves region and sales-channel context, generates opaque cart IDs, writes the existing cart schema, and persists/replays the response through `worker_idempotency_records`. Storefront resume and line mutations use the same Worker cart contract when configured. `cart.test.ts` proves the 201 response and insert path; live Hyperdrive, reconciliation, and browser evidence remain pending. |
| W-29 | Native CMS admin page writes | IN_PROGRESS | `workers/backend/src/cms-admin.ts` and `cms-admin.test.ts`; signed admin JWTs require `CMS_ADMIN_JWT_SECRET`, `organization_id`/`org_id`, and owner/admin or `content:write` claims. POST/PUT page writes require bounded JSON and idempotency, lock the tenant row, create a version snapshot, enforce optimistic revisions, preserve canonical tree nodes including unknown component IDs, validate published trees, and append mutation records transactionally. The direct regression test proves unauthorized denial, tenant-bound lookup, revision update, snapshot/mutation SQL, and an 82/82 Worker test suite; live Hyperdrive, staff-session exchange, delete/publish API parity, and browser evidence remain open. |
| W-30 | Native CMS navigation draft/live/publish writes | IN_PROGRESS | `workers/backend/src/navigation-admin.ts` and `navigation-admin.test.ts`; PUT draft/live and POST publish paths require the explicit staff JWT contract, tenant claims, idempotency, bounded recursive link validation, safe URL schemes, tenant-scoped upserts, and draft clearing after live publish. Direct tests prove unauthorized denial and tenant-scoped draft persistence; the Worker suite is 82/82 and strict compilation passes. Live Hyperdrive, staff-session exchange, browser authoring, media reference handling, and full navigation API parity remain open. |
| W-31 | Native CMS announcement admin list/upsert/delete | IN_PROGRESS | `workers/backend/src/announcement-admin.ts` and `announcement-admin.test.ts`; GET lists only the token tenant, PUT upserts using the repository's `(organization_id, id, locale)` key, and DELETE requires idempotency and tenant-scoped identity. Signed staff claims, bounded body/link/schedule fields, unsafe URL rejection, and inverted-window rejection are enforced. Direct tests prove unauthorized denial and tenant-scoped persistence; the Worker suite is 82/82, strict compilation passes, and `git diff --check` passes. Analytics, browser proof, and live Hyperdrive evidence remain open. |
| W-32 | Native CMS blog admin list/create/update/delete | IN_PROGRESS | `workers/backend/src/blog-admin.ts` and `blog-admin.test.ts`; GET/POST/PUT/DELETE require signed staff claims, tenant identity, bounded validated content, safe media URLs, tenant-scoped SQL, composite tenant/slug/locale upsert keys, and idempotency for mutations. Tests prove unauthorized denial, authenticated-tenant ownership despite a conflicting client field, composite upsert SQL, and missing-key rejection; the Worker suite is 85/85, strict compilation passes, and `git diff --check` passes. Live Hyperdrive, staff-session exchange, browser authoring, production schema, and full CMS parity remain open. |
| W-33 | Native CMS category-content admin list/upsert | IN_PROGRESS | `workers/backend/src/category-admin.ts` and `category-admin.test.ts`; GET/POST require signed staff claims and organization identity, list queries are tenant-scoped, POST validates bounded locale/handle/content/media fields, and upserts use the tenant composite key with durable idempotency. Tests prove unauthorized denial, tenant-scoped listing, tenant identity overriding client fields, and composite upsert SQL; the Worker suite is 88/88, strict compilation passes, and `git diff --check` passes. Live Hyperdrive, staff-session exchange, browser authoring, production schema, and full category sync parity remain open. |
| W-34 | Native CMS media metadata listing, upload, and deletion guard | IN_PROGRESS | `workers/backend/src/media-admin.ts` and `media-admin.test.ts`; GET requires signed staff claims, accepted content/catalog read permissions, tenant identity, bounded limit and allowlisted sort values, and parameterized tenant/search/MIME/tag filters while excluding soft-deleted rows. POST validates bounded multipart media, uploads through the existing Supabase Storage HTTP contract, persists tenant-scoped `cms_media` metadata, and deletes the object if metadata persistence fails. DELETE re-resolves the tenant row, scans CMS and Medusa payloads before soft deletion, returns `409 media_in_use` for references, and removes the Supabase object for unreferenced assets. Focused tests prove permission denial, tenant scoping, upload, reference protection, metadata deletion, and storage cleanup; live storage evidence, catalog synchronization, admin browser cutover, and migration proof remain open. |
| W-35 | Native compliance export, erasure, and retention contracts | IN_PROGRESS | `workers/backend/src/compliance.ts` and `compliance.test.ts`; native routes enforce `X-Internal-API-Key`, normalize and validate emails, aggregate parameterized APP and Medusa records, preserve commerce order history by deterministic customer anonymization, execute APP erasure transactionally, fail closed on database errors, and apply bounded APP retention windows. Tests prove unauthorized denial, cross-database export, parameterization, separate erasure transactions, and unsafe retention rejection; Worker suite is 99/99 and strict compilation passes. Live APP/MEDUSA schema, DSAR runtime, rollback, and production evidence remain pending. |
| W-36 | Native authenticated wishlist and canonical product resolution | IN_PROGRESS | `workers/backend/src/wishlist.ts` and `wishlist.test.ts`; GET/POST/DELETE plus bounded batch sync authenticate through the Worker JWT contract, resolve the customer in Medusa, re-resolve every product against published Medusa catalog data, and write only canonical identity/name/handle fields to APP wishlists. Storefront wishlist GET/POST/DELETE/sync now proxy the Worker whenever `API_URL` is configured, preserving schema, origin, rate limits, bearer identity, and generic outage errors. Worker suite is 151/151 and strict compilation passes; live schema, rate-limit backing, and browser evidence remain pending. |
| W-37 | Native authenticated order cancellation and return requests | IN_PROGRESS | `workers/backend/src/order-mutations.ts` and `order-mutations.test.ts`; cancellation enforces Supabase-authenticated ownership and cancellable statuses, updates the Medusa order transactionally, and is safe to replay after cancellation. Return requests enforce bounded payloads, fulfillment eligibility, ownership, unique item IDs, remaining quantities, transactional `return_requested_quantity` increments, and APP review job/audit recording. Worker suite is 107/107 and strict compilation passes. Live schema, storefront route cutover, provider/refund reconciliation, and browser evidence remain pending. |
| W-38 | Native staff payment refund contract | IN_PROGRESS | `workers/backend/src/admin-refund.ts`, `providers.ts`, and `admin-refund.test.ts`; staff JWT authorization, required idempotency, Medusa order/payment selection, payment-data capture/refund balance calculation, Stripe/PayPal/Xendit provider calls, and APP refund audit recording are implemented. Worker suite is 113/113, strict Worker compilation, migration-boundary check, and diff check pass. Live Medusa schema, payment-state reconciliation after provider success, provider sandbox/live delivery, staff-session cutover, and browser evidence remain pending. |
| W-39 | Native staff payment health/readiness contract | IN_PROGRESS | `workers/backend/src/payment-health.ts`, `payment-health.test.ts`, and router wiring; authenticated staff claims require `payments:read`, provider connections are scoped to the token organization, Stripe/PayPal/Xendit webhook configuration is reported without exposing secrets, sandbox mode is surfaced, and COD is explicit. Worker suite is 115/115 and strict compilation/migration-boundary checks pass. Live APP schema, admin client cutover, provider connectivity, and browser evidence remain pending. |
| W-40 | Native staff invoice creation, list, and lifecycle contract | IN_PROGRESS | `workers/backend/src/invoice-admin.ts`, `invoice-admin.test.ts`, and router wiring; invoice creation re-resolves the customer in Medusa, persists a tenant-scoped APP draft with idempotency, delegates canonical lifecycle transitions, and sends through Resend with explicit failed/draft outcomes. Reads are bounded and tenant-scoped; lifecycle actions require `receipts:send`, validate UUID/action/idempotency inputs, and delegate to `record_invoice_lifecycle` from migration `099_invoice_lifecycle_state_machine.sql`. Worker suite is 125/125 and strict Worker compilation passes. Live schema/RPC/email evidence, fiscal-provider reconciliation, admin client cutover, and browser evidence remain pending. |
| W-41 | Native staff delivery shipment and event mutations | IN_PROGRESS | `workers/backend/src/delivery-admin.ts`, `delivery-admin.test.ts`, and router wiring; GET listing, tenant-scoped shipment upsert, and event append are native, with `orders:write`/`dashboard:read` authorization, bounded status values, durable idempotency, fixed-column SQL, and canonical `append_delivery_logistics_event` delegation. Worker suite is 125/125 and strict Worker compilation passes. Live APP schema/RPC execution, Pancake logistics callbacks, admin client cutover, and browser evidence remain pending. |
