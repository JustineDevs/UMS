# Archived documentation

This folder holds **accepted, frozen** records so active docs in `docs/` stay easy to scan.

| Path | Contents |
|------|----------|
| `adr/` | Architecture Decision Records with status **Accepted**. New proposals stay in `docs/adr/` until accepted, then move here. |

**Still live at repo root under `docs/`:** `spec.md`, `data-ownership.md`, `security-program.md`, `runbooks/`, and other operational or legal references.
